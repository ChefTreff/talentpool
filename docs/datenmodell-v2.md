# Datenmodell v2 — Überblick (Stand 08.09.2026, nach Welle-0-Migrationen)

> Menschlich lesbare Ergänzung zur generierten Schema-Doku (`docs/schema.md`, Build-Session B11). Quelle der Wahrheit sind die Migrationen unter `supabase/migrations/`. Änderungen nur per Migration und Eintrag im Entscheidungslog.

## Prinzipien
- **RLS auf jeder Tabelle**, default deny. Nutzer (`authenticated`) lesen nur eigene oder veröffentlichte Daten; Spalten-Whitelists blenden interne Felder aus (`slot.internal_*`, `application.status/rank`, `stage.stage_lead_person_id`).
- **Schreiben nur über RPCs** (SECURITY DEFINER, `search_path` gepinnt, Autorisierung im Funktionsrumpf) oder serverseitig über `service_role` nach `requireRole()`. `anon` kann keine SECURITY-DEFINER-Funktion aufrufen.
- **Rollen mit Scope** (`role_assignment`): global · edition · portal · org · stage · stage_day · slot; edition-gebunden, zeitlich begrenzbar. Helper: `has_role()`, `is_admin()`, `is_staff()`, `my_roles()`, `can_edit_stage()`, `can_edit_slot()`, `can_decide_session()`, `is_member_of_org()`.
- **Audit**: `audit_log` über `log_audit()` für Rollen, Slots, Entscheidungen, Ticket-Personalisierung, Profil-Löschung.
- **Löschen = Anonymisieren** (`delete_my_profile()`): PII entfernt, Historie pseudonym, Adressen gehasht in `suppression`, Auth-User danach serverseitig gelöscht.

## Domänen und Tabellen
| Domäne | Tabellen | Kern-RPCs / Views |
|---|---|---|
| Identität | `person` (+ title, city, pronouns, photo_url, tier, deleted_at), `person_email`, `organization` (+ type, slug, website, active, sevdesk_contact_id), `org_membership`, `role_assignment`, `consent_record`, `suppression`, `audit_log`, `staff_user` (Legacy) | `claim_or_create_person`, `set_primary_email`, `delete_my_profile`, View `consent_current` **Login-Regel:** Die Verbindung Auth-Konto ↔ `person` entsteht erst beim ersten Login über `claim_or_create_person` (Callback). Personen, die Team oder Skripte anlegen (z. B. `upsert_speaker`, Partner-Ingest), haben bis dahin `auth_user_id = null`; für sie liefert `current_person_id()` NULL und jede RPC antwortet 28000 — im Portal unerreichbar, für Skripte und Automationen die Regel: entweder mit service_role arbeiten oder den Claim vorher ausführen. |
| Edition & Programm | `event` (+ slug, is_edition, edition_id, timezone, venue, status), `event_day`, `stage`, `stage_day`, `track`, `slot`, `session`, `session_speaker`, `slot_history` | `create_slot`, `move_slot` (Warnungen: before_open, after_close, off_grid_5min, changeover_short, speaker_conflict; hart: Überlappung je Bühne 23P01, Bestätigung nach Veröffentlichung P0001), `set_slot_status`, Views `programme_public`, `stage_day_slot_stats` |
| Bewerbung & Anmeldung | `question_catalog`, `session_question` (max. 2 eigene), `application`, `decision_release`, `registration` (+ session_id) | Talent: `apply_to_session`, `withdraw_application`, `confirm_application` (Ticketpflicht, Kollision → `p_replace_conflicting`), `register_for_session`, `cancel_registration`, `my_applications()` (maskiert bis Freigabe) · Entscheider: `decide_application`, `release_decisions`, `promote_waitlist`, `expire_overdue_applications` |
| Tickets | `ticket_type_map`, `ticket`, `org_ticket_allocation`, `checkin` | `personalize_ticket` (Käufer/Inhaber, für mich oder andere Person) **Freitickets aus dem Speaker-Portal (0034):** `status` zusätzlich `requested`/`approved`, `barcode` bis zur Ausstellung NULL (bleibt unique), `speaker_profile_id`, `lounge_access`, `team_note`, `requested_by`, `approved_by/at`; `source` `speaker` bzw. `speaker_companion`, je Profil ein aktives eigenes und ein aktives Begleitticket (partielle Unique-Indizes). |
| Integration | `integration.webhook_event` (unique Quelle+ID), `integration.sync_job`, `integration.sync_error`, `external_ref` | — (Route Handler schreiben über service_role) |
| Kommunikation | `mail_template` (key × locale), `mail_log` (auch **Warteschlange**: Status `queued` + `meta.vars`, `related_type`/`related_id`) | `is_suppressed()` (nur service_role) · `queue_mail()` (intern: Empfängerin, Sprache, Suppression, Dedupe je Vorlage × Person × Bezugsobjekt, solange `queued`; 0042) · Trigger `trg_application_mail`, `trg_decision_release_mail`, `trg_registration_mail` legen die Aufträge an · `run_application_housekeeping()` (Cron: Fristen ablaufen lassen, Warteliste nachrücken) · `session_mail_vars()`, `mail_fmt_ts()` · Versand: `lib/mail/queue.ts` über `/api/cron/mail` (Vercel Cron alle 10 Min., Resend, Retry 3×, Idempotenz-Schlüssel je Zeile) |
| Speaker (Welle 2) | `speaker_profile` (je Edition: `speaker_type`, `pipeline_status`, Owner/Lead, Bio EN/DE, Socials, Tech-Rider, Staff-Flags `reception_eligible`, `lounge_access`, `pass_type`, `hotel_tier`, `hospitality_status`, `travel_costs_covered` + finale Freigabe, `assistant_person_id`, `internal_notes`, `invited_at`) | Rechte: `is_speaker_team()`, `can_manage_speaker(profile)` (Team · Owner/Ersteller · Edition-Scope · Speaker auf Session in Bühne/Bühne×Tag/Slot des Scopes); RLS-Select nur für Manager/Team, Speaker lesen über `my_speaker_profile()`. RPCs: `update_my_speaker_profile(jsonb)` (Speaker/Assistenz, Whitelist), `upsert_speaker(jsonb)` (Manager/Team; Person per E-Mail finden/anlegen, Suppression, Rolle `speaker` je Edition, Pass-Regel R5), `update_speaker`, `set_speaker_pipeline`, `approve_travel_costs` (area_lead_speaker/Admin), `invite_speaker` (ab `confirmed`, Mail `speaker_invite`), `invite_assistant`/`remove_assistant` (Rolle `speaker_assistant`, Mail `assistant_invite`), `manager_speakers(edition?)`, `speaker_next_steps(profile)`. Vokabulare `speaker_type`, `speaker_pipeline`, `hospitality_status`, `hotel_tier`. **Inhalte (0028):** `session_submission` (eingereicht; final bleibt `session`), `my_sessions()`, `submit_session_content` (Speaker/Assistenz, ersetzt offene Einreichung), `approve_session_content(submission, overrides)` (Team/Scope; Sprache der Einreichung entscheidet DE/EN, Overrides gewinnen), `reject_session_content`, `pending_submissions(event?)`. **Uploads (0028):** Bucket `speaker-assets` (privat, 100 MB, PDF/PPT/Keynote/Bilder), Pfad `<edition>/<profile>/<kind>/<datei>`, Policies über `speaker_asset_path_allowed()`; `speaker_asset` (Versionen, `is_current`, **`late` statt Sperre**, Technik-Check, Slid@Home nur mit Consent `slides_publication`), `register_speaker_asset` (nach Upload; prüft Pfad, Objekt, Session), `presentation_window(session)` (Deadline ∧ 48 h vor Slot), `set_slides_release` (nur Speaker), `set_tech_check` (Team), `my_speaker_assets`. Tabelle `deadline` je Edition (Seed `presentation_upload` FLS27 14.04.2027 12:00), `upsert_deadline` (Team). **Hospitality (0030):** `hospitality_quota` (je Edition, hotel mit Tier · shuttle; Kapazität hotel = Zimmer, shuttle = Plätze; Seed Radisson Blu Dammtor/Grand Elysée/The Fontenay + zwei inaktive Shuttles) und `hospitality_booking` (requested/confirmed/waitlisted/cancelled). Gate `hospitality_block_reason()` = Status eligible/requested/booked **und** Consent `hospitality_data`; Hotels nur bis eigenem `hotel_tier`; ein aktives Hotel je Profil, ein Platz je Shuttle; Überbuchung ⇒ `waitlisted`. RPCs `hospitality_options`, `my_hospitality`, `book_hospitality`, `cancel_hospitality` (Wartende rücken nach), Team: `confirm_hospitality` (Mail `hospitality_confirmed`), `decline_hospitality`, `upsert_hospitality_quota`, `hospitality_admin_overview`. **Reisekosten (0031–0033):** `expense_claim` (je Profil; `status` draft/submitted/approved/rejected/paid, `positions` jsonb [{date, category (Vokabular `expense_category`), description, amount_cents, receipt_asset_id}], `amount_cents` = Summe serverseitig, `bank_secret_id` → **Vault**, in der Tabelle nur `bank_masked` (`DE****1234`) + `bank_holder`; `invoice_no` `RK-JJJJ-NNNN` aus `expense_invoice_seq`; `invoice_asset_id` (PDF, Kind `invoice`), Review-/SevDesk-/Qonto-/Zahlungsfelder). Spalten-Grants ohne `bank_secret_id` (kein `select *` per PostgREST; Lesewege sind die RPCs). Gate `expense_eligibility()` = `travel_costs_covered` ∧ `travel_costs_approved_at`. RPCs Speaker/Assistenz: `my_expense_claims()`, `upsert_expense_claim(jsonb)` (Entwurf, Positionsprüfung, Ablehnung ⇒ zurück in Entwurf); **nur Speaker:** `set_expense_bank_details(claim, iban, bic?, holder)` (IBAN-Prüfsumme, `vault.create_secret`), `submit_expense(claim)` (Beleg je Position, Bankdaten Pflicht, Nummer bleibt bei Wiedereinreichung, Mail `expense_submitted` an `area_lead_speaker`, sonst Admins). Freigeber (`is_expense_approver()` = Admin oder `area_lead_speaker`): `expense_queue(edition?)`, `expense_bank_details(claim)` (entschlüsselt, Audit `expense.bank_view`), `approve_expense`/`reject_expense(note)` (Mails `expense_approved`/`expense_rejected`), `mark_expense_paid(claim, ref?)`; `set_expense_integration(claim, invoice_asset?, sevdesk_ref?, qonto_sent?)` für die Server-Route (service_role; kein anon). `speaker_asset.kind` um `receipt`/`invoice` erweitert (`register_speaker_asset` nimmt `receipt`; `invoice` schreibt nur die Route). **Tickets (0034):** Speaker-Ticket entsteht automatisch ab Pipeline `confirmed` (Trigger; `ticket.source = speaker`, Status `requested`, Pass/Lounge aus dem Profil, Ausstellung über vivenu ⇒ `set_ticket_issued` ⇒ `valid`), Begleitticket `request_companion_ticket(profile, email, first, last)` (eins je Speaker ⇒ sonst 23505, gleicher Pass-Typ, ohne Lounge, keine `person` für die Begleitung), `cancel_companion_ticket`; Team: `confirm_companion_ticket` (Mail an Speaker), `decline_companion_ticket(note)`, `speaker_tickets_admin(edition?)`, `ensure_speaker_ticket`; Speaker/Assistenz: `my_speaker_tickets()` (ohne Begleit-Barcode). `hospitality_block_reason` liefert `declined` als eigenen Grund. **Erinnerung + Scope (0035):** `deadline.reminder_lead_hours` (Default 48, Pflege über `upsert_deadline`), `send_presentation_reminders()` im Housekeeping (einmal je Speaker × Session, nur Slot in der Zukunft, ohne aktuelle Präsentation; Vorlage `presentation_reminder`), `my_manager_scope()` (team/all/is_manager, editions/stages/stage_days/slots/owned_profiles). **0036:** `my_speaker_tickets` gibt den Barcode nur dem Speaker selbst (`issued` für die Assistenz). **0037:** `approve_session_content`/`reject_session_content`/`pending_submissions` auch für `can_manage_speaker(speaker_profile_id)` (Lead gibt Inhalte seiner Speaker frei), `manager_speakers` liefert `internal_notes`. **0039:** `expense_bank_details` VOLATILE (Audit in Lesetransaktion), Speaker schreiben nicht in `invoice/`, `expense_queue` mit `currency`. Mail-Übersicht: `docs/mail-plan.md`. Migrationen 0025–0039. |
| Partner (Welle 3) | `product` (SKU = Item-ID `I-nnnnn`, Typ package/addon/shop_item, Kategorie-Vokabular `product_category`, Netto-/Einkaufspreis in Cent, `vat_rate` (vorerst 7), Bestand, Shop-Felder `shop_visible`/`shop_sort`/`late_orderable`/Hinweise, `merch_config`, `images`, Quelle HubSpot/Shop), `product_component` (Stückliste Paket → Teile), `deliverable_template` (Checklisten-Vorlagen je Produkt/Kategorie/alle mit `due_rule`, `file_rules`, `audience_roles`) | Rechte: Partner lesen aktive Produkte über Spalten-Grants **ohne** Einkaufspreis/Marge/Lieferant (kein `select *`), Team über `admin_products()`; Pflege `upsert_product(jsonb)`/`upsert_product_component` nur `is_partner_team()` (Admin, `area_lead_partner`) mit Audit. Import `scripts/import-products.mjs` aus `docs/referenz/item-liste-2026.csv` + `product-bundles-2026.csv` (idempotent, Platzhalter entfallen). Fristen FLS27 für Partner in `deadline` (`booth_backdrop`, `shop_phase_1/2/3`, `ticket_codes`, `lunch_package`). **Org-Kontext (0040):** `org_edition` (je Org × Edition: `onboarding_status` none/invited/filled/call_done, Beschreibung DE/EN, Rechnungsdaten `invoice_email`/`invoice_name`/`vat_id`/`po_number` = Accounting ohne Login, `pass_type_choice` talent/startup, `sponsoring_level`, `hubspot_deal_id`, `notes_internal` nur Team), `org_product` (gebuchte Leistungen mit Menge, Einzelpreis, HubSpot-Line-Item, Status), `org_membership` + `roles` aus Vokabular `contact_role` (`primary_ops` genau einer je Org — partieller Unique-Index, `additional`, `signing`, `event_app_member`) + `contact_position`; Portal-Zugang über `role_assignment partner_contact` (Scope org). Helfer `partner_roles(org)`, `is_partner_of`, `partner_can_edit` (primary/additional/signing/Team), `partner_can_manage_contacts` (primary/Team), `is_partner_team()` (Admin, `area_lead_partner`). RPCs: `my_partner_orgs()`, `partner_overview(org, edition?)` (Preise/Rechnungsdaten nur für Ops/Signing/Team), `partner_contacts(org)`, `upsert_partner_contact(org, email, first, last, roles[], position?, edition?)` (Person per E-Mail, Suppression, Membership, Rolle, Mail `partner_contact_invite`), `set_contact_roles`, `transfer_primary_contact`, `remove_partner_contact` (Rolle weg, Person bleibt), `update_partner_onboarding(org, jsonb)` (Whitelist, setzt `filled`), Team: `partner_set_onboarding_status`, `partner_admin_overview(edition?)`. **Checkliste, Uploads, Stand (0041):** `deliverable` (je `org_edition` × `deliverable_template`; Status open/submitted/accepted/rejected/overdue/not_required, `due_at` aus `due_rule` — `deadline_key` → `deadline`, `offset_days` ab Einladung —, `asset_ids`, `answers`, Review-Felder) entsteht **ausschließlich per Trigger** aus gebuchten `org_product`-Zeilen (`sync_deliverables`: Vorlage global / Kategorie / SKU; Storno ⇒ `not_required`, erneute Buchung ⇒ `open`, Eingereichtes bleibt); Team `resync_deliverables(edition?)` nach Vorlagenänderung, `overdue` setzt das Housekeeping (A5). Bucket `partner-assets` (privat, 50 MB, Pfad `<edition>/<org>/<kind>/<datei>`, Policies über `partner_asset_path_allowed(name, write)`: lesen alle Mitglieder, schreiben `primary_ops`/`additional`/`signing`/Team). `partner_asset` (versioniert je Org-Edition × Art × Pflicht, `is_current`, Status pending/accepted/rejected); `register_partner_asset(org, kind, path, filename, mime?, size?, deliverable?, edition?)` prüft Pfadpräfix, Objekt und die `file_rules` der Vorlage (Endung, MIME, Größe; Logo ohne Vorlage: svg/eps/ai/pdf). Onboarding wird erst `filled`, wenn zusätzlich ein aktuelles `logo_vector` liegt (`partner_onboarding_recheck`). RPCs: `my_deliverables(org, edition?)`, `submit_deliverable(id, asset_ids, answers)` (Upload braucht Datei, Formular Antworten; Mail `partner_deliverable_received` an Einreichende + `primary_ops`), Team `review_deliverable(id, accepted, note)` (Ablehnung nur mit Grund ⇒ `partner_deliverable_rejected`; setzt auch den Datei-Status), `partner_review_queue(edition?)`, `upsert_deliverable_template(jsonb)`; `booth` (Nummer, Typ, Segment, Fläche, Rückwand-Maße in mm, `notes` nur Team) über `upsert_booth(org, jsonb, edition?)`; `partner_overview` liefert `booth` (ohne Notizen) und `checklist` {total, done, open, rejected, overdue, next_due}, `partner_admin_overview` zählt offene/eingereichte/überfällige Pflichten. Seeds FLS27: `logo_vector` (global, svg/eps/ai/pdf), `backdrop_print` (Premium I-39709, General I-50131, Intro I-39740, Lounge I-79031; PDF; Frist `booth_backdrop`), `lunch_package` (Kategorie `standflaeche`, Typ booking, Frist `lunch_package`; Shop-Artikel I-79520 `late_orderable`), `digital_branding` (I-95690), `ticket_codes` (Kategorie `tickets`, Info, nicht Pflicht). Migrationen 0038, 0040, 0041, 0042. |
| Vokabular | `vocab_term` (33 Vokabulare, 309 Begriffe) | — |
| Migration/Dedup (P0) | `import.staging_contact`, `import.source_person_map`, `potential_duplicate`, `person_merge_log` | — |

## Status-Maschinen
- **Slot** (`slot_status`, Farbe im Board): open → requested → confirmed_title_open → final · unused.
- **Session** (`publish_status`): draft → review → published → cancelled. Pflichtfelder beim Veröffentlichen: Slot (Bühne/Raum), Titel DE+EN, Beschreibung.
- **Bewerbung**: applied → shortlisted → accepted → confirmed → attended / no_show · waitlisted → promoted (→ confirmed) · declined · expired (Frist `confirm_by` = Freigabe + `session.confirm_by_hours`) · withdrawn. Sichtbarkeit von Zusagen/Absagen erst nach `decision_release`.
- **Ticket**: valid → checked_in · cancelled / refunded / blocked; `personalization_status` pending → partial → complete.

## Fehlercodes für das Frontend
`42501` nicht erlaubt · `23P01` Überlappung auf Bühne · `23514` Pflichtfeld/Regel verletzt · `23505` bereits vorhanden (Doppelbewerbung) · `P0001` fachliche Ablehnung mit `message` = Schlüssel (`confirmation_required`, `deadline_passed`, `not_eligible` + detail, `not_released`, `ticket_required`, `collision` + detail = kollidierende IDs, `confirm_deadline_passed`, `cannot_withdraw`, `not_confirmable`) · `P0002` nicht gefunden.

## Startdaten (Seed)
Edition `fls27` mit `summit-27` (16./17.04.2027, CCH) und `hackathon-27` (15./16.04.2027), vier Tage, fünf Bühnen 2027 (Main, Leadership & Growth, Industry, Startup, Impact & Tech) mit Wechselzeit/Standarddauer aus der FLS26-Analyse, `stage_day` je Bühne × Tag (Öffnungszeiten offen), Fragenkatalog (motivation, expectations, linkedin, cv_upload), Mail-Templates `test`/`welcome` DE+EN.

## Tests
SQL-Smoke-Tests laufen als Transaktion mit Rollback über den Supabase-MCP (`execute_sql`); Vorlagen liegen in `supabase/tests/`. Vor jedem Merge einer Migration: Tests + Security-Advisor ohne ERROR.

## Ergänzung 08.09. abends — Programm-Editor-Backend (Migration `20260908182849_v2_programme_editor`)
- **Views:** `programme_board` (ein Tag, alle Bühnen: Slot × Session × Speaker, `can_edit` für den Aufrufer) und `programme_backlog` (Sessions ohne Slot). Speaker-Namen kommen über `session_speakers_public()` nur für sichtbare Sessions.
- **Rechte:** `is_programme_editor(event_id)` (Programm-Team/Admin, Edition oder global) · `can_edit_session(session_id)` (Editor, oder Slot im eigenen Scope, oder eigene Backlog-Session als Speaker-Manager/Standbühnen-Editor).
- **RPCs:** `upsert_session(jsonb)` (anlegen/ändern, Teilupdate über vorhandene Schlüssel; Textfelder: Schlüssel vorhanden + leer ⇒ NULL — seit 0018) · `set_session_speakers(session_id, jsonb[])` (ersetzt komplett; `confirmed` bleibt erhalten, wenn der Aufrufer es nicht mitschickt — seit 0018) · `attach_session_to_slot` / `detach_session` (Detach nur unveröffentlicht: `unpublish_first`) · `publish_session` (nur Programm-Team; Pflichtfelder + mindestens ein Speaker bei Inhaltsformaten; setzt Slot auf `final`) · `unpublish_session(id, reason)`. Alle mit Audit-Log; Attach/Detach zusätzlich in `slot_history`.
- **Admin-RPCs (0022, für B6):** `applications_for_session(session_id)` (Entscheider per `can_decide_session`; Gastgeber-Org ohne `consent_share` sieht weder Name noch Profil noch Antworten) · `applications_overview(event_id?)` (Sessions mit Zählern je Status, `released`) · `roles_of_person(person_id)`, `assign_role(person_id, role, scope_type, scope_id?, edition_id?, portal?, valid_from?, valid_to?, note?)` (Admin; Vokabular-Prüfung; gleiche Rolle im gleichen Scope wird reaktiviert), `revoke_role(assignment_id, note?)` (Ablaufdatum statt Löschen; letzter globaler Admin geschützt) · `search_people(query, limit?)` (Team; E-Mail nur Admin). Audit `role.assign`/`role.revoke` unter dem Actor der Session.
- **Fehlercodes ergänzt:** `slot_occupied` (23505), `unpublish_first` (P0001), `publish requires at least one speaker` (23514).
- Getestet: 13 Prüfungen (Scope über Bühne, Backlog-Rechte, Veröffentlichungsregeln, Detach-Sperre, Audit/History).

## Ergänzung 08.09. Nacht — Board-Realtime, Fragen-RPCs, Demo-Programm (Migration `20260908194632_v2_board_realtime_questions`)
- **Realtime aus der Datenbank:** Trigger auf `slot`, `session`, `session_speaker` senden über `realtime.send()` ein Ereignis `changed` mit minimaler Payload `{table, id, op}` auf den **privaten** Kanal `programme-board:<event_id>`. Clients senden nichts selbst; empfangen dürfen nur Programm-Leser (Policy `programme_board_receive` auf `realtime.messages`). Keine Postgres-Changes-Publikation für `slot`, weil Realtime interne Spalten nicht spaltenweise filtert.
- **Fragen je Session:** `set_session_questions(session_id, jsonb[], p_replace_custom = false)` ersetzt die Katalogfragen und lässt eigene Fragen stehen; eigene Fragen sind erst nach `approve_session_questions(session_id)` (Programm-Team) freigegeben, außer ein Programm-Editor legt sie selbst an.
- **Programmzeiten Summit 27** (Vorschlag nach FLS26): Fr Einlass 12:00, Programm 13:00–20:30; Sa Einlass 11:00, Programm 12:00–19:30; `stage_day.open_from/open_to` entsprechend. Änderbar im Editor.
- **Demo-Programm:** `supabase/seed/demo_programme.sql` legt 6 Demo-Personen, 13 Slots und 8 Sessions für Freitag an (Opening, Keynotes, Panel, Platzhalter, Company Tour mit Bewerbung + u35, Get-together mit Kapazität 2, Backlog-Talk). Idempotent; Lösch-Block oben; vor Go-live entfernen.
