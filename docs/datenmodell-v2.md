# Datenmodell v2 — Überblick (Stand 08.09.2026, nach Welle-0-Migrationen)

> Menschlich lesbare Ergänzung zur generierten Schema-Doku (`docs/schema.md`, Build-Session B11). Quelle der Wahrheit sind die Migrationen unter `supabase/migrations/`. Änderungen nur per Migration und Eintrag im Entscheidungslog.

## Prinzipien
- **RLS auf jeder Tabelle**, default deny. Nutzer (`authenticated`) lesen nur eigene oder veröffentlichte Daten; Spalten-Whitelists blenden interne Felder aus (`slot.internal_*`, `application.status/rank`, `stage.stage_lead_person_id`).
- **Schreiben nur über RPCs** (SECURITY DEFINER, `search_path` gepinnt, Autorisierung im Funktionsrumpf) oder serverseitig über `service_role` nach `requireRole()`. `anon` kann keine SECURITY-DEFINER-Funktion aufrufen.
- **Rollen mit Scope** (`role_assignment`): global · edition · portal · org · stage · stage_day · slot; edition-gebunden, zeitlich begrenzbar. Helper: `has_role()`, `is_admin()`, `is_staff()`, `my_roles()`, `can_edit_stage()`, `can_edit_slot()`, `can_decide_session()`, `is_member_of_org()`.
- **Audit**: `audit_log` über `log_audit()` für Rollen, Slots, Entscheidungen, Ticket-Personalisierung, Profil-Löschung.
- **Löschen = Anonymisieren** (`delete_my_profile()`): PII entfernt, Historie pseudonym, Adressen gehasht in `suppression`, Auth-User danach serverseitig gelöscht.

## Domänen und Tabellen
| Domäne | Tabellen | Kern-RPCs / Views |
|---|---|---|
| Identität | `person` (+ title, city, pronouns, photo_url, tier, deleted_at), `person_email`, `organization` (+ type, slug, website, active, sevdesk_contact_id), `org_membership`, `role_assignment`, `consent_record`, `suppression`, `audit_log`, `staff_user` (Legacy) | `claim_or_create_person`, `set_primary_email`, `delete_my_profile`, View `consent_current` **Login-Regel:** Die Verbindung Auth-Konto ↔ `person` entsteht erst beim ersten Login über `claim_or_create_person` (Callback). Personen, die Team oder Skripte anlegen (z. B. `upsert_speaker`, Partner-Ingest), haben bis dahin `auth_user_id = null`; für sie liefert `current_person_id()` NULL und jede RPC antwortet 28000 — im Portal unerreichbar, für Skripte und Automationen die Regel: entweder mit service_role arbeiten oder den Claim vorher ausführen. |
| Edition & Programm | `event` (+ slug, is_edition, edition_id, timezone, venue, status), `event_day`, `stage`, `stage_day`, `track`, `slot`, `session`, `session_speaker`, `slot_history` | `create_slot`, `move_slot` (Warnungen: before_open, after_close, off_grid_5min, changeover_short, speaker_conflict; hart: Überlappung je Bühne 23P01, Bestätigung nach Veröffentlichung P0001), `set_slot_status`, Views `programme_public`, `stage_day_slot_stats` |
| Bewerbung & Anmeldung | `question_catalog`, `session_question` (max. 2 eigene), `application`, `decision_release`, `registration` (+ session_id) | Talent: `apply_to_session`, `withdraw_application`, `confirm_application` (Ticketpflicht, Kollision → `p_replace_conflicting`), `register_for_session`, `cancel_registration`, `my_applications()` (maskiert bis Freigabe) · Entscheider: `decide_application`, `release_decisions`, `promote_waitlist`, `expire_overdue_applications` |
| Tickets | `ticket_type_map`, `ticket`, `org_ticket_allocation`, `checkin` | `personalize_ticket` (Käufer/Inhaber, für mich oder andere Person) **Freitickets aus dem Speaker-Portal (0034):** `status` zusätzlich `requested`/`approved`, `barcode` bis zur Ausstellung NULL (bleibt unique), `speaker_profile_id`, `lounge_access`, `team_note`, `requested_by`, `approved_by/at`; `source` `speaker` bzw. `speaker_companion`, je Profil ein aktives eigenes und ein aktives Begleitticket (partielle Unique-Indizes). |
| Integration | `integration.webhook_event` (unique Quelle+ID), `integration.sync_job`, `integration.sync_error`, `external_ref` | — (Route Handler schreiben über service_role) |
| Kommunikation | `mail_template` (key × locale), `mail_log` (auch **Warteschlange**: Status `queued` + `meta.vars`, `related_type`/`related_id`) | `is_suppressed()` (nur service_role) · `queue_mail()` (intern: Empfängerin, Sprache, Suppression, Dedupe je Vorlage × Person × Bezugsobjekt, solange `queued`; 0042) · Trigger `trg_application_mail`, `trg_decision_release_mail`, `trg_registration_mail` legen die Aufträge an · `run_application_housekeeping()` (Cron: Fristen ablaufen lassen, Warteliste nachrücken) · `session_mail_vars()`, `mail_fmt_ts()` · Versand: `lib/mail/queue.ts` über `/api/cron/mail` (Vercel Cron alle 10 Min., Resend, Retry 3×, Idempotenz-Schlüssel je Zeile) |
| Speaker (Welle 2) | `speaker_profile` (je Edition: `speaker_type`, `pipeline_status`, Owner/Lead, Bio EN/DE, Socials, Tech-Rider, Staff-Flags `reception_eligible`, `lounge_access`, `pass_type`, `hotel_tier`, `hospitality_status`, `travel_costs_covered` + finale Freigabe, `assistant_person_id`, `internal_notes`, `invited_at`) | Rechte: `is_speaker_team()`, `can_manage_speaker(profile)` (Team · Owner/Ersteller · Edition-Scope · Speaker auf Session in Bühne/Bühne×Tag/Slot des Scopes); RLS-Select nur für Manager/Team, Speaker lesen über `my_speaker_profile()`. RPCs: `update_my_speaker_profile(jsonb)` (Speaker/Assistenz, Whitelist), `upsert_speaker(jsonb)` (Manager/Team; Person per E-Mail finden/anlegen, Suppression, Rolle `speaker` je Edition, Pass-Regel R5), `update_speaker`, `set_speaker_pipeline`, `approve_travel_costs` (area_lead_speaker/Admin), `invite_speaker` (ab `confirmed`, Mail `speaker_invite`), `invite_assistant`/`remove_assistant` (Rolle `speaker_assistant`, Mail `assistant_invite`), `manager_speakers(edition?)`, `speaker_next_steps(profile)`. Vokabulare `speaker_type`, `speaker_pipeline`, `hospitality_status`, `hotel_tier`. **Inhalte (0028):** `session_submission` (eingereicht; final bleibt `session`), `my_sessions()`, `submit_session_content` (Speaker/Assistenz, ersetzt offene Einreichung), `approve_session_content(submission, overrides)` (Team/Scope; Sprache der Einreichung entscheidet DE/EN, Overrides gewinnen), `reject_session_content`, `pending_submissions(event?)`. **Uploads (0028):** Bucket `speaker-assets` (privat, 100 MB, PDF/PPT/Keynote/Bilder), Pfad `<edition>/<profile>/<kind>/<datei>`, Policies über `speaker_asset_path_allowed()`; `speaker_asset` (Versionen, `is_current`, **`late` statt Sperre**, Technik-Check, Slid@Home nur mit Consent `slides_publication`), `register_speaker_asset` (nach Upload; prüft Pfad, Objekt, Session), `presentation_window(session)` (Deadline ∧ 48 h vor Slot), `set_slides_release` (nur Speaker), `set_tech_check` (Team), `my_speaker_assets`. Tabelle `deadline` je Edition (Seed `presentation_upload` FLS27 14.04.2027 12:00), `upsert_deadline` (Team). **Hospitality (0030):** `hospitality_quota` (je Edition, hotel mit Tier · shuttle; Kapazität hotel = Zimmer, shuttle = Plätze; Seed Radisson Blu Dammtor/Grand Elysée/The Fontenay + zwei inaktive Shuttles) und `hospitality_booking` (requested/confirmed/waitlisted/cancelled). Gate `hospitality_block_reason()` = Status eligible/requested/booked **und** Consent `hospitality_data`; Hotels nur bis eigenem `hotel_tier`; ein aktives Hotel je Profil, ein Platz je Shuttle; Überbuchung ⇒ `waitlisted`. RPCs `hospitality_options`, `my_hospitality`, `book_hospitality`, `cancel_hospitality` (Wartende rücken nach), Team: `confirm_hospitality` (Mail `hospitality_confirmed`), `decline_hospitality`, `upsert_hospitality_quota`, `hospitality_admin_overview`. **Reisekosten (0031–0033):** `expense_claim` (je Profil; `status` draft/submitted/approved/rejected/paid, `positions` jsonb [{date, category (Vokabular `expense_category`), description, amount_cents, receipt_asset_id}], `amount_cents` = Summe serverseitig, `bank_secret_id` → **Vault**, in der Tabelle nur `bank_masked` (`DE****1234`) + `bank_holder`; `invoice_no` `RK-JJJJ-NNNN` aus `expense_invoice_seq`; `invoice_asset_id` (PDF, Kind `invoice`), Review-/SevDesk-/Qonto-/Zahlungsfelder). Spalten-Grants ohne `bank_secret_id` (kein `select *` per PostgREST; Lesewege sind die RPCs). Gate `expense_eligibility()` = `travel_costs_covered` ∧ `travel_costs_approved_at`. RPCs Speaker/Assistenz: `my_expense_claims()`, `upsert_expense_claim(jsonb)` (Entwurf, Positionsprüfung, Ablehnung ⇒ zurück in Entwurf); **nur Speaker:** `set_expense_bank_details(claim, iban, bic?, holder)` (IBAN-Prüfsumme, `vault.create_secret`), `submit_expense(claim)` (Beleg je Position, Bankdaten Pflicht, Nummer bleibt bei Wiedereinreichung, Mail `expense_submitted` an `area_lead_speaker`, sonst Admins). Freigeber (`is_expense_approver()` = Admin oder `area_lead_speaker`): `expense_queue(edition?)`, `expense_bank_details(claim)` (entschlüsselt, Audit `expense.bank_view`), `approve_expense`/`reject_expense(note)` (Mails `expense_approved`/`expense_rejected`), `mark_expense_paid(claim, ref?)`; `set_expense_integration(claim, invoice_asset?, sevdesk_ref?, qonto_sent?)` für die Server-Route (service_role; kein anon). `speaker_asset.kind` um `receipt`/`invoice` erweitert (`register_speaker_asset` nimmt `receipt`; `invoice` schreibt nur die Route). **Tickets (0034):** Speaker-Ticket entsteht automatisch ab Pipeline `confirmed` (Trigger; `ticket.source = speaker`, Status `requested`, Pass/Lounge aus dem Profil, Ausstellung über vivenu ⇒ `set_ticket_issued` ⇒ `valid`), Begleitticket `request_companion_ticket(profile, email, first, last)` (eins je Speaker ⇒ sonst 23505, gleicher Pass-Typ, ohne Lounge, keine `person` für die Begleitung), `cancel_companion_ticket`; Team: `confirm_companion_ticket` (Mail an Speaker), `decline_companion_ticket(note)`, `speaker_tickets_admin(edition?)`, `ensure_speaker_ticket`; Speaker/Assistenz: `my_speaker_tickets()` (ohne Begleit-Barcode). `hospitality_block_reason` liefert `declined` als eigenen Grund. **Erinnerung + Scope (0035):** `deadline.reminder_lead_hours` (Default 48, Pflege über `upsert_deadline`), `send_presentation_reminders()` im Housekeeping (einmal je Speaker × Session, nur Slot in der Zukunft, ohne aktuelle Präsentation; Vorlage `presentation_reminder`), `my_manager_scope()` (team/all/is_manager, editions/stages/stage_days/slots/owned_profiles). **0036:** `my_speaker_tickets` gibt den Barcode nur dem Speaker selbst (`issued` für die Assistenz). **0037:** `approve_session_content`/`reject_session_content`/`pending_submissions` auch für `can_manage_speaker(speaker_profile_id)` (Lead gibt Inhalte seiner Speaker frei), `manager_speakers` liefert `internal_notes`. **0039:** `expense_bank_details` VOLATILE (Audit in Lesetransaktion), Speaker schreiben nicht in `invoice/`, `expense_queue` mit `currency`. Mail-Übersicht: `docs/mail-plan.md`. Migrationen 0025–0039. |
| Partner (Welle 3) | `product` (SKU = Item-ID `I-nnnnn`, Typ package/addon/shop_item, Kategorie-Vokabular `product_category`, Netto-/Einkaufspreis in Cent, `vat_rate` (vorerst 7), Bestand, Shop-Felder `shop_visible`/`shop_sort`/`late_orderable`/Hinweise, `merch_config`, `images`, Quelle HubSpot/Shop), `product_component` (Stückliste Paket → Teile), `deliverable_template` (Checklisten-Vorlagen je Produkt/Kategorie/alle mit `due_rule`, `file_rules`, `audience_roles`) | Rechte: Partner lesen aktive Produkte über Spalten-Grants **ohne** Einkaufspreis/Marge/Lieferant (kein `select *`), Team über `admin_products()`; Pflege `upsert_product(jsonb)`/`upsert_product_component` nur `is_partner_team()` (Admin, `area_lead_partner`) mit Audit. Import `scripts/import-products.mjs` aus `docs/referenz/item-liste-2026.csv` + `product-bundles-2026.csv` (idempotent, Platzhalter entfallen). Fristen FLS27 für Partner in `deadline` (`booth_backdrop`, `shop_phase_1/2/3`, `ticket_codes`, `lunch_package`). **Org-Kontext (0040):** `org_edition` (je Org × Edition: `onboarding_status` none/invited/filled/call_done, Beschreibung DE/EN, Rechnungsdaten `invoice_email`/`invoice_name`/`vat_id`/`po_number` = Accounting ohne Login, `pass_type_choice` talent/startup, `sponsoring_level`, `hubspot_deal_id`, `notes_internal` nur Team), `org_product` (gebuchte Leistungen mit Menge, Einzelpreis, HubSpot-Line-Item, Status), `org_membership` + `roles` aus Vokabular `contact_role` (`primary_ops` genau einer je Org — partieller Unique-Index, `additional`, `signing`, `event_app_member`) + `contact_position`; Portal-Zugang über `role_assignment partner_contact` (Scope org). Helfer `partner_roles(org)`, `is_partner_of`, `partner_can_edit` (primary/additional/signing/Team), `partner_can_manage_contacts` (primary/Team), `is_partner_team()` (Admin, `area_lead_partner`). RPCs: `my_partner_orgs()`, `partner_overview(org, edition?)` (Preise/Rechnungsdaten nur für Ops/Signing/Team), `partner_contacts(org)`, `upsert_partner_contact(org, email, first, last, roles[], position?, edition?)` (Person per E-Mail, Suppression, Membership, Rolle, Mail `partner_contact_invite`), `set_contact_roles`, `transfer_primary_contact`, `remove_partner_contact` (Rolle weg, Person bleibt), `update_partner_onboarding(org, jsonb)` (Whitelist, setzt `filled`), Team: `partner_set_onboarding_status`, `partner_admin_overview(edition?)`. **Checkliste, Uploads, Stand (0041):** `deliverable` (je `org_edition` × `deliverable_template`; Status open/submitted/accepted/rejected/overdue/not_required, `due_at` aus `due_rule` — `deadline_key` → `deadline`, `offset_days` ab Einladung —, `asset_ids`, `answers`, Review-Felder) entsteht **ausschließlich per Trigger** aus gebuchten `org_product`-Zeilen (`sync_deliverables`: Vorlage global / Kategorie / SKU; Storno ⇒ `not_required`, erneute Buchung ⇒ `open`, Eingereichtes bleibt); Team `resync_deliverables(edition?)` nach Vorlagenänderung, `overdue` setzt das Housekeeping (A5). Bucket `partner-assets` (privat, 50 MB, Pfad `<edition>/<org>/<kind>/<datei>`, Policies über `partner_asset_path_allowed(name, write)`: lesen alle Mitglieder, schreiben `primary_ops`/`additional`/`signing`/Team). `partner_asset` (versioniert je Org-Edition × Art × Pflicht, `is_current`, Status pending/accepted/rejected); `register_partner_asset(org, kind, path, filename, mime?, size?, deliverable?, edition?)` prüft Pfadpräfix, Objekt und die `file_rules` der Vorlage (Endung, MIME, Größe; Logo ohne Vorlage: svg/eps/ai/pdf). Onboarding wird erst `filled`, wenn zusätzlich ein aktuelles `logo_vector` liegt (`partner_onboarding_recheck`). RPCs: `my_deliverables(org, edition?)`, `submit_deliverable(id, asset_ids, answers)` (Upload braucht Datei, Formular Antworten; Mail `partner_deliverable_received` an Einreichende + `primary_ops`), Team `review_deliverable(id, accepted, note)` (Ablehnung nur mit Grund ⇒ `partner_deliverable_rejected`; setzt auch den Datei-Status), `partner_review_queue(edition?)`, `upsert_deliverable_template(jsonb)`; `booth` (Nummer, Typ, Segment, Fläche, Rückwand-Maße in mm, `notes` nur Team) über `upsert_booth(org, jsonb, edition?)`; `partner_overview` liefert `booth` (ohne Notizen) und `checklist` {total, done, open, rejected, overdue, next_due}, `partner_admin_overview` zählt offene/eingereichte/überfällige Pflichten. Seeds FLS27: `logo_vector` (global, svg/eps/ai/pdf), `backdrop_print` (Premium I-39709, General I-50131, Intro I-39740, Lounge I-79031; PDF; Frist `booth_backdrop`), `lunch_package` (Kategorie `standflaeche`, Typ booking, Frist `lunch_package`; Shop-Artikel I-79520 `late_orderable`), `digital_branding` (I-95690), `ticket_codes` (Kategorie `tickets`, Info, nicht Pflicht). **HubSpot-Ingest (0043/0044):** `event.hubspot_pipeline_id`/`hubspot_onboarding_stage_id` (Team-RPC `set_edition_hubspot`, Liste `hubspot_editions`), `partner_deal` (jeder verarbeitete Deal je Org-Edition, deny-all für die API; Team `partner_deals(org)`), `product.pass_type` (Ticket-Produkte ⇒ `org_ticket_allocation`) und `product.grants_role` (Bühnenprodukt ⇒ `standbuehne_editor` Scope **org** für `primary_ops`; `can_edit_stage`/`can_edit_slot` akzeptieren `stage.partner_org_id`). RPC `ingest_partner_deal(jsonb)` nur service_role: Gate (Pflichtfelder, genau ein Hauptkontakt, bekannte SKUs, Rollen-Vokabular, `primary_conflict`) ⇒ bei Fehler `integration.sync_error` + Mail `partner_gate_failed` (Owner, Fallback `notify_partner_leads`), sonst Org (nur ergänzen), `org_edition invited`, `org_product`, Kontingent, Kontakte über `partner_contact_upsert_internal` (gemeinsam mit `upsert_partner_contact`; Rolle `partner_contact` bis Editionsende), Audit. Integrations-RPCs für Routen (service_role): `record_webhook_event`/`finish_webhook_event`, `start_sync_job`/`finish_sync_job`/`record_sync_error`, `hubspot_deals_ingested`; Team: `partner_ingest_log`, `resolve_sync_error`. **Housekeeping (0045/0046):** `run_partner_housekeeping()` (intern, aus `run_application_housekeeping()` im Mail-Cron): `refresh_deliverable_due` (Fälligkeiten folgen geänderten Fristen), `mark_overdue_deliverables` (`open`↔`overdue`), `send_partner_reminders` (Digest `partner_reminder_digest` je Org × Edition an `primary_ops` + `additional`, höchstens alle 7 Tage; Inhalt aus `partner_digest_items`). **Bewerber-Auswahl und Standbühne (0047):** `can_decide_session` für die Host-Org nur mit aktiver Rolle `partner_contact` und Kontaktrolle primary_ops/additional/signing (Mitgliedschaft allein oder `event_app_member` reicht nicht); `partner_sessions(org)` (Sessions mit `host_org_id` und Zählern, keine Personendaten), `partner_applications(session)` (gleicher Consent-Filter wie `applications_for_session`, **jeder Abruf im Audit** `application.partner_view` — deshalb VOLATILE), Entscheidung über `decide_application`, Versand nur `release_decisions` (Team); `my_partner_stages()` (Bühnen mit `stage.partner_org_id` für `standbuehne_editor`, Scope org oder stage). **Messeshop (0048):** `shop_order` (Org × Edition × Phase, `MS-JJJJ-NNNN`, Status draft/pending/editing/completed/cancelled, eine aktive je Org und Phase), `shop_order_line` (Snapshots Name/Kategorie/Einheit/USt/Netto, `merch_config`), `stock_ledger` (append-only, verfügbar = `stock_total` + Σ delta, nur für `track_stock`), `shop_request` (Anfrage-Produkte = Nettopreis 0). Phasen aus `deadline` (`shop_phase_1/2/3`; Phase 3 nur `late_orderable`), serverseitig in jeder Schreib-RPC (P0001 `phase_closed`/`late_only`/`order_pending`/`out_of_stock`). RPCs Partner: `shop_phase_info`, `shop_catalogue` (`orderable`, `request_only`, `stock_available`), `shop_upsert_line`, `shop_remove_line`, `shop_confirm` (Snapshots aktualisieren, Reservierung delta-bewusst, Mail `shop_order_confirmed`), `shop_edit`, `shop_cancel` (Freigabe), `shop_my_orders`, `shop_request_product` (intern `shop_request_received`); Housekeeping `run_shop_finalization` (pending/editing ⇒ completed + `shop_order_completed`, Entwürfe ⇒ cancelled, idempotent); Team: `shop_orders_admin`, `shop_admin_set_line`, `shop_admin_set_status`, `shop_requests_admin`, `shop_request_answer`, `shop_report` (nur completed). Rechte: bestellen `primary_ops`/`additional`/`signing` (`partner_can_edit`), lesen alle Mitglieder; Lagerbuch nur intern. **Ticket-Kontingente (0049):** `org_ticket_allocation` + `org_edition_id`, `status` pending_vivenu/active/error/disabled, `vivenu_coupon_id`, `vivenu_undershop_id`, `last_error`, `synced_at`; entsteht per Trigger aus Ticket-Produkten (`sync_ticket_allocations`, `effective_pass_type`: talent ⇒ `pass_type_choice` bzw. Org-Typ), `event.vivenu_event_id` (`set_edition_vivenu`). Partner `my_ticket_allocations` (Code erst bei `active`), `request_ticket_increase` (⇒ `shop_request` + interne Mail); Team `ticket_allocations_admin`, `set_ticket_allocation`; Route (service_role) `ticket_allocations_pending`, `set_ticket_allocation_vivenu`. Schreibgrants für `authenticated` auf `org_ticket_allocation`/`ticket_type_map` entzogen. **Produktbilder (0050):** Bucket `product-images` (öffentlich lesbar, 10 MB, nur Bildtypen; Schreiben nur service_role), `product.images` = `[{path, url, name, type, size}]`, Import `scripts/import-product-images.mjs` aus einem Manifest (Airtable-Anhänge über den Connector). `partner_overview` liefert seit 0051 zusätzlich `sessions_count`, `has_stage` und den Status der Kontingente. **SevDesk-Entwürfe (0052):** `shop_invoice_candidates(edition)` (Team; completed-Bestellungen ohne Referenz, Positionen je SKU zusammengefasst), `record_shop_invoice(org, orders[], invoice_id, contact_id?)` (Referenz je Bestellung in `external_ref` system `sevdesk`/object_type `shop_order`, `external_id` = `<Rechnung>#<Bestellnummer>`), `set_org_sevdesk_contact`, `shop_invoice_refs`; `external_ref` ohne Grants für anon/authenticated. **Pflichten-Nachträge (0053/0054):** `deliverable_template.answers_schema` (Feldliste für Formular-Pflichten, `submit_deliverable` prüft `required` ⇒ P0001 `answers_incomplete`), `deliverable_template.fulfilled_by_sku` (bestätigte Shop-Bestellung setzt die Pflicht per Trigger auf `accepted`, Storno öffnet sie wieder; Partner dürfen sie nicht manuell einreichen, P0001 `fulfilled_by_order`), `my_partner_assets(org, edition?)` (alle Fassungen mit Pflicht-Schlüssel und -Beschriftung). **Event-App (0055):** `event.swapcard_event_id` (`set_edition_swapcard`), Export `event_app_exhibitors(edition?)` (service_role/Team; Name, Beschreibung, Website, Level, Stand, freigegebenes Logo, `event_app_member`-Kontakte), Referenz `set_event_app_ref` in `external_ref` (system `swapcard`, object_type `exhibitor`, object_id = `org_edition`). **0056:** `upsert_deliverable_template` synchronisiert laufende Editionen selbst; `applications_for_session` gibt `person_id` ohne Einwilligung nur dem Team. **0057:** Logo in zwei Pflichten (`logo_vector` nur SVG, `logo_png`), `filled` braucht beide; öffentlicher Bucket `partner-logos` (nur freigegebene Logos, Schreiben service_role); `event_app_exhibitors` mit `logo_svg_path`, `logo_png_path`, `logo_png_asset_id`. **0058:** `sync_granted_roles(org)` + Trigger (Rollen aus `product.grants_role` für Hauptkontakte, `note = auto:product`), `stage_editor_orgs`, `upsert_session` prüft `host_org_id` für Bühnen-Editoren, `event.hubspot_done_stage_id` + `set_edition_hubspot(…, done_stage?)`. **0059/0060:** Vokabular `organization_type` + `foundation` (Stiftung) und `service` (Service / Kooperation). **0061:** `shop_upsert_line` prüft den Bestand sofort (eigene Reservierung zählt nicht gegen sich). **0062:** `set_external_ref`/`list_external_refs` (Systeme sanity, swapcard; service_role oder Team) — Sanity merkt sich je Org×Edition Dokument-ID und veröffentlichte Logo-Fassung. **0063:** `admin_products` nur Partner-Team; Welle-1-Grants aufgeräumt (anon nur `vocab_term` lesen, authenticated ohne tote Schreib-/Lese-Grants). **Merch-Konfiguration (0064):** Schema am Produkt (`product.merch_config`: Feldliste mit `key`, `type` text/textarea/number/select/boolean/date/`logo`/`sizes`, `required`, `options`, `max_length`; leere Liste = kein Merch-Artikel), Antwort an der Zeile (`shop_order_line.merch_config`, bleibt bei reiner Mengenänderung erhalten). Helfer `merch_fields(jsonb)` und `merch_problem(schema, werte, menge)` (intern, ohne Grants); `shop_confirm` weist eine unvollständige Zeile mit P0001 `merch_incomplete`, detail `<sku>:<feldschlüssel>` ab — eine Größenverteilung muss die Bestellmenge genau treffen. Pflege über `upsert_product`, Oberfläche `lib/partner/merch.ts`. Migrationen 0038, 0040–0064. **0097** (`20260915112530`, 15.09.): Vokabular `sponsoring_level` (HubSpot-Standtyp ohne Größe als Schlüssel, `sort_order` = Rang für die Logo-Wand), `sponsoring_level_key(text)` (intern), `event_app_exhibitors` zusätzlich mit `sponsoring_key`/`sponsoring_rank`; Pflicht `logo_vector` verlangt Transparenz. Sanity-Dokument v2: ID `portalPartnerLogo-<org_id>` (Punkt = privater Pfad), `sponsoringRank`, `logoTransparent` (Runbook). |
| Volunteers (Welle 4) | `volunteer_profile` (Person × Edition; Status applied/accepted/declined/withdrawn, `shirt_size` aus Vokabular `shirt_size`, `areas[]` aus Vokabular `volunteer_area` — Liste folgt aus dem Airtable-Export 2026 und wird über den Vokabular-Admin gepflegt —, `day_prefs[]` (event_day), `availability` jsonb, Buddy-Wunsch, `notes_internal` nur Team), `shift` (Edition × Tag, Bereich, Position, Zeitraum, `capacity` + `overbook`, Ort, Lead, `briefing_md`), `shift_assignment` (Schicht × Person; assigned/confirmed/declined/no_show/waitlisted, `reminded_at`). Alle drei mit RLS und **ohne Grants** — gelesen und geschrieben wird nur über RPCs. Volunteers: `apply_volunteer(jsonb)` (Einwilligung zu terms+privacy nötig ⇒ P0001 `consent_required`; Mindestalter **18 am ersten Eventtag** ⇒ P0001 `too_young`, Geburtsdatum landet in `person.birthdate`; Bereichswünsche freiwillig, aber gegen das Vokabular geprüft), `my_volunteer_profile()`, `update_my_volunteer_profile(jsonb)` (Zurückziehen erlaubt, Status sonst Team), `my_shifts()`, `confirm_shift(id)`, `decline_shift(id, grund?)` (zieht die Warteliste sofort nach). Team (`is_volunteer_team()` = admin, `area_lead_volunteers`, `volunteer_lead`): `volunteer_admin_overview(edition?)`, `set_volunteer_status(profil, status, note?)` (accepted schreibt die Rolle `volunteer` Scope edition bis Editionsende, declined/withdrawn beendet sie — direkt, weil `assign_role` `admin` verlangt), `upsert_shift(jsonb)`, `assign_shift(schicht, person, status?)` (ohne Platz ⇒ Warteliste; ausdrückliches `assigned` ⇒ P0001 `shift_full`; Zeitüberschneidung ⇒ P0001 `shift_overlap`; nur für angenommene Bewerbungen ⇒ P0001 `not_accepted`), `unassign_shift(id)`, `shift_plan(edition?, tag?)`. Intern: `shift_taken`, `promote_shift_waitlist`, `send_shift_reminders` (48 h vorher, genau einmal je Zuweisung), `run_volunteer_housekeeping()` aus `run_application_housekeeping()`. Mails `volunteer_applied`, `volunteer_accepted`, `volunteer_declined`, `shift_assigned` (auch beim Nachrücken), `shift_reminder`. **0066:** `volunteer_day_prefs(jsonb, edition)` (intern) liest die Tageswünsche — leere Einträge und JSON-null fallen weg, unbekannte und unförmige Ids ⇒ P0002 `day_not_found`; `update_my_volunteer_profile` prüft sie damit genauso wie `apply_volunteer`. **0067:** eine Absage oder ein Rückzug beendet die Rolle `volunteer` per `delete` statt über `valid_to` — in derselben Transaktion steht `now()` still, die Rolle hätte sonst eine Sekunde weitergegolten; entfernt wird nur die automatisch vergebene Zuweisung (`note = 'auto:volunteer_accepted'`), eine von Hand vergebene bleibt. **0068:** `day_of_edition(tag, edition)` — Eventtage hängen an den Events der Edition (`summit-27`, `hackathon-27`), nicht an der Edition selbst; `volunteer_day_prefs` und `upsert_shift` prüfen dagegen, fremde Tage ⇒ P0002 `day_not_found`. **0069:** `volunteer_days(edition?)` liest die Tage der Edition als RPC (Anlass war eine leere Tagesliste im Wizard; die Policy `event_day_read` existiert seit v2 mit `using (true)`, `event_day` ist direkt lesbar — geklärt im Security-Review 14.09.2026). **0070:** `is_volunteer_team()` nur noch Admin und `area_lead_volunteers` (Entscheidung Konrad: Schichtleads sehen keine Bewerbungen); `my_lead_shifts()` gibt einer Leitung ihre eigenen Schichten mit Namen und Stand — ohne Mailadressen, ohne Warteliste, ohne fremde Schichten. Migrationen 0065–0070. |
| Tickets (Welle 4) | `ticket` ergänzt um `meta`, `extra_fields`, `vivenu_discount_id`; **`ticket_secret`** (eigene Tabelle, RLS an, **keine Grants** — das vivenu-Secret autorisiert die Personalisierung und gehört nie in den Browser). Ingest über `ingest_vivenu_ticket(jsonb)` (nur service_role): Event über `event.vivenu_event_id`, Pass-Typ über `ticket_type_map`, Person über die Inhaber-Mail aus dem Extra-Feld (Personen werden **nicht** angelegt), Status über `vivenu_ticket_status()` / `vivenu_personalization_status()` auf unsere Werte abgebildet (Unbekanntes lässt den alten Wert stehen), Secret über `set_ticket_secret()`. Coupon-Einlösungen: `ticket.vivenu_discount_id` ⇒ `recount_allocation_usage()` zählt `org_ticket_allocation.used_count` **neu** statt hoch — vivenu wiederholt einen Webhook bis zu siebenmal. `vivenu_editions()` für den Sweep. Route `/api/webhooks/vivenu` (Signatur über den Raw-Body, Idempotenz über die Webhook-Id in `integration.webhook_event`), Sweep `/api/cron/vivenu-tickets`. **0075–0078:** `ticket.vivenu_updated_at` (älterer Stand ⇒ `stale`, kein Überschreiben), `ticket.vivenu_ticket_type_id` + `backfill_ticket_pass_types()` (unbekannter Tickettyp lässt `pass_type` leer statt zu raten; der Sweep trägt nach, sobald `ticket_type_map` steht), alle NOT-NULL-Spalten mit Rückfall — ein Webhook mit fehlenden Feldern darf nicht scheitern, sonst ist das Ticket nach sieben Versuchen verloren. Migrationen 0071–0078. |
| Produktion (Welle 4) | `regie_cue` (Bühne × Tag, optionaler `slot_id` — die Hälfte der Zeilen der Regie-Vorlage 2026 hat keine Session; Spalten der Vorlage, `mic_assignments`/`media` jsonb), `booth_service_check` (Stand × Artikel abgehakt), Vokabular `supplier` mit Trigger `product_supplier_chk` (Shop-Artikel brauchen einen Dienstleister aus dem Vokabular ⇒ P0001 `supplier_required` / `supplier_unknown`). RLS, **keine Grants**. `is_production_team()` = admin, `production_team`, `area_lead_production` (ohne `programme_team` — Review 14.09.). RPCs `regie_view`, `regie_open_slots`, `upsert_regie_cue` (Bühne und Tag derselben Veranstaltung, Slot muss zu Bühne und Tag gehören ⇒ 22023 `invalid_cue`), `delete_regie_cue`, `booth_checklist`, `set_booth_service_check`, `supplier_order_list` (mit Einkaufspreisen — nur Produktion). Migration 0082 (`20260914094832`). **0087** (`20260914104754`): Ticket-Kontingente (`addon`, Kategorie `tickets`, gesetzter `pass_type`) stehen weder auf der Stand-Checkliste noch in der Bestellliste — sie laufen über vivenu. |
| Wissensbasis (Welle 4) | `kb_article` (Slug × Sprache × Edition; `edition_id` NULL = evergreen, ein Artikel mit Edition überlagert ihn; `audience[]` aus `kb_audience`, `roles[]` für Volunteer-Rollenseiten, `phase` aus `kb_phase`, `valid_until`, Status draft/published/archived). RLS, **keine Grants**. Lesen `kb_articles(audience, sprache, edition?, rolle?)` — welche Zielgruppen jemand lesen darf, kommt aus den Rollen (`my_kb_audiences()`, Team sieht alles, Teilnehmer-Wiki für jede angemeldete Person), `kb_article_by_slug`. Editor `kb_articles_admin`, Schreiben `upsert_kb_article`, `publish_kb_article`, `delete_kb_article` (archiviert) nur für Admin oder die Bereichsleitung **aller** Zielgruppen des Artikels (`can_edit_kb_all`), beim Ändern auch der bisherigen (Review 14.09.). Migration 0083 (`20260914095053`). **0088** (`20260914104823`): `kb_articles` filtert nicht mehr nach Sprache, sondern sortiert — Überlagerung der Edition vor evergreen, darin die gefragte Sprache vor der anderen; die Spalte `language` sagt, was kam (Oberfläche kennzeichnet den Rückfall). Unbekannte Sprache = Deutsch. |
| Volunteer-Tickets (Welle 4) | `volunteer_profile` + Coupon-Spalten (`coupon_status` none/pending/issued/redeemed/error/revoked, `coupon_code`, `vivenu_coupon_id`, `ticket_id`, `reminded_at`), `event.vivenu_volunteer_undershop_id`, **`volunteer_coupon_revocation`** (Widerrufe, die bei vivenu noch zu deaktivieren sind; keine Grants). Trigger `ticket_volunteer_redeem` (Einlösung über Coupon oder Undershop + Person, auf jedem Ingest-Weg) und `volunteer_status_coupon` (Absage/Rückzug ⇒ `revoked` + Widerrufsliste; erneute Zusage ⇒ `none`). RPCs `volunteer_coupons_pending`, `set_volunteer_coupon` (service_role), `set_edition_volunteer_undershop`, `volunteer_coupon_revocations_pending`, `mark_volunteer_coupon_revoked` (service_role), `volunteer_tickets_admin` (mit `ticket_status`), `remind_volunteer_tickets` (7 Tage, einmal), `my_volunteer_profile` (Code nur bei issued/redeemed). Mail `volunteer_ticket_reminder` (Link über `{{portal_url}}`). Migration 0084 (`20260914095318`). |
| Hackathon (Welle 4) | `hack_challenge` (Edition, Org, `deliverable_id`, Kriterien mit Gewichten, Mentoren), `hack_team` (`join_code` ohne I/O/0/1, Status forming/confirmed/withdrawn, `note_internal` nur Team), `hack_team_member` (eine Person, ein Team je Edition als Constraint; Maximum 8 als Trigger), `hack_application`, `hack_submission`, `hack_judging_score`. RLS, **keine Grants**. Teilnehmer: `hack_challenges`, `my_hack`, `apply_hackathon`, `create_hack_team`, `join_hack_team`, `leave_hack_team`, `submit_hack` (≥ 3 Personen). Team (`is_hack_team()` = admin, `area_lead_hackathon`): `hack_admin_overview`, `assign_challenges` (gleichmäßig, fasst Gesetztes nicht an), `set_team_challenge`, `set_hack_application_status`, `publish_hack_challenge` (aus der Partner-Pflicht `hackathon_challenge` am Produkt `I-37220`; Mentoren aus `mentor_names`). Jury (`is_hack_judge()` = Team oder `hackathon_partner`): `hack_judging`, `set_hack_score` (Gesamtnote rechnet die Datenbank) — eine Partner-Jury nur für Teams der Challenge ihrer eigenen Organisation (`can_judge_hack_team`, Review 14.09.). Migration 0085 (`20260914095624`). **0089** (`20260914104906`): `hack_challenges`, `my_hack`, `hack_admin_overview`, `hack_judging` mit zweitem Parameter `p_language` (Vorgabe `en`, E7); `hack_text(de, en, sprache)` liefert die gefragte Fassung und fällt auf die andere zurück — alte Ein-Argument-Signaturen gedroppt (sonst 42725). |
| Hygiene (Welle 4) | 0086 (`20260914095907`): sieben Trigger-Funktionen aus den Wellen 1–2 ohne EXECUTE für authenticated (Advisor-Fund; Trigger-Funktionen sind nicht direkt aufrufbar, die Konvention gilt trotzdem). |
| Check-in (Welle 4) | `checkin` ergänzt um `edition_id` (denormalisiert) und `scan_day` (Tag in der Zeitzone der Edition; eigene Spalte, weil `at time zone` nicht IMMUTABLE ist); eindeutiger Index je Ticket × Tag **nur für `result = 'ok'`** — abgewiesene Versuche bleiben protokollierbar. Kiosk-Rolle `checkin_operator` (Scope edition, ein Konto je Gerät, E8) ist die schmalste Rolle: `checkin_scan(barcode, device)` (Antwort `ok`/`already` mit Zeit des ersten Scans/`invalid`/`unknown`; nur Name, Pass-Typ, Uhrzeit; sucht nur in der eigenen Edition, fremde Codes werden nicht gespeichert; `checked_in` von vivenu gilt als gültig; race-sicher über `on conflict`), `checkin_stats(edition?, tag?)` (Kiosk der Edition oder Team), `purge_checkins()` (Scans 30 Tage nach Editionsende löschen; service_role aus dem Housekeeping-Cron `/api/cron/mail` oder Team). `lib/areas.ts`: `isKioskOnly()` — ein reines Gerätekonto öffnet nur `/checkin`, nicht das Teilnehmer-Portal. Migration 0090 (`20260914112220`). |
| Vokabular | `vocab_term` (33 Vokabulare, 309 Begriffe) | — |
| Migration/Dedup (P0) | `import.staging_contact`, `import.source_person_map`, `potential_duplicate`, `person_merge_log` | — |
| Welle 5 (15.09.) | **0091** (`20260915113046`): `edition_contact` (Pool je Edition, Typen `edition_contact_type` partner_lead/partner_buddy/speaker_lead/speaker_buddy, dienstliche Mail per CHECK `@chef-treff.de`, Telefon Pflicht, `is_default` je Typ, Fotos im öffentlichen Bucket `contact-photos` nur service_role), `edition_info` (Auskünfte je Edition und Zielgruppe, Text ohne Zeitstempel), Zuordnung `org_edition.lead_contact_id/buddy_contact_id` und `speaker_profile.lead_contact_id/buddy_contact_id`. RPCs `my_contacts` (nur innerhalb einer bestehenden Beziehung, sonst leer), `edition_infos(audience)`, Team `upsert/delete_edition_contact`, `set_org_contacts`, `set_speaker_contacts` (prüfen Typ und Edition ⇒ 22023 `invalid_contact`), `edition_contacts_admin`; Auskünfte `upsert/delete_edition_info`, `edition_infos_admin` für jede Bereichsleitung (`can_edit_edition_info`). **0092** (`20260915114209`): `portal_video` (Schlüssel, Zielgruppen, nur Loom per CHECK — CSP `frame-src` gehört dazu), `portal_video_for`, Team `portal_videos_admin`/`upsert_portal_video`/`delete_portal_video`. **0093** (`20260915114240`): `org_step` (Katalog je Thema, Seed `event_app` sechs Schritte) + `org_step_check` (Selbstauskunft je Org-Edition), `my_org_steps`, `set_org_step` (`partner_can_edit`, idempotent), Team `org_steps_progress`. **0095** (`20260915114654`): `shop_order.po_number`; `shop_confirm(order, note?, po_number?)`, `shop_my_orders`/`shop_orders_admin`/`shop_invoice_candidates` mit PO (Rechnung: alle PO-Nummern der Bestellungen, sonst Stammdaten). **0096** (`20260915114852`): zehn Wiki-Artikel aus Notion als `draft`, evergreen. **0094** (`20260915115415`): `product.area_sqm`/`size_note`, Frist `booth_backdrop` ⇒ `booth_changes_until` (02.04.2027, Pflichten nachgezogen), Vokabular `edition_file_kind`, `edition_file` + privater Bucket `edition-files` (SELECT für angemeldete Konten, Schreiben nur service_role aus `POST /api/produktion/edition-files`), `edition_files(audience)`, Team `edition_files_admin`/`set_edition_file` (prüft Pfad unter der Edition ⇒ 22023 `invalid_path`, Zielgruppen gegen `kb_audience`)/`delete_edition_file`, `booth_packages()` (nur Zielgruppe partner), `exhibitor_list()` (Partner/Speaker, ohne Logo und Personen), `upsert_booth` auch `is_production_team()`. **0098** (`20260915115707`): Vokabular `travel_mode`, `speaker_travel` (eine Zeile je Profil, Datum/Uhrzeit getrennt), `my_speaker_travel`, `set_my_speaker_travel` (Speaker oder Assistenz, Teilupdate, Audit bei Assistenz), `speaker_travel_list` (Speaker-Team/Lead über `can_manage_speaker`, Produktion alle); Helfer `check_travel_mode` intern. **0099** (`20260915115751`): `speaker_profile.confirmed_at/declined_at/decline_reason`, Vokabular `speaker_decline_reason`, `set_speaker_pipeline(profile, status, reason?)`, `person.salutation_de/en` mit `set_person_salutation` und `suggest_salutation` (nur Team), `manager_speakers` mit den neuen Spalten. **0100** (`20260915115505`): Vokabular `diet`, `person.diet`/`diet_note` (Art. 9: nur eigene Zeile lesbar, `set_diet` nur für sich, kein Wert im Audit), Team `catering_summary`/`catering_notes` (ohne Person)/`catering_coverage`, `purge_diet_data(30)` im Housekeeping. **0101** (`20260915115544`): `can_edit_regie` = Produktion oder `can_edit_stage`; Regie-RPCs prüfen darüber, `my_regie_stages`. **0102** (`20260915115807`): `upsert_edition_contact` ⇒ 22023 `invalid_email` für fremde Domains. **0103** (`20260915123741`): `is_speaker_manager(person)` (intern), `handover_speaker(profile, to_person)` (weiterreichen, was man betreut; Empfänger muss Speaker-Lead sein ⇒ 22023 `invalid_owner`; Freigabe nur Team), `speaker_managers()`, `update_speaker` mit `owner_person_id` als Team-Feld, `speaker_detail(profile)` (jsonb, `internal_notes` nur Team), `manager_speakers` wieder mit `internal_notes`. **0104** (`20260915124834`): `can_manage_speaker_leads()`, `speaker_leads_admin(edition?)` (Lead-Personen mit Rollen-IDs, betreut/zugesagt/abgesagt, offene Schritte), `unassigned_speakers(edition?)`, `my_manager_scope()` mit `person_id`. |
| Welle 6 · `20260917183022` | **`20260917183022` v6_aufraeumen_feldmatrix** (Architektur-Session): `organization.description_de/en` ersetzen `organization.description` und `org_edition.description_de/en` (Bestand übernommen; Onboarding, Recheck, Übersicht, Event-App-Aussteller, HubSpot-Ingest lesen/schreiben an der Organisation, JSON-Schlüssel der Übersicht unverändert); `person.photo_url` entfernt (Fotos nur als Asset, `my_speaker_profile` liefert `photo_asset_id`); `staff_user` + `staff_users_without_admin()` entfernt; Trigger `trg_speaker_profile_prefill` übernimmt `person.employer_name` als `organization_name` beim Anlegen (Snapshot). Weitere Welle-6-Migrationen folgen aus den Chats (`docs/arbeitsauftrag-welle-6.md`). | — |
| Welle 6 · 0110 · `20260917184553` | **Welle 6 · Das Programm-Grundgerüst wird pflegbar (M8)** (`20260917184553`, `v6_programm_grundgeruest`; Details im Migrationskopf) | — |
| Welle 6 · 0112 · `20260917185108` | **Welle 6 · Mail-Vorlagen im Admin bearbeiten (ADM-029)** (`20260917185108`, `v6_mail_vorlagen`; Details im Migrationskopf) | — |
| Welle 6 · 0111 · `20260917185916` | **Welle 6 · Bilder am Auftritt: Bühnenfotos und Slot-Grafiken (A6, ADM-039)** (`20260917185916`, `v6_session_grafiken`; Details im Migrationskopf) | — |
| Welle 6 · 0110 · `20260917190103` | **0110** (`20260917190103`, `v6_partner_format_key`): `product.format_key` (Vokabular `partner_format`: booth, masterclass, company_tour, side_event, interview_table, hackathon, branding, talk, stage) sagt, welche Seite der Menuegruppe „Eure Formate“ ein gebuchtes Produkt im Partner-Portal oeffnet (PART-042). Loest die SKU-Liste im Code ab (`app/(partner)/partner/nav.ts`, Konstante `STAGE_SKU` entfaellt): die Zuordnung kennt der Vertrieb, nicht der Code. Seed nach Konrads Bestaetigung vom 17.09. — per Kategorie, wo die Seite nur informiert (`standflaeche` ⇒ booth, `branding`, `hackathon`), SKU-genau, wo der Partner selbst liefern muss (sechs Talk-Artikel, Masterclass I-33783, Standbuehne I-79895, sechs Company-Tour-Spots, Side Event I-81745); `interview_table` bleibt ohne Produkt, bis der Artikel entsteht (PROD-006). Pflege ueber `upsert_product` (22023 `invalid_format`, leerer Text loescht) und den Produkte-Reiter im Partner-Admin; `partner_overview` liefert `format_key` je gebuchtem Produkt mit. | — |
| Welle 6 · 0111 · `20260917190402` | **0111** (`20260917190402`, `v6_challenge_frist`): Frist `hackathon_challenge` je Edition (FLS27: 18.03.2027, vier Wochen vor dem Hackathon), Vorlage der Challenge-Pflicht von `weeks_before` auf `deadline_key` umgestellt, bestehende `deliverable.due_at` nachgezogen (PART-040 loest PART-032: `deliverable_due` wertete `weeks_before` nie aus, die Pflicht blieb ohne Frist und wurde nie ueberfaellig). `upsert_deliverable_template` prueft die Regel jetzt (22023 `invalid_due_rule`; `{}` und `offset_days` bleiben erlaubt), damit derselbe stille Ausfall nicht wiederkehrt. | — |
| Welle 6 · 0112 · `20260917192416` | **0112** (`20260917192416`, `v6_shop_zwei_phasen`): Messeshop auf **zwei Phasen** (`deadline`-Schluessel `shop_phase1_end` 19.03.2027 und `shop_phase2_end` 09.04.2027; `shop_phase_2` entfaellt, `shop_phase` und `run_shop_finalization` rechnen zweistufig ueber `shop_phase_deadline_key`), Kennzeichen bleibt `product.late_orderable` (PART-038). **Zugang nur mit Messestand** (PART-037): `org_has_booth(org_edition)` = Standflaeche oder Standbuehne als gebuchtes Produkt (ueber `format_key`) oder zugewiesener Stand — der Hackathon-Stand faellt heraus; geprueft in `shop_catalogue` (leere Liste) und `shop_upsert_line` (P0001 `booth_required`). **Lunch-Paket aus dem Shop** (PART-049): I-79520 `shop_visible = false`, Vorlage `lunch_package` fuer alle Partner mit `required = false`, `shop_sku_via_deliverable` laesst Artikel durch, die an einer Pflicht der Org haengen, `order_lunch_package(org, qty, edition?)` bestellt ueber `shop_upsert_line` + `shop_confirm` (kein zweiter Schreibweg auf `shop_order`) und **bestaetigt nur, wenn sonst nichts im Warenkorb liegt** — sonst bleibt die Zeile dort und die Antwort meldet `confirmed = false` (Review: ein fremder Entwurf haette sonst ungefragt das Haus verlassen). `mark_overdue_deliverables` und `partner_digest_items` unterscheiden jetzt Pflicht und Angebot: nur `deliverable_template.required` wird `overdue` und gemahnt; ein Angebot steht vor seiner Frist als Hinweis im Digest und danach gar nicht (dessen Text formuliert „ueberfaellig seit" anhand des Datums, nicht des Status). | — |
| Welle 6 · 0113 | **Welle 6 · Das Mail-Protokoll als Arbeitsmittel (ADM-030)** (`20260918104946`, `v6_mail_protokoll`; Details im Migrationskopf) | — |
| Welle 6 · 0114 | **Welle 6 · Freelancer als Ansprechpersonen (ADM-040, D7)** (`20260918105038`, `v6_freelancer_kontakte`; Details im Migrationskopf) | — |
| Welle 6 · 0115 | **Welle 6 · Profil löschen (ADM-031, Art. 17 DSGVO)** (`20260918105546`, `v6_profil_loeschen`; Details im Migrationskopf) | — |
| Welle 6 · 0116 · `20260918122700` | **0116** (`20260918122700`, `v6_branding`, 18.09.): Die Pflicht `digital_branding` (Produkt I-95690, seit 20260910163331) bekommt eine Frist — sie hatte `due_rule = {}` und damit weder Countdown noch Erinnerung, derselbe stille Ausfall wie bei der Hackathon-Challenge. Jetzt `booth_changes_until` (02.04.2027, die Datei geht in die Produktion), Maße 1920 × 1080 (16:9) in der Beschreibung DE/EN, Dateiregeln auf PDF/PNG/JPG verengt (SVG und ZIP raus). Die Maße prueft die Datei **nicht** — Bildmasse brauchen eine Bildbibliothek; das Team prueft sie bei der Freigabe. | — |
| Welle 6 · 0116 · `20260918141036` | **Welle 6 · Initiativen: Funnel und Leistungen ohne HubSpot (A3.1, A3.2, A3.4)** (`20260918141036`, `v6_initiativen`; Details im Migrationskopf) | — |
| Welle 6 · 0117 · `20260918141037` | **Welle 6 · Technik am Slot: die Ansage des Speakers (A7.2, SPK-018, PROD-007)** (`20260918141037`, `v6_session_tech`; Details im Migrationskopf) | — |
| Welle 6 · 0118 | **Welle 6 · Rechteprüfungen NULL-sicher (Hotfix, Architektur-Session)** (`20260918142035`, `v6_rechte_nullsicher`; Details im Migrationskopf) | — |
| Welle 6 · 0119 | **Welle 6 · Shuttle-Fahrten als eigenes Objekt (A7.1, SPK-016, LEAD-011, ADM-028)** (`20260918142855`, `v6_shuttle`; Details im Migrationskopf) | — |
| Welle 6 · 0121 | **Welle 6 · Die Technik-Ansage des Speakers im Regieplan (A7.2, SPK-018, PROD-007)** (`20260918143817`, `v6_regie_tech`; Details im Migrationskopf) | — |
| Welle 6 · 0120 | **Welle 6 · Produktstamm abgleichen: HubSpot und SevDesk (A4.3, PROD-006)** (`20260918144512`, `v6_produktabgleich`; Details im Migrationskopf) | — |
| Welle 6 · 0122 | **Welle 6 · Belege aus SevDesk im Partnerportal (A5, PART-036)** (`20260918145925`, `v6_sevdesk_belege`; Details im Migrationskopf) | — |
| Welle 6 · 0125 | **Welle 6 · Speaker Reception als eigenes Objekt (A7.4, SPK-003)** (`20260918151535`, `v6_reception`; Details im Migrationskopf) | — |
| Welle 6 · 0126 | **Welle 6 · Zähler für Assistenten ausserhalb der Wissensbasis (SPK-012)** (`20260918204334`, `v6_ai_rate_limit`; Details im Migrationskopf) | — |
| Welle 6 · 0123 | **Welle 6 · Rabattstufen je Kontingent (A3.3, ADM-022)** (`20260921101601`, `v6_rabattstufen`; Details im Migrationskopf) | — |
| Welle 6 · 0127 | **Welle 6 · Kontakt ohne Portalzugang am Speaker-Profil (SPK-005)** (`20260921101742`, `v6_speaker_kontakt`; Details im Migrationskopf) | — |
| Welle 6 · 0124 | **Welle 6 · Stände tagesweise (A3.5, ADM-022)** (`20260921103627`, `v6_staende_tagesweise`; Details im Migrationskopf) | — |
| Welle 6 · 0130 | **Welle 6 · Vokabular vollständig pflegen (ADM-032)** (`20260921104956`, `v6_vokabularpflege`; Details im Migrationskopf) | — |
| Welle 6 · 0129 | **0129** (`20260921110522`, `v6_wiki_hackathon`, 21.09.): Sechs jahresunabhaengige Wiki-Artikel fuer Partner aus dem Notion-Wiki „AI Hackathon 2026" (Konrads Auftrag vom 21.09., PART-018) — Challenge definieren, Mentorinnen und Jury, Preise, Vorstellung am ersten Morgen, Teilnehmende, Rueckwand mit den Grafikanforderungen. **Alle als `draft`**, Konrad schaltet frei. **Vier Dinge bewusst nicht uebernommen** und im Migrationskopf begruendet: die Mobilnummer der Ansprechperson (gehoert in `edition_contact` mit Foto und Einwilligung, und eine Migration bleibt dauerhaft in der Historie), die Challenge-Liste 2026, die ausformulierten Challenge-Beschreibungen 2025 (interne Aufgaben fremder Firmen in einem Portal, das alle Partner lesen) sowie Daten, Ort und Zeitplan 2026. | — |
| Welle 6 · 0117 · `20260921111140` | **Welle 6 B4: Backdrop der Challenge Area (PART-033, PART-052)** (`20260921111140`, `v6_hackathon_backdrop`; Details im Migrationskopf) | — |
| Welle 6 · 0131 | **Welle 6 · Messeshop-Artikel gehen nach SevDesk (Nachtrag zu A4.3)** (`20260921112022`, `v6_sevdesk_shopartikel`; Details im Migrationskopf) | — |
| Welle 6 · 0135 | **EA1 Aussteller: Level und Kategorie aus den gebuchten Produkten** (`20260921112508`, `v6_ea1_aussteller_level`; Details im Migrationskopf) | — |
| Welle 6 · 0137 | **Oberflächen zu 0123 (Rabattstufen) und 0124 (Stände tagesweise)** (`20260921114217`, `v6_oberflaechen_rabatt_staende`; Details im Migrationskopf) | — |
| Welle 6 · 0132 | **Welle 6 A1, Teil 1: Schema für „Eure Formate" (PART-034, PART-044–048)** (`20260921115330`, `v6_formate_schema`; Details im Migrationskopf) | — |
| Welle 6 · 0133 | **Welle 6 A1, Teil 2: Partner-RPCs für „Eure Formate" (PART-034, PART-044–048)** (`20260921115331`, `v6_formate_rpcs`; Details im Migrationskopf) | — |
| Welle 6 · 0134 | **Welle 6: Company Tours mit Stopps und Tour Lead (PART-046, ADM-026)** (`20260921115332`, `v6_company_tours`; Details im Migrationskopf) | — |
| Welle 6 · 0138 | **Branche des Ausstellers (Vokabular `industry`, Swapcard `Exhibitor.type`)** (`20260921120434`, `v6_branche_aussteller`; Details im Migrationskopf) | — |
| Welle 6 · 0139 | **Welle 6 B6: Der Partner pflegt seinen Speaker (PART-044)** (`20260921121115`, `v6_partner_speaker_pflege`; Details im Migrationskopf) | — |
| Welle 6 · 0140 | **Welle 6 B7: `partner_format_sessions` nennt die Fläche und den Tag (PART-047, PART-048)** (`20260922095138`, `v6_format_sessions_flaeche`; Details im Migrationskopf) | — |
| Welle 6 · 0141 | **Sponsoren-Kategorien: vom gebuchten Paket zur Logo-Wand in Swapcard** (`20260922095139`, `v6_sponsoren_kategorien`; Details im Migrationskopf) | — |
| Welle 6 · 0142 | **Welle 6 · Hochgeladene Folien wieder entfernen (SPK-028)** (`20260922102936`, `v6_folien_loeschen`; Details im Migrationskopf) | — |
| Welle 6 · `20260923105946` | **0129? · Welle 6 · „Ich möchte weggebracht werden" an der Abreise (SPK-032)** (`20260923105946`, `v6_speaker_abreise`; Details im Migrationskopf) | — |
| Welle 6 · 0144 | **Welle 6 · Themen als gepflegte Liste statt Freitext (SPK-027)** (`20260923105947`, `v6_session_themen`; Details im Migrationskopf) | — |
| Welle 6 · 0145 | **Welle 6 · Weg zur Wallet über die vivenu-Ticketseite (SPK-037)** (`20260923110335`, `v6_ticket_wallet_link`; Details im Migrationskopf) | — |
| Welle 6 · 0136 | **EA2 Speaker in die Event-App: Quelle und Rückverweis** (`20260923111232`, `v6_ea2_speaker_eventapp`; Details im Migrationskopf) | — |
| Welle 6 · 0146 | **Welle 6 · Technik neu geschnitten: Ansage beim Speaker, Disposition** (`20260923111840`, `v6_technik_neu_geschnitten`; Details im Migrationskopf) | — |
| Welle 6 · 0147 | **Welle 6 · Reisekosten als Pauschale oder per Beleg (SPK-042)** (`20260923120811`, `v6_reisekosten_pauschale`; Details im Migrationskopf) | — |
| Welle 6 · 0149 | **Welle 6 · Aufgaben, die der Speaker selbst abhakt (SPK-024)** (`20260923120912`, `v6_speaker_aufgaben`; Details im Migrationskopf) | — |
| Welle 6 · 0148 | **Welle 6 · Assistenz und Agentur zu einem Kontakt (SPK-040)** (`20260923121327`, `v6_speaker_kontakte`; Details im Migrationskopf) | — |
| Welle 6 · 0150 | **consent_current mit security_invoker (Sicherheitsbefund Security-Check, 24.09.2026)** (`20260924101124`, `v6_consent_current_invoker`; Details im Migrationskopf) | — |
| Welle 6 · 0151 | **Welle 6 · Porträt im Teilnehmer-Profil (TAL-012): Spalte, privater Bucket, Pfadregel, RPC** (`20260924101125`, `v6_person_portraet`; Details im Migrationskopf) | — |
| Welle 6 · 0152 | **Welle 6 · Logo-Wand: Einwilligung zum Weißen, nur noch SVG oder EPS (PART-053)** (`20260924101126`, `v6_logo_druck_einwilligung`; Details im Migrationskopf) | — |
| Welle 6 · 0153 | **Welle 6 · Tagesrahmen für Stage Leads (LEAD-016): create_slot/move_slot weisen speaker_manager außerhalb stage_day hart ab** (`20260924101335`, `v6_lead_tagesrahmen`; Details im Migrationskopf) | — |
| Welle 6 · 0154 | **Welle 6 · Zusatztickets strukturiert (PART-070): shop_request.pass_type/quantity, eigene Mailvorlage, my_ticket_requests, ticket_requests_admin** (`20260924102901`, `v6_zusatztickets`; Details im Migrationskopf) | — |
| Welle 6 · 0155 | **Welle 6 · Board-Suche für Stage Leads (LEAD-019/020): can_search_board, board_search_people, board_search_partners, board_session_refs** (`20260924103055`, `v6_board_suche`; Details im Migrationskopf) | — |
| Welle 6 · 0156 | **Welle 6 · Partner am Slot ist der buchende (Korrektur zu 0155/#147): set_session_partner, board_session_refs liest partner_org_id** (`20260924103948`, `v6_session_partner`; Details im Migrationskopf) | — |
| Welle 6 · 0157 | **Welle 6 · Freigabe durch die Programmleitung (LEAD-022): partner_sessions_pending/release_partner_session prüfen is_programme_editor mit der richtigen Veranstaltung** (`20260924105441`, `v6_freigabe_programmleitung`; Details im Migrationskopf) | — |
| Welle 6 · 0158 | **ip_hash aus audit_log und consent_record gestrichen (Konrad, 24.09.2026: „streichen, sofern kein Sicherheitsrisiko“)** (`20260924133016`, `v6_ip_hash_streichen`; Details im Migrationskopf) | — |
| Welle 6 · 0159 | **Welle 6 · Kontakte bearbeiten und CC-Kontakt (PART-062/063): update_partner_contact, partner_mail_cc, mail_cc_recipients, Kopie an fünf Partner-Mails** (`20260924133947`, `v6_kontakte_bearbeiten_cc`; Details im Migrationskopf) | — |
| Welle 6 · 0160 | **Welle 6 · Folien mit den Teilnehmenden teilen (SPK-055): set_slides_release mit NULL-sicherer Eigentümerprüfung, nur Präsentationen** (`20260924134337`, `v6_folien_teilen`; Details im Migrationskopf) | — |
| Welle 6 · 0161 | **Welle 6 · Profilfelder Teilnehmer-Portal (TAL-013): Vokabular, Spalten, person_language, Lebenslauf-Bucket person-cv, set_my_cv, Vokabular-Wächter** (`20260924135111`, `v6_profilfelder`; Details im Migrationskopf) | — |
| Welle 6 · 0162 | **Welle 6 · Rollenmodell (ADM-053): Team-Rollen je Bereich, speaker_manager/volunteer_lead/checkin_operator nicht mehr Team, admin_section_override je Rolle und Person** (`20260924135400`, `v6_rollenmodell_abschnitte`; Details im Migrationskopf) | — |
| Welle 6 · 0163 | **Welle 6 · Laptop und Video mit Ton in der Technik (SPK-067): session_tech_keys, update_session_tech als Wahrheitswert** (`20260924140426`, `v6_technik_laptop_video`; Details im Migrationskopf) | — |
| Welle 6 · 0164 | **Welle 6 · Eure Daten (PART-059/061): organization.address_extra und customer_number, set_org_customer_number, partner_overview, shop_invoice_candidates** (`20260924140443`, `v6_eure_daten`; Details im Migrationskopf) | — |
| Welle 6 · 0165 | **Welle 6 · Next Up im Teilnehmer-Portal (TAL-006): next_up_item, next_up_items, Pflege im Admin-Abschnitt nextUp** (`20260924140714`, `v6_next_up`; Details im Migrationskopf) | — |
| Welle 6 · 0166 | **Welle 6 · Folien nach dem Summit für Teilnehmende (TAL-001): my_session_slides (Ticket der Edition, veröffentlicht, Slot vorbei, freigegeben)** (`20260924141539`, `v6_folien_teilnehmende`; Details im Migrationskopf) | — |
| Welle 6 · 0167 | **Welle 6 · Belege im Dateibereich einordnen (PART-065): my_partner_documents (Angebot, Rechnung, Messeshop-Rechnung)** (`20260924142604`, `v6_partner_belege`; Details im Migrationskopf) | — |
| Welle 6 · 0168 | **Welle 6 · Community-Events aus Luma (TAL-007 Stufe 2, D12): luma_sync_event, luma_sync_registration (nur Server), my_community_registrations** (`20260924142616`, `v6_luma_events`; Details im Migrationskopf) | — |
| Welle 6 · 0169 | **Admin-Sicht Community-Events (TAL-008, D12)** (`20260924185858`, `v6_community_events_admin`; Details im Migrationskopf) | — |
| Welle 6 · 0170 | **Eigene Tickets im Teilnehmer-Portal (TAL-015)** (`20260924190531`, `v6_my_tickets`; Details im Migrationskopf) | — |
| Welle 6 · 0171 | **person.pronouns und person.cv_url streichen (SPK-066, TAL-013 B3)** (`20260924190700`, `v6_pronomen_cv_url`; Details im Migrationskopf) | — |
| Welle 6 · 0172 | **Rückgabegrund der Programmleitung für den Partner (PART-083)** (`20260924191722`, `v6_rueckgabegrund`; Details im Migrationskopf) | — |
| Welle 6 · 0173 | **Format-Details für Teilnehmende (TAL-002/003)** (`20260924191735`, `v6_format_details_public`; Details im Migrationskopf) | — |
| Welle 6 · 0174 | **Status der Session-Inhalte für Speaker (SPK-050)** (`20260924191954`, `v6_session_inhalt_status`; Details im Migrationskopf) | — |
| Welle 6 · 0175 | **Speaker-Ticket ausstellen: Lesefunktion und Wettlaufschutz (SPK-068)** (`20260924192658`, `v6_speaker_ticket_ausstellen`; Details im Migrationskopf) | — |
| Welle 6 · 0176 | **Verkehrsmittel „Fernbus“ und „Wohnt in Hamburg“ stillgelegt (SPK-059)** (`20260924193425`, `v6_reisemittel`; Details im Migrationskopf) | — |
| Welle 6 · 0177 | **Kundennummer aus HubSpot übernehmen (ADM-057)** (`20260924193812`, `v6_kundennummer_ingest`; Details im Migrationskopf) | — |
| Welle 6 · 0178 | **Eine Sprache je Session: „Gemischt“ fällt weg (SPK-052)** (`20260924194015`, `v6_eine_sprache`; Details im Migrationskopf) | — |
| Welle 6 · 0179 | **Standbühne: Zeitfenster und Partner-Status (PART-079, PART-080)** (`20260925070346`, `v6_standbuehne_regeln`; Details im Migrationskopf) | — |
| Welle 6 · 0180 | **Programm lesen: Entwürfe nur intern und für die eigenen Bühnen (LEAD-032, F10)** (`20260925070949`, `v6_programm_lesen`; Details im Migrationskopf) | — |
| Welle 6 · 0181 | **session_mail_vars nur intern (Security-Check Teil 2, F11)** (`20260925071823`, `v6_mail_vars_intern`; Details im Migrationskopf) | — |
| Welle 6 · 0182 | **Regieanweisungen der Stage Leads; Regieplan nur Produktion und Team (LEAD-031, LEAD-012)** (`20260925072814`, `v6_regie_anweisungen`; Details im Migrationskopf) | — |
| Welle 6 · 0183 | **testdaten_person: Testperson mit primärer Adresse in einer Transaktion (nur service_role, nur +zztest)** (`20260925074058`, `v6_testdaten_person`; Details im Migrationskopf) | — |
| Welle 6 · 0184 | **Einordnung der Speaker-Pipeline und Bühnen in Frage (LEAD-039 Schnitt 1, LEAD-026)** (`20260925084552`, `v6_lead039_einordnung`; Details im Migrationskopf) | — |
| Welle 6 · 0185 | **Eine Wahrheit für „bestätigt“: Export nach Pipeline-Status, Trigger für confirmed_at (QS-049)** (`20260925084741`, `v6_bestaetigt_eine_wahrheit`; Details im Migrationskopf) | — |
| Welle 6 · 0186 | **Admin-Abschnitte → Rollen in der Datenbank: admin_section_role, has_admin_section (PORT1b, ADM-056)** (`20260925085557`, `v6_port1b_abschnitt_rollen`; Details im Migrationskopf) | — |
| Welle 6 · 0187 | **Moderation durch Stage Leads: board_search_people mit p_moderation (LEAD-042)** (`20260925090527`, `v6_moderation_stage_leads`; Details im Migrationskopf) | — |
| Welle 6 · 0188 | **Standbühnen- und Talk-Gäste: stage_guest mit Sperren, Gästeliste, Zuordnung (PART-081, PART-088)** (`20260925091057`, `v6_standbuehnen_gaeste`; Details im Migrationskopf) | — |
| Welle 6 · 0189 | **Company-Tour-Sektion im Admin: Tour ↔ Session, Stopps, has_admin_section (ADM-058, ADM-052)** (`20260925091507`, `v6_company_tours_admin`; Details im Migrationskopf) | — |
| Welle 6 · 0190 | **Verlauf und Aufgaben der Speaker-Pipeline: speaker_activity (LEAD-039 Schnitt 2, LEAD-025, LEAD-027)** (`20260925092258`, `v6_lead039_verlauf`; Details im Migrationskopf) | — |
| Welle 6 · 0191 | **Standpakete Eigenproduktion: Stücklisten, Agency Area stillgelegt, booth_packages nur Stände (PART-085/086)** (`20260925093130`, `v6_standpakete_eigenproduktion`; Details im Migrationskopf) | — |
| Welle 6 · 0192 | **Ticket-Mail ticket_final beim Ausstellen (SPK-068 Teil 2)** (`20260925093316`, `v6_ticket_final_mail`; Details im Migrationskopf) | — |

## Status-Maschinen
- **Slot** (`slot_status`, Farbe im Board): open → requested → confirmed_title_open → final · unused.
- **Session** (`publish_status`): draft → review → published → cancelled. Pflichtfelder beim Veröffentlichen: Slot (Bühne/Raum), Titel DE+EN, Beschreibung.
- **Bewerbung**: applied → shortlisted → accepted → confirmed → attended / no_show · waitlisted → promoted (→ confirmed) · declined · expired (Frist `confirm_by` = Freigabe + `session.confirm_by_hours`) · withdrawn. Sichtbarkeit von Zusagen/Absagen erst nach `decision_release`.
- **Ticket**: valid → checked_in · cancelled / refunded / blocked; `personalization_status` pending → partial → complete.

## Fehlercodes für das Frontend
`42501` nicht erlaubt · `23P01` Überlappung auf Bühne · `23514` Pflichtfeld/Regel verletzt · `23505` bereits vorhanden (Doppelbewerbung) · `P0001` fachliche Ablehnung mit `message` = Schlüssel (`confirmation_required`, `deadline_passed`, `not_eligible` + detail, `not_released`, `ticket_required`, `collision` + detail = kollidierende IDs, `confirm_deadline_passed`, `cannot_withdraw`, `not_confirmable`) · `P0002` nicht gefunden.

## Startdaten (Seed)
Edition `fls27` mit `summit-27` (16./17.04.2027, CCH) und `hackathon-27` (15./16.04.2027), vier Tage, fünf Bühnen 2027 (Main, Leadership & Growth, Industry, Startup, Impact & Tech) mit Wechselzeit/Standarddauer aus der FLS26-Analyse, `stage_day` je Bühne × Tag (Öffnungszeiten offen), Fragenkatalog (motivation, expectations, linkedin, cv_upload), Mail-Templates `test`/`welcome` DE+EN.

## Tests
SQL-Smoke-Tests laufen als Transaktion mit Rollback über den Supabase-MCP (`execute_sql`); Vorlagen liegen in `supabase/tests/`. Vor jedem Merge einer Migration: Tests + Security-Advisor ohne ERROR.

## Ergänzung 08.09. abends — Programm-Editor-Backend (Migration `20260908182849_v2_programme_editor`)
- **Views:** `programme_board` (ein Tag, alle Bühnen: Slot × Session × Speaker, `can_edit` für den Aufrufer) und `programme_backlog` (Sessions ohne Slot). Speaker-Namen kommen über `session_speakers_public()` nur für sichtbare Sessions.
- **Rechte:** `is_programme_editor(event_id)` (Programm-Team/Admin, Edition oder global) · `can_edit_session(session_id)` (Editor, oder Slot im eigenen Scope, oder eigene Backlog-Session als Speaker-Manager/Standbühnen-Editor).
- **RPCs:** `upsert_session(jsonb)` (anlegen/ändern, Teilupdate über vorhandene Schlüssel; Textfelder: Schlüssel vorhanden + leer ⇒ NULL — seit 0018) · `set_session_speakers(session_id, jsonb[])` (ersetzt komplett; `confirmed` bleibt erhalten, wenn der Aufrufer es nicht mitschickt — seit 0018) · `attach_session_to_slot` / `detach_session` (Detach nur unveröffentlicht: `unpublish_first`) · `publish_session` (nur Programm-Team; Pflichtfelder + mindestens ein Speaker bei Inhaltsformaten; setzt Slot auf `final`) · `unpublish_session(id, reason)`. Alle mit Audit-Log; Attach/Detach zusätzlich in `slot_history`.
- **Admin-RPCs (0022, für B6):** `applications_for_session(session_id)` (Entscheider per `can_decide_session`; Gastgeber-Org ohne `consent_share` sieht weder Name noch Profil noch Antworten) · `applications_overview(event_id?)` (Sessions mit Zählern je Status, `released`) · `roles_of_person(person_id)`, `assign_role(person_id, role, scope_type, scope_id?, edition_id?, portal?, valid_from?, valid_to?, note?)` (Admin; Vokabular-Prüfung; gleiche Rolle im gleichen Scope wird reaktiviert), `revoke_role(assignment_id, note?)` (Ablaufdatum statt Löschen; letzter globaler Admin geschützt) · `search_people(query, limit?)` (Team; E-Mail nur Admin). Audit `role.assign`/`role.revoke` unter dem Actor der Session.
- **Fehlercodes ergänzt:** `slot_occupied` (23505), `unpublish_first` (P0001), `publish requires at least one speaker` (23514).
- Getestet: 13 Prüfungen (Scope über Bühne, Backlog-Rechte, Veröffentlichungsregeln, Detach-Sperre, Audit/History).

## Ergänzung 08.09. Nacht — Board-Realtime, Fragen-RPCs, Demo-Programm (Migration `20260908194632_v2_board_realtime_questions`)
- **Realtime aus der Datenbank:** Trigger auf `slot`, `session`, `session_speaker` senden über `realtime.send()` ein Ereignis `changed` mit minimaler Payload `{table, id, op}` auf den **privaten** Kanal `programme-board:<event_id>`. Clients senden nichts selbst; empfangen dürfen nur Programm-Leser (Policy `programme_board_receive` auf `realtime.messages`). Keine Postgres-Changes-Publikation für `slot`, weil Realtime interne Spalten nicht spaltenweise filtert.
- **Fragen je Session:** `set_session_questions(session_id, jsonb[], p_replace_custom = false)` ersetzt die Katalogfragen und lässt eigene Fragen stehen; eigene Fragen sind erst nach `approve_session_questions(session_id)` (Programm-Team) freigegeben, außer ein Programm-Editor legt sie selbst an.
- **Programmzeiten Summit 27** (Vorschlag nach FLS26): Fr Einlass 12:00, Programm 13:00–20:30; Sa Einlass 11:00, Programm 12:00–19:30; `stage_day.open_from/open_to` entsprechend. Änderbar im Editor.
- **Demo-Programm:** `supabase/seed/demo_programme.sql` legt 6 Demo-Personen, 13 Slots und 8 Sessions für Freitag an (Opening, Keynotes, Panel, Platzhalter, Company Tour mit Bewerbung + u35, Get-together mit Kapazität 2, Backlog-Talk). Idempotent; Lösch-Block oben; vor Go-live entfernen.
