# Entscheidungslog — TalentCRM / FLS27-Plattform

Format: Datum · Entscheidung · Begründung · Quelle. Änderungen nur ergänzen, nie löschen.

## 2026-09-07 — Plattform-Scope & Integrationen (Konrad, Chat)
- **Scope = Plattform**: ein Supabase-Projekt, eine Identität (`person`) + Rollen, eine Codebasis; Portale: Talent, Speaker, Speaker-Manager (scoped), Partner (+Messeshop, +Hackathon-Variante), Volunteers, Initiativen, Programm-DB, Gesamt-Admin. Ziel: Anfang–Mitte Oktober 2026.
- **Fünf Grundpfeiler bestätigt**: eine Identität · Rechtemodell überall · ein Projekt, sauber getrennte Domänen · dokumentierte Integrationsschicht · eine Codebasis/ein Deploy-Pfad.
- **Doku-Pflicht**: regelmäßige, vollständige Dokumentation; Reproduktion aus Doku muss jederzeit möglich sein. **Sicherheit = Backbone** (DSGVO, keine Angriffsflächen; Ausfall wäre existenzbedrohend).
- **Side-Formate**: Masterclasses/Company Tours sind Programm-Objekte; Bewerbung erfolgt **aus dem Programm im Portal mit einem Klick** (keine Neueingabe). Zielmodell `offering` + `application` (Kapazität, Frist, Warteliste, Ranking, Consent-Gate) bestätigt.
- **Vivenu — Variante A**: Personalisierung wandert ins Portal; Vivenu erhält nur Minimaldaten (Name, E-Mail). Badge-Druck-Datenquelle prüfen (voraussichtlich unsere DB). „Kauf aus dem Portal starten" **offen** — Kaufprozess so kurz wie möglich.
- **Swapcard**: Branded App vorhanden, **kein SSO** (stabile Lösung wie 2026); Daten per API/Webhooks. Swapcard bleibt 2027, Eigenbau 2028.
- **Luma**: nur unterjährige Community-Events; alle Summit-Formate ins Portal.
- **Messeshop — Prämisse korrigiert**: Handover beschreibt einen **fertigen Next.js-16-Neubau auf Airtable** („Messeshop 3.0", Phasen 0–6, 542 Tests; nur Ship offen), *kein* WooCommerce-Rebuild. Integrationsentscheidung offen (Fragenkatalog 16).
- **Dokumentation**: Drive-Ordner „AI Projekt - Talentpool & FLS27 Systeme" = Team-Doku + Inputs; Repo `docs/` = technische Wahrheit (versioniert); Kernedokumente werden gespiegelt.
- **Design**: Briefing v0.1 aus Rebranding-Figma + Partner-Portal-Design v1 + Schriftordner; Portale minimalistisch/clean/UX-first.

## 2026-09-07 (abends) — Antworten Fragenkatalog A + B (Konrad)
- **Termine:** Summit 27 = **Fr 16. + Sa 17.04.2027**; Hackathon 27 = **Do 15. + Fr 16.04.2027** (überlappt am Freitag → gemeinsame Ressourcen: Volunteers, Ticketing, Event-App). Academy/Bootcamp für den Plan irrelevant.
- **Go-live 14.10.2026**; **alle Prozesse starten 01.11.2026** (Partner-Onboarding, Speaker-Akquise, Bewerbungen, Volunteer-Recruiting, Initiativen, Ticketverkauf) → 14.10.–01.11. = Härtungs-/Design-/Security-Fenster. Bau-Reihenfolge bestätigt: Teilnehmer → Partner → Speaker → Speaker-Manager → Volunteering → Hackathon.
- **Team:** vorerst nur Konrad (Admin, Repo, Doku).
- **Tier-Modell:** `contact` heißt **`lead`**; **`talent` = ab Registrierung/Login** (nicht Ticketkauf). Alter (≤35) **kein hartes Kriterium**, nur Flag/View — langfristig neu festlegen.
- **Speaker-Manager-Scope:** Rechte werden nach Feldfestlegung geschärft; Tendenz: **nicht alles editierbar**. Einschränkung auf **Bühne + Tag, ideal pro Slot** → jedem Slot eine verantwortliche Person zuordnen, die ihn bearbeiten darf (`slot`-Scope im Rechtemodell).
- **Partner-Kontaktrollen:** werden neu evaluiert (offen).
- **Interne Rollen: differenziert.** Bereichsleads (z. B. Programm) mit Vollzugriff auf ihren Bereich, ohne Zugriff auf andere (z. B. Partner). Erste Schnittebene = **Portale/Bereiche** (Teilnehmer, Speaker/Programm, Partner, Hackathon, Volunteers, Initiativen).
- **Rollen pro Event-Edition:** ja. Klassifizierung **Team** (dauerhaft) vs. **Volunteers/Leads mit Zugang** (wie Kunden behandelt: Login/Rolle nur für ein Event).

## 2026-09-08 — Antworten Fragenkatalog C, D, E, 25 (Konrad)
### C · Programm & Bewerbungen
- **Bühnen wie 2026:** 6 Hauptbühnen + Partner-„Standbühnen" (nur Inhalte ins Programm). Partner mit Produkt **„Standbühne"** tragen ihre Slots **selbst im Portal** ein → Programm lebt in Supabase; `stage.owner_org_id` + produktbasierte Rolle mit Scope „eigene Bühne".
- **Hierarchie bestätigt:** `slot` = exakte Zeit/Bühne; `session` = Inhalt + Speaker (oft **mehrere Speaker je Session**, `session_speaker` n:m mit Rolle).
- **Zugangsart je Angebot** (Pflichtauswahl beim Eintragen): `open` · `registration` · `application`. Bewerbung für Masterclasses, Company Tours, teils Side-Events. **Speaker Reception**: nur Anmeldung, nur für Speaker mit bestimmtem Status (Statusfeld beim Eintragen) → `eligibility`-Regel; wird umbenannt.
- **Überschneidungen:** Bewerbungen auf zeitgleiche Angebote **erlaubt, aber geflaggt**; bei zwei Zusagen zeitgleicher Slots **Entscheidung erzwingen** (nur eine bestätigbar). **Zusage mit Bestätigungsfrist** (No-Show-Reduktion), **automatisches Nachrücken** von der Warteliste.
- **Zusatzfragen:** Katalog + eigene Fragen je Partner **nur mit unserer Freigabe, max. 2**. **Freigabe-Gate für Mails**; alle Mails werden vom System getriggert und von uns definiert — **Partner versenden nie selbst**.
- **Consent:** Einwilligung zur Datenweitergabe ist Teil der **AGB beim Ticketkauf** (wird 2026/27 überarbeitet); Teilnehmer stimmen ausdrücklich zu. **Partner sieht alle Bewerbungsdaten** seiner Bewerber. (Umsetzung: Consent-Record mit AGB-Version + Zeitstempel; Sicht nur auf eigene Bewerber, zeitlich begrenzt, Zugriffe geloggt.)
- **Ticketpflicht ja; Kapazität setzen wir** (Raumgröße). „Other" aus Company Tours nicht mehr zuordenbar → Migration als „unentschieden".
### D · Partner, Messeshop, Initiativen
- **Messeshop: in die Plattform, sauber neu gebaut** (Patricks V1 auf Airtable war Test; live lief WooCommerce). Code von Patrick als Referenz (GitHub anfragen). „Alles aus einer Hand."
- Fristen/Rollenwerte beim Shop-Bau definieren. **Rechnung immer SevDesk.** Keine Alt-Bestellungen migrieren — Neustart, Historie ab jetzt.
- **HubSpot triggert Onboarding:** Deal → Pipeline-Phase „Onboarding" → Datenübertragung (Org, Kontakte, Produkte/Leistungen). **Deliverable-Checkliste leistungsbezogen/dynamisch** je gekauftem Produkt (Vorlagen je Produkt). **Reminder automatisiert per Resend** zu Fristen.
- **Tickets: ein Code je Partner** bestätigt; Portal verteilt Code (+ ggf. Shop-Embed). Je Partner automatisiert ein **Secret Shop (Vivenu-Undershop)** mit gebuchten Tickets; Code schaltet final frei. Pass-Typen im Onboarding wählen: Standard **Partner Pass** (Team) + **Talent Pass** (junge Teammitglieder als Besucher); Startup-Partner: **Startup Pass + Investor Pass**.
- **Ein Portal, Rollen/Sichtbarkeit produktbasiert** (gilt auch für Hackathon-Partner).
- **Initiativen:** stark reduziert (Logo, Code, Standinfos) — Empfehlung Claude: **Partner-Portal mit Org-Typ „Initiative" + Produkt „Initiativen-Partnerschaft"** (nutzt Onboarding, Checkliste, Reminder, Code-Verteilung mit).
### E · Speaker
- **Speaker-Datensatz:** alles vom Vorjahr rein + Empfehlungsliste fehlender Felder (Claude); on-the-go evaluieren.
- **Keine öffentliche Speaker-Bewerbung.** Pipeline **Akquise → Onboarding**; Akquise läuft über das **Speaker-Lead-Portal** (Leads verantwortlich).
- **Hospitality im Portal:** Shuttle + Hotel buchbar, freigeschaltet über ein **von uns gesetztes Statusfeld**. **Reisekosten:** Speaker lädt Beleg hoch + Bankdaten-Formular → Portal erzeugt **Auslagenrechnung** → Speaker gibt frei → Beleg nach **SevDesk** + Mail an **Qonto-Rechnungseingang** → Konrad gibt Zahlung frei.
### F (Teil)
- **Hackathon-Teilnehmer-App in den Scope** (mit Testpuffer); **Discord bleibt**.

## 2026-09-08 — Antworten Fragenkatalog 26, G, H, I, J (Konrad)
### Volunteers (26)
- Schichten = Position mit **Start, Ende, Soll-Kapazität**. Bewerbung mit **Präferenzen**, **Zuteilung durch uns** (Zeiten stehen bei Bewerbung noch nicht). **QR-Check-in** vor Ort mit eigener **Check-in-Rolle** (Gerät hat ausschließlich Scan-Recht, sonst nichts). T-Shirt-Größe bleibt (jeder bekommt ein Shirt). **Unterkunft als Add-on im Vivenu-Shop** beim Ticket-Einlösen, zusätzlich **Bahnticket** (einzeln + Bundle). **Buddy-System** wichtig.
### G · Talent-Portal & Felder
- Pflichtfelder Talent-Schwelle wie vorgeschlagen (Name, E-Mail, Status, Erfahrung, Level, Consent) — wird später überarbeitet.
- **Status × Level × Erfahrung bleiben.** Studium in **drei Ebenen**: Hintergrund (z. B. Wirtschaft) → Studienrichtung (z. B. BWL) → **Studiengangsbezeichnung als Freitext** (z. B. „International Business & Innovation") → `study_field`, `study_program`, neu `study_program_label` (Text).
- **Founder-Felder konditional.** **Land/Nationalität nach ISO**, **Stadt neu (wichtig für Auswertung)**. Sensible Felder: Klärung durch Claude (Art.-9-Kategorien) → Empfehlung weglassen.
- **Community/FLC nur Talents; WhatsApp vorerst nicht.**
- 29/31/32: Rückfragen von Konrad → Claude formuliert Klartext + Default (siehe Fragenkatalog).
### H · Integrationen
- **make.com-Szenarien (Swapcard, Vivenu) = Referenz für Feldmappings** des Vorjahres (Notion-Briefings ggf. leicht abweichend, Live-Änderungen).
- **Vivenu:** Keys werden bereitgestellt (→ `.env.local`, nie Chat/Drive). Personalisierungsfelder = Airtable-Felder (daraus abgeleitet). Support-Text von Claude. **Badge-Druck-System: Oktober.** **Kauf aus Portal: nein** (verlängert den Kaufprozess).
- **Swapcard:** API-Key vorhanden (→ `.env.local`). **Alle Objekte syncen** (Teilnehmer, Speaker, Sessions, Exhibitors, Sponsor-Tier). **Tracks als Custom Field** mitdenken.
- **HubSpot → Supabase** einseitig; wenn möglich **Rücksync der Unternehmens-Stammdaten**. Trigger = Deal in **FLS27-Pipeline** → Phase **„Onboarding Automation"**.
- **ActiveCampaign:** Segmente final ausarbeiten, dann pushen; **Opt-in-Rückfluss** gewünscht; Export später. **Kampagne: alle AC-Kontakte anschreiben → Registrierung im Portal (Lead → Talent).**
- **Regel: make.com nur Transport, Logik in Supabase** — bestätigt.
- **Regie + Booth wandern in die Plattform.** Regie = erweiterte Programm-Ansicht für Technik/Regie; Booth = Übersicht der Messestände. **Neues Portal: Event-Produktion** — alle Stände + zugebuchte Leistungen, Vor-Ort-Checklisten („hat jeder Kunde alle Leistungen erhalten?").
- **Luma:** wenn genutzt, direkt ans CRM angeschlossen (Webhook).
- **Regel: SevDesk für ALLE Rechnungen.** Auslösen macht Konrad; **Entwurfserstellung automatisiert**; **immer direkt nach dem Summit** (Rechnungsdatum nach Leistungsdatum, vollständige Abrechnung).
### I · Sicherheit, Datenschutz, Betrieb
- **Datenschutz-Ansprechpartner = Konrad.** AVVs werden geschlossen. Aufbewahrung/Löschung gemeinsam erarbeiten. **Pflicht: „Profil löschen"-Button** + Sperrvermerk (E-Mail wird nie wieder angefasst → Suppression-Liste, gehasht).
- **Consent wird komplett neu aufgearbeitet** durch Konrads separaten Consent-Agent → **Abschluss-Checkliste**: Agent mit allen Tools/Verbindungen briefen; Anwaltsprüfung.
- **Mail:** Team-Postfächer Gmail; **automatisierte Mails via Resend**, Domain `@chef-treff.de`. **Staff-2FA Pflicht, bevorzugt Google-SSO.**
- **Ein Portal mit Bereichen** (Rollen-Umschalter) bevorzugt → Empfehlung Claude bestätigt sich.
- **Supabase Pro** vorhanden; **PITR anschaffen** (Checkliste). **Vercel Pro** sicherstellen. **Alarm-Empfänger `alarm@chef-treff.de`** (Alias, eigener Postfach-Abschnitt) — Checkliste.
- **Doku:** Repo + Drive; vorerst nur Konrad liest/schreibt.
### J · Design
- **Figma-Board = Single Point of Truth.** Themes FLC/Education/Media noch zu exportieren; JSON-Export-Anleitung von Claude.
- **Schrift-Lizenzen vorhanden** (werden im Ordner abgelegt). Laica nur Italic. Frage „Textschnitt" durch Claude erklärt.
- **Sub-Brand-Farben nur für Events. Kein Dark Mode** (Übersichtlichkeit).
- **Token-basiertes Komponenten-System ok; Designer steigt am Ende ein** (wenn alle Funktionen stehen).
### Bestätigungen
- **21 Initiativen = Partner-Portal, Org-Typ „Initiative"** ✅ · **22 Speaker-Felder: alle Empfehlungen aufnehmen** (ggf. später löschen) ✅

## 2026-09-08 (nachmittags) — Antworten 29–32, 45, 49, Übergaben, Sicherheit (Konrad)
- **Felder:** alle vorgeschlagenen Matching-/Profilfelder **aufnehmen** (später streichen). **Sensible Felder (Art. 9 / Gehalt etc.) komplett weglassen.** Founder-Felder konditional, ISO-Länder, Stadt.
- **Onboarding-UX = Default:** 3-Schritt-Wizard (Basis → Studium & Beruf → Interessen & Matching) + Fortschritt + Progressive Profiling.
- **Segmentierung kommt vom Marketing:** nach Portal-Bau erhält Marketing eine **Übersicht der Segmentierungsmöglichkeiten** (alle Felder/Kombinationen) → liefert Segmente zurück → wir bauen Views + AC-Tags. (→ Deliverable + Checkliste)
- **Ein Portal** bestätigt. Domain: aktuelles Team-Portal läuft auf `porta(l).chef-treff.de`; Entscheidung Umzug auf `team.` vs. neue Domain (`login.`/`app.`) offen — Empfehlung Claude: Plattform = `portal.chef-treff.de`, Team-Portal → `team.chef-treff.de` (Umzug im Härtungsfenster).
- **Schriften:** Lizenz deckt **Web-Einbettung** ab. Vorhandene Schnitte: Sharp Sans Display No.1 SemiBold/ExtraBold (+ Italic), ABC Laica Regular Italic. **SemiBold = Textschnitt** (Konrad). Claude konvertiert OTF → WOFF2 im UI-Kit; Lesbarkeits-Check von SemiBold als Fließtext im ersten UI-Kit-Review (Alternative: Book/Medium nachlizenzieren).
- **Design-Themes:** nur **Events** (FLC/Education/Media nicht nötig) → Portal-Akzent Indigo `#6D6DEF` / Hover `#5B5BD9` / Soft `#E8E8FC`. **Kein Dark Mode.**
- **Keys:** Quelle der Wahrheit = **Vercel-Env** (Production/Preview/Development); lokal per `vercel env pull .env.local` (Konrad, einmalig nach `vercel login`). Nie im Chat/Drive/Repo.
- **Produktionsportal:** Nutzer = Event-Team + Regie; enthält den **Regieplan auf Basis des Programms** (Vorlage: Google Sheet „Regieplan 2026", Struktur ins Inventar §11) sowie Stand-/Leistungs-Checklisten.
- **Speaker Reception:** neuer Name folgt (Checkliste); technisch ein **Haken „Reception-berechtigt"** beim Eintragen des Speakers (Staff-gesetzt) → steuert Anmeldeberechtigung.
- **Sicherheit make.com (Entscheidung Konrad):** Vivenu-Key rotieren ✅ Liste · Swapcard-Credential ersetzen ✅ Liste · **verwaiste Webhooks jetzt deaktivieren** (Claude, nur `enabled=false`, keine Löschung, Liste dokumentiert) · Sample-Daten bereinigen ✅ Liste.
- **Secret Shop je Partner = neue Funktion 2027** (bestätigt): ein Undershop mit den gebuchten Pass-Typen statt mehrerer Codes; Machbarkeit per API → Vivenu-Support-Frage 5.
- Konrad fasst jetzt das **Live-Feedback** zusammen → fließt in den Masterplan.

## 2026-09-08 (abends) — Domain, Feedback FLS26 (Konrad)
- **Domain: Plattform → `portal.chef-treff.de`**; bestehendes Team-Portal zieht auf `team.chef-treff.de` um (Konrad). `partner.chef-treff.de` → Redirect später.
- **Regieplan-Ableitung bestätigt** („klingt gut").
- **Feedback FLS26** vollständig als Register erfasst: `docs/feedback-fls26.md` (48 Punkte, Prio-Vorschlag M/S/C — Bestätigung im Masterplan).
- Daraus neue **Grundsätze**: (1) **Portal bilingual DE/EN** von Tag 1; (2) **E-Mail-Minimierung** — bedarfsgesteuerte Reminder, Eingangsbestätigungen bei Uploads, Digest statt Einzelmails; (3) **ein QR/Barcode** (Vivenu) in allen Systemen; (4) **Portal sendet die finale Ticket-Bestätigung** nach Personalisierung; (5) **Vivenu-Personalisierung im eigenen System ist mit vivenu validiert** (Bestätigungsseite wird nachgebaut; Anleitung folgt).
- Neue Integration: **Website (Supabase + Vercel + Sanity)** ← Partner-Logos automatisch.
- Neue Rolle: **Assistenz** (Delegation auf Speaker-Datensatz). Neues Feld: **Titel (Dr.)**. Regel: **Masterclass-Speaker = Professional Pass** (Lounge-Zugang als Flag).
- Messeshop-Anpassungen: keine Startseite, keine Rollentrennung (Hinweise auf Produktebene), Merch-Kategorie, Lunch-Paket als Pflicht-Checklistenpunkt.
- HubSpot-Gate: Onboarding startet erst, wenn Basisinfos vollständig; sonst Deal zurücksetzen + Hinweis an Sales.

## 2026-09-08 — Vivenu-Call (Granola) ausgewertet
- **Variante A ist mit vivenu validiert:** Kauf im vivenu-Shop → Redirect mit Transaction-ID auf unsere Confirmation Page → Personalisierung im Portal (verpflichtend) → Rückschreiben von **Vorname, Nachname, Position, Unternehmen** (Badge-Druck) per Ticket-Endpunkt; Parallelbetrieb möglich. vivenu liefert Doku + Endpunktliste.
- **Badge-Druck:** vivenu-Standarddrucker, Badges vorgedruckt + beklebt → Datenquelle Portal (Frage 34.4 damit weitgehend geklärt; Oktober: Details).
- **Identität:** E-Mail + **vivenu-Customer-ID** mitführen. SSO/IdP frühestens 2027 (Auth0/OIDC).
- **Offen (Support-Fragen 10/11):** Ticket-Mail bei abweichender E-Mail; Einlass-Setup und Scan-Rückfluss.

## 2026-09-08 (abends) — Alte Portale & Wiki/Chatbot (Konrad)
- **Walkthrough der alten Portale** (Partner Hub, Speaker Hub, Messeshop) per Chrome, read-only, Konrad loggt selbst ein → Inventar §13. Informationsarchitektur des Partner Hubs wird übernommen, aber produktbasiert und mit DB-Deadlines.
- **Wikis + Chatbots kommen wieder — je einzeln für Speaker, Partner, Teilnehmer, jeweils eigenes Wiki.** Umsetzung als Wissensbasis im Portal (Inventar §14, Masterplan Ergänzung v0.1b); Zielgruppen-Trennung im Retrieval ist Pflicht.
- **Keine privaten Kontaktdaten** von Freelancern/Team in Portalen oder Wissensbasis → Rollen-Postfächer (Checkliste).

## 2026-09-08 (abends) — Master-Programm FLS26 ausgewertet (Konrad)
- **Anforderung Programm-Board:** Speaker-Lead-Portal bekommt eine Ansicht wie der Tab „Master-Programm" (Tag × Bühnen × Zeit) mit Slots per Drag & Drop. Umsetzung als gemeinsame Komponente: Welle 1 im Programm-Editor (Admin), Welle 2 im Speaker-Lead-Portal auf den Scope begrenzt (Masterplan v0.1c, Inventar §15).
- **Befund:** Das Sheet ist eine sortierte Liste je Bühne mit Statusfarben ohne Legende, ohne IDs und ohne Formeln. Im Portal gilt: Zeit, Status, Format und Sprache sind strukturierte Felder, Farbe kommt nur aus dem Status.

## 2026-09-08 (spät) — Antworten 56–77, Freigabe Masterplan (Konrad)
- **Masterplan v0.1c freigegeben** (Prio M/S/C bestätigt, Frage 56). Welle 0 startet am 08.09. Offen bleibt nur 73 (Zeitlogik); bis zur Antwort gilt die Empfehlung (5-Min-Raster, Bühnenparameter, Warnung statt Sperre).
- **Rollen:** Partner-Kontaktrollen = Primary Ops, CC, Event-App-Member, Signing, Accounting **+ Shop**. Assistenz ohne Consent/Bankdaten; Einladung per Mail durch Speaker oder durch Stage Leads im Onboarding. Bühnenpartner pflegen ihre Spalte selbst (Freigabe durch Programm).
- **Programm:** Status-Enum statt Farben. Keine ZEIT-Bühne 2027; **allgemeine Slot-Logik** mit Kontingent/Zähler je Bühne × Tag. Raum/Bühne Pflicht, Moderation optional, **getrennte Felder für Regie (intern) und App (öffentlich)**. Website-Programm über Swapcard-Embed, Sanity nur Logos.
- **Wissensbasis:** eigener Chatbot (kein Chatbase). Sprachen Partner DE+EN, Speaker EN, Teilnehmer DE+EN. Owner Pauli (Speaker), Konrad (Partner, Teilnehmer). Volunteer-Wiki aus bestehender Notion-DB.
- **Verschoben (C):** Strategy-Calls (optional), Slot-Grafik-Generator (neues Figma-Template folgt), Slid@Home nach Empfehlung.
- **Betrieb:** Tokens werden gesammelt über `docs/zugangs-liste.md` erfasst und von Konrad in Vercel gesetzt. Kontingente Hotel/DB/Locker über Laura bis 01.11.
- **Neu:** Item-Liste 2026 (Airtable Working List) ist das Produkt-Inventar → `product`-Stamm im Portal, Spiegel für HubSpot und SevDesk.
- **73 (Nachtrag):** Zeitlogik des Programm-Boards nach Empfehlung (5-Min-Raster, Bühnenparameter, Warnung statt Sperre; Überlappung je Bühne hart). Damit ist der Fragenkatalog vollständig beantwortet; Anpassungen laufen über dieses Log.

## 2026-09-08 (spät) — Welle 0 gestartet: Schema v2 Teil 1–3 live
- **Migrationen angewendet** (Supabase-MCP, Historie vollständig inkl. P0): `20260908141744_v2_identity_roles`, `20260908142441_v2_edition_programme`, `20260908142920_v2_security_hardening`. Repo-Dateien tragen dieselben Versionen.
- **Getestet (Transaktion mit Rollback):** Scope-Rechte (`has_role`, `can_edit_slot/stage`), Überlappungs-Constraint je Bühne (23P01), Warnungen des Boards (vor Öffnung, 5-Min-Raster, Wechselzeit), Pflichtfelder beim Veröffentlichen (23514), Bestätigungspflicht nach Veröffentlichung, History- und Audit-Einträge, Slot-Statistik, `programme_public`.
- **Sicherheits-Härtung:** Views aus P0 auf `security_invoker`, `search_path` in allen Funktionen gepinnt, `anon` darf keine SECURITY-DEFINER-Funktion mehr aufrufen, Default-Privilegien für neue Funktionen entzogen. Bewusst offen gelassen: „RLS enabled, no policy" auf service-role-only-Tabellen (organization, org_membership, staff_user, audit_log, suppression, import.*, Dedup) und „authenticated kann RPCs aufrufen" (die Autorisierung steckt in den RPCs).
- **Bugfix aus dem Test:** plpgsql-Array-Verkettung mit `||` in `move_slot` durch `array_append` ersetzt (Repo und live identisch).

## 2026-09-08 (spät) — Schema v2 Teil A abgeschlossen
- Live: `v2_application_ticket` (Bewerbungs-Pipeline mit Freigabe-Gate, Kollisions-Entscheidungszwang, Ticketpflicht bei Bestätigung; Session-Anmeldung mit Warteliste; Tickets, Kontingente, Check-in, Personalisierung), `v2_integration_comms` (Schema `integration`, `external_ref`, `mail_template`, `mail_log`), `v2_seed_vocab_fls27` (34 Vokabulare, Edition FLS27 mit Summit 27 / Hackathon 27, fünf Bühnen 2027, Fragenkatalog, Mail-Templates).
- **Modell-Entscheidungen dabei:** Bewerber sehen Entscheidungen nur über `my_applications()` (Statusspalte nicht lesbar) · Pass-Typen nutzen das bestehende Vokabular `ticket_type` · `registration` bekommt `session_id` statt einer eigenen Tabelle · Bühnenparameter 2027 vorläufig aus FLS26 (L&G 25 + 5 Wechsel, sonst 30 + 0), Öffnungszeiten je Bühne × Tag noch leer.
- Doku: `docs/datenmodell-v2.md` (Überblick, Status-Maschinen, Fehlercodes), `supabase/tests/README.md`.

## 2026-09-08 (abends) — Review PR #1 „Welle 0 · Teil B" (Architektur-Session)
- **Design (Entscheidungen zu den drei PR-Fragen):** (1) Primär-Buttons und alle Flächen mit weißem Text füllen mit **`#5B5BD9`**; `#6D6DEF` bleibt Akzent für Nicht-Text (Badges, Fokusring, Icons, Linien) und für Text ≥ 24 px. Regel: Kontrast ≥ 4,5:1 schlägt Token-Ästhetik. (2) Neues Token **`--ct-border-strong: #7F8A9C`** nur für Bedienelemente (Feldränder, Checkboxen); `#DCDFE5` bleibt für Trennlinien/Karten. (3) **Rollen-Namen: das Vokabular `role` ist kanonisch.** Neu aufgenommen: `hackathon_participant`, `hackathon_partner`. Bereichsleitungen heißen `area_lead_talent | speaker | partner | volunteers | hackathon | production`; `lib/areas.ts` mappt explizit statt Namen abzuleiten.
- **Architektur-Korrekturen aus dem Review (Blocker für den Merge):** offener Redirect über `next` (Login → Callback) schließen · Proxy = reiner Login-Gate ohne DB-Aufrufe, Cookies auf Redirects übernehmen · jede Admin-Seite/-Action prüft selbst `requireArea("admin")` vor dem service_role-Client (Layouts schützen verschachtelte Routen nicht zuverlässig, Next-Auth-Guide) · Suppression-Prüfung über RPC `is_suppressed()` und **fail-closed** · unterdrückte Mails ohne Klartext-Adresse loggen · Dry-Run ohne Resend-Key nicht als „sent" protokollieren · Admin-Aktionen schreiben `log_audit` · Root-Layout darf ohne Env nicht werfen.
- **Vereinfachungen (im PR):** `session_context()` als ein RPC statt drei · Mail-Vorlagen: **Datenbank ist kanonisch**, TS-Fallback nur `test`, `login_magic_link` entfällt (Supabase Auth verschickt) · `requireStaff` → `requireArea("admin")`, Team-Definition nur in SQL `is_staff()` · Fonts: Italics und Laica ohne Preload · `getI18n()` liest die Session-Sprache selbst.
- **Später (Follow-up-Issues):** Placeholder-Seiten zu einer dynamischen Route, tote CSS-Tokens verdrahten oder löschen, Barrel-Exports bereinigen, `Select` nutzt die `Input`-Klassen, `CardHeader` als Kartentitel, `gen-schema-doc` nur mit Service-Key.
- **Neu (Migration 0014):** service_role darf alle Funktionen aufrufen (Default-Privilegien), `session_context()`, Hackathon-Rollen; Mail-Template-Zeilenumbrüche korrigiert (Migration 0013, Seed 0011 mit `E''`).
- **Event-App:** Konrad möchte die Event-App-Option vor Welle 3 noch einmal prüfen; **Conferras** (conferras.com) als Idee neben Swapcard → Frage 86, Checkliste. Der Swapcard-Sync (Welle 3) bleibt bis zur Entscheidung geplant, die Schnittstelle wird als austauschbarer Adapter gebaut.
- **08.09. spät, Welle 1 A4 vorgezogen:** Programm-Editor-Backend live (`v2_programme_editor`: `programme_board`, `programme_backlog`, Session-RPCs mit Scope-Rechten, Veröffentlichen nur Programm-Team). Conferras-Erstsichtung im Fragenkatalog (86): stark im Networking, offen bei API/Lead-Scanning/Embed → Demo mit Kriterienliste, Entscheidung bis 24.09.

## 2026-09-08 (Nacht) — PR #1 gemergt: Welle 0 Teil B live auf main
- **Zweiter Review-Durchgang bestanden** (Commit 793b1d3, Merge b44c7e4): alle neun Blocker umgesetzt und nachgelesen (safeNextPath an Login-Seite, Formular und Callback; Proxy nur Login-Gate mit Cookie-Übernahme; `requireArea("admin", pfad)` in allen zehn Admin-Dateien; Suppression über `is_suppressed()` fail-closed; unterdrückte Adressen nur als Hash im Mail-Log; Dry-Run nur in development; `session_context()` als ein RPC; `NEXT_PUBLIC_SITE_URL` Pflicht außerhalb development).
- **Akzeptierte Abweichung:** Audit-Einträge aus Server Actions schreibt `lib/audit.ts` per direktem Insert (service_role) mit Urheber aus der Session, nicht über `log_audit()` — unter service_role wären `auth.uid()`/`current_person_id()` leer. `log_audit()` bleibt für SQL-interne Aufrufe (RPCs).
- **Kursivschnitte** als eigene Font-Familie ohne Preload (next/font kennt `preload` nur je Aufruf).
- **Welle 0 damit fachlich abgeschlossen.** Offen: Konrads 80-%-Feedback zu UI-Kit und Bereichs-Umschalter (läuft in mehreren Runden), Doku-Spiegelung um Runbooks und generierte Schema-Doku ergänzt.
- **Nächster Schritt Build-Session:** Welle 1 Teil B auf Branch `welle-1/talent-programm`, Start mit B5 (Programm-Board auf `programme_board`/`move_slot`), B2 (Programm-Ansicht) und B1 (Onboarding-Wizard); Backend dafür ist live. A1/A2 (vivenu) warten auf den Sandbox-Key.
- **08.09. Nacht — Vercel↔Supabase-Integration aktiviert (Konrad):** Supabase-Variablen kommen in Vercel jetzt aus der Integration. Die App liest URL/Key über `lib/supabase/env.ts` und akzeptiert klassische (`ANON_KEY`, `SERVICE_ROLE_KEY`) wie neue Namen (`PUBLISHABLE_KEY`, `SECRET_KEY`). `NEXT_PUBLIC_SITE_URL` in Production gesetzt.

## 2026-09-08 (Nacht) — Datenstandort: Einordnung und Maßnahmen (Nachfrage Konrad)
- **Rechtlich:** Dublin (eu-west-1) und Frankfurt (eu-central-1) sind beide EU; kein Drittlandtransfer, gleiche AWS-Sicherheitskontrollen. Zuständige Aufsicht bleibt die Hamburger (Sitz des Verantwortlichen), unabhängig vom Serverstandort.
- **Restrisiko liegt beim Anbieter, nicht bei der Region:** Supabase, Vercel, Resend und Anthropic sind US-Unternehmen (CLOUD Act, Support-Zugriffe). Gegenmaßnahmen: AVV mit SCC, Datenminimierung, Verschlüsselung sensibler Felder, kurze Aufbewahrung, Zugriff nur über Rollen. Der Regionswechsel ändert daran nichts.
- **Trotzdem Frankfurt:** konservative Wahl, vereinfacht Partner-Fragebögen („Server in Deutschland?") und kostet jetzt eine Stunde. Entscheidung: Umzug vor Welle 1 (Reproduktionstest inklusive).
- **Wichtiger Fund:** Vercel führt Server-Code standardmäßig in der US-Region (iad1) aus. Ab jetzt `vercel.json` mit `regions: ["fra1"]`, damit Rendering, Server Actions und Route Handler in Frankfurt laufen. Statische Auslieferung über das CDN bleibt global (keine Personendaten).
- **Offen (Checkliste):** Resend EU-Verarbeitung, AVV-Sammlung, Standort von Supabase-Logs/Backups.

## 2026-09-08 (Nacht) — Review PR #2 „Welle 1 Teil B" (erster Durchgang)
- **Board-Realtime:** Datenbank sendet (Trigger → `realtime.send`, privater Kanal `programme-board:<event_id>`); Programm-Leser dürfen empfangen und als Fallback senden (Policies auf `realtime.messages`, Migrationen `20260908194632`, `20260908194933`). Keine Postgres-Changes-Publikation für `slot` (interne Spalten). Befund: ohne verbundenen Realtime-Client fehlen die Partitionen von `realtime.messages`, `realtime.send` schlägt dann stumm fehl — daher der Client-Fallback.
- **Fragen je Session** nur noch per RPC (`set_session_questions`, `approve_session_questions`); service_role-Pfad im PR wird ersetzt.
- **Merge-Bedingungen PR #2:** privater Kanal je Event, Fragen-RPC, Tests für Zeitzone/Geometrie im Repo, Consent-Schritt ohne Duplikate. Rest (Speaker-Suche einschränken, Board unter `/speaker-leads`) → Welle 2.
- **Demo-Programm** für Summit 27 Freitag auf der Dev-Datenbank (Tag `demo`), Programmzeiten Summit 27 als Vorschlag gesetzt.

## 2026-09-09 — Supabase-Umzug nach Frankfurt (Reproduktionstest bestanden)
- Neues Projekt **`jqmqvgaiyjudkvtncijw`** („FLS27 System & CRM", eu-central-1) ersetzt `fsjexlrapilzftwibocu` (eu-west-1). Grund: konservative Wahl beim Datenstandort (Einordnung 08.09.), jetzt ohne Nutzerdaten billig.
- **Vorgehen:** Alle 17 Migrationen aus `supabase/migrations/` in fünf Paketen per Supabase-MCP `execute_sql` in Dateireihenfolge eingespielt (Kommentare entfernt, Inhalt identisch), danach `supabase_migrations.schema_migrations` mit den Repo-Versionen befüllt. Damit bleiben Dateinamen und Historie identisch; `supabase db push` erkennt den Stand.
- **Reproduktionstest:** Tabellen, Views, Funktionen, Policies, Trigger und Vokabular stimmen zwischen altem und neuem Projekt überein (Zahlen im Runbook `supabase-umzug.md`, Historie). Das bestätigt: Das System lässt sich vollständig aus dem Repo neu aufsetzen.
- Alle Verweise auf die alte Projekt-Ref in AGENTS.md, README, Runbooks und Zugangs-Liste ersetzt. Altes Projekt bleibt eine Woche pausiert als Rückfallebene, dann Löschung (Checkliste).
- **09.09., Umzug Schritt D:** Die Vercel-Integration legt Variablen unter neuen Namen an (`NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`, `SUPABASE_SECRET_KEY`, `SUPABASE_URL`) und überspringt vorhandene Namen. Die drei alten manuellen Dublin-Variablen wurden entfernt, `NEXT_PUBLIC_SUPABASE_URL` für alle Umgebungen gesetzt, Publishable Key für Preview/Development ergänzt; Skripte akzeptieren beide Namensschemata. **PITR** (100 €/Monat) bewusst erst vor Go-live; Compute Small.

## 2026-09-09 (mittags) — Umzug abgeschlossen: Login, Bootstrap-Admin, Tests; Secret Key lokal
- **Login auf Frankfurt läuft** (Build-Session hatte die Ursache parallel gefunden: Code las die alten Schlüsselnamen; Fix auf dem Branch `2d04785`, auf `main` identisch in `295f921`/`4bcff40` → beim Merge zusammenführen, siehe Arbeitsauftrag Welle 1).
- **Bootstrap-Admin** für Konrad per SQL (Runbook Schritt E): `role_assignment` admin/global + `staff_user` + Audit-Eintrag `bootstrap_admin`. Weitere Rollen ab jetzt nur über die Rollenverwaltung (B6) oder dokumentiert per SQL.
- **SQL-Smoke-Tests** entziehen der Testperson innerhalb der Transaktion Rollen und Staff-Eintrag. Grund: Mit dem Admin-Konto als erster Person liefen drei Negativtests „durch" (Admin darf fremde Bühnen und Entscheidungen), was wie ein Fehler aussah, aber Absicht ist. Alle vier Tests auf Frankfurt grün.
- **`SUPABASE_SECRET_KEY` ist in Vercel sensibel** (Integration legt ihn als Typ „Secret" an). Laufzeit in Production bekommt den echten Wert; `vercel env pull` liefert nur einen Platzhalter (11 Zeichen). Folge lokal: alles über service_role meldet „Invalid API key". Entscheidung: **nicht** in eine normale Variable umwandeln (sensibel ist richtig), sondern lokal einmal von Hand eintragen; `scripts/env-pull.sh` bewahrt den Wert bei jedem Pull und kopiert optional in Worktrees. Empfehlung: eigener Secret Key „local-dev", getrennt widerrufbar.
- **Preview/Development** haben von der Integration bisher keinen Secret Key und keine `SUPABASE_URL` (nur Production) → Resync in der Integration (Checkliste).
- **Merge-Anweisung an die Build-Session** in `docs/arbeitsauftrag-welle-1.md` (Abschnitt „Anweisung 09.09.").

## 2026-09-09 (mittags) — Realtime privater Kanal: verifiziert, keine Migration nötig
- **Befund der Build-Session** („`realtime.messages` hat null Partitionen, `pg_cron` fehlt, Migration nötig") ist überholt: Der Realtime-Dienst legt die Partitionen beim ersten Verbinden eines Clients selbst an (Realtime-Log 09:23/10:23 „Creating partitions for realtime.messages", Janitor); in Frankfurt existieren `messages_2026_09_08` … `_09_12`. `pg_cron` wird dafür nicht gebraucht. Die 7 Trigger-Nachrichten aus dem Walkthrough (10:27–10:29) liegen in `realtime.messages`, die Datenbank sendet also.
- **`Unauthorized` im Browser** kam vom anon-Token: Der Kanal wurde vor `setAuth()` abonniert; als `anon` fehlt das EXECUTE auf `is_programme_reader()` (Härtung) → Policy `false`. Mit Nutzer-Token (`authenticated`) liefert die Policy `true` (SQL-Nachbau der Realtime-Autorisierung inkl. `set role authenticated`, auch mit leerem `search_path`). Fix der Build-Session (`77c946e`: erst `getSession()`/`setAuth()`, dann `subscribe`) ist der richtige; nach dem Fix nochmals im Browser prüfen.
- **Beweis Ende-zu-Ende:** `scripts/realtime-probe.mjs` (Wegwerf-Testnutzer, Rolle `speaker_manager` global, privater Kanal `programme-board:<summit-27>`): `SUBSCRIBED`, Client-Send `ok` (Policy `programme_board_send`), Trigger-Nachricht `changed {table: session, op: UPDATE}` empfangen; Testnutzer, Person und Rolle danach entfernt. Probe gehört ab jetzt zur Prüfung nach Umzug/Neuaufbau (Runbook, Tests-README).
- Entscheidung: **keine** manuellen Partitionen, kein `pg_cron` für Realtime; Client-Fallback `notifyPeers()` bleibt als Sicherheitsnetz.

## 2026-09-10 — Review PR #2 „Welle 1 Teil B" (zweiter Durchgang) + Migration 0018
- **Verfahren:** Die Review-Agenten (`/code-review`) sind an Kontingent- und Verbindungsfehlern gescheitert; drei Teilergebnisse (Removed-Behavior, Cross-File-Contracts, Reuse) lagen vor. Die Architektur-Session hat die Kandidaten am PR-Stand `77c946e` selbst verifiziert und die Sicherheitsprüfung (Auth-Callback, Realtime-Token, Consent-Diff, service_role-Pfade, Grants) manuell gemacht. Ergebnis als PR-Kommentar.
- **DB-seitig behoben (Migration `20260910064227_v2_session_text_nullif_speakers_confirmed_grants`, Datei 0018):** Nachtrag 0019 `20260910064540_v2_security_hardening_3_views`: die Revoke-Schleife schließt Views/Materialized Views ein (die acht Views trugen dieselben Standardgrants); Ergebnis 0 TRUNCATE/REFERENCES/TRIGGER-Grants für anon/authenticated in `public`.
  - `upsert_session` speicherte Leerstrings wörtlich, `session_publish_check` prüfte nur `is null` → die Pflichtfeldregel beim Veröffentlichen (Titel DE+EN, Beschreibung) war mit `""` umgehbar. Jetzt: Schlüssel vorhanden + leer/Whitespace ⇒ NULL (Schlüssel fehlt ⇒ Wert bleibt); der Trigger normalisiert die vier Textfelder vor jeder Prüfung. Folge für die UI: eine Session ohne jeden Titel wird beim Anlegen abgelehnt (23514, `session_title_chk`), der Editor muss Titel (DE) als Pflicht durchsetzen.
  - `set_session_speakers` setzte `confirmed` bei jeder Änderung auf false zurück, wenn der Aufrufer den Schlüssel nicht mitschickt (der Editor tat das). Jetzt bleibt der bisherige Wert der Person erhalten.
  - `harden_definer_functions()` entzieht anon/authenticated zusätzlich TRUNCATE/REFERENCES/TRIGGER auf allen Tabellen in `public` und setzt die Default-Privilegien für neue Tabellen (Eigentümer postgres). Diese Rechte steuert RLS nicht; über die API sind sie nicht erreichbar, gehören API-Rollen aber trotzdem nicht.
- **Vor dem Merge (klein, Build-Session):** `login/page.tsx` Fehlertext nur für bekannte Schlüssel (`?error=__proto__` liefert sonst ein Objekt an die Client-Komponente → 500); `admin.ts` ersten *gültigen* Schlüssel wählen statt Reihenfolge (`[SENSITIVE]` in `SUPABASE_SERVICE_ROLE_KEY` gewinnt sonst); Speaker-Editor reicht `confirmed` durch; Titel (DE) im Editor als Pflicht; `withdrawn`/`expired` zeigen wieder „Bewerben"; `QuestionOption` akzeptiert `key` (dokumentiertes Format) neben `value`; Katalogfragen vom Typ `file` bis zum Upload nicht anbieten.
- **Hinweise, kein Blocker:** `classify()`-Zweig „used" ist toter Code (GoTrue meldet benutzte Links als expired); Onboarding-Wizard auf 1200 px statt Formularspalte 800; `/profil` lädt alles, bevor es zum Onboarding umleitet; `verify-write.mjs` prüft den Publishable Key nicht; Board-Kommentar zu Realtime veraltet (DB sendet per Trigger).
- **Aufräumliste Welle 2 (Reuse):** gemeinsamer Person-Write-Helfer (Onboarding schreibt `city`, Profil kennt es nicht), Vokabular-Schlüssellisten statt Literale (Flag `active` wird ignoriert), ein `pickText()` für DE/EN-Fallback, `Modal`/`ConfirmDialog`/`ChipGroup`/`SubNav` ins UI-Kit, `dateLocale` aus `t.meta`.
- **Bestätigt in Ordnung:** alle Schreibwege über RPCs mit Session-Client; `requireArea`/`requireUser` vor jeder Action; Auth-Callback kürzt `code`/`token_hash`/`type` auf das erste Wort und prüft `type` gegen die OTP-Typen, `next` über `safeNextPath`, kein Token im Log; Realtime `setAuth` vor `subscribe`, privater Kanal je Event, Fallback bei Tab-Wechsel; Consent-Diff gegen `consent_current`; Fragen-RPC ohne service_role; `searchPeople` bleibt der einzige service_role-Pfad (Welle 2: auf Speaker-Bezug einschränken).

## 2026-09-10 — PR #2 gemergt; Review-Rhythmus ab Teil C; Feedback-Zeitpunkt
- **PR #2** (B5 Board, B2 Programm-Ansicht, B1 Onboarding) nach dem zweiten Durchgang gemergt (`cbb345b`); alle sieben Punkte umgesetzt, Lint/Tests/Build grün. Realtime-Zwei-Tab-Synchronisation von der Build-Session zweifach verifiziert (Editor-Tab und direkte DB-Änderung per Skript). Nebenbefund behoben: dnd-kit erzeugte bei jedem Laden einen Hydration-Fehler (Modul-Zähler in `aria-describedby`), fester `id` am `DndContext`.
- **Review-Rhythmus (Antwort auf Konrads Frage nach dem Token-Verbrauch; gilt ab jetzt, solange Konrad nicht widerspricht):** ein PR = ein kompletter Baustein des Arbeitsauftrags (z. B. B3 + B6 zusammen), keine Zwischenstände; **ein** Review-Durchgang je PR, Nachbesserungen werden nur als Diff geprüft; Pflichtprüfung bleibt für Sicherheitsgrenzen (Auth/Callback, service_role-Pfade, RLS- und RPC-Kontrakte, Env/Secrets, Migrationen) und für Datenverlust-Risiken; UI-Details laufen über Konrads Feedback-Runden, nicht über den Review. Kein Elf-Agenten-Review mehr (zweimal gescheitert, teuer), stattdessen fokussierte Prüfung mit Sicherheits-Checkliste. Qualitätsnetz bleibt die Datenbank (RLS, RPC-Prüfungen, SQL-Tests); der Review sichert Grenzen und Kontrakte, nicht Pixel.
- **Feedback-Zeitpunkt:** Strukturelles Feedback (Abläufe, Navigation, fehlende Schritte, Wording) jetzt nach dem Merge für Talent-Teil (Login → Onboarding → Profil → Programm → Bewerben) und Board; visuelle Feinheiten erst nach der Design-Umsetzung. Konrad sammelt je Runde eine Liste; die Architektur-Session sortiert in „jetzt (Welle 1)", „geplant (Welle 2+)", „Entscheidung nötig" und trägt Letzteres hier ein.

## 2026-09-10 — Welle 1 A5: Bewerbungs-Mails und Cron (Migrationen 0020/0021)
- **Architektur:** Die Datenbank entscheidet, *welche* Mail fällig ist, und legt sie als Auftrag in `mail_log` ab (`status = 'queued'`, `meta.vars`, `related_type`/`related_id`); der Versand läuft im Route Handler `/api/cron/mail` (Vercel Cron alle 10 Minuten, `lib/mail/queue.ts`, Resend). Grund: Freigabe-Gate und Statuswechsel sind DB-Regeln; so kann kein Client eine Mail auslösen oder unterdrücken, und jeder Schreibweg (RPC, Housekeeping, spätere Importe) erzeugt dieselben Mails.
- **Auslöser (Trigger):** `application` → `applied` ⇒ `application_received`; nach Freigabe je Statuswechsel `accepted`/`promoted`/`waitlisted`/`declined` ⇒ passende Mail; `decision_release` (Insert) ⇒ genau eine Mail je bereits entschiedener Bewerbung; `registration` → `confirmed` (nur mit Session) ⇒ `registration_confirmed`. `release_decisions()` setzt die Fristen jetzt **vor** dem Freigabe-Eintrag, damit die Zusage-Mail `confirm_by` kennt. Kein `expired`-Mailing, keine Erinnerungs-Kaskaden (E-Mail-Minimierung, 07.09.).
- **`queue_mail()`** (intern, nicht für `authenticated`): löst Empfängerin (primäre E-Mail), Sprache (`preferred_language`) und Suppression auf (gesperrte Adresse ⇒ Eintrag `suppressed` mit Hash, kein Klartext), verhindert doppelte offene Aufträge je Vorlage und Objekt.
- **Worker:** prüft Suppression vor dem Versand erneut (fail-closed), Resend-Idempotenz-Schlüssel `mail_log-<id>`, bis zu drei Versuche, dann `failed` mit Fehlertext; ohne `RESEND_API_KEY` (außer lokal) oder ohne `NEXT_PUBLIC_SITE_URL` bleibt die Warteschlange stehen statt Fehler zu produzieren. Lokal ohne Key: Dry-Run mit `provider = 'dev'`.
- **Housekeeping** `run_application_housekeeping()` (nur service_role/Admin): `expire_overdue_applications()` + Nachrücken für freigegebene Sessions mit Kapazität (`promote_waitlist` je freiem Platz, Mail per Trigger); Audit-Eintrag `application.housekeeping` nur bei Änderungen.
- **Infrastruktur:** `/api/cron/` im Proxy als öffentlich eingetragen (die Route prüft `CRON_SECRET` zeitkonstant selbst). `portalUrl()` nach `lib/mail/portal-url.ts` verschoben. `scripts/env-pull.sh` bewahrt jetzt auch lokal ergänzte Variablen (z. B. `CRON_SECRET` für den Dev-Server). **Offen bei Konrad:** `RESEND_API_KEY`, `RESEND_FROM`, `CRON_SECRET` in Vercel, Resend-Domain verifizieren (Checkliste).
- **Vorlagen:** sechs Schlüssel DE/EN in `mail_template` (nur angelegt, wo der Schlüssel fehlte; Pflege im Admin), Platzhalter `first_name`, `session_title`, `session_time`, `stage_name`, `event_name`, `confirm_by`, `portal_url`, `programme_url`. 0021 korrigiert das Zeitformat („13:30–14:00 Uhr").

## 2026-09-10 — PR #3 (B3) gemergt; Migration 0022: Lesewege für B6
- **PR #3** „Meine Teilnahme" (`/meine`) nach einem Review-Durchgang gemergt (`8ad098b`): Aktionen über RPCs mit Session-Client, Lesen nur unter RLS, Kollisionsdialog mit Sessionnamen, Ticketpflicht-Hinweis nur bei fehlendem eigenem Ticket; Aufräumpunkte (gemeinsame Aktionen, `Modal`/`ConfirmDialog` im UI-Kit, `dateLocale`) erledigt.
- **Befund der Build-Session bestätigt:** Entscheider konnten entscheiden, aber weder Bewerbungen noch Personen sehen; Rollen waren nur lesbar für die eigene Person und nirgends schreibbar. Entscheidung: **RPCs statt Policies** (Migration `v2_admin_read_paths_roles`, Datei 0022) — die Rechteprüfung bleibt an einer Stelle, die Rückgabe lässt sich je Rolle maskieren, und das Frontend braucht keinen service_role-Pfad mehr (`searchPeople` im Board kann auf `search_people()` umgestellt werden).
- **Regeln in 0022:** Gastgeber-Org (Entscheider über `is_member_of_org`, aber nicht Team) sieht Name, Profil und Antworten nur bei `consent_share` — sonst NULL (Datenminimierung, Consent-Zweck „Weitergabe an Partner"). Rollen-Entzug ist ein Ablaufdatum (`valid_to`), keine Löschung: Historie und Audit bleiben. Der letzte aktive globale Admin kann nicht entzogen werden (`last_admin`). `assign_role` ist idempotent über den bestehenden Unique-Index (gleiche Rolle im gleichen Scope wird reaktiviert). E-Mail-Adressen in der Personensuche nur für Admins; Team sieht Name, Tier, Stadt.
- Tests: `supabase/tests/v2_roles_admin.sql` (15 Schritte). B6 kann starten.
- **Nachtrag 0023** (`v2_roles_of_person_org_name`): `organization` hat keine Spalte `name`; `roles_of_person` liest `communication_name`/`legal_name`. Gefunden durch den neuen Test, bevor B6 darauf aufsetzt.
- **A5 in Betrieb:** Konrad hat `RESEND_API_KEY`, `RESEND_FROM` und `CRON_SECRET` gesetzt (Domain in Resend verifiziert). Der Vercel-Cron hat um 11:00 zehn Aufträge über Resend verschickt (alle an Wegwerf-Adressen `@example.invalid` aus dem B3-Walkthrough, die bouncen). Regel ab jetzt: **Test-Talente der Build-Session bekommen Resend-Testadressen** (`delivered@resend.dev`, bei mehreren `delivered+<name>@resend.dev`), damit keine Bounces auf `chef-treff.de` laufen.

## 2026-09-10 — Arbeitsauftrag Welle 2 (Speaker + Speaker-Leads) als Entwurf
- `docs/arbeitsauftrag-welle-2.md`: A1–A10 (Backend) und B1–B8 (Frontend) mit Akzeptanzkriterien, Regeln (Sprache, Rechte, Datenschutz, Status statt Sichtbarkeit), Definition of Done und acht offenen Punkten mit Defaults. Start der A-Migrationen nach Konrads Freigabe der Defaults, B-Bausteine nach Merge von B6.
- **Architekturentscheidungen im Entwurf (gelten mit der Freigabe):** `speaker_profile` edition-bezogen mit Pipeline-Status und Staff-Flags (Reception, Lounge, Pass-Typ, Hospitality, Reisekosten) · Schreiben für Speaker/Assistenz/Manager **nur per RPC mit Feld-Whitelist** (Assistenz ohne Consent und Bankdaten; Manager nur im Scope, Kontaktdaten nur im Scope) · Session-Inhalte als `session_submission` (eingereicht) getrennt von `session` (final), Freigabe kopiert · privater Storage-Bucket `speaker-assets` mit Pfad-Policies, Versionen, Technik-Check und Slid@Home-Freigabe nur mit Consent · minimale Tabelle `deadline` je Edition (später Wissensbasis) · Hospitality nur bei `hospitality_status = eligible` **und** Consent `hospitality_data`, Überbuchung ⇒ Warteliste · **Bankdaten ausschließlich im Supabase Vault**, Zugriff über eine RPC mit Audit, PDF-Auslagenrechnung serverseitig, SevDesk/Qonto erst mit Tokens (A6b) · Begleitticket als `ticket`-Zeile `requested`, Ausstellung mit vivenu (Welle 1 A1/A2) · Speaker-Oberfläche EN zuerst, Support nur über Rollen-Postfach.
- **Nicht in Welle 2 (C-Prio, Feedback FLS26):** Talk-Generator (R1), Hear-Me-Speak (R2), Slot-Grafik (R3), Timetable-Bild (R12); Foto-Pipeline (R6, S) nur bei Restzeit.

## 2026-09-10 — PR #4 (B6) gemergt; Welle 2 freigegeben; Migration 0024
- **PR #4** (Bewerbungs-Queue `/admin/bewerbungen`, Rollenverwaltung `/admin/rollen`) nach einem Durchgang gemergt (`e0d9c54`): RPCs mit Session-Client, `requireArea("admin")` überall, service_role-Suche im Board ersetzt durch `search_people()`. Offen war nur der Scope `org`: `organization` hat keine Lesepolicy → **Migration 0024 `search_organizations(query, limit)`** (Team, RPC statt Policy) — der Scope wird als kleiner Diff nachgeliefert.
- **Wegwerf-Konten im Walkthrough (Regel):** erlaubt, solange keine echten Nutzerdaten im Projekt liegen; nur Resend-Testadressen; Rollen mit Ablauf ≤ 2 h; Konto, Person, Rolle danach löschen; Konrads Konto bleibt unangetastet; Vorgang steht in der PR-Beschreibung. Ab Prozessstart 01.11. laufen Tests nicht mehr gegen die Produktionsdatenbank (Supabase-Branch).
- **Welle 2 freigegeben — Konrads Entscheidungen zu den acht Punkten** (eingearbeitet in `docs/arbeitsauftrag-welle-2.md`, Abschnitt E): Reception-Name bleibt vorerst · Deadline 48 h vor Slot / 14.04.2027 12:00, **nach der Deadline weiter annehmen, als `late` markieren** · Reisekosten: Status beim Eintragen + finale Freigabe und Antragsfreigabe durch den Program Lead Paulina (Rolle `area_lead_speaker`) · SevDesk direkt per API (Token vorhanden; make.com-Szenario Team-Portal als Vorlage), Qonto-Eingangsadresse als `QONTO_INBOX_EMAIL` in Vercel · `speaker_type` ohne company_tour_host · Begleitticket direkt über vivenu-API (make.com-Prozess Vorjahr als Vorlage) · Hotels Radisson Blu Dammtor (Standard), Grand Elysée (Premium), The Fontenay (VIP) mit `hotel_tier` · Foto-Pipeline nur bei Restzeit.

## 2026-09-10 — Welle 2 A1 + A2 live (Migrationen 0025–0027); PR #5 gemergt; Gate-Skript
- **A1 Speaker-Datenmodell + A2 Anlegen/Einladen** live: `speaker_profile` je Edition, Vokabulare `speaker_type`/`speaker_pipeline`/`hospitality_status`/`hotel_tier`, Scope-Prüfung `can_manage_speaker()`, RPCs für Speaker/Assistenz (Whitelist), Manager/Team (Whitelist je Rolle, Team-Felder gesperrt), Einladung erst ab `confirmed`, Assistenz mit eigener Rolle je Edition, Reisekosten-Freigabe durch `area_lead_speaker`/Admin, Mails `speaker_invite`/`assistant_invite` EN/DE. Speaker lesen ihr Profil ausschließlich über `my_speaker_profile()` (kein Zugriff auf `internal_notes`); RLS-Select auf der Tabelle nur für Manager/Team.
- **Zwei Befunde des neuen Tests, sofort behoben:** (0026) `text[] || 'literal'` in `speaker_next_steps` — derselbe plpgsql-Fallstrick wie in `move_slot`, Regel: immer `array_append`. (0027) Ersteller/Owner behielten nach Entzug der Manager-Rolle Schreibrechte → Owner/Ersteller/Scope zählen nur noch mit **aktiver** Rolle `speaker_manager`; Team immer.
- **Test-Regeln, die bleiben:** In einer SQL-Anweisung sieht eine Unterabfrage den Snapshot vor dem Funktionsaufruf (Ergebnis nach dem Aufruf in einer eigenen Anweisung prüfen); in einer Transaktion ist `now()` konstant, deshalb bei Rollen-Entzug im Test `valid_from` zurückdatieren.
- **PR #5** (Scope Organisation über `search_organizations`) als Diff geprüft und gemergt (`f811002`). Neu: **`scripts/gate-pr.sh <branch>`** — frischer Worktree mit `npm ci`, Lint, Test, Build; ersetzt den Versuch mit symbolisch verlinktem `node_modules` (Turbopack lehnt Symlinks außerhalb des Projekts ab). Ab jetzt der Standardweg, wenn der Build-Worktree auf einem anderen Branch steht.
- **Sprachregel Speaker-Bereich (Vorschlag Build-Session, akzeptiert):** Fallback je Bereich — wer eine Sprache gewählt hat, bekommt sie; ohne Wahl landet man im Speaker-Bereich auf EN statt DE. Keine neue Sprachlogik.
- **Offen bei Konrad (Checkliste):** Rollen-Postfach `speaker@chef-treff.de` anlegen oder als Alias auf das Team-Postfach legen — die Einladungsmails nennen es als Antwortadresse.

## 2026-09-10 — Welle 2 A3 + A4 live (Migration 0028)
- **A3 Session-Inhalte:** Einreichungen liegen in `session_submission`, die finale Fassung bleibt in `session`; `approve_session_content()` kopiert nach Sprache der Einreichung (DE ⇒ `*_de`, sonst `*_en`), Overrides des Teams gewinnen, ältere offene Einreichungen werden `superseded`, eine Freigabe ist nicht wiederholbar. Speaker **und** Assistenz (`is_speaker_side_of`) reichen ein; Freigabe nur `can_edit_session` (Team, Scope). `my_sessions()` liefert dem Speaker Zeit/Bühne/Raum aus `slot`/`stage`, Co-Speaker, letzte Einreichung und bei Assistenz „im Namen von".
- **A4 Uploads:** privater Bucket `speaker-assets` (100 MB; PDF, PPT, Keynote, Bilder), Pfad `<edition>/<profile>/<kind>/<datei>`, Storage-Policies über `speaker_asset_path_allowed()` (Speaker/Assistenz, Manager im Scope, Team). Der Client lädt direkt in den Bucket und registriert danach per `register_speaker_asset()` — die RPC prüft Pfad, Existenz des Objekts und Session-Zugehörigkeit, zählt Versionen, setzt `is_current` um. **Keine Sperre nach der Deadline:** `late = true` (Deadline der Edition ∧ 48 h vor Slot, `presentation_window()`), das Team sieht es. Slid@Home-Freigabe nur durch den Speaker selbst und nur mit Consent `slides_publication`; Technik-Check durch Produktion/Programm/Admin. Fotos laufen über denselben Bucket (`photo_asset_id`); ihre öffentliche Verwendung (Programm, Swapcard) folgt mit signierten URLs bzw. Sync, nicht in Welle 2 A.
- **Tabelle `deadline`** je Edition (Seed `presentation_upload` FLS27 = 14.04.2027 12:00, Zielgruppe speaker), lesbar für alle Angemeldeten, Pflege per `upsert_deadline()` (Team). Wird später von Wissensbasis, Checklisten und Countdowns mitbenutzt (Masterplan v0.1b).
- Test `supabase/tests/v2_speaker_content.sql` (26 Schritte) grün: Rechte, Ersetzen, Freigabe-Kopie, Pfadregel, Objektprüfung, Versionen, `late`, Consent-Pflicht, Technik-Check, Audit.

## 2026-09-10 — PR #6 (B1+B2) gemergt; Sprache ohne Zwangs-Default (Migration 0029)
- **PR #6** Speaker-Portal-Shell und Onboarding nach einem Durchgang gemergt (`5c9032a`): `requireArea("speaker")` überall, RPC-Whitelist, Consent-Schreiben nur für die eigene Person und serverseitig für die Assistenz gesperrt (die Policy hätte sonst die Einwilligung auf den Namen der Assistenz geschrieben — richtige Erkenntnis der Build-Session: eine Server-Action ist eine Route, kein Formular). Karten zeigen offene Schritte aus `next_steps.open`, erledigte kompakt darunter.
- **Sprache:** `person.preferred_language` war NOT NULL mit Default 'de' — „keine Wahl" existierte nicht, der Bereichs-Fallback konnte nie greifen. Entscheidung: Spalte **nullable ohne Default** (0029); explizite Wahl nur über Onboarding-Wizard und Profil; `claim_or_create_person`, `upsert_speaker`, `invite_assistant` setzen keine Sprache. Bestehende Personen ohne Login (nie gewählt, nur Testdaten) → NULL, Personen mit Login behalten ihre Wahl. Reihenfolge in der App: Wahl → Cookie → Bereichs-Fallback (Speaker EN) → Accept-Language → DE. Mails: `queue_mail` nimmt bei NULL Englisch für Speaker/Assistenz, sonst Deutsch.

## 2026-09-10 — Welle 2 A5 Hospitality live (Migration 0030)
- Kontingente je Edition (`hospitality_quota`): drei Hotels nach Tier (Radisson Blu Dammtor standard, Grand Elysée premium, The Fontenay vip) mit Platzhalter-Kapazitäten, zwei inaktive Shuttle-Platzhalter; Pflege durch das Team (`upsert_hospitality_quota`), Zahlen von Laura bis 01.11.
- **Freischaltung** ausschließlich über `speaker_profile.hospitality_status` (Team setzt `eligible`) **und** Consent `hospitality_data` des Speakers; die Assistenz darf buchen, den Consent gibt nur der Speaker. Hotels nur bis zum eigenen `hotel_tier` (VIP darf tiefer buchen, Standard nicht höher). Ein aktives Hotel je Profil, ein Platz je Shuttle; Shuttle-Kapazität zählt Gäste, Hotel zählt Zimmer.
- **Überbuchung ⇒ Warteliste** statt Fehler (Entscheidung Arbeitsauftrag); Storno lässt den ersten Wartenden als Anfrage nachrücken (Team bestätigt), Status geht ohne aktive Buchung zurück auf `eligible`. Bestätigung nur Team (`is_staff`), löst genau eine Mail `hospitality_confirmed` aus (EN/DE nach Sprache, Speaker ohne Wahl EN).
- Test `supabase/tests/v2_hospitality.sql`.

## 2026-09-10 — PR #7 (B3 „Deine Session") gemergt
- Nach einem Durchgang gemergt (`a8ddd42`): Einreichung/Registrierung/Slid\@Home über RPCs, Direkt-Upload in den Bucket mit entschärftem Objektschlüssel, signierte URLs, kein service_role.
- **Regel geschärft:** Ein zweiter Riegel in der Server-Action ist nur dort nötig, wo RLS den Aufruf still umbiegt (Insert „eigene Zeile" wie beim Consent). Wo die RPC mit 42501 antwortet (Slid\@Home), bleibt die RPC die einzige Wahrheit.
- Build-Session hat `|| "de"` in Onboarding- und Profil-Action zu `|| null` korrigiert — sonst hätte jedes Speichern die Sprache festgelegt und den Bereichs-Fallback ausgehebelt (Ergänzung zu 0029).

## 2026-09-10 — Welle 2 A6 Reisekosten live (Migrationen 0031–0033)
- **Bankdaten nur im Vault:** `set_expense_bank_details` legt/aktualisiert ein Vault-Secret (`expense_bank:<claim>`); die Tabelle hält nur Maske (`DE****1234`) und Kontoinhaber. Entschlüsselt wird ausschließlich über `expense_bank_details()` für Admin/`area_lead_speaker`, jeder Aufruf mit Audit `expense.bank_view`. Nur der Speaker selbst legt Bankdaten an und reicht ein (Assistenz: 42501 `speaker_only`); Entwurf und Belege darf die Assistenz vorbereiten.
- **Voraussetzung** zweistufig wie in Abschnitt E des Arbeitsauftrags: `travel_costs_covered` (Lead beim Eintragen) **und** `travel_costs_approved_at` (Paulina/Admin). Ohne beides kein Antrag (`not_eligible` mit Grund `not_covered`/`not_approved`).
- **Einreichung:** jede Position braucht einen Beleg (`receipt_required`), Betrag > 0 und ≤ 5.000 € je Position, höchstens 50 Positionen, Kategorie aus dem neuen Vokabular `expense_category` (Bahn, Flug, Auto/Kilometer, ÖPNV/Taxi, Hotel, Sonstiges). Rechnungsnummer `RK-JJJJ-NNNN` wird bei der ersten Einreichung vergeben und bleibt bei Ablehnung/Wiedereinreichung erhalten. Ablehnung braucht eine Begründung (geht per Mail an den Speaker), der Antrag fällt zurück in den Entwurf.
- **Mails:** `expense_submitted` an alle aktiven `area_lead_speaker`, ohne solche an globale Admins (Fallback); `expense_approved`/`expense_rejected` an den Speaker in seiner Sprache, Betrag über `fmt_cents` formatiert.
- **Lektion Spalten-Grants:** `revoke select (spalte)` wirkt nicht gegen einen Tabellen-Grant (Postgres-Regel) — 0031 ließ `bank_secret_id` deshalb lesbar. 0032 nimmt den Tabellen-Grant weg und vergibt eine explizite Spaltenliste (Muster wie `stage`/`slot`/`application`/`ticket`). Folge für die App: `expense_claim` nie per `select *` lesen, sondern über `my_expense_claims()`/`expense_queue()`.
- **A6b (App, offen):** Server-Route erzeugt die Auslagenrechnung als PDF (Absender Speaker, Empfänger ChefTreff, Positionen, IBAN maskiert), legt sie mit service_role als `speaker_asset` (Kind `invoice`) ab, schickt sie per Resend-Anhang an `QONTO_INBOX_EMAIL`, legt den SevDesk-Beleg per API an (`SEVDESK_API_TOKEN`) und schreibt Referenzen über `set_expense_integration()`. Der make.com-Prozess des Team-Portals dient nur als Feldvorlage.
- Test `supabase/tests/v2_expenses.sql` (25 Schritte grün).

## 2026-09-10 — Welle 2 A7 Speaker-Ticket + Begleitticket live (Migration 0034)
- **Anspruch:** ab `pipeline_status = confirmed` (und allen späteren Stufen) entsteht das Speaker-Ticket automatisch als Freiticket im Status `requested` (Trigger auf `speaker_profile`; Pass-Typ und Lounge aus dem Profil, Änderungen ziehen nach, solange nicht ausgestellt). Absage/Rückstufung zieht offene Freitickets zurück; ausgestellte bleiben und storniert das Team über vivenu.
- **Ticket-Modell erweitert:** `ticket.status` kennt `requested` und `approved`; `barcode` ist bis zur Ausstellung NULL (bleibt unique); neue Spalten `speaker_profile_id`, `lounge_access`, `team_note`, `requested_by`, `approved_by/at`; `source` `speaker` bzw. `speaker_companion`; je Profil höchstens ein aktives eigenes und ein aktives Begleitticket (partielle Unique-Indizes ⇒ 23505 bei der zweiten Anfrage).
- **Begleitticket:** Speaker, Assistenz oder Manager fragen mit E-Mail + Name an (Formatprüfung; nicht die eigene Adresse). Für die Begleitung entsteht **keine `person`** (Datenminimierung), nur die Holder-Felder am Ticket; die Verknüpfung mit einem Konto passiert wie bei gekauften Tickets über `holder_email` beim Login. Team (`is_speaker_team`) bestätigt (`approved`) oder lehnt mit Grund ab; die Mails `companion_ticket_confirmed`/`companion_ticket_declined` gehen an den **Speaker**, das Ticket selbst bekommt die Begleitung über vivenu. Jede neue Anfrage benachrichtigt `area_lead_speaker` (Fallback globale Admins) über `companion_ticket_requested`.
- **Ausstellung:** `set_ticket_issued(ticket, vivenu_ticket_id, barcode, transaction?, ticket_type_map?)` durch service_role (App-Route mit vivenu-API) oder Team; eigenes Ticket direkt aus `requested`, Begleitticket erst aus `approved`. **Offen (A7b):** die Route wartet auf den vivenu-Zugang (Support-Anfrage, Sandbox-Key). Der Ingest aus Welle 1 A1 muss auf `vivenu_ticket_id` upserten, damit der Webhook zum selben Freiticket keine zweite Zeile anlegt.
- **Lesewege:** `my_speaker_tickets()` für Speaker/Assistenz (der Begleit-Barcode wird nicht ausgegeben), `speaker_tickets_admin(edition?)` für das Team (B8). Interne Helfer `speaker_ticket_create` und `notify_speaker_leads` sind für `authenticated` gesperrt. **Regel:** neue Funktionen sind per Default-Privileg für `authenticated` ausführbar; interne Helfer brauchen deshalb immer ein explizites `revoke execute … from authenticated` (wie `queue_mail`, `log_audit`).
- **Hospitality:** `hospitality_block_reason()` liefert bei `hospitality_status = declined` jetzt `declined` statt `status`; die Oberfläche (B4) braucht die Ableitung über den Status nicht mehr.
- Test `supabase/tests/v2_speaker_tickets.sql` (28 Schritte grün).

## 2026-09-10 — Auslagenrechnung als PDF: Ablage erst bei der Freigabe (B8), nicht aus dem Speaker-Portal
- Der Speaker-Bereich (B6, PR #9) erzeugt die Auslagenrechnung nur als **Live-Vorschau** (Route Handler, Nutzer-Client, IBAN maskiert) und legt nichts ab. Das Dokument für SevDesk und das Qonto-Postfach darf nicht aus einem Upload stammen, den der Client beeinflussen könnte; deshalb bleibt `register_speaker_asset` für die Art `invoice` gesperrt und es gibt kein `register_invoice_asset`.
- Die **Freigabe (B8)** erzeugt das endgültige PDF serverseitig aus den eingefrorenen Antragsdaten (gleiche Render-Funktion wie die Vorschau), legt es nach Rollenprüfung (`is_expense_approver`) mit service_role als `speaker_asset` (Art `invoice`, Pfad `<edition>/<profile>/invoice/<invoice_no>.pdf`) ab, ruft `set_expense_integration(claim, invoice_asset_id, sevdesk_ref, qonto_sent)` und hängt das PDF an SevDesk-Beleg und Qonto-Mail. Der Speaker sieht danach dasselbe Dokument über `invoice_asset_id` (signierte URL; die Storage-Policy erlaubt ihm den `invoice`-Pfad lesend).

## 2026-09-10 — Welle 2 A8/A9 live (Migrationen 0035/0036): Präsentations-Erinnerung, `my_manager_scope`, QR nur für den Speaker
- **Erinnerung bedarfsgesteuert** (Feedback T9/P4): genau eine Mail `presentation_reminder` je Speaker × Session, nur wenn die Session einen Slot in der Zukunft hat, nicht abgesagt ist, der Speaker ab `confirmed` steht und keine aktuelle Präsentation (für die Session oder ohne Session-Bezug) vorliegt. Zeitpunkt: `reminder_lead_hours` (Default 48) **vor der wirksamen Fälligkeit** (Deadline ∧ 48 h vor Slot) — Lesart von „48 h vor Slot" im Arbeitsauftrag als Vorlauf vor der Fälligkeit, damit die Mail vor dem Stichtag kommt; das Team kann den Vorlauf je Deadline ändern (`upsert_deadline`, 0–720 h). Empfänger ist der Speaker, nicht die Assistenz. Ausgelöst im Housekeeping des Mail-Crons (alle 10 Minuten), Rückgabe `reminders`; Audit `presentation.reminder`. ICS-Anhang (S) offen, der Worker kennt noch keine Anhänge.
- **`my_manager_scope()`** liefert `team`, `all` (Team oder globaler `speaker_manager`), `is_manager` sowie `editions`, `stages`, `stage_days`, `slots` (je mit `edition_id` für die Board-Vorauswahl) aus aktiven `speaker_manager`-Zuweisungen und `owned_profiles`. `manager_speakers(edition?)` (mit `next_open`) und `speaker_next_steps(profile)` bestehen seit 0025/0026.
- **QR nur für den Speaker (0036):** `my_speaker_tickets()` gibt der Assistenz `issued`, aber keinen `barcode` — der QR ist die Eintrittsberechtigung, Ausblenden nur in der Oberfläche wäre Schein (Hinweis der Build-Session zu B5). Begleit-Barcodes kommen ohnehin nicht ins Portal.
- **Mail-Plan** als Doku (`docs/mail-plan.md`): alle Vorlagen mit Auslöser, Empfänger, Einmaligkeit und Sprachregel; Pflicht bei jeder neuen Vorlage.
- **Advisor:** WARN `authenticated_security_definer_function_executable` (114 Funktionen) ist das Muster dieser Architektur — RPCs prüfen Rechte innen, interne Helfer sind für `authenticated` explizit gesperrt; INFO `rls_enabled_no_policy` auf Systemtabellen (`audit_log`, `external_ref`, `import.*`, `integration.*`) heißt bewusst „kein API-Zugriff, nur service_role". Kein ERROR.

## 2026-09-10 — PR #9 (B6 Reisekosten) und PR #8 (B4 Travel) gemergt
- **#9** nach einem Review-Durchgang plus Diff: Beträge ab 1.000 € (Gruppierung im Eingabefeld ⇒ NaN beim Zurückrechnen) behoben, PDF-Aufbau als gemeinsames Modul `lib/expenses/invoice-pdf.ts` für die Vorschau (B6) und das endgültige Dokument (B8). Gate grün, Merge `a6d55e1`.
- **#8** war gegen `main` nach #9 konfliktbehaftet (Navigation, beide Wörterbücher, `rpc-error.ts`, jeweils additive Blöcke). Die Architektur-Session hat den Merge lokal aufgelöst (Anreise vor Reisekosten in der Navigation, beide Wörterbuchblöcke, beide Schlüsselgruppen) und den Merge-Stand durch das Gate geschickt, statt eine weitere Rebase-Runde zu drehen. Diff-Prüfung 7e16618: `declined` als eigener Sperrgrund, Datumsfix mit `timeZone: "UTC"`.
- **Ergänzung Regel:** Bei rein additiven Konflikten zwischen zwei fertigen PRs löst die Architektur-Session den Merge selbst (Gate auf dem Merge-Stand ist Pflicht); inhaltliche Konflikte gehen zurück an die Build-Session.

## 2026-09-10 — Arbeitsauftrag Welle 3 (Partner + Messeshop) als Entwurf
- `docs/arbeitsauftrag-welle-3.md` angelegt: A1–A13 Backend (Produktstamm-Import aus der Item-Liste, HubSpot-Ingest mit Gate und Sweep, Org-Kontext/Kontaktrollen, produktabhängige Checkliste mit Datei-Regeln und Eingangsbestätigung, Erinnerungs-Digest je Org, Ticket-Codes/Secret Shop, Bewerber-Auswahl mit Audit, Standbühne, Messeshop mit Lagerbuch und Phasen, SevDesk-Entwürfe für Shop-Bestellungen, Sanity/Event-App als S), B1–B9 Frontend, Regeln, DoD, 14 offene Entscheidungen (Kontaktrollen-Matrix, Fristen 2027, USt/Erlöskonten, HubSpot-SKUs, Paket-Rechnungen, Bewerberfelder, Strategy-Calls, Secret-Shop-Fallback, Dashboard-Versand, Gate-Benachrichtigung, Initiativen-Produkt, Bühnen-Rolle, Event-App, Merch).
- **Messeshop:** Fachlogik (Bestell-Lebenszyklus draft/pending/editing/completed/cancelled, append-only Lagerbuch mit Delta-Abgleich, Phasen aus Fristen, serverseitige Fristprüfung, Snapshots auf Zeilen) wird aus Patricks Plan (Messeshop 3.0, Drive-Handover) **übernommen**, Code und Airtable nicht (Antwort 16: sauber neu in der Plattform). Rollentrennung entfällt zugunsten von Produkt-Hinweisen (S2).
- **HubSpot-Gate** wie P15: fehlende Pflichtdaten setzen die Deal-Phase zurück und benachrichtigen Sales; Ingest genau einmal je Event, nächtlicher Sweep gegen verpasste Webhooks.

## 2026-09-10 — Welle 3 freigegeben: Konrads Entscheidungen zu den 14 Punkten
- Eingearbeitet in `docs/arbeitsauftrag-welle-3.md` Abschnitt E. Kern: Kontaktrollen `primary_ops` / `additional` / `signing` / `event_app_member`, **Accounting ohne Login** (Rechnungs-E-Mail); Fristen 2027 mit **Nachbestellphase 3 bis 09.04.2027 nur für Lunch-Paket & Co.**; USt vorerst 7 % überall; SKU = Item-ID; nur Shop-Rechnungen aus dem Portal; Bewerber-Export als v2 fest eingeplant; Strategy-Calls Welle 5; Secret Shop bevorzugt (vivenu-Prüfung); Dashboard-Versand im Härtungsfenster; Gate-Fehler an Slack `#fls27-onboarding` + Deal-Owner; **Swapcard bleibt 2027**; Merch-Artikel 2027 offen.

## 2026-09-10 — PR #11 (B7 Lead-Portal) gemergt; drei Funde der Build-Session entschieden
- **Personensuche bleibt team-only.** `search_people` prüft `is_staff()`; ein reiner `speaker_manager` bekommt 42501. Das bleibt so: eine Suche über alle Personen wäre Kontaktdaten außerhalb des Scopes (Masterplan §4). `upsert_speaker` verknüpft eine bestehende Person ohnehin über die E-Mail (Dublettenprüfung), das Lead-Portal zeigt den Hinweis statt des Suchfelds.
- **Einreichungen freigeben durch den Lead (Migration 0037):** `approve_session_content`, `reject_session_content` und `pending_submissions` akzeptieren zusätzlich `can_manage_speaker(speaker_profile_id)` — der Manager im Scope gibt Inhalte seiner Speaker frei, auch wenn die Session außerhalb seines Slot-Scopes liegt (Arbeitsauftrag Welle 2 D „Freigabe durch Lead"). Programm-Team weiter über `can_edit_session`. `manager_speakers` liefert jetzt `internal_notes` (der Drawer war schreib-only). Test `supabase/tests/v2_content_manager.sql`.
- **Board als Scope-Variante im Lead-Portal = eigener kleiner Baustein B7b:** Das Board unter `/admin/programm` prüft `requireArea("admin")`; die Actions nutzen ausschließlich den Nutzer-Client, Schreibrechte kommen aus `can_edit_slot`/`can_edit_session` (kennen `speaker_manager`-Scopes). Das Bereichs-Gate wird auf „Admin **oder** Speaker-Leads" erweitert (Helfer `requireAnyArea`), das Board unter `/speaker-leads/board` mit Vorauswahl `edition_id` aus `my_manager_scope` gemountet; Sicherheitsgrenze bleibt die RPC. Dazu im selben PR die Einreichungs-Queue (`pending_submissions`) im Lead-Portal.

## 2026-09-10 — Secret Shop je Partner per vivenu-API bestätigt
- Prüfung im eingeloggten Dev-Dashboard und in der API-Doku (siehe `docs/vivenu-support-anfrage.md`, Nachtrag): Undershops sind `underShops[]` am Event und werden über `PUT /api/events/{id}` angelegt; ein Coupon mit `unlocks[{eventId, underShopId}]` schaltet den Shop frei, `maxTickets`/`allowedTickets` bilden das Kontingent. **Entscheidung 8 (Welle 3) damit: Secret Shop wird gebaut**, Coupon allein nur als Fehler-Fallback. Regel: nur unsere Route schreibt das `underShops`-Array (serialisiert je Event, idempotent), nie parallel aus dem Dashboard während eines Ingests.

## 2026-09-10 — vivenu-Antwort zur Bestätigungsseite eingearbeitet
- vivenu bestätigt den Ansatz „eigene Bestätigungsseite + Personalisierung bei uns": Custom Confirmation Page URL je Event, Redirect mit **`?transactionId=`** (Welle 1 B4 liest diesen Parameter statt `tx`), Transaktion und Tickets per API, Rückschreiben über `POST /api/tickets/personalize/{id}/{secret}`; Zusatzfelder werden aus `event.ticketExtraFields` nach vivenus Filterregeln abgeleitet (in `docs/vivenu-support-anfrage.md`). Das eingebettete Ticket-Modal bleibt Rückfalloption, nicht der Standard: unser Formular bestimmt Design, Sprache und Datenminimierung.
- Offene Punkte (Inhaber-E-Mail, DETAILSREQUIRED, Ticket-Mail, Webhook-Signatur/Retry/Event-ID, Freitickets, Undershops in Serie, Add-ons, `meta`, Sandbox, Kaution, Einlass) gehen als **eine Sammelmail** an vivenu.

## 2026-09-10 — Welle 3 A1 Produktstamm live (Migration 0038, Import gelaufen)
- `product`, `product_component`, `deliverable_template` und Vokabular `product_category` angelegt; **Import aus der Item-Liste 2026**: 186 Zeilen → **162 Produkte** (24 namenlose Platzhalter entfallen, Antwort 83), 29 Pakete / 57 Zusatzleistungen / 75 Shop-Artikel, 75 im Shop sichtbar, 109 mit Listenpreis; **23 Stücklistenzeilen** für die fünf Standpakete (drei Zeilen mit dem Platzhalter I-25643 entfallen). `I-69384` „Agency Area Partner (Premium)" hatte keine Kategorie → `specials` (Skript-Override). USt überall 7 % (Entscheidung 3), Preise in Cent, SKU = Item-ID (Entscheidung 4).
- **Bilder folgen** in einem eigenen Schritt: die Attachment-URLs der CSV sind abgelaufen; Import braucht einen Airtable-Token (Konrad) oder manuelle Ablage im Bucket `product-images`.
- Fristen FLS27 für Partner als `deadline`-Zeilen (Entscheidung 2): Rückwand 12.03., Shop Phase 1 19.03., Phase 2 02.04., Phase 3 (Nachbestellung) 09.04., Ticket-Codes 31.03., Lunch-Paket 09.04.2027.
- Ab jetzt gilt: **Produkte nur noch im Portal pflegen** (`upsert_product`, Admin B9); die Item-Liste in Airtable ist eingefroren (Abschluss-Checkliste).

## 2026-09-10 — Nachbesserungen aus dem B8-Review (Migration 0039)
- `expense_bank_details` war `STABLE`, schreibt aber ein Audit ⇒ in PostgREST-Lesetransaktionen 25006, Funktion unbenutzbar (Fund der Build-Session). Jetzt `VOLATILE`. **Regel:** Funktionen mit Audit- oder Mail-Schreibzugriff sind nie `stable`; geprüft über `pg_proc` — keine weitere betroffen.
- Speaker/Assistenz dürfen den Pfad `…/invoice/…` im Bucket nicht mehr beschreiben (Insert/Update/Delete-Policies), lesen bleibt; damit kann nach der Freigabe keine andere Datei untergeschoben werden. Revidiert die Aussage „keine Policy-Änderung" aus dem #9-Review.
- `expense_queue` liefert `currency` (die Freigabe hatte „undefined" im PDF riskiert).

## 2026-09-10 — Welle 3 A3 Org-Kontext und Kontaktrollen live (Migration 0040)
- `org_edition` trägt den Onboarding-Stand und die Rechnungsdaten je Partner × Edition; **Accounting ist kein Login**, sondern `invoice_email` (Entscheidung 1). `org_product` hält die gebuchten Leistungen (HubSpot-Line-Items) und steuert später Checkliste und Sichtbarkeit.
- Kontaktrollen liegen in `org_membership.roles` (Vokabular `contact_role`): `primary_ops` genau einer je Organisation (partieller Unique-Index + `primary_exists`/`primary_required`), `additional` (alles außer Mitgliederverwaltung), `signing`, `event_app_member`. Der Portal-Zugang hängt an `role_assignment partner_contact` mit Scope `org`; Entfernen eines Kontakts löscht Membership und Rolle, nicht die Person (Löschkonzept bleibt bei „Profil löschen").
- Rechte: Kontakte verwaltet nur `primary_ops` (oder Partner-Team = Admin/`area_lead_partner`); Onboarding-Daten pflegen `primary_ops`, `additional`, `signing`; Preise und Rechnungsdaten sieht `event_app_member` nicht. Neue Kontakte bekommen `partner_contact_invite` (dedupliziert je Membership).
- Onboarding gilt als `filled`, wenn Firmenname, Kommunikationsname, Adresse, Rechnungs-E-Mail und Beschreibung stehen; die Logo-Pflicht kommt mit A4 dazu.
- Lektion: `position` ist in `returns table (…)` ein reserviertes Wort — Spalte heißt `contact_position`.
- Sicherheits-Sweep dabei: alle 47 öffentlichen Tabellen haben RLS; Tabellen ohne Policy (organization, org_membership, mail_log, audit_log, …) sind für die API deny-all. Offen für das Härtungsfenster: die pauschalen Insert/Update-Grants für `authenticated` auf Tabellen ohne Schreib-Policy (u. a. `role_assignment`, `event`, `slot`, `ticket`, `person_email`) systematisch entziehen, damit eine spätere Policy nicht versehentlich Schreibwege öffnet.

## 2026-09-10 — Welle 3 A4 Checkliste, Uploads, Stand live (Migration 0041)
- Die Checkliste entsteht **nur** aus gebuchten Leistungen: Trigger auf `org_product`/`org_edition` leiten `deliverable`-Zeilen aus `deliverable_template` ab (global, Kategorie, SKU). Storno setzt offene Pflichten auf `not_required`, erneute Buchung öffnet sie wieder; Eingereichtes und Angenommenes bleibt stehen. Fristen kommen aus `deadline` (`due_rule.deadline_key`) oder relativ zur Einladung (`offset_days`).
- Datei-Regeln je Vorlage (`file_rules`: Endung, MIME, Größe) prüft `register_partner_asset` serverseitig. Die MIME-Liste des Buckets ist bewusst weit (SVG, EPS/PostScript, PDF, PNG, JPEG, WebP, ZIP, `application/octet-stream` als Browser-Fallback für EPS), die scharfe Prüfung sitzt in der RPC. Logo (P2) nur svg/eps/ai/pdf. Dateien sind versioniert (`is_current`); die Onboarding-Stufe `filled` verlangt jetzt zusätzlich ein aktuelles Vektorlogo.
- Eingangsbestätigung (P1) geht an die einreichende Person **und** den Hauptkontakt; Ablehnung nur mit Grund, der in der Mail steht; erneute Einreichung erlaubt. Team-Warteschlange `partner_review_queue`, Stand-Maße über `upsert_booth` (Partner sehen keine internen Notizen).
- Abweichungen zum Arbeitsauftrag: `deliverable` referenziert `template_id` statt `template_key` (Schlüssel wie `backdrop_print` gibt es je SKU mehrfach); `partner_asset.kind` ist ein freier Bezeichner (Regex) statt fester Liste, weil Vorlagen im Admin entstehen (`upsert_deliverable_template` + `resync_deliverables`); Status `overdue` setzt erst das Housekeeping in A5.
- Lunch-Paket (Entscheidung 2): Shop-Artikel I-79520 ist `late_orderable`; die Pflicht `lunch_package` hängt an jedem Stand (Kategorie `standflaeche`) mit Frist 09.04.2027.

## 2026-09-10 — `queue_mail` dedupliziert je Empfänger (Migration 0042)
- Fund beim A4-Test: `queue_mail` verwarf einen Auftrag schon, wenn Vorlage + Bezugsobjekt noch in der Warteschlange standen — unabhängig vom Empfänger. Bei Fan-outs (`notify_speaker_leads` an mehrere Leads, Eingangsbestätigung an zwei Personen) bekam nur der erste Empfänger die Mail. Jetzt gehört die Person zum Schlüssel; der Schutz „dieselbe Mail nicht doppelt an dieselbe Person, solange sie noch nicht raus ist" bleibt. Mail-Plan angepasst, Test `v3_partner_deliverables.sql` Schritt 11 prüft zwei Empfänger.

## 2026-09-10 — PR #13 (B7b + B8-Nachträge) gemergt; Welle 2 Frontend komplett
- Das Programm-Board lebt in `components/programme/` und bedient `/admin/programm` und `/speaker-leads/board`. Gate `requireAnyArea` regelt nur den Eintritt; Schreibrechte kommen ausschließlich aus den RPCs — gegengeprüft: `move_slot`/`set_slot_status` → `can_edit_slot`, `create_slot` → `can_edit_stage`, `attach_session_to_slot` → `can_edit_session ∧ can_edit_slot`, `detach_session`/`set_session_speakers`/`set_session_questions` → `can_edit_session`, `publish_session`/`unpublish_session` → `is_programme_editor` (Manager veröffentlichen nicht), `upsert_session` neu → Programm-Editor oder Manager/Bühnen-Editor.
- `storeInvoice` legt Rechnungen nur für `approved`/`paid` ab (`lib/expenses/state.ts`); dieselbe Regel kommt serverseitig in `set_expense_integration` (nächste Migration). SevDesk erhält das PDF über `uploadTempFile` — **ungeprüft bis zum ersten Lauf mit Token**: erste echte Freigabe im Vercel-Log gegenlesen.
- `deadline.audience` bleibt Freitext in der Tabelle; die Oberfläche bietet die vier verabredeten Werte (speaker, partner, volunteer, all). Ein Vokabular kommt, wenn ein fünfter Wert nötig wird.
- Welle 2 Frontend ist damit komplett (B1–B8 + B7b). Offen: A7b vivenu-Ausstellung (Antwort ausstehend), E2E-Protokoll nach Konrads Feedback-Runde.

## 2026-09-10 — Welle 3 A2 HubSpot-Ingest live (Migrationen 0043/0044, Webhook-Route + Sweep)
- Der Ingest ist eine service_role-RPC (`ingest_partner_deal`): Gate und Schreiben in **einer** Transaktion. Bei Fehlern entsteht nichts außer `integration.sync_error` und der Mail `partner_gate_failed` an den Deal-Owner (Person per E-Mail; Fallback `area_lead_partner`, dann Admins); die Route setzt die Deal-Phase auf den vorherigen Wert aus der Eigenschafts-Historie zurück und meldet über make nach Slack `#fls27-onboarding` (Entscheidung 10).
- Abweichungen zum Arbeitsauftrag: Pipeline und Phase stehen **in der Datenbank** (`event.hubspot_pipeline_id`/`hubspot_onboarding_stage_id`, Team-RPC `set_edition_hubspot`) statt in Env-Variablen — mehrere Editionen, Sweep je Edition, kein Deploy für FLS28. **Mehrere Deals je Partner × Edition** (Nachbuchungen) über `partner_deal`; `org_edition.hubspot_deal_id` bleibt der erste Deal. Kontaktrolle *Buchhaltung* aus HubSpot ⇒ nur `invoice_email`, kein Login (Entscheidung 1). Der Bühnen-Editor bekommt Scope **org**, weil die Bühne beim Ingest noch nicht existiert; `can_edit_stage`/`can_edit_slot` akzeptieren `stage.partner_org_id` — Produktion legt die Bühne später mit der Org an, die Rechte greifen sofort. Ticket-Kontingente aus `product.pass_type` (I-32776 partner, I-46500 talent, I-62740 investor), Bühnen-Rolle aus `product.grants_role` (I-79895) — im Produktstamm pflegbar, kein Code je SKU. Rolle `partner_contact` läuft bis Editionsende (Tag nach dem Summit, lokale Zeit), auch für im Portal angelegte Kontakte (gemeinsamer `partner_contact_upsert_internal`).
- Route: Signatur v3 prüfen, Ereignis festhalten, sofort antworten, danach verarbeiten (`after`); Duplikate über `(source, external_id)`. Sweep täglich 03:15 UTC (max. 25 Deals je Lauf), Einzel-Deal über `?deal=` mit Cron-Secret. Runbook `docs/runbooks/hubspot-ingest.md` (Scopes, Subscription, IDs, Labels, Property-Namen, Fehlerschlüssel).
- **Betriebsvoraussetzung:** HubSpot muss die Route erreichen — die `vercel.app`-URL steht hinter der Deployment-Protection. Der Webhook geht erst mit der eigenen Domain (Domain-Umzug) oder einer früheren Zwischen-Domain in Betrieb; bis dahin Sweep und `?deal=`. Offen bei Konrad: Private App anlegen (Token, Client Secret), Labels und Custom Properties in HubSpot, IDs mit `scripts/hubspot-pipelines.mjs` ermitteln, make-Szenario für Slack.
- Nebenbei: `set_expense_integration` akzeptiert nur noch `approved`/`paid` (P0001 `not_approved`) — serverseitiges Gegenstück zur Regel aus PR #13.

## 2026-09-10 — Welle 3 A5 Erinnerungs-Digest live (Migrationen 0045/0046)
- Das Partner-Housekeeping hängt im bestehenden Cron `/api/cron/mail` (`run_application_housekeeping()` liefert jetzt `partner`): Fälligkeiten der Checkliste folgen geänderten Fristen (`refresh_deliverable_due`), `overdue` wird gesetzt und wieder aufgehoben, wenn das Team eine Frist verschiebt (`mark_overdue_deliverables`).
- Digest `partner_reminder_digest`: **eine** Mail je Org × Edition an `primary_ops` und `additional` (nicht `event_app_member`) mit allen Pflichten, die überfällig, zurückgewiesen oder innerhalb `reminder_lead_hours` der zugehörigen Frist fällig sind (Default 7 Tage, Partner-Fristen stehen auf 168 h, Shop-Phase 3 auf 72 h). Frühestens alle 7 Tage (`mail_log`-Prüfung), keine Einzel-Reminder (T9/P4). Zurückgewiesene Pflichten ohne Frist (z. B. Logo) erscheinen, sobald ein Digest fällig ist; offene Pflichten ohne Frist nicht — die Logo-Pflicht läuft über den Onboarding-Status (Dashboard-Versand im Härtungsfenster, Entscheidung 9).
- Lektion (0046 als Korrektur zu 0045): `mail_log` hat kein `created_at`, nur `queued_at` — plpgsql prüft Spalten erst zur Laufzeit, der Test hat es gefangen. Test-Regel ergänzt: interne Funktionen ohne Grant nicht als Superuser „aufrufen und Fehler erwarten“, sondern das Privileg prüfen.

## 2026-09-10 — Welle 3 A7/A8 Bewerber-Auswahl und Standbühne live (Migration 0047)
- Partner entscheiden über Bewerbungen ihrer Formate (Session mit `host_org_id`, gesetzt vom Programm-Team über `upsert_session`) mit den bestehenden RPCs `decide_application`; der Versand bleibt beim Team (`release_decisions`). Neu: `can_decide_session` akzeptiert die Host-Org nur mit **aktiver** Rolle `partner_contact` und Kontaktrolle `primary_ops`/`additional`/`signing` — bisher genügte die Mitgliedschaft (`is_member_of_org`), also auch ein `event_app_member` oder eine abgelaufene Rolle.
- Bewerberdaten für Partner nur über `partner_applications(session)`: gleicher Consent-Filter wie im Team-RPC (Name, Antworten und Profil nur bei `consent_share`), **jeder Abruf im Audit** (`application.partner_view` mit Org und Zeilenzahl, Masterplan §4). Kein Export (Entscheidung 6, v2 im Härtungsfenster). `partner_sessions(org)` liefert nur Zähler.
- Standbühne (A8): Die Rolle kommt aus dem Bühnenprodukt (0043, Scope org), `can_edit_stage`/`can_edit_slot` kennen `stage.partner_org_id`; `my_partner_stages()` listet die eigenen Bühnen. Produktion legt die Bühne mit `partner_org_id` an, Veröffentlichung bleibt Programm-Team (`publish_session` prüft `is_programme_editor`).

## 2026-09-11 — Welle 3 A9 Messeshop-Backend live (Migration 0048)
- Bestellung je Org × Edition × **Phase**; die Phasen kommen aus `deadline` (`shop_phase_1` 19.03., `shop_phase_2` 02.04., `shop_phase_3` 09.04.2027; Phase 3 nur `late_orderable`, Entscheidung 2) und werden in jeder Schreib-RPC serverseitig geprüft. Ablauf: Entwurf → `shop_confirm` (bestätigt, Bestand reserviert, Mail) → optional `shop_edit`/erneut bestätigen → zur Frist verbindlich (`completed`, Mail) oder storniert. Nie bestätigte Entwürfe verfallen zur Frist ohne Mail.
- **Bestand nur über das Lagerbuch** (`stock_ledger`, append-only): verfügbar = `stock_total` + Σ delta, nur für Produkte mit `track_stock` (nach dem Import steht das überall auf `false`; das Team schaltet es je Artikel im Produktstamm ein). Reservierung delta-bewusst, zweite Bestätigung mit gleicher Menge = Delta 0, Storno gibt frei, `out_of_stock` nennt SKU und Rest.
- Abweichungen zum Arbeitsauftrag: keine Rolle `shop` (Entscheidung 1 kennt nur primary_ops/additional/signing/event_app_member) — bestellen dürfen `primary_ops`, `additional`, `signing`, lesen alle Mitglieder. **Anfrage-Produkte = Nettopreis 0** (z. B. Cocktailbar, Fotobooth): nicht bestellbar, sondern `shop_request_product` mit interner Mail an `area_lead_partner`. Preis- und Namens-Snapshots werden bei jeder Bestätigung aus dem Produktstamm aufgefrischt (die Bestellung ist ab Bestätigung verbindlich, nicht ab Warenkorb). Eine Zeile je SKU und Bestellung; Merch-Konfiguration liegt als `merch_config` an der Zeile (generisches Schema, Entscheidung 14).
- Finalisierung läuft im Partner-Housekeeping (`run_partner_housekeeping().shop`), idempotent; Bestandskonflikte bei der Finalisierung werden protokolliert (`shop.finalize_stock_conflict`), die Bestellung wird trotzdem verbindlich — das Team klärt nach. Reporting (`shop_report`) zählt ausschließlich `completed`.

## 2026-09-11 — Welle 3 A6 Ticket-Kontingente und Secret Shop, Backend live (Migration 0049, Route im Trockenlauf)
- Kontingente entstehen **nur** aus gebuchten Ticket-Produkten (Trigger), nie von Hand; der Pass-Typ der Talente-Tickets folgt der Onboarding-Wahl, sonst dem Org-Typ (Startup ⇒ `startup`, P10). Jedes Kontingent startet als `pending_vivenu`; Partner sehen Code und Link erst nach dem Anlegen in vivenu. Menge folgt der Buchung; Storno deaktiviert, die Route schaltet den Coupon ab. „Mehr Tickets“ ist eine Anfrage an das Team (kein Mail-Fallback).
- Die Route `/api/cron/vivenu-allocations` (alle 30 Min., `CRON_SECRET`, `?allocation=` für Einzelfälle) legt je Partner einen Undershop und je Kontingent einen 100-%-Coupon an (Muster aus dem Dev-Dashboard und der API-Doku, Entscheidung 8). **Vorbehalt:** Die Feldnamen der vivenu-Objekte und das Format des Undershop-Links sind bis zum ersten Sandbox-Lauf mit einem Dev-Event nicht gegen echte Antworten geprüft; bis dahin bleibt alles `pending_vivenu` (Akzeptanz A6), das Team kann Codes und Links manuell setzen (`set_ticket_allocation`, mit Audit). Sandbox-Lauf ist Pflicht vor der Produktion; `docs/runbooks/vivenu-kontingente.md`.
- Härtung nebenbei: pauschale Schreibgrants für `authenticated`/`anon` auf `org_ticket_allocation` und `ticket_type_map` aus Welle 1 entzogen (Schreiben nur über RPCs), Lesen für Team ergänzt.
- **Slack statt make (Ergänzung zu Entscheidung 10):** Gate-Fehler gehen direkt über einen Slack Incoming Webhook des Kanals (`SLACK_ONBOARDING_WEBHOOK_URL`); make bleibt als Alternative im Code (`MAKE_ONBOARDING_WEBHOOK_URL`), wird aber nicht mehr eingerichtet — ein Hop und ein Zugang weniger.

## 2026-09-11 — Produktbilder aus der Item-Liste importiert (Migration 0050, Bucket product-images)
- Kein Airtable-Token nötig: Die Anhänge der Felder „Front Picture“, „Gallery Pictures“ und „Bild“ wurden über die Airtable-Verbindung der Architektur-Session gelesen (186 Datensätze), als Manifest gespeichert und mit `scripts/import-product-images.mjs` in den Bucket geladen: **71 Produkte, 198 Dateien** (JPEG, WebP, PNG; Duplikate gleicher Dateinamen je Produkt zusammengeführt, das KI-Feld „AI assist“ bewusst ausgelassen). Reihenfolge je Produkt: Front Picture zuerst.
- Der Bucket ist **öffentlich lesbar** (Produktbilder sind Marketingmaterial ohne Personenbezug), Schreiben nur mit service_role; `product.images` hält `[{path, url, name, type, size}]`. Weitere Bilder pflegt das Team später im Admin (B9) oder erneut über das Skript (`--replace`).
- Nebenbei entdeckt: Die Airtable-Tabelle „Logos Gipfel Partner 2026“ (Company ID, Communication Name, Logo) eignet sich als Quelle für `organization.logo_*`, sobald die Orgs aus HubSpot da sind.

## 2026-09-11 — PR #14 (Welle 3 B1–B3) gemergt; partner_overview erweitert (Migration 0051)
- Partner-Shell, Dashboard, Onboarding-Wizard (Logo = Pflicht `logo_vector`) und Kontakte sind live; alles über Session-RPCs, Org-Wahl per httpOnly-Cookie, das beim Lesen gegen `my_partner_orgs()` geprüft wird. Sidebar-Gerüst `SidebarShell` steht für Speaker und Volunteers bereit.
- Befund aus dem Review: Der Bewerber-Menüpunkt hing an Produktkategorien, die es im Vokabular nicht gibt. **Regel ab jetzt:** Bewerber ⇐ `partner_overview.sessions_count > 0` (Sessions mit `host_org_id`), Bühne ⇐ Produkt I-79895 oder `has_stage`; Kategorien werden dafür nicht mehr benutzt. `ticket_allocations[]` in `partner_overview` tragen den Status aus A6, Code und Link erscheinen nur bei `active`, `disabled` wird nicht ausgegeben. Korrektur der Navigation kommt mit B4.
- `lib/rpc-error.ts` kannte zehn Partner-Schlüssel nicht (Fund der Build-Session); Regel: neue P0001-Schlüssel gehören mit der Migration in `BUSINESS_KEYS` und in beide Wörterbücher — der Kontrakt listet sie, der Test `tests/partner.test.ts` prüft beides.

## 2026-09-11 — Welle 3 A10 SevDesk-Rechnungsentwürfe, Backend und Route live (Migration 0052)
- Je Partner ein Entwurf (SevDesk-Status 100) aus **allen** abgeschlossenen Shop-Bestellungen einer Edition, Positionen je SKU und Preis zusammengefasst, Kopftext mit PO-Nummer und Bestellnummern, Lieferdatum = Editionsende. Die Referenz steht je Bestellung in `external_ref` (`<Rechnungs-ID>#<Bestellnummer>`, weil `external_id` je System und Objekttyp eindeutig sein muss) — Idempotenz je Bestellung wie im Arbeitsauftrag; ein zweiter Lauf findet keine Kandidaten mehr.
- Auslösen im Admin durch das Partner-Team (`POST /api/admin/sevdesk/shop-invoices`, Gate Admin-Bereich + `is_partner_team()`), **Trockenlauf ist Standard**; der erste echte Lauf passiert gemeinsam mit Konrad nach dem Summit, zunächst für eine einzelne Org (`orgId`), Entwurf in SevDesk gegenlesen. Die festen SevDesk-IDs (Kategorien, Länder, Einheit, Steuerregel) stehen an einer Stelle (`lib/sevdesk/mapping.ts`) und sind bis dahin ungeprüft.
- Kontakt je Org wird beim ersten Lauf in SevDesk angelegt (`organization.sevdesk_contact_id`), mit Rechnungsadresse und Rechnungs-E-Mail (kein Login, Entscheidung 1). Erlöskonto/Kostenstelle: Kostenstelle optional über Env, Erlöskonten über die Steuerregel — eine Aufteilung je Produkt kommt erst, wenn die Buchhaltung sie braucht.
- Härtung nebenbei: `external_ref` hatte pauschale Grants für anon/authenticated aus Welle 1 — entzogen, Team liest über RPCs.

## 2026-09-11 — PR #15 (Welle 3 B4 Checkliste & Dateien) gemergt; Nachträge Migrationen 0053/0054
- Checkliste und Dateien-Hub live: alles über Session-RPCs, Upload-Kette wie beim Logo (`register_partner_asset` → `submit_deliverable`), signierte URLs 60 s; im Kennzeichen schlägt „Zurückgewiesen“ „überfällig“ (die Rückmeldung ist der Grund, das Datum bleibt rot).
- **Dateien-Hub liest über RPC, nicht direkt aus `partner_asset`:** `my_partner_assets(org, edition?)` liefert alle Fassungen mit Schlüssel und Beschriftung der Pflicht. Der Direktzugriff unter RLS war korrekt, aber die RPC hält Datei und Pflicht zusammen und bleibt die einzige Lesestelle (Build-Session stellt im nächsten PR um).
- **Formular-Pflichten bekommen ein Schema:** `deliverable_template.answers_schema` = `[{key, label_de, label_en, type, required, options[]}]` mit sechs Feldtypen (text, textarea, select, number, boolean, date); `submit_deliverable` prüft Pflichtfelder (P0001 `answers_incomplete`, detail = Schlüssel). Ohne Schema bleibt Freitext `{"note": …}` der Fallback — heute gibt es keine Vorlage vom Typ `form`.
- **Buchungs-Pflichten erledigt die Bestellung, nicht der Partner:** `deliverable_template.fulfilled_by_sku` (Lunch-Paket → I-79520). Trigger auf `shop_order`/`shop_order_line` setzen die Pflicht bei bestätigter Bestellung auf `accepted` (die Bestellung ist der Beleg, keine Team-Prüfung; `answers = {auto, order_id, order_no}`), Storno der letzten passenden Bestellung öffnet sie wieder (nur automatisch erledigte). Partner können solche Pflichten nicht mehr selbst abhaken (P0001 `fulfilled_by_order`, detail = SKU); das Team darf weiterhin manuell, etwa bei Bestellungen außerhalb des Shops. Abweichung vom PR-Stand („Als erledigt melden“ für `booking`): die Oberfläche zeigt stattdessen den Hinweis auf den Messeshop.
- Zwei Funde der Build-Session übernommen: `npx tsc --noEmit` ist jetzt Gate-Schritt (`npm test` entfernt Typen nur, `next build` prüft `tests/` nicht — ein Typfehler in `tests/` lag unbemerkt auf `main`); `.gitignore` filtert macOS-/Editor-Dubletten `* [0-9].ts`, `* [0-9].tsx`, `* [0-9].mjs`, die der Build sonst mit typprüft.

## 2026-09-11 — HubSpot: Service-Schlüssel statt Legacy Private App, Abgleich alle 15 Minuten statt Webhook-Pflicht
- HubSpot legt neue „Legacy Private Apps“ ab 26.10.2026 nicht mehr an (bestehende laufen weiter) und empfiehlt **Service-Schlüssel** (Public Beta seit 10.02.2026): gleiche Scopes, Admin-Kontrolle, Audit-Log, Rotation mit 7 Tagen Karenz, ein Schlüssel bekommt nie mehr Rechte als der anlegende Nutzer. **Service-Schlüssel kennen keine Webhooks.**
- Entscheidung: Service-Schlüssel als `HUBSPOT_ACCESS_TOKEN`; der Sweep `/api/cron/hubspot-sweep` läuft **alle 15 Minuten** statt nachts und ist der Hauptweg des Ingests (Deal erreicht „Onboarding Automation“ ⇒ Ingest innerhalb von 15 Minuten, Gate-Fehler setzen die Phase zurück wie bisher). Der Webhook `/api/webhooks/hubspot` bleibt im Code, ist aber optional und bräuchte eine Projekt-App über die HubSpot-CLI; `HUBSPOT_CLIENT_SECRET` entfällt bis dahin. Sicherheitsgewinn: kein von außen erreichbarer Endpunkt nötig, ein Geheimnis weniger. Kosten: eine Such-Anfrage je Edition alle 15 Minuten.
- Abweichung vom Masterplan (Integrationstabelle „Webhook“) und vom Arbeitsauftrag A2; dieser Eintrag gilt.

## 2026-09-11 — Slack-Benachrichtigung über Incoming Webhook einer eigenen Slack-App
- App „ChefTreff Portal“ nur mit dem Feature *Incoming Webhooks* (Scope `incoming-webhook`, kommt automatisch), Kanal `#fls27-onboarding`; kein Bot-Token, keine Event-Subscriptions, nicht verteilt. Die Webhook-URL ist ein Geheimnis (wer sie hat, kann in den Kanal posten) ⇒ nur Vercel `SLACK_ONBOARDING_WEBHOOK_URL`. make.com bleibt Fallback (`MAKE_ONBOARDING_WEBHOOK_URL`). Schritte im Runbook HubSpot-Ingest, Schritt 6.

## 2026-09-11 — Sanity (A11): Schutz der Website-Baustelle
- Der Relaunch der Website läuft in Sanity; das Portal darf ihn nicht stören. Regeln: (1) bis zur Freigabe durch das Web-Team nur ein **Viewer-Token** (lesen, nichts ändern); (2) für den Schreiblauf ein Editor-Token, mit dem das Portal ausschließlich Dokumente eines eigenen Typs `portalPartnerLogo` mit eigener `_id` (`portalPartnerLogo.<org_id>`) anlegt und patcht — nie fremde Dokumente, nie Schema oder Studio; (3) den Dokumenttyp nimmt das Web-Team ins Studio-Schema auf, wenn die Seite ihn anzeigen soll — vorher sind die Dokumente unsichtbar und stören nichts; (4) Trockenlauf Standard wie bei SevDesk, erster Echtlauf gemeinsam; (5) Sanity hält die Dokumenthistorie, das Token ist jederzeit widerrufbar. Dataset-genaue Token gibt es nur im Enterprise-Plan (Free: viewer/editor/deploy-studio) — der Schutz liegt in Code, Review und Trockenlauf, nicht im Token.

## 2026-09-11 — Welle 3 A12 Event-App-Adapter (Swapcard), Migration 0055
- Vertrag `lib/event-app/types.ts` (Aussteller: `clientId` = Org-ID, Name, Beschreibung DE/EN, Website, Logo, Typ = Sponsoring-Level), Swapcard-Implementierung `lib/event-app/swapcard`, Sync `lib/event-app/sync.ts` (nur Neues und Geändertes; App-ID je Org×Edition in `external_ref` system `swapcard`/object_type `exhibitor`), Admin-Route `POST /api/admin/swapcard/exhibitors` (Team, **Trockenlauf Standard**). Event-ID je Edition in `event.swapcard_event_id` (`set_edition_swapcard`), nicht aus der Env — wie vivenu und HubSpot; `SWAPCARD_EVENT_ID` dient nur dem Probe-Skript.
- Die Feldnamen der Content-API sind bis zum Probelauf ungeprüft (`scripts/swapcard-probe.mjs`, nur lesend, druckt Query-/Mutation-Namen und `ExhibitorInput`). Konrads Swapcard-Event ist ein Duplikat von 2026 — Schreibläufe erst, wenn das Event eingerichtet ist und nicht öffentlich sichtbar.
- **Offen (Entscheidung Konrad): Logos.** `partner-assets` ist privat, Swapcard braucht eine öffentlich abrufbare Rasterdatei; Vektor-Logos (SVG/EPS/AI/PDF) müssen gerendert werden. Vorschlag: öffentlicher Bucket `partner-logos` mit den freigegebenen Logos (nutzen auch Sanity/A11 und später der Grafik-Generator), PNG aus SVG per `sharp`, für EPS/AI/PDF eine zusätzliche Pflicht `logo_png` oder Konvertierung durch das Team.
- Kontakte mit Rolle `event_app_member` liefert der Export bereits (`members`), übertragen werden sie noch nicht (Personen-Sync Welle 4/5).

## 2026-09-11 — PR #16 (Welle 3 B5+B6 Tickets, Bewerber) gemergt; Migration 0056
- Tickets und Bewerber live, alles über Session-RPCs; Code/Secret-Shop-Link nur bei `active`, `pending_vivenu` und `error` sehen für Partner gleich aus; Bewerberliste ungefiltert mit „Name verborgen“ (Zähler und Liste bleiben deckungsgleich), Audit-Hinweis im ersten Absatz, Entscheidungen ohne Mail (Freigabe-Gate). Formular-Pflichten rendern `answers_schema` feldweise; `fulfilled_by_sku` ohne Knopf.
- **Vorlagenpflege synchronisiert selbst (0056):** `upsert_deliverable_template` ruft `resync_deliverables` für alle laufenden Editionen (Enddatum ≥ heute); neue Pflichten erscheinen sofort bei bestehenden Organisationen, das Audit nennt die Zahl (`resynced_org_editions`). B9 braucht keinen Resync-Knopf (Fund Build-Session).
- **Datenminimierung (0056):** `applications_for_session` gibt `person_id` ohne Einwilligung nur noch dem Team heraus; Partner sehen bei verborgenen Bewerbungen keine Personen-ID mehr.

## 2026-09-11 — Swapcard: Probelauf erledigt, Adapter an das echte Schema angepasst
- Key gültig, Event „FUTURE LEADER SUMMIT 2027“ (Duplikat 2026, 0 Aussteller), Community mit 191 Ausstellern aus den Vorjahren (`type` = Branche). Aussteller hängen an der Community; Lesen über `exhibitorsV2(filter: {eventIds})`, Schreiben über `upsertEventExhibitorsV2` mit `validateOnly`.
- Entscheidungen: (1) **Vorjahres-Aussteller werden wiederverwendet** — Treffer per Name in der Community bekommen ihre `id` mit und werden ans 2027-Event gehängt statt verdoppelt; (2) **Trockenlauf = echte Validierung** durch Swapcard (`validateOnly`), nichts wird geschrieben; (3) **`type` wird nicht gesendet**, bis geklärt ist, ob dort Branche (wie 2026) oder Sponsoring-Level steht — Swapcard kennt Sponsoren-Kategorien als eigenen Begriff; (4) Beschreibung DE als Standard, EN als Übersetzung `en_US`; (5) Standnummer über `booth`. Offen bleiben Logos (öffentlicher Bucket + Raster) und Mitglieder (Welle 4/5). `set_edition_swapcard` ist noch nicht gesetzt.

## 2026-09-11 — HubSpot: Service-Schlüssel gesetzt, Bestandsaufnahme, Ingest bleibt bis zur Freigabe aus
- Pipeline „Future Leader Summit“ `379213775`, Phase „Onboarding Start (Automation)“ `3019026648`; heute 0 Deals dort, 1 in „Signed“. **`set_edition_hubspot` wird erst nach Konrads Freigabe ausgeführt**, weil der Abgleich alle 15 Minuten ab dann Organisationen und Kontakte anlegt und Einladungsmails an echte Adressen schickt (Prozessstart 01.11.).
- Befunde: Zuordnungs-Labels Deal → Kontakt fehlen noch (nur Standard); die Firmen-Eigenschaften heißen anders als vom Ingest erwartet (`fls_booth_type` als Level, `fls_partner_type`, `ct_company_type`, `purchase_ordner`, `invoice_contact`) — Zuordnungsvorschlag im Runbook, Umsetzung in `lib/hubspot/mapping.ts` nach Freigabe. Vorschlag außerdem: Erfolgs-Phase „Onboarding Operations (Automation Complete)“ `3569180889` nach gelungenem Ingest setzen (heute nur Rücksetzen bei Gate-Fehlern).

## 2026-09-11 — Konrads Entscheidungen (Swapcard, Logos, HubSpot); Migrationen 0057/0058; PR #17 (B7 Standbühne) gemergt
- **Aussteller-Typ in Swapcard später:** Kategorien werden noch erarbeitet und dann in Swapcard nachgezogen; bis dahin sendet der Sync keinen `type`.
- **Logo in zwei Formaten (0057):** `logo_vector` nimmt nur noch **SVG** (Website, Drucksachen), neue Pflicht **`logo_png`** (Event-App, Bildschirme; PNG ≤ 10 MB, transparenter Hintergrund, ≥ 1000 px). `onboarding_status` wird erst `filled`, wenn beide Fassungen da sind; der Onboarding-Wizard bekommt zwei Upload-Felder (Kontrakt B2, Build-Session). Freigegebene PNG-Logos kopiert der Swapcard-Sync in den **öffentlichen Bucket `partner-logos`** (eine Datei je Fassung, Schreiben nur service_role) und gibt die URL als `logoUrl` mit — Swapcard und Website brauchen eine frei abrufbare Datei; im Bucket liegt nur, was das Team freigegeben hat. Bestehende Organisationen bekamen die neue Pflicht mit der Migration.
- **Swapcard-Event-ID gesetzt** (`set_edition_swapcard`, „FUTURE LEADER SUMMIT 2027“); Konrad befüllt das Event nach und nach, geschrieben wird weiterhin erst im ausdrücklichen Echtlauf.
- **HubSpot:** (1) Ingest bleibt aus, bis alles andere steht — `set_edition_hubspot` erst nach Konrads Go; (2) Kontakt-Labels trägt das Sales-Team nach, für den Start genügt der Hauptkontakt, den Rest ergänzt der Partner im Portal; (3) Zuordnung der Firmen-Eigenschaften umgesetzt (`lib/hubspot/mapping.ts`, Portal-Eigenschaften gleichen Namens haben Vorrang): Level aus `fls_booth_type` ohne Größenangabe („18qm Premium“ ⇒ „Premium“), Kategorie aus `fls_partner_type` (HR ⇒ `talent`, Startup ⇒ `startup`, sonst leer), Firmentyp aus `ct_company_type` (Corporate, Startup, Universität, Initiative, Stiftung ⇒ `initiative`, Media, Service/Kooperation ⇒ `agency`; Rest leer ⇒ `corporate`), PO aus `purchase_ordner`, Rechnungs-E-Mail aus `invoice_contact` nur, wenn es eine E-Mail ist, Rechnungsname = Firmenname; (4) **Erfolgs-Phase (0058):** `event.hubspot_done_stage_id`, `set_edition_hubspot(edition, pipeline, stage, done_stage?)`; nach gelungenem Ingest schiebt der Sweep den Deal dorthin („Onboarding Operations (Automation Complete)“ `3569180889`), ein Fehler dabei kippt den Ingest nicht.
- **Rollen aus Produkten automatisch (0058):** `sync_granted_roles(org)` mit Triggern auf `org_product` und `org_membership` — der Hauptkontakt bekommt `standbuehne_editor` (Scope org, bis Editionsende + 1 Tag, `note = auto:product`) mit der Buchung von I-79895 und verliert sie bei Storno oder Übergabe des Hauptkontakts; manuell vergebene Rollen bleiben. Korrektur zum Befund der Build-Session: der Ingest vergab die Rolle bereits (0044, datengetrieben über `product.grants_role`), es fehlte alles außerhalb des Ingests.
- **`upsert_session` (0058):** reine Bühnen-Editoren legen Sessions nur für die eigene Organisation an — `host_org_id` wird gesetzt, wenn sie fehlt, eine fremde antwortet 42501 (detail `host_org_required`), auch beim Ändern; Programm-Team und Speaker-Manager unverändert.
- PR #17 (B7 Standbühne) gemergt: dasselbe Board wie Admin/Leads, `can_edit` je Zeile aus der DB; Veröffentlichen-Knopf nur noch mit `canPublishSessions` (Admin/Programm-Team) — Fund der Build-Session: `publish_session` verlangt `is_programme_editor()`, der Knopf lief für Speaker-Manager immer in 42501. Nächster Build-PR: B8 Shop.

## 2026-09-11 — Korrektur Konrad: Stiftung ist kein Initiative-Typ; Kommunikationsname aus HubSpot (Migration 0059)
- Vokabular `organization_type` bekommt **`foundation` = Stiftung** (0059); HubSpot „CT Company Type“ = Stiftung wird darauf abgebildet statt auf `initiative`. Oberflächen lesen das Vokabular, keine Codeänderung nötig; der Ingest-Gate akzeptiert den Schlüssel über `is_vocab_key`.
- Rechnungsname aus dem Firmennamen bleibt („passt erstmal“). **Kommunikationsname:** kommt aus der HubSpot-Eigenschaft `communication_name` („Company (Communication Name)“), Fallback Firmenname; im Portal unter Stammdaten vom Partner änderbar (Wizard B2, Feld „Kommunikationsname“). Beides war bereits so gebaut, hier festgehalten.

## 2026-09-11 — „Service / Kooperation“ als eigener Organisationstyp (Migration 0060)
- Vokabular `organization_type` + `service` („Service / Kooperation“); HubSpot „CT Company Type“ = Service / Kooperation wird darauf abgebildet statt auf `agency`. Die Typen Unternehmen, Startup, Initiative, Stiftung, Hochschule, Agentur, Service / Kooperation, Medien, Öffentliche Einrichtung stehen damit getrennt; VC, Catering, Important, Other bleiben ohne Zuordnung (⇒ `corporate`).

## 2026-09-11 — Sicherheits-Header und Content-Security-Policy; Domain live; PR #18 (B8 Shop) gemergt; Migration 0061
- **Fund aus dem Team-Portal-Chat:** auf `portal.chef-treff.de` stand nur HSTS. Jetzt auf jeder Antwort `X-Frame-Options: DENY`, `X-Content-Type-Options: nosniff`, `Referrer-Policy: strict-origin-when-cross-origin`, `Permissions-Policy` (Kamera nur eigener Ursprung — Check-in-Scanner Welle 4), HSTS mit `includeSubDomains`, Grund-CSP `frame-ancestors 'none'; base-uri 'self'; object-src 'none'; form-action 'self'` (`next.config.ts`). Dazu die **vollständige CSP mit Nonce je Anfrage** aus `proxy.ts` (Skripte nur mit Nonce + `'strict-dynamic'`, Supabase-Ursprung für Bilder/REST/Realtime, `report-uri /api/csp-report`) — **zunächst Report-Only**, Verstöße landen in den Vercel-Logs (`[csp]`); scharf über Vercel-Env `CSP_ENFORCE=true` nach einigen Tagen ohne eigene Verstöße. Runbook `docs/runbooks/sicherheits-header.md`. Grund für die zwei Stufen: Clickjacking-Schutz sofort und risikolos, die Skript-Richtlinie erst nach Beobachtung, weil sie eingeloggte Seiten (Board, Realtime, Uploads) betrifft, die ich ohne Login nicht durchklicken kann.
- **Domain-Umzug erfolgt** (Konrad, 11.09.): `portal.chef-treff.de` liefert die Plattform, alte Team-Pfade leiten nach team. weiter. `NEXT_PUBLIC_SITE_URL` (Production) auf `https://portal.chef-treff.de` gesetzt (vorher vercel.app — Mail-Links hätten dorthin gezeigt), wirksam mit dem nächsten Deploy. Offen bei Konrad: Supabase Auth → Site URL und Redirect `https://portal.chef-treff.de/auth/callback`.
- **PR #18 (B8 Messeshop + Logo-Nachtrag) gemergt.** Wizard liest Logo-Pflichten über das Präfix `logo_`. Fund der Build-Session übernommen: **Migration 0061** prüft den Bestand schon in `shop_upsert_line` (P0001 `out_of_stock`, detail `<sku>:<verfügbar>`; die eigene Reservierung einer wieder geöffneten Bestellung zählt nicht gegen sich selbst), `shop_confirm` prüft weiterhin. Merch-Konfiguration (S4) wartet auf Artikel. Nächster Build-PR: **B9 Admin**.

## 2026-09-11 — Domain-Umzug abgeschlossen
- `portal.chef-treff.de` ist produktiv (Konrad: DNS/Vercel; Claude: `NEXT_PUBLIC_SITE_URL`, Weiterleitungen, Header), Supabase Auth auf die neue Domain umgestellt, Magic-Link-Login getestet. Die vercel.app-Adresse bleibt als Deployment-URL erreichbar; Deployment-Protection schützt weiterhin Preview-Deployments, die Produktionsdomain ist öffentlich.

## 2026-09-11 — Welle 3 A11 Sanity-Veröffentlichung gebaut (Trockenlauf), Migration 0062
- Dokumenttyp **`portalPartnerLogo`** mit fester ID `portalPartnerLogo.<org_id>` (ein Dokument je Organisation, erneute Freigabe ersetzt), Felder orgId, editionSlug, name, website, sponsoringLevel, partnerCategory, logoSvg (Asset), logoSvgPath, logoPngUrl, publishedAt — Kontrakt für das Web-Team im Runbook `sanity-partner-logos.md`.
- Quelle ist der Aussteller-Export (nur Zeilen mit freigegebenem SVG); „genau ein Upsert je Freigabe“ über `external_ref` (0062: `set_external_ref`/`list_external_refs`, Systeme sanity/swapcard, service_role oder Team) mit der veröffentlichten Fassung im `meta` — nur geänderte Fassungen werden erneut geschrieben.
- Trockenlauf Standard und über die Sanity-API selbst (`dryRun=true`, prüft ohne zu schreiben; keine Assets); Echtlauf erst mit Editor-Token nach Freigabe des Web-Teams. Admin-Route `POST /api/admin/sanity/partner-logos` (Team), Läufe in `integration.sync_job` (system `sanity`).

## 2026-09-11 — Arbeitsteilung ab Welle 4: Build-Session schreibt auch Migrationen, Architektur-Session prüft und wendet an
- Anlass: Wochenkontingent der Architektur-Session (Fable) fast erreicht, Build-Session (Opus) hat Luft; das Backend der Welle 3 ist durch. Sicherheit hängt am Prozess, nicht am Modell: Migrationen entstehen künftig als Dateien im Feature-Branch (mit SQL-Test, nach `docs/db-konventionen.md`), **angewendet werden sie nur von der Architektur-Session** nach Review gegen die Konventionen — damit bleibt die Vier-Augen-Prüfung, die Datenbank wird nur von einer Stelle geändert. Bei der Architektur-Session bleiben: Arbeitsaufträge, Auth, RLS-/Storage-Policies, Webhook-Signaturen, Entscheidungslog, Merges.
- Neue Datei `docs/db-konventionen.md` (Migrationskopf, Rechteprüfung, Fehlerschlüssel-Vertrag, plpgsql-Fallen, Grants/Storage, Testaufbau, Doku, Ablauf) — bündelt, was bisher verstreut im Entscheidungslog stand. AGENTS.md-Checkliste angepasst (Migrationen als Dateien ja, Anwenden nein).

## 2026-09-11 — Altes Supabase-Projekt (Dublin): pausieren, später löschen
- `fsjexlrapilzftwibocu` („ARCHIV FLS27 Systems & Talentpool“, eu-west-1) lief seit dem Umzug am 09.09. ungenutzt weiter (kein Verweis mehr in Code, Env oder Vercel). Nach Runbook Supabase-Umzug Schritt G: **Konrad pausiert es im Dashboard** (Settings → General → Pause project; das Pausieren über die Session hat die Berechtigungsprüfung abgelehnt), Löschen frühestens eine Woche später ebenfalls durch Konrad (irreversibel, deshalb nie durch eine Session). Vorher nichts mehr zu sichern: Reproduktionstest bestanden, Auth-Nutzer und Daten in Frankfurt.

## 2026-09-11 — PR #19 (Welle 3 B9 Partner-Admin) gemergt; Migration 0063 Grants aufgeräumt
- Welle 3 ist auf der Oberfläche komplett (B1–B9). B9: sieben Reiter unter `/admin/partner` hinter `is_partner_team()`, Trockenlauf-Routen als Server-Actions mit Nachfrage vor dem scharfen Lauf, Rollenvergabe nur für Admins, `CRON_SECRET` bleibt serverseitig.
- Funde der Build-Session: (1) `admin_products` gab jedem Staff die Produktzeile samt Einkaufsdaten — jetzt nur Partner-Team (42501 sonst); die Tabelle `product` hatte für `authenticated` schon Spalten-Grants ohne Einkaufsspalten, dort war nichts offen. (2) `hubspot_editions` bleibt die Liste eingerichteter Editionen für den Sweep; der Admin liest `event` direkt und schreibt über `set_edition_*`. (3) Das pauschale `grant all … to anon, authenticated` der Welle 1 lag noch auf 39 Tabellen und Sichten: `anon` behält nur `select` auf `vocab_term`; `authenticated` verliert tote Schreib-Grants (Tabellen ohne Schreib-Policy) und Lese-Grants auf Tabellen ohne Lese-Policy. Verhalten unverändert (RLS überall aktiv), Grundsatz „Spalten-Grants“ wiederhergestellt. Regel in `docs/db-konventionen.md`: neue Tabellen bekommen keine Grants für `anon`.

## 2026-09-11 — Arbeitsauftrag Welle 4 als Entwurf; altes Supabase-Projekt löschen
- `docs/arbeitsauftrag-welle-4.md` angelegt (Volunteers, Check-in, Produktion, Wissensbasis startbar; Hackathon und Volunteer-Tickets warten auf E2–E5). Erste PR-Reihenfolge: Volunteers → Volunteer-Admin → Check-in → Produktion → Wiki → Hackathon. Neue Arbeitsteilung gilt (Migrationen als Dateien, Anwenden hier).
- Altes Projekt `fsjexlrapilzftwibocu` lässt sich im Pro-Plan nicht pausieren. Empfehlung: **löschen** — alles ist seit 09.09. in Frankfurt (Reproduktionstest bestanden), keine Verweise mehr; vorher optional ein letztes Backup aus dem Dashboard herunterladen (Database → Backups). Löschen nur durch Konrad.

## 2026-09-11 — Welle 4 freigegeben (Konrads Antworten E1–E9)
- Volunteers: Mindestalter als Pflichtfeld (Wert bestätigt Konrad), Bereiche aus dem Volunteer-Airtable 2026 als Vokabular; Ticket als vivenu-Coupon je Person (Einlösen = Aktivierungsschritt, Nicht-Einlöser sichtbar), Hotel/Zug als vivenu-Add-ons; Warteliste mit bewusster Überbuchung. Hackathon: Discord bleibt, Teilnehmer-App in Welle 4, Team-Anmeldung Pflicht (3–8, Ziel 6), Challenges werden zugeteilt (gleichmäßig), Judging-Kriterien trägt der Partner mit der Challenge ein. Produktion: Stand-Checkliste = Shop-Bestellungen, Exportlisten je Dienstleister (`product.supplier` Pflicht), Abhaken je Stand; Regie nach Vorlage 2026 (CSV-Export von Konrad). Wikis DE+EN, Hackathon EN; Owner Anna-Lena/Konrad/Paulina/(Emilio). Kiosk: Vorschlag ein Konto je Gerät.
- Erster PR der neuen Arbeitsteilung: #20 (Merch-Konfiguration, Migration 0064 als Datei) — Review nach `db-konventionen`, Anwenden hier.

## 2026-09-11 — Migration 0064 (Build-Session, PR #20): Merch-Konfiguration serverseitig geprüft
- Erster Durchlauf der neuen Arbeitsteilung: Migration von der Build-Session als Datei, Review hier nach `db-konventionen`, angewendet als `20260911134144`, Smoke-Test 10/10. `shop_confirm` weist unvollständige Merch-Konfigurationen ab (P0001 `merch_incomplete`, detail `<sku>:<feld>`); Feldarten wie `answers_schema` plus `logo` (bereits eingereichte Datei) und `sizes` (Größenverteilung = Bestellmenge). Prüfung bewusst erst beim Bestätigen; Logo-Bezug als Asset-Id ohne Fremdschlüssel (verbindliche Bestellung zeigt auf die damalige Fassung). Merch-Artikel selbst legt Konrad an — „Produkte anlegen genügt“ gilt jetzt wirklich.

## 2026-09-11 — Altes Supabase-Projekt gelöscht; Reihenfolge der externen Freigaben
- Dublin-Projekt von Konrad gelöscht (Pro-Plan ließ kein Pausieren zu; nichts mehr zu sichern). Externe Schritte: Sanity-Werte nächste Woche (Trockenlauf A11), HubSpot-Ingest **ganz am Ende** vor dem Prozessstart 01.11., Swapcard-Kategorien ebenfalls am Ende (bis dahin kein `type`).
