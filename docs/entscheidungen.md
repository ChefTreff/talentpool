# Entscheidungslog — TalentCRM / FLS27-Plattform

Format: Datum · Entscheidung · Begründung · Quelle. Änderungen nur ergänzen, nie löschen.

Ältere Einträge (07.09.–13.09.2026) stehen in `docs/entscheidungen-archiv.md`. Ein Chat liest hier nur die Einträge ab seinem eigenen Startdatum, das Archiv nur bei Bedarf.

## 2026-09-14 — Rückkehr aus der Pause: Review des Durchlaufpakets, Migrationen 0082–0086, F7-Entscheidungen
- **Gemergt:** #29 (F1–F3 Bereichs-Shell, Admin nach Portalen, Programm-Tabelle; Gate grün), #34 (F7 Design-Durchgang; Gate grün nach eigener Ergänzung), #35 (Dateinamen 0080/0081), #36 (Bericht der Build-Session, `docs/bericht-welle-4-pause.md`). **Offen bis Walkthrough:** #30–#33 (Migrationen live, Merge durch die Architektur-Session). **Lehre:** beim Merge der Basis #29 mit Branch-Löschung hat GitHub die fünf gestapelten PRs geschlossen; Basis-Branch kurz neu angelegt, PRs wieder geöffnet und auf `main` umgehängt — Regel in `db-konventionen` §8.
- **Rückwirkender Security-Check 0067–0081:** keine Sicherheitslücke. 282 Funktionen, keine Definer-Funktion mit anon-EXECUTE oder ohne gepinnten `search_path`; anon nur `select vocab_term`; RLS überall; Storage unverändert; Webhook (Signatur hex über Raw-Body, Redaktion nach der Prüfung), Login-Umleitung (`safeNextPath`), Server-Actions mit Sitzungs-Client, CSV-Route und Markdown-Renderer sauber. Der Fund „`event_day` ohne Policy“ war falsch: `event_day_read using (true)` existiert seit v2; 0069 bleibt als harmlose Lese-RPC. Details `docs/security-review-2026-09-14.md`.
- **Migrationen 0082–0085 nach Review angewendet** (`20260914094832`, `…095053`, `…095318`, `…095624`), mit Korrekturen direkt in den PR-Branches (Kopf „Review Architektur-Session“, Tests erweitert, alle grün): **0082** `is_production_team()` ohne `programme_team` (Bereich `/produktion` kennt nur Produktion; Einkaufspreise in der Bestellliste), `slot_id` muss zu Bühne × Tag gehören; **0083** Schreiben und Editor nur für die Bereichsleitung **aller** Zielgruppen, beim Ändern auch der bisherigen (`can_edit_kb_all` — sonst Übernahme fremder Artikel über eine neue Zielgruppenliste); **0084** Widerruf eines Volunteer-Coupons wird in `volunteer_coupon_revocation` festgehalten und muss bei vivenu deaktiviert werden (RPCs `volunteer_coupon_revocations_pending`/`mark_volunteer_coupon_revoked`), erneute Zusage beginnt bei `none`, Mail-Link `{{portal_url}}/volunteers` statt unbekanntem `{{link}}`, Team-Liste mit `ticket_status`; **0085** Partner-Jury sieht und bewertet nur Teams der eigenen Challenge (`can_judge_hack_team`), Mentoren aus `mentor_names`. Alle vier: Trigger-/Hilfsfunktionen ohne EXECUTE für authenticated. **0086** (`20260914095907`): sieben Trigger-Funktionen aus den Wellen 1–2 ebenso (Advisor-Fund, Hygiene).
- **Abweichungen der Build-Session akzeptiert:** `regie_cue` an Bühne × Tag mit optionalem Slot (die Vorlage 2026 hat zur Hälfte keine Session); `hack_team_member` als Tabelle statt `members[]`; vier Judging-Kriterien als feste Felder; Hackathon-Zeitplan aus `programme_public`; Teilnehmer-Portal zählt nur als einziger Bereich; Einstieg nach Login für das Team auf `/admin`.
- **F7:** die drei Typo-Rollen `.ct-small`, `.ct-display`, `.ct-wordmark` sind **akzeptiert** — „keine neue Schriftgröße“ heißt: fehlt eine Größe, kommt sie als Rolle nach `globals.css`, nie als roher Wert in eine Seite (Skill-Regel 5 ergänzt). **Highlight-Wort:** Highlight-Pink `#FF88CF` des Brandbooks als Token `highlight`, einzig für das Highlight-Wort auf Navy (Login/Welcome/Begrüßung; 8,0:1), nie UI-/Statusfarbe, nie auf hellem Grund (2,2:1) — Konrads Wunsch „mehr Farbe“ brandkonform, mit einer Zeile in `globals.css` zurücknehmbar. Briefing v0.6.
- **Aufträge an die Build-Session (vor dem Echtlauf A6):** Widerrufs-Sync im Cron `/api/cron/volunteer-tickets` (`updateCoupon(id, {active:false})`, dann Quittung); Volunteer-Undershop nur mit Tickettypen, die in `ticket_type_map` auf `pass_type = 'crew'` stehen (heute alle Typen zu 0 € — ein Volunteer könnte sich einen Partner-Pass ziehen); optional `ticket_status` in der Team-Liste anzeigen. Danach Walkthroughs #30–#33, dann **PR 24 Check-in (A2 + B4)**.
- **Offen für Konrad:** „Verantwortliche“ in der Programm-Tabelle (Vorschlag: Bühnen-Lead ableiten, kein neues Feld); Notion-Export des Volunteer-Wikis für den Import-Trockenlauf; Discord-URL später; F6-Termin; F4-Abgleich mit den Alt-Portalen.

## 2026-09-14 — Walkthrough-Funde der Build-Session: Korrekturmigrationen 0087–0089 angewendet
- **0087** (`20260914104754`, #30): Ticket-Kontingente sind im Katalog `addon` mit Kategorie `tickets` und `pass_type` und rutschten auf Stand-Checkliste und Bestellliste; jetzt ausgeschlossen (Kategorie **und** `pass_type` geprüft). **0088** (`20260914104823`, #31): `/speaker/wiki` war leer, weil Artikel nur auf Englisch vorlagen — `kb_articles` sortiert jetzt statt zu filtern (Edition vor evergreen, Wunschsprache vor der anderen), die Oberfläche kennzeichnet den Rückfall. **0089** (`20260914104906`, #33): deutsche Fassungen der Challenges waren nie lesbar (`coalesce(title_en, …)` mit `title_en not null`); vier Lesefunktionen bekommen `p_language` (Vorgabe `en`, E7), gemeinsame Regel `hack_text()`, alte Signaturen gedroppt. Alle drei Tests vorher rot, nachher grün; von mir nur `set search_path` als erste Code-Zeile ergänzt.
- **Code #32:** Widerrufs-Sync und Crew-Tickettypen umgesetzt und im Dev-Event belegt (widerrufener Coupon ⇒ `invalid` im Checkout, sowohl über `active:false` als auch über Grenzen 0); Shop-Bereinigung läuft auch ohne offene Coupons (derselbe Fehler wie im Partner-Sync vor 0079). **Gate und Merge #30–#33 durch die Architektur-Session, danach PR 24 Check-in.**
- **Merge #30–#33** (14.09., nach grünem Gate je Branch): Produktion 8977aa7, Volunteer-Tickets c9f6eb1, Wiki 6590730, Hackathon 8579030. Die gestapelten Branches kollidierten nur in `lib/i18n/de.json`/`en.json` (beide Seiten neue Schlüssel); gelöst per Drei-Wege-Merge auf Schlüsselebene, Wörterbücher strukturgleich, Gate danach erneut grün. Repo-Hygiene: fünf Finder-Duplikate („… 2.sql/.md“) entfernt, Gate prüft das jetzt (`dateien: ok`).

## 2026-09-14 — PR 24 Check-in (#37): Migration 0090 angewendet, Kiosk-Regeln bestätigt
- **0090** (`20260914112220`) nach Review mit vier Korrekturen im Branch: `purge_checkins()` auch für service_role und im Housekeeping-Cron eingehängt (vorher rief es niemand, und der Cron wäre an 42501 gescheitert); Scan-Tag in der Zeitzone der Edition; `checked_in` (vivenu-Stempel) gilt am Einlass als gültig; gelungener Scan `on conflict … do nothing` gegen den eindeutigen Index (zwei Kioske, ein Code). Test 17/17.
- **Bestätigt:** Idempotenz je Ticket × Tag gilt nur für gelungene Scans (abgewiesene Versuche bleiben sichtbar); unbekannte Codes werden nicht gespeichert und ein gültiger Code einer fremden Edition antwortet `unknown` statt 42501 (Datenminimierung vor Forensik); `isKioskOnly()` in `lib/areas.ts` hält ein reines Gerätekonto aus dem Teilnehmer-Portal, Mitrollen behalten ihre Bereiche; manuelles Eingabefeld am Kiosk bleibt (Gegenlicht). Neue Abhängigkeit `jsqr` akzeptiert (pure JS, Safari kennt `BarcodeDetector` nicht) — neue Abhängigkeiten künftig mit einem Satz im PR begründen, keine Vorabsprache nötig.
- Überholter Branch `welle-4/checkin` (Stand 11.09., Inhalt über #26 gemergt) remote gelöscht. Merge #37 nach Walkthrough der Build-Session (Kamera iPad-Safari, abgelehnte Kamera, zwei Geräte gleichzeitig) und grünem Gate.
- **#37 gemergt (e209ebe):** Walkthrough der Build-Session mit 15 Prüfungen, darunter echte Nebenläufigkeit (zwei Clients, `ok` + `already`, genau ein `ok`), Landung des Kiosk-Kontos ohne `next` auf `/checkin`, 404 in allen anderen Bereichen, kein Lesezugriff auf `ticket`. Offen für Konrad (Abschluss-Checkliste): Kamerapfad auf echtem iPad-Safari, kein Merge-Blocker, weil die Handeingabe als Rückfall belegt ist. **Welle 4 vollständig auf `main`.**

## 2026-09-14 — F4 Abgleich mit den Alt-Portalen (#38): Lückenliste ist Eingabe für Feedback-Runde 2
- `docs/feedback-runde-1-abgleich.md` (Build-Session, am Gerenderten geprüft) benennt zehn Lücken gegenüber Speaker- und Partner-Hub und hält fest, was **bewusst** nicht nachgebaut wurde (drei Logins, feste Countdowns, Mail-Rückfälle, private Freelancer-Kontaktdaten, Speaker-Artikel für Partner). Nichts davon wird ohne Konrads Entscheidung gebaut — die Liste geht in seine Feedback-Runde 2.
- **Korrektur zu Lücke 5:** der Anfrage-Weg für mehr Tickets **existiert** (`/partner/tickets` → `requestTicketIncrease` → RPC `request_ticket_increase`, Bearbeitung im Partner-Admin); am Testkonto war er nicht sichtbar, weil Konrad kein Kontingent hat. Auch die **Anfrage-Produkte** im Shop sind da (`ShopView` → `shopRequestProduct` → `shop_request_product`, Bearbeitung über `shop_requests_admin`/`shop_request_answer`); sichtbar nur bei Artikeln, die im Katalog als Anfrage-Artikel geführt sind — im Testkatalog gab es keinen. **Lücke 5 ist damit keine Lücke**, sondern ein Testdaten-Effekt; für den Abgleich-Walkthrough braucht Konrads Konto ein Kontingent und der Katalog einen Anfrage-Artikel.
- **Vorsortierung nach Masterplan §6** (Konrad entscheidet): Media Kit / Bühnenfotos / Speaker- und Partnergrafik gehören zu den C-Features nach dem 01.11. (Slot-Grafiken, Timetable-Bild, Bild-Normalisierung) — Bühnenfotos brauchen ohnehin den Summit. **Chatbots** (Lücke 10): kommen als Aufsatz auf die Wissensbasis (`kb_chunk`/pgvector, Kopf von 0083) **nach dem Go-live**, je Zielgruppe wie die Wikis; vor dem 01.11. kein Platz und kein Bedarf, weil die Wikis erst gefüllt werden. Vor dem Go-live sinnvoll und klein: Leistungsumfang je Paket (Anzeige aus `product_component`), Hallenplan als Datei je Edition (Wiki-Artikel oder Produktions-Upload), Anfrage-Produkte-Knopf, Speaker-Reception-Anmeldung, Profilfoto-Upload (Bucket `speaker-assets` steht), Agentur als zweiter Kontakt (Datenmodell-Entscheidung, klein). Mittel: Event-App-Selbstpflege für Partner (Swapcard-Weg steht admin-seitig), SevDesk-Belege im Partner-Dateibereich (Leserichtung fehlt). Lücken 2, 3 und 5 als ein Baustein am Produktmodell, wie die Build-Session vorschlägt.
- Runde 1 damit bis auf F6 (Termin mit Konrad) abgeschlossen.
- **#39 Testdaten-Nachtrag (F5):** Kontingent, Session, Standbühne und Bewerbung für Konrads Testkonto; Skript setzt `synced_at` (sonst hätte der Cron einen echten vivenu-Coupon angelegt) und entfernt bei `--remove` nur eigene Zeilen (vorher wären die Sandbox-Kontingente mit echten Coupons verwaist). Konvention ergänzt. Methodenfehler im F4-Abgleich korrigiert: Anfrage-Artikel gibt es sieben, der Shop filtert Kategorien clientseitig — serverseitige Abzüge zeigen nur die erste Kategorie (Abschnitt 7 der Lückenliste).

## 2026-09-14 — Feedback-Runde 2, Teil 1 (Grundstruktur, Partner-Portal): sieben Entscheidungen
Auftrag der Build-Session: `docs/feedback-runde-2-2026-09-14.md` (F8 Hülle, F9 Partner-Portal). Die Hülle (F8.1–F8.5: Seitenleiste am Rand über die volle Höhe, Logoplatz, Portalauswahl im Menü, globales Profilmenü) ist reine Oberfläche und läuft. Entschieden:
1. **Teilnehmer-Portal ist immer ein eigenes Portal** (F8.7, kehrt Runde 1 Punkt 2 um — Konrad: das Teilnehmer-Portal ist das Front-End des Talent-CRM für alle Formate). `areasFor()` zählt es wieder mit, der Umschalter zeigt es. **Ausnahme bleibt das Kiosk-Konto:** `isKioskOnly()` öffnet weiterhin nur `/checkin` — ein Gerätekonto am Eingang bekommt kein Profil (E8).
2. **Ansprechpartner je Edition als Datenobjekt** (F9.1): Tabelle `edition_contact` (edition_id, `audience[]` aus `kb_audience`, `role_key` speaker_buddy/partner_buddy/general/…, Anzeigename, Rollenbezeichnung, `photo_path` in einem neuen öffentlichen Bucket `contact-photos` (Schreiben nur service_role, wie `partner-logos`), `email` **nur Rollen-Postfach** — CHECK auf `@chef-treff.de`, keine privaten Adressen —, `phone` optional und nur Firmennummer, Sortierung). RLS, keine Grants; Lesen `edition_contacts(audience, edition?)` für Angemeldete der Zielgruppe (Regel wie `my_kb_audiences()`), Schreiben `upsert_edition_contact`/`delete_edition_contact` für Admin und Bereichsleitung. Private Kontaktdaten von Freelancern bleiben draußen (Runde 1). Migration als Datei durch die Build-Session, Review hier.
3. **Allgemeine Zeiten und Orte als Editionsdaten** (F9.1): Tabelle `edition_info` (edition_id, `key`, `audience[]`, `label_de/en`, `value_de/en`, `sort_order`) — Öffnungszeiten, Einlass, Aufbau, Adresse, Anfahrt, WLAN; einmal im Admin gepflegt, in Partner- und Speaker-Portal und auf der Wiki-Startseite gezeigt. Text statt strukturierter Zeiten: es gibt keine Logik darauf. RPCs `edition_info(audience, edition?)`, `upsert_edition_info`, `delete_edition_info` (Admin/Bereichsleitung). Migration durch die Build-Session.
4. **Keine Google-Maps-Einbettung.** Maps setzt Cookies und überträgt die IP beim Laden; im Portal stattdessen ein statisches Anfahrtsbild je Edition (Upload wie Hallenplan) plus Link „In Google Maps öffnen“ (neuer Tab). CSP bleibt unangetastet. Wenn Konrad die lebende Karte will: Zwei-Klick-Komponente mit Hinweis und `frame-src https://www.google.com` — seine Entscheidung, Empfehlung dagegen.
5. **Kein Ticketshop im iFrame.** Zahlungsvorgänge gehören nicht in einen Rahmen unter unserer Domain (Phishing-Muster, 3-D-Secure-Weiterleitungen brechen, Cookie-Grenzen); der Knopf „Zum Ticketshop“ öffnet vivenu im neuen Tab, der Einlöse-Stand kommt über den Ingest zurück ins Portal. Bleibt so, auch wenn vivenu das Framing erlaubt.
6. **Loom-Videos: ja, mit Zwei-Klick und zentraler Liste.** Tabelle `portal_video` (`key`, Titel de/en, `url` mit CHECK auf Host `www.loom.com`/`loom.com`, `audience[]`, optional `edition_id`, Sortierung), Admin-Liste zum zentralen Austauschen, Seiten binden über `key`. Anzeige über eine Komponente `EmbedGate` (Poster + „Video laden“, erst dann der iFrame). CSP: `frame-src https://www.loom.com` — Änderung an `proxy.ts` kommt im PR und wird hier geprüft (Tabu für die Build-Session ohne Review bleibt). Migration durch die Build-Session.
7. **Admin ist kein Portal im Umschalter** (F8.6): eigener Bereich, aus jedem Portal über das Profilmenü erreichbar, mit Sprung in den passenden Admin-Abschnitt (aus dem Speaker-Portal nach `/admin/speaker`). Rechte unverändert (`is_staff()`), nur `AREAS`-Bedeutung.
- **Offen bei Konrad:** Logo-SVGs (im Repo liegt keins, Marke ist heute Text) und Bilder der Ansprechpartner; die eingekaufte UX-Skill (drei Kandidaten im Auftrag, keiner installiert): fremde Anleitungen sind kein App-Code, dürfen aber nichts ausführen und ersetzen `/portal-design` nicht — Konrad entscheidet, ob eine dazukommt.
- **Prozess:** Die Build-Session hält ihren Arbeits-Branch im Haupt-Checkout; die Architektur-Session arbeitet für `main` **nur noch über eigene Worktrees** (ein `git add -A` im Haupt-Checkout hat am 14.09. kurz fremde WIP-Dateien mitgenommen — zurückgesetzt, nichts verloren).

## 2026-09-14 — Runde 2, Konrads Antworten: Maps statisch, kein Shop-iFrame, Ansprechpartner mit dienstlichen Kontaktdaten
- **Maps:** statisches Anfahrtsbild + Link, wie vorgeschlagen. **vivenu:** kein iFrame, Knopf bleibt.
- **Ansprechpartner (F9.1), verfeinert nach Konrad:** ein Pool je Edition mit den Typen `partner_lead`, `partner_buddy`, `speaker_lead`, `speaker_buddy`; Zuordnung **je Partner** (`org_edition.lead_contact_id`, `buddy_contact_id`) und **je Speaker** (`speaker_profile.lead_contact_id`, `buddy_contact_id`); Anzeige Lead als allgemeiner Ansprechpartner und Buddy nebeneinander, bei gleicher Person oder nur einer Zuordnung eine Karte. Pflicht sind **dienstliche** Mailadresse (CHECK `@chef-treff.de`, z. B. konrad@chef-treff.de) **und Handynummer** des Team-Mitglieds — Konrad: ohne beides ist die Funktion nutzlos. Die Regel aus Runde 1 gilt weiter für **private** Adressen und Nummern von Freelancern (die fallen durch den Domain-CHECK); dienstliche Erreichbarkeit des Teams ist ausdrücklich gewollt. Sichtbar nur für die zugeordneten Partner (Kontakte der Organisation) und Speaker (Profil, Assistenz), nicht für alle Angemeldeten: RPC `my_contacts()` statt Liste je Zielgruppe. Bilder über den Admin (Sektion „Ansprechpartner“, Upload in Bucket `contact-photos`, Schreiben service_role). Admin-RPCs `upsert_edition_contact`, `set_org_contacts(org_edition, lead, buddy)`, `set_speaker_contacts(profile, lead, buddy)`; Fallback `is_default` je Typ und Edition, wenn nichts zugeordnet ist.
- **Markendateien:** die SVGs aus dem Design-Ordner liegen jetzt unter `public/brand/` (FLS-Wortmarke, Events-Formen mit `currentColor`, Sicherheitsprüfung ohne Skripte/externe Verweise); das ChefTreff-Logo gibt es dort nur als PDF — SVG-Export bei Konrad angefragt. UX-Skill: Konrad prüft selbst.
- **Kiosk im Umschalter (Frage der Build-Session):** nein — `checkin` wird wie `admin` nicht in der Portalauswahl geführt; das Gerätekonto landet direkt dort, das Team erreicht den Einlass über den Admin-Bereich (Volunteers/Check-in).
- **Admin-Zugang unten in der Seitenleiste** statt im Profilmenü (Abweichung der Build-Session von Entscheidung 7, PR #40): akzeptiert — Konrads Screenshot zeigt genau das, und das Profilmenü bleibt der Person vorbehalten. Sprung in den passenden Abschnitt über `?von=<bereich>`.

## 2026-09-14 — Feedback-Runde 2, Teil 2 (Event-App, Messestand, Messeshop): fünf Entscheidungen
Aufgenommen in `docs/feedback-runde-2-2026-09-14.md` (F10 Messestand, F11 Messeshop); gebaut wird erst nach F9.1, wie Konrad es will. Entschieden:
1. **Standardausstattung je Paket (F10.2) kommt aus dem Produktmodell, nicht aus dem Wiki.** Die Pakete (Start Up 1,5 qm, Basic 4 qm, All In 9 qm, Premium 18 qm) sind Produkte vom Typ `package`, ihre Ausstattung sind `product_component`-Zeilen — gepflegt im Produkte-Reiter des Partner-Admins (`upsert_product_component`), einmal für alle Editionen. Die Messestand-Seite rendert daraus die Übersichtstabelle aller Pakete und beim gebuchten Paket „das ist in deinen 18 qm drin“ (schließt zugleich Lücke 3 aus F4). Freitext je Paket über `product.description_de/en`. Keine Einbettung von Wiki-Artikeln in Seiten — das Wiki bleibt Wiki.
2. **Standliste und Hallenplan (F10.4):** Standnummern liegen in `booth` (`upsert_booth`, heute Partner-Admin) — die Produktion bekommt denselben Schreibweg (`is_production_team()` zusätzlich zu `is_partner_team()`). Ausstellerliste je Edition als RPC `exhibitor_list(edition?)` für angemeldete Partner und Speaker: Organisationsname, Standnummer, Logo, Kategorie — nichts Personenbezogenes. Spalten nach der Airtable-Standliste des Vorjahres. **Dateien je Edition in einem neuen privaten Bucket `edition-files`** (Pfad `<edition_id>/<kind>/<datei>`, Lesen für `authenticated`, Schreiben nur service_role über eine Admin-/Produktions-Route), Verzeichnis in Tabelle `edition_file` (edition_id, `kind` aus Vokabular `edition_file_kind`: hallenplan, anfahrt, aufbauplan …, path, label_de/en, audience[], uploaded_by/at) mit RPCs `edition_files(audience, edition?)`, `set_edition_file`, `delete_edition_file` (Admin, Produktion). Das Anfahrtsbild (Maps-Ersatz) liegt dort ebenfalls. Nicht der Partner-Bucket: dessen Pfadregel ist je Organisation, und Editionsdateien sind für alle da.
3. **Eine Frist für den Messestand (F10.3), geklärt mit Konrad:** der **02.04.2027** ist richtig, der 13.03. stammte noch aus dem Vorjahr und entfällt. Ein Eintrag in der bestehenden `deadline`-Tabelle je Edition (`booth_changes_until`: bis dahin Änderungen und Uploads über das Portal); danach nur noch ein **Änderungswunsch über das Portal** als Anfrage an das Partner-Team (kein Mail-Rückfall) — Konrad: „super“. Kein neues Feld, kein zweiter Countdown.
4. **PO-Nummer und Rechnungsadresse im Bestellweg (F11.2):** `shop_order.po_number` als neue Spalte (je Bestellung, Vorgabe aus `org_edition.po_number`), `shop_confirm` nimmt sie entgegen; die Rechnungsadresse wird aus den Stammdaten (`organization`, `org_edition.invoice_*`) **angezeigt und bestätigt**, nicht neu erfasst — Änderung führt ins Onboarding, nicht in die Bestellung (eine Adresse, ein Ort, Audit dort). SevDesk-Rechnungsentwurf nimmt die PO-Nummer der Bestellung mit.
5. **Freier Bestand im Shop (F11.1):** `shop_catalogue` liefert für Artikel mit `track_stock` den **freien** Bestand (`shop_stock_available`: Gesamtbestand abzüglich bestätigter und offener Bestellungen), nicht `stock_total`; Rückgabetyp der Funktion ändert sich ⇒ `drop function` + neu anlegen, Aufrufer prüfen (Konvention §1). Anzeige „Noch N verfügbar“ ab einer Schwelle und „Ausverkauft“ bei 0 ist Sache der Oberfläche.
- Migrationen (2, 4, 5) als Dateien unter `vorschlag/` mit Test durch die Build-Session; Review und Anwenden hier. Reihenfolge: erst F9.1 fertig, dann F10/F11.
- **Markendatei:** `public/brand/cheftreff-logo.svg` (mit `currentColor`, Original unter `public/brand/original/`) kommt mit dem F9.1-PR — Prüfung auf Skripte/externe Verweise beim Review.

## 2026-09-15 — Sanity-Kontrakt v2 nach Antwort des Website-Teams; Migration 0097
- **Anlass:** Das Website-Team hat Projekt-ID, Dataset und Viewer-Token geliefert (Werte nur in Vercel/`.env.local` über `env-set.sh`, nie im Chat) und vier Bitten formuliert: veröffentlicht statt Entwurf, freigestellte SVGs (Maske auf Navy) oder Flag `logoTransparent: false`, numerisches `sponsoringRank`, feste Wertelisten für `editionSlug`/`partnerCategory`. Antwort steht in `docs/website-team-sanity-briefing.md` (v2), der Kontrakt in `docs/runbooks/sanity-partner-logos.md`.
- **Dokument-ID `portalPartnerLogo-<org_id>` statt `portalPartnerLogo.<org_id>`.** Befund beim Nachlesen der Sanity-Doku: jede ID mit Punkt gilt als Pfad (wie `drafts.`) und ist **nur mit Token lesbar** — die statisch gebaute Website liest ohne Token und hätte unsere Dokumente nie gesehen. Noch nichts veröffentlicht (`external_ref` system `sanity` leer), daher ohne Altlast umgestellt.
- **`logoTransparent`** je Dokument aus einer Heuristik (`lib/sanity/svg.ts`, rein, getestet): `background` im `style` des Wurzelelements, `<rect>`/`<polygon>`/gerade `<path>`-Rechtecke über die ganze Zeichenfläche mit Füllung (außerhalb `defs`/`clipPath`/`mask`/`pattern`) ⇒ `false`; unklare Fälle gelten als transparent, eingebettete Rasterbilder werden gemeldet. Der eigentliche Schutz bleibt die Freigabe der Pflicht `logo_vector` durch das Team — ihre Beschreibung verlangt jetzt „freigestellt, transparenter Hintergrund“ (DE/EN). Die SVG-Datei wird deshalb auch im Trockenlauf gelesen. Manuelles Überstimmen der Heuristik erst, wenn ein echter Fall es verlangt.
- **`sponsoringRank` aus Daten, nicht aus Code:** neues Vokabular `sponsoring_level` (Schlüssel = HubSpot-Standtyp ohne Größe, `sort_order` = Rang): main_stage_loge 10 · signature 20 · lounge 30 · premium 40 · general 50 · intro 60 · start_up 70 · gemeinschaftsstand 80. **Reihenfolge ist ein Vorschlag nach Paketgröße — Konrad bestätigt oder ändert sie im Vokabular.** `sponsoring_level_key(text)` normalisiert den Freitext aus HubSpot beim Lesen (klein, Nicht-Alphanumerisches zu `_`), `org_edition.sponsoring_level` bleibt unverändert; unbekanntes Level ⇒ Schlüssel normalisiert, Rang null ⇒ im Dokument 999 (kommt zuletzt). `event_app_exhibitors()` liefert `sponsoring_key` und `sponsoring_rank` (Rückgabetyp geändert ⇒ drop + create; Aufrufer lesen nach Spaltenname). Das Sanity-Dokument trägt `sponsoringLevel` = Schlüssel.
- **Wertelisten dokumentiert:** `editionSlug` = `event.slug` (`fls27`, Muster `fls<jj>`), `partnerCategory` = Vokabular `partner_category` (`talent`, `startup`, kann fehlen), `sponsoringLevel` = Vokabular oben.
- **Trockenlauf von der Kommandozeile:** `scripts/sanity-dryrun.mjs` nutzt dieselben Bausteine wie die Admin-Route (`lib/sanity/publish.ts`; Node lädt die TypeScript-Dateien mit `--experimental-transform-types`, `--conditions=react-server` neutralisiert `import "server-only"`), protokolliert in `integration.sync_job` (`partner_logos_preview`, `script`), prüft optional ein Musterdokument (`--beispiel`) und schreibt das Prüfergebnis als JSON (`--json`). Mit dem Viewer-Token weist Sanity die Mutationsprüfung vermutlich mit 401/403 ab — erwartet und so dokumentiert. Läuft, sobald Konrad die drei Werte mit `env-set.sh` gesetzt hat.
- **Migration 0097** (`20260915112530`), Test `supabase/tests/v5_sponsoring_level.sql` (acht Schritte grün, vor und nach dem Anwenden). Umgesetzt in PR `welle-5/sanity-kontrakt-v2` durch die Architektur-Session selbst (A11 ist ihr Baustein seit 11.09.); Review = Konventionen, Gate, Tests.

## 2026-09-15 — Welle 5, Nachmittag: Merges #42/#44, Migrationen 0091–0096 live, Review PR #43 (Teil 1)
- **#44 (Sanity-Kontrakt v2) gemergt** (67f744e, Gate grün), Antwort-Dokument für das Web-Team steht (`docs/website-team-sanity-briefing.md` v2). Trockenlauf läuft, sobald Konrad `SANITY_PROJECT_ID`/`SANITY_DATASET` (`--config`) und `SANITY_API_TOKEN` mit `env-set.sh` gesetzt hat.
- **#42 (F12 „Eure Daten“) gemergt** (24cd3e7) mit einem Review-Commit: sieben Kundentexte je Sprache ohne Systemnamen (SevDesk, vivenu) und ohne „Stammdaten“ — Konrads Leitsatz „wie ein Kunde, der draufschaut“ gilt auch für Rechnungs- und Ticket-Hinweise. Zwei offene Punkte aus dem PR entschieden: (a) **`org_edition.pass_type_choice`** wird künftig beim HubSpot-Abgleich aus `partner_category` vorbelegt (startup ⇒ startup, sonst talent), das Partner-Team kann es im Partner-Admin überstimmen — Aufgabe für die Build-Session (Migration: Ingest + Team-RPC mit Audit, Feld im Org-Detail), bis dahin greift der Rückfall in `effective_pass_type` (Org-Typ). (b) **„Verpflichtend“ (F12.2)** bleibt Führung statt Sperre: nur die Startseite leitet im Status `invited` ins Formular; eine harte Sperre wäre Konrads eigene Entscheidung.
- **0091 (Ansprechpartner, `20260915113046`) live** nach Review, PR #41: Domain-CHECK mit `lower()`, Pflichtfelder als 22023 `fields_required`, Zuordnung prüft Typ **und** Edition des Kontakts (`check_edition_contact` ⇒ 22023 `invalid_contact`), Auskünfte pflegt jede Bereichsleitung (`can_edit_edition_info`), Ansprechpartner nur Admin/Partner-/Speaker-Leitung; Foto-Upload nur png/jpg/webp mit UUID-Prüfung; fünf Fehlerschlüssel ergänzt. Test 24/24. Gate grün, Merge nach dem Walkthrough der Build-Session.
- **PR #43** (gestapelt, sieben Migrationen + UI): angewendet und mit den Tests der Build-Session grün: **0092** `portal_video` (`20260915114209`, 6/6), **0093** `org_step`/`org_step_check` (`20260915114240`, 9/9), **0095** `shop_order.po_number` (`20260915114654`, 5/5; `shop_confirm` Zeile für Zeile gegen die Live-Fassung geprüft, nur die PO-Logik ist neu), **0096** zehn Wiki-Artikel als Entwurf (`20260915114852`, 7/7). Noch nicht angewendet: **0094** (Messestand) — drei Korrekturen angefordert: `booth_packages()` nur für die Zielgruppe partner (Paketpreise nicht für Talente/Volunteers), `set_edition_file` prüft `storage_path` gegen die Edition (22023 `invalid_path`) und die Zielgruppen gegen `kb_audience`; Route `edition-files` prüft `edition_id`/`kind` vor dem Pfadbau. Oberfläche 0092: `loomEmbedUrl` normalisiert `loom.com` auf `www.loom.com` (die CSP kennt nur die www-Fassung).
- **Sechs Abweichungen aus #43 entschieden:** (1) `product.area_sqm` + `size_note` statt Freitext — angenommen (eine Tabellenspalte aus Prosa ist keine Datenhaltung). (2) `exhibitor_list` ohne Logo — angenommen; falls gewünscht später über den öffentlichen Bucket `partner-logos` (0057), nie quer aus `partner-assets`. (3) kein Migrationsbedarf für den freien Bestand — richtig. (4) Änderungswunsch als `shop_request_product(p_sku = null)` — geht. (5) alle Wiki-Artikel Entwurf — ja. (6) Auslassungen (Konrads Mobilnummer, Ausstattungstabelle, Notion-Anhänge) — ja; der DB-Schenker-Geschäftskontakt bleibt (Firmenkontakt, keine Privatperson).
- **Bucket `edition-files`** (0094): privat, eine SELECT-Policy für alle angemeldeten Konten, Schreiben nur service_role aus der Produktionsroute. Der Zielgruppenfilter greift nur in der Liste (`edition_files`), nicht am Objekt — Pfade tragen eine Zufalls-UUID. Regel daraus: **keine vertraulichen Dateien in `edition-files`**, nur Hallenplan, Anfahrt, Aufbauplan.
- **Speaker-Migrationen 0098–0101** (Abgleich mit Paulinas Master-Liste, `docs/speaker-felder-abgleich-2026-09-15.md`): 0098 An-/Abreise (`speaker_travel`, Datum und Uhrzeit getrennt, Assistenz pflegt mit Audit, Liste über `can_manage_speaker` + Produktion) — Korrektur: `check_travel_mode` `stable` statt `immutable` und für authenticated gesperrt. 0099 Zusage/Absage mit Zeitstempel und Grund (Vokabular `speaker_decline_reason`), Briefanrede an `person` (`salutation_de/en`, nur Team) — **Korrektur: `suggest_salutation` ohne Rechteprüfung** hätte jedem Konto Titel/Nachname/Anrede zu jeder Personen-UUID verraten ⇒ `is_staff()`-Prüfung als Bedingung. 0100 Ernährung: `person.diet` (Vokabular) + `person.diet_note` (Freitext, freiwillig, 300 Zeichen, Art. 9) — keine RPC gibt Angabe und Person zusammen heraus, `set_diet` nur für sich selbst, kein Wert im Audit, **Löschfrist 30 Tage nach `end_date` der Edition** im Housekeeping (`purge_diet_data`, Entscheidung 15.09.), Volunteers sehen das Feld erst nach der Zusage — freigegeben. 0101 Regie auch für Speaker-Leads über `can_edit_regie` = Produktion oder `can_edit_stage` — freigegeben; **Hinweis:** `can_edit_stage` schließt `standbuehne_editor` ein, Partner mit Standbühne dürfen die Regie ihrer eigenen Bühne pflegen (angenommen; eine Zeile, falls Konrad es nicht will).
- **Konvention ergänzt** (`db-konventionen.md` §2): in SECURITY-DEFINER-Funktionen nie `select *`/`to_jsonb(person)` auf `person` — seit 0100 stehen dort Gesundheitsangaben, die nur die Person selbst sehen darf.
- Nummerierung: 0097 ist `20260915112530` (Sponsoring-Level); die Build-Session hat ihre neuen Migrationen auf 0098–0101 umnummeriert.
- **Nachtrag (später Nachmittag), PR #43 Teil 2 — angewendet nach den Korrekturen der Build-Session:** **0094** Messestand (`20260915115415`, Test 14/14 inkl. drei Review-Schritten), **0098** An-/Abreise (`20260915115707`, 8/8 + Helfer gesperrt), **0099** Zusage/Absage/Anrede (`20260915115751`, 10/10 inkl. „Vorschlag ohne Rolle ⇒ 42501“), **0100** Ernährung (`20260915115505`), **0101** Regie für Leads (`20260915115544`, 8/8). Befund im Test `v5_catering.sql`: Schritt 10a ruft `purge_diet_data()` mit der Rolle `production_team` — die Funktion erlaubt zu Recht nur Cron/Admin/Programm-Team; Korrektur des Tests bei der Build-Session, die Migration bleibt. Alle sieben Migrationen aus #43 sind damit live; es fehlen Umbenennung, Gate und Walkthrough der Build-Session vor dem Merge.
- **0102** (`20260915115807`, PR #41, Nachtrag nach dem Walkthrough der Build-Session): fremde Mail-Domain beim Ansprechpartner antwortet mit 22023 `invalid_email` statt nacktem 23514 aus dem CHECK — der CHECK bleibt die Schutzlinie, die Redaktion sieht jetzt, woran es liegt. Walkthrough #41 (elf Schritte als angemeldeter Nutzer) grün; Merge nach dem Gate.

## 2026-09-15 — Speaker-Domäne: drei Portale, Admin-Zugriff, Regie (Antwort auf `docs/speaker-portale-abgleich-2026-09-15.md`)
Konrads Zielbild (15.09.): Admin-Sektion für alle Speaker-Daten (nur Konrad, Henni, Paulina), Speaker-Lead-Portal für Akquise/Betreuung/Programm mit Regieübersicht, Speaker-Portal für die eigenen Angaben. Entschieden:
1. **D1 Admin-Zugriff:** Zielregel ist **`has_role('admin')` statt `is_staff()`** für den Admin-Bereich (Weg 1 der Build-Session); `staff_user` bleibt die Teamkennzeichnung für Wiki-Zielgruppen und Produktionslisten. **Der Schnitt kommt erst, wenn die Bereichsleitungen (Partner, Volunteers, Hospitality) ihre eigenen Portale haben** — heute sind Teile des Admin-Bereichs ihre Arbeitsfläche. Bis dahin bleibt `is_staff()`; keine Namensliste im Code, keine zweite Admin-Rolle.
2. **D2 Regie für Partner-Standbühnen:** `can_edit_regie` = Produktion oder `can_edit_stage`, damit auch `standbuehne_editor` für die eigene Bühne — bleibt so, bis Konrad anders entscheidet (liegt ihm vor; Änderung wäre eine Zeile).
3. **D3 `owner_person_id` (Speaker → Buddy):** pflegbar im Admin (alle); im Lead-Portal nur **weiterreichen** an eine andere Lead-Person, nie an sich ziehen (`update_speaker` kann es schon, die Regel gehört in die RPC: Lead darf `owner_person_id` nur ändern, wenn sie heute selbst Owner ist).
4. **Reihenfolge (Vorschlag E der Build-Session angenommen):** (1) Admin-Speaker-Sektion `/admin/speaker` mit Liste, Detail und allen zwanzig Feldern inkl. `owner_person_id` und `internal_notes` (nur Team); (2) Admin-Seite „Speaker-Leads“ (wer ist Lead, welche Bühne, wie viele Speaker; Rolle `speaker_manager` und Zuordnung an einem Ort); (3) danach der Zugriffsschnitt D1. Dazu weiterhin offen aus #42: `pass_type_choice` (HubSpot-Vorbelegung + Team-Override).
- **Prozesslehre (15.09.):** `gh pr merge` meldete Konflikte, mein Skript löschte den Branch trotzdem (`--delete` blind angehängt) — PR #41 war kurz geschlossen und der Branch weg; aus dem lokalen Commit wiederhergestellt, PR wieder geöffnet, main gemergt. Regel in `db-konventionen.md` §8: Merge-Ergebnis prüfen, Branch erst danach löschen.

## 2026-09-15 — PR #43 gemergt (7ab39ac): Videos, Event-App-Schritte, Messestand, Messeshop-PO, Wiki-Inhalte, Speaker-Anreise/-Zusage/-Ernährung, Regie für Leads
- Nach dem Merge von #41 hatte #43 vier additive Konflikte gegen main; die Build-Session hat main eingemergt (2197bde) — die Wörterbücher **auf Objektebene** (Dreiwege-Merge aus `:1:`/`:2:`/`:3:`, „wer die Basis geändert hat, gewinnt“), weil ein textueller Hunk `contacts` in `catering` verschachtelt hätte. Dabei blieben die Kundentexte aus #42 („Unternehmen“ statt „Stammdaten“) erhalten, `nudgeBody` zieht mit. Gate grün (227 Tests), Walkthrough über dreizehn Seiten als angemeldeter Nutzer. Prüfung hier: Schlüsselmengen DE/EN identisch, Navigationsschlüssel als Geschwister, Fehlerschlüssel vollständig.
- **Welle 5, Stand:** alle Migrationen 0091–0102 live und auf main; offene Bausteine als eigene PRs der Build-Session in dieser Reihenfolge: `/admin/speaker` (Liste, Detail, alle zwanzig Felder inkl. `owner_person_id`, `internal_notes` nur Team), Admin-Seite „Speaker-Leads“, `pass_type_choice` (HubSpot-Vorbelegung + Team-Override), zuletzt der Zugriffsschnitt D1.
- **D3 konkretisiert (Vorschlag der Build-Session angenommen):** eigene RPC `handover_speaker(p_profile_id, p_to_person_id)` statt Sonderfall in `update_speaker` — ein Lead darf nur weiterreichen, was er selbst besitzt (sonst zieht jemand fremde Speaker an sich), der Empfänger muss `speaker_manager` sein (sonst zeigt das Feld auf jemanden ohne Lead-Portal); Admin setzt frei. Audit mit vorher/nachher.
- `docs/schema.md` aus der Live-Datenbank regeneriert (78 Tabellen, 6 Views, 353 Funktionen).

## 2026-09-15 — PR #45 Admin-Sektion Speaker: Migration 0103 live (`20260915123741`)
- **Regression aus 0099 behoben:** `manager_speakers` hatte beim Erweitern des Rückgabetyps `internal_notes` (seit 0037) verloren; im Lead-Portal stand das Notizfeld leer, Hineinschreiben hätte eine ungesehene Notiz überschrieben. 0103 setzt die Spalte wieder ein (drop + create). Konvention (§1/§6): bei drop + create einer Tabellenfunktion die alte Spaltenliste gegen die neue halten — der Test prüft die Rückgabespalten vorher/nachher.
- **`owner_person_id` ist Team-Feld** in `update_speaker` (`team_only_fields`): eine Lead-Person konnte sich vorher fremde Speaker zuschreiben. **`handover_speaker(p_profile_id, p_to_person_id)`** setzt D3 um: Nicht-Team reicht nur weiter, was es selbst betreut, und nie ins Leere; der Empfänger muss aktiv `speaker_manager`/`area_lead_speaker`/`admin` sein (`is_speaker_manager`, intern) ⇒ sonst 22023 `invalid_owner`; Freigeben (`null`) nur Team; Audit mit vorher/nachher. `speaker_managers()` liefert die Auswahl.
- **`speaker_detail(p_profile_id)`**: der ganze Datensatz in einem Aufruf; `internal_notes` nur für `is_speaker_team()`, sonst fehlt der Schlüssel und `internal_notes_visible = false` sagt es der Oberfläche. Personenfelder benannt (kein `to_jsonb(person)`).
- Test `v5_admin_speaker.sql` 14/14 nach dem Anwenden. Merge nach Umbenennung und Walkthrough der Build-Session. Übergabe-Oberfläche im Lead-Portal folgt im Baustein „Speaker-Leads“.
- **#45 gemergt** (04acb50) nach Gate auf dem umbenannten Stand und Walkthrough der Build-Session (Leseseiten als Admin über Cookie-Jar, Schreibwege auf RPC-Ebene im Smoke-Test belegt — bewusst kein zweiter Browser-Login, damit kein Token im Protokoll landet). Produktion auf Vercel nach dem Tag: Ready. Nächster Baustein läuft: `welle-5/admin-speaker-leads` mit Migration 0104 (`speaker_leads_admin`, `unassigned_speakers`, `can_manage_speaker_leads`, Übergabe-Oberfläche im Lead-Portal).

## 2026-09-15 — PR #46 Speaker-Leads verwalten: Migration 0104 live (`20260915124834`)
- `/admin/speaker-leads`: unbetreute Speaker mit Zuordnung am Stück (`unassigned_speakers`, Absagen bleiben draußen), Lead-Personen mit aktiven Rollen samt Zuweisungs-ID, betreut/zugesagt/abgesagt und Summe der offenen Schritte (`speaker_leads_admin`, Zugriff `can_manage_speaker_leads()` = Admin, Speaker-Leitung, Programm-Team), Aufnehmen über die Personensuche. **Rolle immer im Edition-Scope, nie global** (`assign_role` mit `scope_type = 'edition'`). **Entziehen räumt `owner_person_id` nicht ab** — die Rückfrage nennt die Zahl der betreuten Speaker, Umhängen bleibt eine bewusste Handlung.
- `my_manager_scope()` nennt zusätzlich `person_id`, damit das Lead-Portal die Übergabe (`handover_speaker`, 0103) nur dort anbietet, wo sie erlaubt ist; Übergabe-Oberfläche im Lead-Portal mit `speaker_managers()` als Auswahl.
- `speaker_next_steps` je betreutem Speaker in `speaker_leads_admin` ist für eine Admin-Seite vertretbar; Mengenabfrage erst, wenn die Seite spürbar wird. Test 9/9 nach dem Anwenden; Merge nach Umbenennung und Walkthrough.

## 2026-09-15 — Abend: #46 gemergt, 0105 live, Konrads Entscheidungen D1/D2, Team-Verwaltung vor dem Zugriffsschnitt
- **#46 gemergt** (905ceb0) nach Gate und Walkthrough. **PR #47 Pass-Typ:** Migration **0105** (`20260915130339`) live — Insert-Trigger `trg_org_edition_prefill_pass_type` belegt `pass_type_choice` aus `organization.partner_category` (startup ⇒ startup, sonst talent; bewusst kein Update-Trigger, damit NULL = Rückfall auf den Org-Typ erreichbar bleibt), `set_pass_type_choice(org, choice|null, edition?)` für das Partner-Team mit Audit und Zahl der betroffenen Kontingente (Trigger aus 0049 zieht `sync_ticket_allocations` nach). Ingest unangetastet; Bestandsdaten nicht nachgezogen (eigene Migration, falls gewünscht). Fund der Build-Session: gleichnamiger Trigger hätte `trg_org_edition_pass_type` (0049) lautlos ersetzt — Konvention: vor `drop trigger if exists` prüfen, ob der Name belegt ist.
- **D2 — Konrad (15.09.): „can edit für eigene Standbühne passt.“** Bleibt wie gebaut.
- **D1 — Konrad zieht den Zugriffsschnitt vor:** „Der Admin-Bereich soll am Ende begrenzt werden über eine Rolle. Ggf. wird es noch weitere Personen geben.“ Reihenfolge bestätigt (Admin-Speaker ✅, Leads ✅, dann Zugriff). Die Sorge vom Nachmittag entfällt: `staff_user` hat genau einen Eintrag (Konrad, hat `admin`), Bereichsleitungen betreten den Admin-Bereich heute nicht.
- **Neu von Konrad:** eine Admin-Sektion, in der er Personen und Rollen des Teams verwaltet. **Entschieden:** (1) PR `/admin/team` zuerst — Roster der Teamrollen (admin, `area_lead_*`, `programme_team`, `production_team`, `speaker_manager`, `volunteer_lead`, `checkin_operator`) mit Scope und Portalzugang, vergeben/entziehen an einer Stelle, RPC `team_members()` nur `has_role('admin')`. (2) Danach der Schnitt: **`is_staff()` wird `has_role('admin')`** (eine Wahrheit; `staff_user` und `scripts/make-staff.mjs` bleiben als Altlast stehen und entscheiden nichts mehr), `lib/areas.ts` ohne `staff: true` am Admin-Bereich. Bedingungen: Inventar aller Policies/RPCs mit `is_staff()` im Migrationskopf (SQL **und** TypeScript, insbesondere `lib/auth`), Test in beide Richtungen (Admin-Rolle ⇒ Zugang, `staff_user` ohne Rolle ⇒ 42501), vor dem Anwenden Prüfung, dass Konrads Person eine aktive `admin`-Rolle trägt (kein Aussperren), `admin` nur durch Admins vergebbar.
- **Sanity-Trockenlauf steht auf Konrads Checkliste** (Abschluss-Checkliste), mit dem Website-Team in den nächsten Tagen. `env-set.sh` kann jetzt `--local-only`, falls der Vercel-Schritt im Terminal scheitert.
- **Sanity-Trockenlauf gefahren (15.09., 15:13):** Werte gesetzt (der Token war beim unsichtbaren Einfügen doppelt gelandet — 360 statt 180 Zeichen, 401 „Session not found“; programmatisch halbiert und neu gesetzt, ohne Ausgabe), Lesen ok, Musterdokument mit Viewer-Token 403 wie erwartet, keine freigegebenen Logos ⇒ nichts zu validieren. Prüfergebnis für das Web-Team: `docs/sanity-trockenlauf-2026-09-15.md` (+ JSON in `docs/referenz/`).
- **`programme_team` im Bereich `speaker-leads`: umgesetzt** (15.09., Abend; Konrad auf Rückfrage der Build-Session: „Jetzt machen.“ — kommt mit PR #49, Commit f885869: `lib/areas.ts` Rollen `speaker_manager` + `programme_team`, Test positiv). Vorgeschichte: Mein Vorschlag (Bereichsgate an die RPC-Guards `manager_speakers`/`speaker_travel_list`/`speaker_managers` angleichen) hat sich mit Konrads Antwort an die Build-Session gekreuzt: „Lass uns die Rollendefinition im Speaker-Team nochmal ganz am Ende testen.“ Konrads Anweisung gilt — `lib/areas.ts` bleibt in #49 unverändert, `tests/areas.test.ts` beschreibt den Ist-Zustand (Programm-Team allein öffnet das Board nicht). Die fünf Prüfungen je Rolle stehen in `docs/abschluss-checkliste.md` („Ergänzungen 15.09. (Rollen & Zugriff)“, Commit auf `welle-5/admin-rollenschnitt`); die Erweiterung wäre ein Einzeiler, wenn Konrad beim Durchtesten zustimmt. Offen zur Review beim nächsten „Weitermachen“: #48 `/admin/team` (0106) und #49 `is_staff()` := `has_role('admin')` (Inventar im Kopf, Abbruch bei `staff_user`-Zeile ohne aktive Admin-Rolle, Test 8/8). #47 wartet auf den Merge von main in den Branch (Konflikt nur `supabase/tests/README.md`).

## 2026-09-17 — Konrad zurück: ergänzender Plan freigegeben, 0106/0107 live, Chats und Backlog, Design-System v2, Chatbot vor dem Go-live
- **Plan `docs/plan-ergaenzung-2026-09-17.md` von Konrad freigegeben** (Antworten auf §7): (1) Reihenfolge Struktur → Design → Feinfeedback. (2) **Fünf Build-Chats nach Datenverbund** (Partner · Speaker-Domäne · Talent & Hackathon · Volunteers/Produktion/Check-in · Admin & Schnittstellen = bestehende Build-Session im Hauptcheckout) plus Design-Chat plus Architektur-Session; höchstens zwei bis drei gleichzeitig aktiv (gemeinsames Kontingent); Startpakete `docs/chat-startpakete.md`, `launch.json` um Ports 3002–3005 ergänzt, Redirect-URLs trägt Konrad ein. (3) **Backlog je Portal als Markdown** `docs/feedback/<portal>.md` mit ID, Prio, Status (Entscheidung der Architektur-Session; Konrad liest den Stand im Chat); Pflichtablauf in `docs/feedback-leitfaden.md` §3: erfassen, mit IDs antworten, bauen, PR nennt IDs, Konrad nimmt ab. (4) **Supabase Studio nur Konrad und Architektur-Session; das Team arbeitet ausschließlich über die Portale** — jedes fachliche Feld braucht einen Pflegeort; Prüfung über die Feld-Eigentümer-Matrix `docs/feld-matrix-2026-09.md` (`scripts/gen-feld-matrix.mjs`). (5) Backend-Walkthrough als zwei Termine (Vorschlag 22./24.09.). (6) Design **„Hell mit Marken-Momenten“**; Welcome, Login und Hero-Bänder dürfen dunkel sein; Arbeitstauglichkeit zählt. (7) **Formen präzisiert** — ersetzt für die Portale die Regel vom 12.09. „Dreieck statt Hexagon“: Hexagon aus den Website-Blöcken als Aufzählungs- und Schrittmarker im Akzent; Gradient-Formen (Pfeil-/Chevron-Hexagone nach A2, Dreiecke) nur für Hintergrundflächen und Porträt-Masken; Line-Art nur dekorativ (Figma `3:20146`, `3:20352`, `3:19981`, `319:692`). (8) Pink nur Marketing-Momente. (9) **Chatbot der Wissensbasis vor dem Go-live** (nicht Härtung); Segmentierungs-Übersicht danach Prio 1; Strategy-Calls nach hinten; C-Features einzeln (Plan §7b). (10) Walkthroughs Alt-Portale und Team-Portal am 17./18.09. (11) Redirect-URLs 3002–3005 durch Konrad.
- **0106** `20260917093050` `v5_team_verwaltung` (PR #48) **live**: `team_role_keys()` (Teamrollen an einer Stelle), `team_members()` nur `has_role('admin')` (42501), Scope als Name, Portalzugang, Admin-Zähler; Personenfelder benannt, E-Mail nur an Admin. Test `v5_team_verwaltung.sql` 8/8 nach dem Anwenden.
- **0107** `20260917093102` `v5_admin_ueber_rolle` (PR #49) **live**: `is_staff()` := `has_role('admin')`; Schutzprüfung bestand — `staff_user` hat genau eine Zeile (Konrad) mit aktiver globaler Admin-Rolle, vor dem Anwenden gegengeprüft; `staff_users_without_admin()` bleibt als Diagnose (nur Owner). Inventar gegen die Live-Datenbank: 31 Funktionen und 14 Policies referenzieren `is_staff()`; für alle heutigen Konten ändert der Schnitt nichts (Bereichsleitungen hängen an eigenen Rollen). Offen für den Rollentest am Ende (Checkliste 15.09.): welche `is_staff()`-RPCs (Hospitality, Editionsdateien, Personensuche, Anrede) später auch Bereichsleitungen öffnen. Test `v5_admin_ueber_rolle.sql` 8/8. Umbenennen und Walkthrough durch die Build-Session, dann Gate und Merge #48 → #49.
- **Chatbot-Technik (Architektur-Session, Abweichung von Masterplan v0.1b):** Retrieval zunächst über **Postgres-Volltextsuche** (`tsvector` DE/EN je H2-Abschnitt, `kb_chunk`) statt pgvector — bei 26 bis 60 Artikeln ausreichend, kein Embedding-Anbieter, kein zusätzlicher AVV; pgvector nur, wenn die Trefferqualität nicht genügt. Antwort serverseitig über die Claude API (Route Handler, `ANTHROPIC_API_KEY` in Vercel durch Konrad), Rate-Limit je Nutzer, Hinweis „keine persönlichen Daten eingeben“, Fragenprotokoll ohne Personenbezug. AVV Anthropic steht auf der Checkliste. Baustein des Chats Admin & Schnittstellen direkt nach #49.
- **Prozess:** Abgleich-Matrizen `docs/abgleich/` (Messeshop, Partner-Hub-Rest, Initiativen, Volunteers, Hackathon, Talent, Team-Werkzeuge), Backlog-Übertragung aus Runde 1/2 und Feld-Matrix laufen als Hintergrundaufträge der Architektur-Session — Codebefund, nicht am Gerenderten; Konrads Walkthrough prüft sie. Design-Auftrag `docs/design-system-v2-auftrag.md` für den Design-Chat.
- **Nachtrag C-Features (Konrad, 17.09. nachmittags), aus Plan §7b:** (1) Slid@Home bauen, mit Dummy-Daten durchklickbar, anderer Name (Arbeitstitel „Folien nach dem Summit“) — SPK-011/TAL-001. (2) Talk-Generator direkt bauen: Speaker-Portal, Sektion Slot, Chat-Oberfläche mit Schärfung, geschärft an alten Titeln und Beschreibungen (Konrad liefert) — SPK-012, Technik wie der Wiki-Assistent. (3) Hear-Me-Speak-Grafik aufnehmen („super wichtig“): Maske mit Zoom/Positionierung, FLS26-Template zuerst — SPK-013. (4) Slot-Grafiken nur im Admin: Sektion „Grafiken“ im Speaker-Admin, alle Grafiken je Speaker/Slot zentral, technisch für die Event-App — ADM-020. (5) Bild-Normalisierung zusammen mit den Slot-Grafiken: Speaker-Grafik ohne, Slot-Grafik mit Freistellung, Gesicht zentral ⇒ Zuschnitt — ADM-020; Verarbeitung ohne externen Anbieter ist Vorgabe der Architektur-Session (Porträts sind Personendaten). (6) Kalender-Blocker aufsetzen, Versand erst im finalen Test — SPK-014. (7) Merch bleibt on hold. (8) vivenu-Add-ons ins Backlog, Erarbeitung im Oktober. Zuständig: Chat Speaker-Domäne (1–6), Talent & Hackathon (Teilnehmerseite von 1).
- **Modell der Chats (Konrad, 17.09.):** alle Build- und Design-Chats laufen mit **Opus**, nicht Fable — sonst ist das gemeinsame Kontingent schnell erschöpft. Die Architektur-Session stellt das Modell neu gestarteter Chats um (`set_session_model`), Konrad wählt beim Start Opus.
- **0108** `20260917102018` `v5_wissensbasis_assistent` (PR #50) **live**, korrigierte Fassung nach Review (Protokoll an den Slot gebunden — `kb_rate_limit.logged`, P0001 `no_slot`; Edition in der Suche; Quellenlinks über Anker; Test für die Auswertung). Test 17/17 nach dem Anwenden. Fünf Abweichungen der Build-Session angenommen (kein Modellaufruf ohne Treffer, `published` statt `live`, Degradierung ohne Schlüssel, `is_kiosk_only()`, `kb_question_report`). Merge nach Umbenennen, Walkthrough und Gate.

## 2026-09-17 — #50 Assistent auf der Wissensbasis gemergt (01c5495): 0108 + 0109 live, Chatbot vor dem Go-live steht
- **#50 gemergt** nach Gate (241 Tests) und zwei Walkthroughs der Build-Session. Live: **0108** `20260917102018` (kb_chunk, kb_search, Zähler, Protokoll, Auswertung, Aufräumen; Test 17/17) und **0109** `20260917102942` (Suche erst UND, dann ODER als Rückfall aus der geparsten tsquery; Test 5/5). Konrad hat die zehn Wiki-Artikel freigeschaltet; der Trigger baute daraus 39 Abschnitte, ohne dass jemand etwas anstossen musste.
- **Befund aus dem Walkthrough, der zur Konvention wurde** (db-konventionen §6): Suchtests nehmen ihre Wörter nicht aus dem eigenen Testartikel — die erste menschliche Frage („Bis wann muss die Rückwand geliefert sein?“) fand nichts, weil im Wiki „einsenden“ steht. Zusätzlich: die Quellenzeile zeigt bei mehreren Treffern desselben Artikels keine irreführende Einzelüberschrift mehr.
- **Nächster Schritt Konrad:** `ANTHROPIC_API_KEY` in Vercel setzen (Admin-Chat sagt an) — bis dahin zeigt der Assistent die passenden Abschnitte ohne formulierte Antwort (`no_model`). Erster echter Lauf prüft, ob der Systemtext hält (nur aus Abschnitten, „steht nicht im Wiki“).
- Nächster Baustein des Admin-Chats: Segmentierungs-Übersicht fürs Marketing (Plan §2, Prio 1 nach dem Chatbot).

## 2026-09-17 — Walkthrough Partner Hub, Messeshop, Speaker Hub: Konrads 24 Antworten (Matrizen partner-hub-rest, messeshop, initiativen)
- **Formate werden ein eigener Bereich „Eure Formate“ im Partner-Portal:** Masterclass, Company Tour, neu **Interview Tables** (ehemals Speed-Dating, 2026 nur Information) und **Side-Events** (2026 nicht abgebildet). Der Partner legt sein gebuchtes Format selbst an (Titel, Beschreibung, Bewerbungsfragen aus dem Fragenkatalog, neue Fragen auf Antrag — z. B. Geschlecht für Frauen-Formate, Datenschutz prüfen), verwaltet Bewerbungen mit Export; Teilnehmende bewerben sich für alle Formate gleich. Auch Hackathon (Challenge, Preise, eigener Backdrop, Fristen) und Messestand (Rückwand) hängen dort. `/hackathon` bleibt die Teilnehmer-App. Konzept und Datenmodell: Architektur-Session (Arbeitsauftrag Welle 6); Backlog PART-033/034/035, HACK-005, TAL-002.
- **Belege aus SevDesk im Portal** (Angebot, Rechnung, Messeshop-Rechnung) über die Kundennummer, Rückfall händischer Upload — PART-036; SevDesk-Lesezugriff ist neu (bisher nur Entwürfe hinaus).
- **Messeshop 2027:** keine Shop-Rollen, alle Artikel für alle, Plausibilität über eine interne Checkliste je Stand in der Produktion (PROD-005); Shop nur mit gebuchtem Messestand (PART-037); **zwei Phasen** 19.03. und 09.04.2027 mit Artikel-Flag „kurzfristig bestellbar“ (PART-038, ersetzt die drei Phasen vom 10.09.); Shop-Bestellungen fließen in die Produktionsliste je Stand (PROD-004, P1); **Produktstamm-Pflege im Produktionsportal** mit Abgleich nach HubSpot und SevDesk (PROD-006, ersetzt Airtable + make.com); Video als Pop-up (PART-039); Paket-Einzelteile bleiben kaufbar; USt vorerst 7 %.
- **Media Kit** im Partner-Portal (Marken-Material + Partnergrafik), gepflegt vom Marketing in einem Admin-Grafikbereich mit eigener Rolle `marketing_team` (PART-041, ADM-023; Rolle als Migration durch die Architektur-Session, Vergabe in /admin/team).
- **Initiativen:** eigener Funnel im Admin wie ein CRM (nie HubSpot), Leistungen direkt zuweisen, Rabattstufen 100 %/50 % je Kontingent, SKU „Initiativen-Partnerschaft“, Beachflag als Leistung, Volunteer-Zusage als Checklistenpunkt, Standprodukte für zwei Tage und für einen Tag mit tagesweise teilbarem Stand (ADM-022); **öffentlicher Award** mit Missbrauchsschutz (ADM-024, P3).
- **Challenge-Frist vier Wochen vor dem Event** (PART-040, löst den Fristregel-Fehler PART-032). Wikis bleiben frei, Überarbeitung vor dem finalen Launch (Checkliste).
- **Einordnung:** Formate-Bereich, Initiativen-CRM und Produktstamm-Abgleich sind neuer Umfang (etwa zwei Wochen Bauzeit im Partner-Cluster plus Architektur). P1 nur für Sichtbarkeit des Shops, die zwei Phasen, die Challenge-Frist und die Produktionsliste; alles andere P2 vor dem 01.11., Award P3.

## 2026-09-17 — Konrads Speaker-Punkte, Struktur des Partner-Portals, Antworten auf drei Rückfragen
- **Ansprechpersonen mit Kontaktdaten im Portal (Regeländerung).** Konrad: „Ich möchte keine Rollenpostfächer … Das sind zwar private Kontaktdaten, aber die sind elementar für unser Serviceversprechen.“ Die Regel „keine privaten Kontaktdaten von Team/Freelancern in Portalen“ (Antwort 71, AGENTS.md) ist damit aufgehoben: Ansprechpersonen (Team, Speaker-Buddys) stehen mit Name, Foto, E-Mail und Telefon im Speaker- und Partner-Portal. Auflagen der Architektur-Session: dienstliche Adressen bevorzugen (`@chef-treff.de`, Dienstnummer), für Freelancer die Einwilligung zur Anzeige im Vertrag festhalten (Checkliste), Daten nur im eingeloggten Portal, nie in Mails an Dritte, nie in öffentlichen Seiten. AGENTS.md angepasst; SPK-015.
- **Speaker-Portal:** Shuttle-Buchung als Formular mit den Feldern der Airtable-Shuttle-Tabelle inkl. Zwischenfahrten (SPK-016, Export ADM-028) · Einwilligung Hotel/Shuttle als Pop-up nach „Buchen“ (SPK-017) · Technik unter „Slot“, Werte in die Regie (SPK-018, PROD-007) · „Media Kit & Bühnenfotos“ mit Fotos unmittelbar nach dem Slot, LinkedIn-Vorlagen, Speaker-Grafik (SPK-019, ADM-027) · Assistent im Speaker-Wiki vorhanden (SPK-020).
- **Partner-Portal, Struktur (Konrad):** Seitenleiste mit Gruppe **„Euer Summit“** (Checkliste, Dateien, Tickets, Event-App, Messeshop, Media Kit) und Gruppe **„Eure Formate“** — nur Menügruppe, keine eigene Seite — mit den **produktabhängigen Seiten** Messestand, Masterclass, Company Tour, Side-Event, Interview Table, Hackathon, **Branding** (Digital Branding 16:9 und weitere Standelemente) und **Talk** (gebuchte Keynote/Panel: Slot gespiegelt aus dem Speaker-Portal, Partner trägt selbst einen Speaker ein, der das normale Onboarding bekommt). Seite nur bei gebuchtem Produkt (HubSpot-Position). Dafür bekommt der Slot eine Partner-Organisation (Admin und Speaker-Leads, ADM-025/LEAD-010). PART-042…048.
- **Formate einheitlich:** Masterclass, Company Tour, Side-Event, Interview Table je zwei Sektionen — Inhalte einsenden und Bewerbungen verwalten (Fragen pflegen, Liste Bewerbungen, Tab Teilnehmer, Export); Teilnehmer-Portal spiegelt alle vier gleich (TAL-003). Besonderheiten: Interview Tables — fester Tisch, Partner legt Slots (Tag, Beginn, Ende, Länge), Jobausschreibung (Text + Link), gesuchte Profile über die Teilnehmerfelder; Side-Event — Zeit, Ort, Titel, Beschreibung, Hintergrundbild; Masterclass — fester Slot, Inhalte wie ein Talk; Company Tour — fester Slot, Zuweisung im Admin unter Programm → Company Tours (ADM-026), Partner beantwortet die neun Fragen aus 2026 (Ansprechperson, Adresse, Zeitslot, Snacks, Hinweise, Profile, Fotografieren).
- **Marketing ist immer Team** → Rolle `marketing_team` mit dienstlichen Adressen (ADM-023). Interview Tables und Side-Events sind Produkte im Angebot (HubSpot) → öffnen die Seite wie der Messestand.
- **Tempo:** P1 und P2 kurzfristig, Konrad will mehrfach testen. Arbeitsauftrag Welle 6 (Formate-Datenmodell, Partner am Slot, Initiativen, Produktstamm, SevDesk-Belege, Rolle Marketing) schreibt die Architektur-Session bis 18.09.
- **Backend-Vorentscheidungen (Konrad, 17.09. nachmittags, aus der Feld-Matrix):** (1) **Jobtitel und Organisation:** `person` führt den aktuellen Stand, `speaker_profile` friert den Stand der Edition ein (wie auf Website und Grafik gezeigt) — keine Redundanz, sondern Historie; Vorbelegung aus `person` beim Anlegen des Profils. (2) **Partner-Beschreibung je Organisation**, nicht je Edition: ein zweisprachiges Feld an `organization` (Migration: `description_de/en` von `org_edition` nach `organization` ziehen, Bestand übernehmen), Partner aktualisieren es jährlich und starten nie leer. (3) **Profilfotos** einheitlich als Datei in einem Bucket mit Rechten, nie als offene URL: `person.photo_url` wird zu einem Asset-Verweis migriert. (4) **`staff_user` und `scripts/make-staff.mjs` werden mit der nächsten Migration entfernt** (entscheiden seit 0107 nichts mehr). Umsetzung in Arbeitsauftrag Welle 6.
- **Welle 6, Korrekturen aus den Chats (17.09. abends):** `product.late_orderable` (0038) ist das Zwei-Phasen-Kennzeichen, keine neue Spalte · `product.format_key` als kleine Migration mit B1 · Shuttle-Buchung **additiv** neben Hospitality, ohne Kontingent (D8 offen) · `session.tech` = Ansage des Speakers, `regie_cue` = Disposition der Regie · Ansprechperson im Speaker-Portal über das bestehende `my_contacts()` (0091), keine neue RPC; Speaker-Buddys brauchen einen `edition_contact` (Hausadresse, D7) · vier weitere Speaker-Bausteine S10–S13 (SPK-002…005), PART-002 an B1. **Backlog-IDs:** fortlaufend gegen `main`; PR #52 nummeriert ADM-022…031 auf ADM-029ff. um. **Programm-Grundgerüst (M8) bleibt beim Chat Admin & Schnittstellen** (Konrad im Admin-Chat, 17.09.: „damit ich alles in einem testen kann“); Reihenfolge Grundgerüst → Mail-Vorlagen zentral bearbeitbar → Mail-Protokoll → Profil löschen; Rechte der Gerüst-RPCs: Admin und `programme_team`, Löschen nur ohne Abhängige (P0001 `in_use`), Editionen erst in eigener Runde.
- **D9/D10 entschieden (Konrad im Partner-Chat, 17.09. abends):** Shop sichtbar bei Standflächen-Paketen **und** Standbühne `I-79895`, nicht beim Hackathon-Stand `I-10729`; Challenge-Frist **18.03.2027** (vier Wochen vor dem Hackathon). **Neu PART-049 (P1):** das Lunch-Paket verlässt den Shop und wird am Checklistenpunkt bestellbar (Vorlage für alle Partner, nicht verpflichtend, Menge + Bestellen legt die `shop_order`-Zeile über denselben Weg an — Phase, PO-Nummer, Bestand bleiben), Karte auf der Partner-Startseite bis zur Frist; zählt in der Produktionsliste. Side-Event hat bereits die SKU `I-81745`; nur Interview Table braucht einen neuen Artikel (Produktstamm). Vokabular `partner_format` (booth, talk, masterclass, company_tour, side_event, interview_table, hackathon, branding, stage) für `product.format_key`, Seed je Kategorie bzw. SKU-genau (Company Tour nur die sechs Spot-Artikel, Talk die sechs Speaking-/Panel-Artikel).

## 2026-09-17 — Aufräumen aus der Feld-Matrix live (20260917183022), Programm-Grundgerüst live (20260917184553), Design-System v2 gemergt (#53)
- **20260917183022 `v6_aufraeumen_feldmatrix` (PR #59, 72ed38a):** Konrads vier Backend-Vorentscheidungen umgesetzt — `organization.description_de/en` (Bestand aus der jüngsten `org_edition` mit Text, sonst `organization.description`), `org_edition.description_de/en` und `organization.description` entfernt; `update_partner_onboarding`, `partner_onboarding_recheck`, `partner_overview`, `event_app_exhibitors`, `ingest_partner_deal` lesen und schreiben an der Organisation (Schlüssel der Übersicht unverändert); `person.photo_url` entfernt (`my_speaker_profile` liefert `photo_asset_id`); `staff_user` und `staff_users_without_admin()` entfernt, `scripts/make-staff.mjs` gelöscht; Trigger `trg_speaker_profile_prefill` friert `person.employer_name` beim Anlegen als `speaker_profile.organization_name` ein (Snapshot). Test `v6_aufraeumen.sql` 8/8. Folge (Fund Admin-Chat): 59 Smoke-Tests ohne die Zeile `delete from staff_user …`, `v5_admin_ueber_rolle.sql` auf 5 Schritte mit Historien-Vermerk.
- **Konvention §1 ergänzt (db43a64):** `create or replace` geht von der Live-Fassung aus (`pg_get_functiondef` oder die letzte Migration, die die Funktion angefasst hat), Abhängigkeiten zwischen noch nicht angewendeten Migrationen stehen im Kopf. Anlass: #56 hätte `upsert_product` um `pass_type`/`grants_role` gekürzt; der Partner-Chat hatte zwei weitere Fälle (`upsert_deliverable_template`, `run_shop_finalization`) vor dem Commit gefangen.
- **20260917184553 `v6_programm_grundgeruest` (PR #54, Admin-Chat, M8):** `upsert_event_day`, `delete_event_day`, `upsert_stage`, `delete_stage`, `upsert_stage_day`, `upsert_track`, `delete_track`, `programme_skeleton`; Recht `is_programme_editor(event)` (Admin oder Programm-Team, global oder je Edition); Löschen nur ohne Slots, Rollenzuweisungen und Sessions (P0001 `in_use` mit Detail `slots:n`/`roles:n`/`sessions:n`); Audit mit Vorherzustand. Probelauf 12/12. Der PR wartet auf Umbenennung, Test ohne `staff_user`, Gate.
- **#53 Design-System v2 gemergt (339a11a):** Konrads Abnahme mit acht Entscheidungen vom 17.09. (7c831de), Gate grün (227 Tests). Rollout je Portal über die Bau-Chats (Plan §3).
- **Reviews offen:** #56 zurück an den Partner-Chat (`partner_overview` und `upsert_product` hinter dem Live-Stand); #57 zwei Korrekturen (Mail-Link absolut über `{{portal_url}}`, Test ohne `staff_user`); #55 ohne `photo_url` in `types.ts`; #60 Mail-Vorlagen im Probelauf; #58/#61 nach #56.

## 2026-09-17 — Abend: vier weitere Migrationen live (Mail-Vorlagen, Session-Grafiken, Formatschlüssel, Challenge-Frist), #52 und #54 gemergt, Gate prüft Konfliktmarker
- **20260917185108 `v6_mail_vorlagen` (PR #60, Admin-Chat, ADM-029):** `mail_template.updated_by`, `mail_templates_admin()` mit der Zahl der wartenden Mails je Vorlage, `upsert_mail_template` (Version zählt hoch, leere Pflichtfelder 22023 `fields_required`, Sprache de/en), `restore_mail_template` (alte Fassung als neue Version), `mail_template_history` aus dem Audit-Log; alles nur Admin. **Entscheidung:** Vorlagen werden beim Versand gerendert, nicht beim Einreihen — ein korrigierter Text erreicht die wartenden Mails; die Zahl der wartenden Mails steht über dem Feld. Probelauf 9/9.
- **20260917185916 `v6_session_grafiken` (PR #57, Admin-Chat, ADM-039, Welle 6 A6):** Rolle `marketing_team`, `is_marketing_team()` (Admin, Marketing, Programm-Team), Tabelle `session_asset` (RLS: Marketing, Session-Editor, eigener Speaker), privater Bucket `session-assets` (25 MB, jpeg/png/webp) mit Pfadregel `<session_id>/<kind>/<datei>` und Schreibrecht nur Marketing, RPCs register/set/delete (Löschen nur Admin, gibt Pfad für die Serverroute zurück), Pflegelisten, `my_session_photos`; Slot-Grafik ersetzt den Vorgänger, Bühnenfotos bleiben alle gültig, erstes Foto ⇒ Mail `stage_photos_ready` je Speaker. Review-Korrektur: Mail-Link über `{{portal_url}}` — `lib/mail/queue.ts` macht eigene Variablen nicht absolut. Probelauf 13/13.
- **20260917190103 `v6_partner_format_key` (PR #56, Partner-Chat, PART-042/PART-002, Welle 6 B1):** Vokabular `partner_format` (9 Schlüssel), `product.format_key` mit Spalten-Grant, Seed nach Konrads Zuordnung vom 17.09., `upsert_product` (von der Live-Fassung aus — die erste Fassung hätte `pass_type`/`grants_role` verloren) und `partner_overview` (von 20260917183022 aus) mit `format_key` je gebuchtem Produkt. Probelauf 17/18: Schritt 02 filtert nach Kategorie `stage_products`, die Standbühne I-79895 liegt aber in `standflaeche` — Testerwartung wird angepasst, der Seed ist richtig.
- **20260917190402 `v6_challenge_frist` (PR #58, Partner-Chat, PART-040, löst PART-032, Welle 6 B3):** Frist `hackathon_challenge` 18.03.2027 23:59 (Erinnerung 336 h vorher), Vorlage auf `deadline_key` statt `weeks_before` (das wertete `deliverable_due` nie aus — die Pflicht stand ein halbes Jahr ohne Frist), Bestand nachgezogen; `upsert_deliverable_template` prüft Regeln (22023 `invalid_due_rule`, erlaubt `deadline_key`/`offset_days`/leer). Probelauf 11/11.
- **Gemergt:** #52 Backlog Admin (ADM-029…038 damit festgelegt; Bühnenfotos/Slot-Grafiken = ADM-039), #54 Programm-Grundgerüst (91ae967, Gate grün). **Vorfall:** `gh pr merge 60` scheiterte an Konflikten mit main (i18n, Test-README nach #54); die Befehlskette der Architektur-Session löschte danach trotzdem den Remote-Branch, GitHub schloss den PR. Wiederhergestellt aus dem gegateten Commit 81bc589 (identisch mit dem lokalen Stand des Admin-Chats), PR wieder offen, nichts verloren. Regel: Branch erst löschen, wenn `state=MERGED` geprüft ist.
- **Gate und Konventionen:** `scripts/gate-pr.sh` bricht bei Konfliktmarkern in verfolgten Dateien ab („dateien: FEHLER (Konfliktmarker)") — der Admin-Chat hatte mit gekürzter Merge-Ausgabe zwei Konflikte übersehen und die Marker gepusht (innerhalb einer Minute selbst bemerkt). §1 präzisiert: Ausgang ist die letzte Migration, die die Funktion **definiert** (`grep -l 'function <name>('`), nicht die letzte, die den Namen erwähnt. §6: Merge-Ausgabe ganz lesen, vor `git add -A` auf Marker prüfen.
- **Review #61 (B2 Messeshop, offen, Rückmeldung an den Partner-Chat):** `order_lunch_package` darf nicht den ganzen offenen Warenkorb bestätigen (nur, wenn außer dem Lunch-Paket nichts drin ist; sonst `confirmed: false`); Lagerprüfung in `shop_upsert_line` über `shop_order_reserved` ist gleichwertig zur Ledger-Unterabfrage (im Kopf als Absicht nennen); Helfer `org_has_booth`/`shop_sku_via_deliverable` nur intern (EXECUTE auch für authenticated entziehen). Fristen-Umbenennung `shop_phase1_end`/`shop_phase2_end` geprüft: live hing nur `shop_phase` an den alten Schlüsseln, keine Bestellung in Phase 3.

## 2026-09-17 — Speaker-Entscheidungen aus dem Speaker-Chat in Welle 6 A7 übernommen (D7, D8, Technik, Reception, Shuttle-Anforderer)
- **D7 — Freelancer als Ansprechpersonen:** Konrad: „Die Regel bitte lockern.“ `edition_contact.contract_consent_at date` als Nachweis der vertraglichen Einwilligung; der CHECK auf `@chef-treff.de` gilt nur noch ohne dieses Datum; Fremdadresse ohne Datum ⇒ 22023 `consent_required`; Feld im Kontakt-Editor der Team-Verwaltung. Baustein ADM-040 beim Admin-Chat (vor dem Mail-Protokoll, weil die Speaker-Startseite #55 daran hängt).
- **D8 — Shuttle-Obergrenze:** fünf Fahrten je Speaker, darüber „Weitere Fahrt beantragen“ mit Freigabe. Anfordern dürfen Speaker/Assistenz (Speaker-Portal), Speaker-Leads für betreute Speaker (Leads-Portal, LEAD-011 — ausnahmsweise ein eigener Bereich dort) und das Team; jede Anforderung aus den Portalen wird einmal vom Speaker-Admin freigegeben. Export CSV und .xlsx (ADM-028).
- **Technik am Slot (A7.2):** Freitext je Feld statt Auswahllisten (Konrad: „Freitext bietet mehr Flexibilität“); Schlüssel und Rollenverteilung (`session.tech` Ansage, `regie_cue` Disposition) bleiben.
- **Speaker Reception (SPK-003, A7.4):** eigenes Objekt mit Datum, Ort, Obergrenze, Beschreibung, Zu-/Absage und Teilnehmerstand (Funktionsumfang einer Luma-Seite); `reception_eligible` bleibt die Einladung; Pflege im Speaker-Admin; Baustein S11 beim Speaker-Chat.
- **Mail „Erneut senden“ (ADM-030, Admin-Chat):** über die Warteschlange (neue `mail_log`-Zeile mit `queued`, `meta.resend_of`), kein zweiter Versandweg; nur Admin, nur für `sent`/`failed`/`delivered`, `suppressed` und `queued` ⇒ P0001 `not_resendable`; gerendert mit der aktuellen Vorlage; einzeln mit Rückfrage; Audit `mail.requeue`.

## 2026-09-17 — Nacht: #56, #57, #58, #60 gemergt; #61 vor dem Anwenden noch zwei Punkte; Gate weist Platzhalter-Zeitstempel ab
- **Gemergt (Gate grün, Migrationen live):** #57 Bilder am Auftritt (02c1c96), #56 Formatschlüssel/Seitenleiste (2437460), #60 Mail-Vorlagen (5c39b21, nach drittem Merge von main), #58 Challenge-Frist (d8ff3d7). #61 hängt jetzt auf main.
- **#61 Probelauf gegen live 20/21:** Schritt 10b scheitert — `mark_overdue_deliverables()` (v3_partner_reminders) kennt `required` nicht und mahnt auch das Lunch-Paket; Konrads Bedingung 3 zu PART-049 braucht eine neue Fassung mit `deliverable_template.required` (Rückmeldung an den Partner-Chat, ebenso Prüfung des Digests). Zweiter Punkt: die Migration lag mit Platzhalter-Zeitstempel außerhalb von `vorschlag/`.
- **Gate:** `scripts/gate-pr.sh` bricht ab, wenn eine Datei `supabase/migrations/2026MMDD2359NN_*.sql` außerhalb von `vorschlag/` getrackt ist (sähe aus wie angewendet); §1 nennt die Regel.
- **ADM-040 (Admin-Chat, vor dem Mail-Protokoll):** Freelancer-Kontakte — Hilfetext „Nur mit ausdrücklicher Einwilligung im Vertrag. Das Datum ist der Nachweis, dass sie vorliegt."; Widerruf = Kontakt entfernen (alle Verweise stehen auf `on delete set null`, `my_contacts()` fällt auf den Standardkontakt zurück), nicht das Datum leeren; Audit `contact.remove` mit Grund.

## 2026-09-17 — 20260917192416 `v6_shop_zwei_phasen` live (PR #61, Partner-Chat, Welle 6 B2, PART-037/038/049)
- Fristen `shop_phase1_end` (19.03.2027) und `shop_phase2_end` (09.04.2027), `shop_phase_2` entfällt, `shop_phase` mit zwei Stufen; **Zugang zum Messeshop nur mit Messestand** (`org_has_booth`: Standfläche oder Standbühne gebucht oder Stand zugewiesen — der Hackathon-Stand öffnet den Shop nicht, D9); Pflichten mit `fulfilled_by_sku` bleiben auch ohne Stand bestellbar.
- **Lunch-Paket (PART-049):** I-79520 im Shop unsichtbar, Vorlage `lunch_package` ist ein Angebot (`required = false`, ohne Kategorie); `order_lunch_package` legt die Zeile über `shop_upsert_line` an und **bestätigt nur, wenn sonst nichts im Warenkorb liegt** (sonst `confirmed = false`, die Oberfläche verweist auf den Warenkorb — Review-Befund: sonst wäre ein fremder Entwurf mit abgeschickt worden).
- **Angebote werden nicht gemahnt:** `mark_overdue_deliverables` setzt nur Pflichten (`required`) auf `overdue` und nimmt eine Mahnung zurück, wenn aus einer Pflicht ein Angebot wurde; `partner_digest_items` nennt Angebote nur **vor** der Frist als Hinweis, danach gar nicht (Befund des Partner-Chats: der Digest formulierte „überfällig seit" nach dem Datum, nicht nach dem Status). Pflichten werden weiter gemahnt (Test 10g).
- Lagerprüfung in `shop_upsert_line` über `shop_order_reserved` (gleichwertig zur Ledger-Unterabfrage, im Kopf als Absicht benannt); Helfer `org_has_booth`, `shop_sku_via_deliverable`, `shop_phase_deadline_key`, `mark_overdue_deliverables`, `partner_digest_items` ohne EXECUTE für `authenticated` (die App ruft keinen davon direkt auf). Probelauf 24/24.

## 2026-09-17 — Zwischenstand spät: Pause bis zum 18.09. morgens (Sitzungslimit, Extra-Nutzung läuft)
- **Für den Morgen offen:** #61 (B2 Messeshop; Datei `20260917192416_v6_shop_zwei_phasen.sql` umbenannt, Basis main, konfliktfrei) → Gate und Merge · #55 (Speaker-Portal; `photo_url` aus `types.ts`) → Gate und Merge, sobald der Speaker-Chat gepusht hat · Admin-Chat: ADM-040 Freelancer-Kontakte im Bau, danach Review ADM-030 Mail-Protokoll (`admin/mail-protokoll`, Migration 0113 unter vorschlag/) und ADM-031 Profil löschen (`admin/profil-loeschen`, Migration 0115) · A1-Tabellenentwurf Formate kommt als Nachricht vom Partner-Chat.
- **Kommentarköpfe:** In `20260917190103_v6_partner_format_key.sql` und `20260917190402_v6_challenge_frist.sql` ist beim Einsetzen des Angewendet-Vermerks die Titelzeile zerrissen (Fund Partner-Chat). Entscheidung: Kommentarköpfe angewendeter Migrationen dürfen berichtigt werden — die Unveränderlichkeit gilt den Anweisungen, nicht dem Kopf. Wird morgen in einem Doku-Commit gerichtet.
- **Backlog:** PART-002/042 (#56), PART-032/040 (#58), PART-037/038/049 (#61) stehen auf `gebaut`; Abnahme durch Konrad im nächsten Walkthrough.
- **Kapazität:** Team-Premium-Limits am 17.09. zweimal erschöpft (5-Stunden-Fenster, Wochenlimit). Analyse mit Empfehlung an Konrad in der Chat-Antwort vom 17.09. spät; Entscheidung steht aus.

## 2026-09-18 — Kapazität: Team-Plan bleibt, Verbrauch strukturell gesenkt (Konrad)
- **Entscheidung Konrad:** Team-Plan mit Extra-Nutzung für Spitzen bleibt; kein Wechsel auf Max 20x, keine API-Werkzeuge (Cursor, LibreChat, TypingMind). Bis zu **fünf Chats gleichzeitig** — Speaker, Partner, Plan/Architektur, Build/Admin, Design laufen einmal komplett durch; Talent & Hackathon sowie Volunteers/Produktion/Check-in starten in der nächsten Phase. Leitsatz: **„Sicherheit schlägt Kosten“** — die Qualität der Architektur-/Security-Reviews hat Vorrang vor Token-Ersparnis (10.000 Teilnehmende, 200 Speaker, 200 Partner; das Portal ist der Backbone des Events).
- **Modell der Architektur-Session:** bleibt Fable 5.1 (von Konrad am 18.09. bestätigt: „Wir bleiben hier bei Fable“; die Einsparung kommt aus dem Prozess, nicht aus der Modellstufe). Bau-Chats weiter auf Opus.
- **Umgesetzt am 18.09.:** `scripts/db.sh` (check, test, dry-run, fn-diff, apply — per psql direkt aus der Datei; die Migration wandert nicht mehr als Text durch den Chat; Eintrag in `supabase_migrations.schema_migrations` wie beim MCP) und `scripts/db-url-set.sh` (Verbindungs-URL ausserhalb des Repos in `~/.config/fls27/db.env`, 600 — nicht in .env.local, das in alle Worktrees kopiert wird, nicht in Vercel; Bau-Chats haben damit weiterhin keinen Datenbankzugang). Entscheidungslog geteilt: Einträge bis 13.09. in `docs/entscheidungen-archiv.md`. AGENTS.md: Nachrichten zwischen Chats je Meilenstein, `docs/schema.md` nur abschnittsweise lesen, grosse Dateien per Skript statt im Chat. Kommentarköpfe von `20260917190103` und `20260917190402` berichtigt (nur Kommentar, Anweisungen unverändert).
- **#61 gemergt (88fd10e):** damit sind alle drei P1-Bausteine des Partner-Chats (B1, B3, B2) live und auf main.
- **Erwartung:** die Fable-Session behält ihre Prüfqualität, verliert aber den grössten Ausgabeblock (Migrationstext zweimal je Migration) und prüft Funktionsänderungen als Diff gegen live statt durch Lesen ganzer Dateien — systematischer und billiger zugleich.

## 2026-09-18 — Reviews #62, #63, #65: Löschroutine ist unvollständig (Befund), Freelancer-Audit ohne Kontaktdaten
- **#63 Mail-Protokoll (0113):** freigegeben ohne Änderung — Filter, LIKE-Escaping, `mail_log_detail` nur Admin, `requeue_mail` mit den fünf Leitplanken (nur `sent`/`failed`/`delivered`, neue Zeile mit `resend_of`, Cron versendet und prüft die Sperrliste). Anwenden folgt über `scripts/db.sh`, sobald die Datenbank-URL hinterlegt ist.
- **#62 Freelancer-Kontakte (0114):** Funktionen gehen von der Live-Fassung aus (md5 identisch). Auflage: beim Widerruf (`consent_withdrawn`) kein `email`/`phone` im `before` des Audit-Eintrags — die Einwilligung galt genau diesen Feldern.
- **#65 Profil löschen (0115), Befund:** `anonymize_person()` übernimmt die Live-Routine `delete_my_profile()` aus Welle 2, und die ist heute unvollständig: an `person` bleiben `diet`/`diet_note` (Art. 9, seit 0100), `gender`, `salutation_de/en`, `self_assessment`; `mail_log` behält `to_email` und `meta.vars`; `speaker_profile` (Bio, Tech-Rider, Foto samt Dateien, Hotel/Shuttle), `volunteer_profile`, Freitexte in `application`/`hack_application`, ggf. `ticket` bleiben unberührt. Auflagen: Tabelle über alle 16 Tabellen mit `person_id` plus Kinder von `speaker_profile` (behalten/anonymisieren/löschen mit Begründung), Umsetzung in `anonymize_person()`, `storage_purge_queue` für Bucket-Dateien (Cron räumt per service_role), generischer Test „keine Textspalte der Person enthält Namen oder Adresse“. Zweiter Punkt: `delete_my_profile()` umgeht die Hürden bei direktem RPC-Aufruf — Hürden dort prüfen oder EXECUTE für authenticated entziehen. Grobe Kategorien dürfen für Statistik bleiben, `consent_record` bleibt als Nachweis.
- **Ablauf ab heute:** Probelauf und Live-Diff je Build-PR fährt die Architektur-Session aus der Datei (`scripts/db.sh dry-run` / `fn-diff`); Bau-Chats prüfen nur gezielt Einzelnes. `.gitignore` fängt Finder-/Drive-Duplikate mit beliebiger Ziffer.

## 2026-09-18 — Löschkonzept: externe Systeme, Reisekosten als Hürde, Ablage-Aufräumen per Cron (aus dem Review von #65)
- **Externe Systeme sind ein eigener Löschvorgang.** vivenu hält am Ticket Name und Adresse, HubSpot und Swapcard je nach Sync ebenfalls. `anonymize_person()` entfernt nur den lokalen Verweis. Regel: Jede erledigte Löschung (`profile_deletion_request.status = done` oder Selbstlöschung) löst einen manuellen Schritt aus — Löschung/Anonymisierung in vivenu, HubSpot und Swapcard durch das Team innerhalb von 30 Tagen; die Admin-Liste der Löschanträge zeigt „extern offen“, bis das erledigt und mit Datum vermerkt ist (Folgebaustein ADM, Konrad legt die Zuständigkeit fest). In die Abschluss-Checkliste aufgenommen.
- **Offener Reisekostenantrag ist eine Löschhürde** (`open_expense`): `expense_claim` trug mit `bank_holder` einen Klarnamen und den Vault-Eintrag der Bankdaten; beim Löschen fällt der Vault-Eintrag, die Buchung bleibt (§ 147 AO). Mit der Hürde muss die Routine nie entscheiden, ob Bankdaten noch gebraucht werden.
- **`org_membership` wird gelöscht** (Ansprechperson kann nur sein, wen es gibt); `consent_record` bleibt als Nachweis; Bucket-Dateien laufen über `storage_purge_queue`, **der Cron-Verbraucher gehört in denselben PR** — eine Warteschlange ohne Abnehmer ist eine Abkürzung.
- **`delete_my_profile()`** verliert EXECUTE für `authenticated`; der einzige Weg von aussen ist `request_profile_deletion()` mit Hürdenprüfung.

## 2026-09-18 — Erster Tag mit `scripts/db.sh`: 20260918104946 Mail-Protokoll und 20260918105038 Freelancer-Kontakte live
- **Datenbankzugang der Architektur-Session steht:** Session-Pooler-URL in `~/.config/fls27/db.env` (Konrad, 18.09.; erster Versuch scheiterte an den mitkopierten Klammern des Platzhalters — das Skript weist das jetzt ab). Alle drei Probeläufe und beide Anwendungen liefen direkt aus den Dateien; durch den Chat wanderten nur noch Ergebnistabellen. `apply` vergibt die Version jetzt streng steigend (zwei Anwendungen in derselben Sekunde kollidierten beim ersten Lauf, die zweite rollte sauber zurück).
- **20260918104946 `v6_mail_protokoll` (PR #63, ADM-030):** `mail_log_admin` (Filter, Suche mit LIKE-Escaping, Gesamtzahl je Zeile, max. 200), `mail_log_stats`, `mail_log_detail` (Variablen nur einzeln, nur Admin), `requeue_mail` (nur `sent`/`failed`/`delivered`, neue Zeile mit `resend_of`, Cron versendet mit aktueller Vorlage und prüft die Sperrliste). Test 14/14.
- **20260918105038 `v6_freelancer_kontakte` (PR #62, ADM-040, D7):** `edition_contact.contract_consent_at`, CHECK `@chef-treff.de oder Einwilligungsdatum`, `upsert_edition_contact` mit 22023 `contact_consent_required`, `delete_edition_contact(p_id, p_reason)` gibt den Bildpfad zurück und protokolliert beim Widerruf ohne E-Mail und Telefon, `edition_contacts_admin` mit Datum. Test 10/10.
- **#65 Profil löschen (0115):** Probelauf rot — `speaker_profile.tech_rider` ist `not null default '{}'`, die Routine setzte null. Korrektur beim Admin-Chat; alle anderen genullten Spalten gegen die NOT-NULL-Constraints geprüft, nur diese eine betroffen.

## 2026-09-18 — 20260918105546 `v6_profil_loeschen` live (PR #65, ADM-031, Art. 17 DSGVO); #63 gemergt
- **Löschung vollständig statt Welle-2-Routine:** `anonymize_person(uuid)` (nicht freigegeben) nullt an `person` zusätzlich `gender`, `diet`, `diet_note`, `salutation_de/en`, `self_assessment`; löscht `org_membership`, `speaker_asset` (Dateien über `storage_purge_queue`, Cron räumt per service_role, gibt nach fünf Fehlversuchen auf) und `speaker_travel`; anonymisiert `mail_log` (`to_email` als Hash, `meta.vars` weg), `application.answers`, `hack_application`, `ticket`-Inhaberfelder, `volunteer_profile`, `speaker_profile` (Bios, Job, Technik als leeres Objekt, Foto), `hospitality_booking`, `expense_claim` (Bankdaten und Vault-Eintrag weg, Buchung bleibt), `session_submission.notes`, `consent_record.user_agent`; `profile_deletion_request.reason` fällt mit. Hürden `team_role`, `speaker`, `volunteer`, `partner`, `open_expense`; ohne Hürde sofortige Selbstlöschung, sonst Antrag mit Team-Mail; `delete_my_profile()` ohne EXECUTE für authenticated. Test 21 Schritte grün, Schritt 11 (Testerwartung) wird nachgezogen; Schritt 17 prüft schema-getrieben, dass Name und Adresse in keiner Zeile mit `person_id` und keinem Kind des Speaker-Profils mehr vorkommen, auch in jsonb-Spalten.
- **Gemergt:** #63 Mail-Protokoll (e1ac368). #62 wartet auf die Auflösung eines Konflikts in `lib/rpc-error.ts` gegen #63, #65 auf Umbenennung und Testkorrektur.
- **Ergebnis des ersten Tages mit `scripts/db.sh`:** drei Migrationen (rund 47 KB SQL, 32 KB Tests) geprüft und angewendet, ohne dass eine davon als Text durch einen Chat lief; ein NOT-NULL-Fehler und eine Testerwartung wurden im Probelauf gefunden, nicht live.

## 2026-09-18 — D1–D6 entschieden (Konrad); D7/D8 nachgetragen
- **D1 Interview Tables:** beides — der Partner legt einmal je Format fest, ob Einzel- oder Gruppengespräch (Kapazität je Slot). **D2 Freigabe:** partner-angelegte Side-Events und Interview-Slots erscheinen erst nach Freigabe durch das Team. **D3 Export:** alle Bewerbungsdaten, mit DSGVO-Hinweis in der Datei, nur mit `consent_share`; Auslegung der Architektur-Session zur Datenminimierung: Bewerbung plus die Profilfelder der Bewerberliste, keine Art.-9-Felder, kein Geburtsdatum, Geschlecht, Telefon, keine internen Notizen; jeder Export im Audit. **D4 Hackathon-Backdrop:** Dateiregeln des Hack 2026 übernommen (Schutzrand 100 mm, PDF/X-4, CMYK ISO Coated v2, ≥ 62 dpi, Schriften eingebettet, Endformat = sichtbarer Rahmen + Beschnitt), Maße 2027 offen — der Link zu den Hack-2026-Unterlagen fehlte in Konrads Nachricht, kommt in den Hackathon-Chat. **D5 Company Tours:** sechs Touren (Finance, Consulting, Marketing, Logistik, Engineering, Sales), Slots später, Dummy-Slots nach dem 2026-Muster (Handelskammer als Sammelpunkt, gestaffelter Start ab 11:15, drei Stopps à 90 Minuten, Ende bis 19:30); keine Personendaten aus dem 2026-Sheet übernehmen. **D6 Award:** Abstimmung vorläufig 01.02.–12.04.2027, Jury-Anteil offen, als pflegbares Fristenpaar.
- **D7/D8** waren am 17.09. im Speaker-Chat entschieden und stehen jetzt auch in Welle 6 §D als erledigt (Freelancer-Einwilligung → 20260918105038; fünf Shuttle-Fahrten).
- Details je Punkt in `docs/arbeitsauftrag-welle-6.md` §D. Konrad startet die Abnahme-Runde in den Portal-Chats.

## 2026-09-18 — D3 bestätigt, Company Tours: Start CCH und Tour Lead, Hack-2026-Wiki verlinkt (Konrad)
- **D3:** Konrad bestätigt die Auslegung — exportiert werden nur die Bewerberdaten, die der Partner ohnehin sieht.
- **Company Tours 2027:** Sammelpunkt CCH, Congressplatz 1, 20355 Hamburg. Je Tour ein **Tour Lead** als interne Begleitperson (oft extern oder Volunteer): `company_tour.lead_contact_id` auf `edition_contact` mit neuem Typ `tour_lead`; Kontaktdaten nach der Freelancer-Regel (`contract_consent_at`); im Partner-Portal unter Company Tour als „Euer Tour Lead“ mit Name, Foto, E-Mail, Telefon. Baustein Partner-Chat (A2), Typ-Erweiterung als Migration.
- **D4:** Unterlagen des AI Hackathon 2026 im Notion-Wiki (Link in Welle 6 §D4); Maße des Backdrops weiterhin offen.
