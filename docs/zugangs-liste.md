# Zugangs- und Token-Liste (ohne Werte) — Stand 08.09.2026

> Konrad erzeugt alle Zugänge selbst und setzt die Werte **nur in Vercel** (Environment Variables, Production und Preview getrennt). Lokal: `sh scripts/env-pull.sh` (Wrapper um `vercel env pull`). **Sensible Variablen** (`SUPABASE_SECRET_KEY`, `SUPABASE_JWT_SECRET`) liefert Vercel dabei nur als Platzhalter; den Secret Key trägt Konrad einmal lokal in `.env.local` ein, das Skript bewahrt ihn bei späteren Pulls. Prüfung ohne Ausgabe des Werts: `node --env-file=.env.local scripts/check-secret.mjs`. Nichts davon gehört in Chat, Drive oder Repo. Rotation und Widerruf werden im Entscheidungslog notiert. Platzhalter stehen in `.env.local.example`.

| Variable | System | Wofür | Woher / minimale Rechte | Welle |
|---|---|---|---|---|
| `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` (alt: `NEXT_PUBLIC_SUPABASE_ANON_KEY`) | Supabase | Client-Zugriff (RLS) | über die **Vercel↔Supabase-Integration** gesetzt (Frankfurt-Projekt seit 09.09.); die App liest beide Namensschemata über `lib/supabase/env.ts` | 0 |
| `SUPABASE_SECRET_KEY` (alt: `SUPABASE_SERVICE_ROLE_KEY`) | Supabase | Server-Aktionen nach Rollenprüfung, Skripte | Integration setzt ihn in Vercel als **sensibel** (nur Production, Stand 09.09.); lokal von Hand eintragen, `vercel env pull` liefert nur Platzhalter; nie im Client | 0 |
| Google OAuth Client-ID/-Secret | Google Cloud → Supabase Auth | Staff-SSO (Domain chef-treff.de) + 2FA | Google Cloud Console, OAuth-Client „Web"; Redirect = Supabase-Callback; **in Supabase Auth Providers eintragen, nicht in Vercel** | 0 |
| `RESEND_API_KEY`, `RESEND_FROM` | Resend | alle System-Mails | Resend → API Keys (Sending only); Domain `chef-treff.de` verifizieren (SPF, DKIM, DMARC) | 0 |
| `CRON_SECRET` | Vercel | schützt `/api/cron/mail` (Bewerbungs-Mails, Fristen, Warteliste) | Vercel → Environment Variables (Production + Preview, sensibel), Wert `openssl rand -hex 32`; Vercel schickt ihn beim Cron als Bearer-Token. Lokal eigener Wert in `.env.local` (bleibt bei `env-pull.sh` erhalten) | 0 |
| `NEXT_PUBLIC_SITE_URL` | App | absolute Portal-URL für Mail-Links und Redirects (kein Host-Header-Fallback in Produktion) | selbst setzen: Production `https://portal.chef-treff.de`, Preview je Deployment; lokal `http://localhost:3000` | 0 |
| `VIVENU_API_KEY`, `VIVENU_WEBHOOK_SECRET`, `VIVENU_SANDBOX` | vivenu | Ticket-Ingest, Personalisierung, Coupons | vivenu Dashboard → Entwickler; **erst Sandbox-Key**, Produktiv-Key nach Rotation des alten (Checkliste) | 1 |
| `HUBSPOT_CLIENT_SECRET` | HubSpot | **optional** — Signatur v3 des Webhooks `/api/webhooks/hubspot`; nur mit einer Projekt-App (HubSpot-CLI). Service-Schlüssel kennen keine Webhooks, der Abgleich alle 15 Min. ist der Hauptweg (Entscheidungslog 11.09.) | Projekt-App → Auth → Client secret; Subscription `deal.propertyChange`/`dealstage`; Route erst mit eigener Domain erreichbar | 3 |
| `SLACK_ONBOARDING_WEBHOOK_URL` | Slack | Gate-Fehler des HubSpot-Ingests nach `#fls27-onboarding` (Entscheidung 10) | Slack → Apps → „Incoming Webhooks“ → Kanal `#fls27-onboarding` → Webhook-URL; postet nur in diesen Kanal, sonst keine Rechte | 3 |
| `MAKE_ONBOARDING_WEBHOOK_URL` | make.com → Slack | Alternative zum Slack-Webhook, make nur als Transport | make-Szenario „Custom Webhook → Slack“; optional Header `x-webhook-secret` = `MAKE_WEBHOOK_SECRET` prüfen | 3 |
| `SEVDESK_CONTACT_PERSON_ID`, `SEVDESK_COST_CENTRE_ID` (optional) | SevDesk | Ansprechpartner (SevUser) und Kostenstelle der Shop-Rechnungsentwürfe; ohne Angabe erster Nutzer bzw. keine Kostenstelle | SevDesk → Einstellungen → Benutzer / Kostenstellen (IDs aus der URL oder `GET /SevUser`, `GET /CostCentre`) | 3 |
| `SWAPCARD_API_KEY`, `SWAPCARD_EVENT_ID` | Swapcard | Aussteller-Sync (A12), später Personen/Sessions | Event Studio → API keys (studio.swapcard.com/api-keys), Nutzer mit Organizer-Rechten auf dem Event; ✅ gesetzt 11.09. (sensibel ⇒ für `scripts/swapcard-probe.mjs` lokal in `.env.local` eintragen wie `SUPABASE_SECRET_KEY`). Der Sync liest die Event-ID aus `event.swapcard_event_id` (`set_edition_swapcard`), die Env-Variable dient nur der Probe. Event FLS27 ist noch ein Duplikat von 2026 — Schreibläufe erst nach Einrichtung | 3 |
| `HUBSPOT_ACCESS_TOKEN` | HubSpot | Deal-Ingest (Onboarding Automation), Phase zurücksetzen, Sweep alle 15 Min. | **Service-Schlüssel** (Settings → Integrationen → Service-Schlüssel; Public Beta seit 02/2026, Legacy Private Apps ab 26.10.2026 nicht mehr neu anlegbar). Scopes: `crm.objects.deals.read/.write`, `crm.objects.companies.read`, `crm.objects.contacts.read`, `crm.objects.line_items.read`, `crm.objects.owners.read`, `crm.schemas.deals.read`, `crm.schemas.companies.read`; Rotation mit 7 Tagen Karenz | 3 |
| `SEVDESK_API_TOKEN` | SevDesk | Rechnungsentwürfe, Kontakte | SevDesk → Einstellungen → Benutzer → API-Token | 2–3 |
| `SANITY_PROJECT_ID`, `SANITY_DATASET`, `SANITY_API_TOKEN` | Sanity | Partner-Logos auf die Website (A11) | sanity.io/manage → API → Tokens. **Bis zur Freigabe durch das Web-Team (Relaunch läuft) Rolle „Viewer“** (lesen, nichts ändern); für den Schreiblauf Rolle „Editor“ — dataset-genaue Rollen gibt es nur im Enterprise-Plan, der Schutz liegt in unseren Regeln: nur eigener Dokumenttyp `portalPartnerLogo`, keine Schema-/Studio-Änderungen, Trockenlauf Standard (Entscheidungslog 11.09.) | 3 |
| `ACTIVECAMPAIGN_API_URL`, `ACTIVECAMPAIGN_API_KEY` | ActiveCampaign | Segmente/Tags push, Opt-in zurück | AC → Settings → Developer | 4–5 |
| `LUMA_API_KEY` / Webhook-Secret | Luma | Community-Events → `registration` | Luma → Settings → API | 4 |
| `MAKE_WEBHOOK_SECRET` | make.com | Shared Secret für Transport-Webhooks (Header) | selbst erzeugen (`openssl rand -hex 32`), in make-Szenario als Header setzen | 3 |
| `ANTHROPIC_API_KEY` | Anthropic | Chatbots (serverseitig) | console.anthropic.com → API Keys, eigener Workspace „Portal" mit Limit | 5 |
| Fehler-Tracking DSN (z. B. `SENTRY_DSN`) | Monitoring | Fehler-/Uptime-Alarm → `alarm@chef-treff.de` | Anbieterentscheidung in Welle 5 | 5 |
| `alarm@chef-treff.de` | Google Workspace | Alarm-Gruppe (Supabase, Vercel, Webhook-Sweeps) | Google Admin → Gruppe anlegen | Betrieb |
| Qonto Rechnungseingang | Qonto | Auslagen-PDFs per Mail | keine API; nur die Eingangsadresse als `QONTO_INBOX_EMAIL` | 2 |

**Regeln:** neue Werte mit `sh scripts/env-set.sh NAME` setzen (unsichtbare Eingabe ⇒ Vercel sensibel + `.env.local` + Worktrees; `--config` für IDs/URLs, `--no-local` ohne lokalen Bedarf) · ein Key pro System und Umgebung · Rechte minimal · Rotation dokumentiert (Datum, Grund) · alte Keys nach Umstellung widerrufen (vivenu, Swapcard, make-Blueprints) · Keys nie in Screenshots oder Support-Tickets.
