# Vorlagen aus dem Vorjahr (Rohmaterial für Welle 4)

| Datei | Quelle | Verwendung |
|---|---|---|
| `regie-2026-main-stage-fr.csv` | Regieplan 2026, Tab „FR – 10.04. Main Stage“ (Google Sheet, Export Konrad 11.09.2026) | Felder von `regie_cue` (A4): Kopf `Stage`, `Tag`, `Anforderungen`, `Lichteinstellung`; je Zeile `Start`, `Dauer`, `End`, `Aktion`, `Slot ID`, `Format`, `Title`, `Moderation` (Personen mit Mikro-Zuordnung, z. B. „Henni (Headset 1)“), `Regie` (Slides/Video/Fonts), `Backstage` (Verkabeln, Wasser, Slides), `Mobiliar` (Rednerpult, Lounge), `Sonstige Notizen, Watchouts`. Die Datei enthält nur diesen einen Tab; weitere Bühnen/Tage folgen bei Bedarf als eigene CSV. Personennamen sind Vorjahresdaten — nur als Strukturvorlage nutzen, nicht importieren. |

Volunteer-Bereiche 2026: Export der Airtable-Ansicht (Base `appRrXacJe9PQ748O`, Tabelle `tblOjtNrvZWYFLf8e`, Feld `fld4Kxm97nOcIEoC0` = Überkategorie; darunter Positionen je Tag und Anzahl) als `volunteer-bereiche-2026.csv` hier ablegen — daraus entsteht das Vokabular `volunteer_area` und die Positionsliste für `shift`.
