# Arbeitsauftrag Welle 2 · Speaker + Speaker-Leads (Start nach Merge B6, Ziel 25.09.2026)

> Voraussetzung: Welle 1 Teil A live (Programm-Backend, Bewerbungen, Mails/Cron, Admin-Lesewege), Teil B bis B6 gemergt. Grundlage: Masterplan §1 (Speaker-Portal `/speaker`, Speaker-Lead-Portal `/speaker-leads`), Datenmodell „Speaker" (Masterplan §3), Ergänzungen v0.1c (Board-Scope) und v0.1d (Assistenz), Entscheidungslog E (Speaker) und Antworten 22–24, 58, 67, 71; Legacy-Inventar §13.2/§13.4 (alter Speaker Hub); Feedback FLS26 R4, R5, R8, R9, R10, R11 (M/S) — R1, R2, R3, R12 (C) sind **nicht** Teil dieser Welle.
> Ziel: Ein Speaker-Lead legt im eigenen Scope einen Speaker an und lädt ihn ein; der Speaker (oder seine Assistenz) loggt sich ein, vervollständigt Profil und Consent, reicht Session-Inhalte ein, lädt die Präsentation hoch, bucht statusgesteuert Hotel/Shuttle, fordert ein Begleitticket an und reicht Reisekosten ein. Das Team gibt Inhalte frei, prüft Technik und Reisekosten. Alles bilingual, Speaker-Oberfläche **Englisch zuerst** (Antwort 67).

## Was schon da ist (wiederverwenden, nicht neu bauen)
- Rollen `speaker`, `speaker_assistant`, `speaker_manager` (Scopes `stage`, `stage_day`, `slot`, `edition`) in `role_assignment`; `has_role()`, `can_edit_slot()`, `can_edit_session()`, `is_programme_reader()`; Portal-Vokabular `speaker`, `speaker_leads`.
- `person` mit `title` (Dr.), `pronouns`, `photo_url`, `linkedin_url`, `preferred_language`; `session_speaker` (Rolle, `confirmed`, Reihenfolge); `set_session_speakers()`; `search_people()`; `moderation_person_id` auf `session`.
- Consent-Typen `photo_video`, `speaker_release`, `slides_publication`, `hospitality_data` (+ `terms`, `privacy`) mit Versionierung in `consent_record`.
- Programm-Board als Komponente (Welle 1) mit `can_edit` je Zeile; Mail-Warteschlange (`queue_mail`, Cron); `ticket` mit `pass_type` (u. a. `speaker`); Audit über `log_audit()`.
- Speicher: noch **kein** Storage-Bucket; `supabase_vault` und `pgcrypto` sind installiert (Bankdaten).

## A · Backend (Architektur-Session)
| # | Thema | Inhalt | Akzeptanz |
|---|---|---|---|
| A1 ✅ (10.09., live 0025/0026) | Speaker-Datenmodell | Tabelle `speaker_profile` (edition-bezogen, `unique (person_id, edition_id)`): `speaker_type` (neues Vokabular `speaker_type`: keynote, panelist, moderator, masterclass_host, jury, other — Company Tours haben keinen Host), `pipeline_status` (Vokabular `speaker_pipeline`: lead → contacted → confirmed → onboarded → ready → published → attended, dazu declined), `owner_person_id` (verantwortlicher Lead), `job_title`, `organization_name` + optional `org_id`, `bio_short_en/de`, `bio_long_en/de`, `socials jsonb`, `photo_asset_id`, `reception_eligible` (Haken „Reception-berechtigt", Antwort 93), `lounge_access`, `pass_type` (Regel R5: `masterclass_host` ⇒ `professional`, sonst `speaker`), `hospitality_status` (Vokabular: none, eligible, requested, booked, declined), `travel_costs_covered` (setzt der Lead beim Eintragen) + `travel_costs_approved_by/at` (finale Freigabe durch den Program Lead, Rolle `area_lead_speaker`), `hotel_tier` (standard/premium/vip, Team), `tech_rider jsonb` (Mikro, eigener Laptop, Video, Hinweise), `assistant_person_id`, `internal_notes`, `created_by`. RLS: Speaker/Assistenz lesen die eigene Zeile, Schreiben **nur per RPC** mit Feld-Whitelist (`update_my_speaker_profile(jsonb)`: keine Staff-Felder, Assistenz zusätzlich ohne Consent-Bezug); Manager im Scope (`can_manage_speaker(profile_id)`: Speaker auf Session in einem Slot des Scopes, oder `owner_person_id` = Manager, oder von ihm angelegt) lesen und schreiben Whitelist inkl. `pipeline_status`, `reception_eligible`, `tech_rider`; Team alles. Kontaktdaten (E-Mail, Telefon) nur für Manager im Scope und Team (Masterplan-Regel). | Speaker sieht nur sich; Manager außerhalb des Scopes bekommt 42501; jede Manager-Änderung im Audit (`speaker.update`) |
| A2 ✅ (10.09., live 0025) | Speaker anlegen + einladen | RPC `upsert_speaker(jsonb)` für Manager im Scope/Team: legt bei Bedarf `person` + primäre E-Mail an (Dublettenprüfung über E-Mail), `speaker_profile`, Rolle `speaker` (Scope `edition`), Mail `speaker_invite` (EN/DE) mit Login-Hinweis; bestehende Person wird verknüpft, kein zweiter Datensatz. `claim_or_create_person()` verbindet den Login später über die verifizierte E-Mail (bereits vorhanden). RPC `invite_assistant(profile_id, email, first_name, last_name)` für Speaker **oder** Lead (Antwort 58): Person, Rolle `speaker_assistant` (Scope `edition`), `assistant_person_id`, Mail `assistant_invite`; `remove_assistant`. | Einladung erzeugt genau eine Mail und einen Audit-Eintrag; Assistenz kann alles außer Consent und Bankdaten (Test) |
| A3 ✅ (10.09., live 0028) | Session-Inhalte: eingereicht vs. final | Tabelle `session_submission` (session_id, speaker_profile_id, title, description, topics[], language, submitted_at, status submitted/approved/superseded, reviewed_by). RPC `submit_session_content(session_id, jsonb)` (Speaker/Assistenz der Session via `session_speaker`), `approve_session_content(submission_id, jsonb overrides)` (Team oder Manager im Scope) übernimmt die finale Fassung nach `session.title_*`/`description_*`/`language` und setzt `superseded` für ältere Einreichungen. View `my_sessions` für Speaker: eigene Sessions mit Slot-Zeit, Bühne, `publish_status`, letzte Einreichung + finaler Stand. Realtime-Trigger auf `session` bleibt (Board). | Speaker sieht „eingereicht" und „final" nebeneinander; Freigabe durch Unbefugte 42501 |
| A4 ✅ (10.09., live 0028) | Uploads, Deadlines, Technik-Check | Storage-Bucket `speaker-assets` (privat) mit Pfad `<edition_id>/<profile_id>/<kind>/<asset_id>-<datei>`; Tabelle `speaker_asset` (profile_id, session_id?, kind: presentation/photo/other, storage_path, filename, mime, size, version, uploaded_by, `tech_check_status` pending/checked/issue + Notiz, `slides_release` bool = Slid@Home-Freigabe nur mit Consent `slides_publication`, `is_current`, **`late` bool**). Storage-Policies: Eigentümer/Assistenz schreiben unter eigenem Pfad, Manager im Scope und Team lesen, Produktion/Programm setzen Technik-Check; PDF/PPTX/KEY ≤ 100 MB, Video nicht (Hinweis). Minimale Tabelle `deadline` (edition_id, key, audience, due_at, label_de/en, description) — `presentation_upload` = 48 h vor Slot, spätestens 14.04.2027 12:00. **Nach der Deadline werden Präsentationen weiter angenommen** (Entscheidung Konrad 10.09.), der Upload wird als `late` markiert und dem Team gemeldet; Erinnerung vorher (A8). Foto-Upload für `person.photo_url` über denselben Bucket (Bild-Pipeline R6 = S, nur wenn Zeit). | Upload nach Deadline wird gespeichert und trägt `late = true`; neue Version ersetzt `is_current`, alte bleibt; Regie sieht „geprüft" |
| A5 ✅ (10.09., live 0030) | Hospitality | Tabellen `hospitality_quota` (edition_id, kind hotel/shuttle, **tier standard/premium/vip**, label_de/en, capacity, window_from/to, notes, active) und `hospitality_booking` (profile_id, quota_id, status requested/confirmed/waitlisted/cancelled, details jsonb: Anreise/Abreise, Zimmer, Abholort/-zeit, Personen). Gate: `speaker_profile.hospitality_status = 'eligible'` **und** Consent `hospitality_data` aktiv; Hotels nur im eigenen `hotel_tier`. Seed 2027: **Radisson Blu Hamburg Dammtor** (standard), **Grand Elysée** (premium), **The Fontenay** (vip) mit Platzhalter-Kapazität; Kontingente kommen über Laura bis 01.11. (im Admin pflegbar). RPCs `hospitality_options()`, `book_hospitality(quota_id, details)`, `cancel_hospitality(booking_id)`, `confirm_hospitality(booking_id)` (Team), Zähler belegt/frei je Quota. Mail `hospitality_confirmed`. | Buchung ohne Status/Consent ⇒ P0001 `not_eligible`; falscher Tier ⇒ 42501; Überbuchung ⇒ `waitlisted` statt Fehler |
| A6 ✅ (10.09., live 0031–0033) | Reisekosten | Tabelle `expense_claim` (profile_id, status draft/submitted/approved/rejected/paid, positions jsonb (Belegzeilen mit Betrag/Datum/Kategorie), `amount_cents`, currency, receipt_asset_ids[], `bank_secret_id` → Bankdaten **nur im Vault** (`vault.create_secret`, kein Klartext in Tabellen/Logs), invoice_asset_id (Auslagenrechnung PDF), reviewed_by, sevdesk_ref, paid_at). Voraussetzung: `travel_costs_covered` gesetzt **und** final freigegeben (`travel_costs_approved_at`). Nur der Speaker selbst (nicht Assistenz) legt Bankdaten an; lesen dürfen nur Admin und `area_lead_speaker` über RPC `expense_bank_details(claim_id)` mit Audit. Route Handler erzeugt die Auslagenrechnung als PDF (Absender Speaker, Empfänger ChefTreff, Positionen, IBAN maskiert) → Speaker „gibt frei" (`submit_expense`) → **Freigabe durch den Program Lead (Paulina, Rolle `area_lead_speaker`) oder Admin** (`approve_expense`) → **A6b:** SevDesk-Beleg direkt per API (`SEVDESK_API_TOKEN` liegt vor; Feldzuordnung aus dem make.com-Szenario des Team-Portals abgucken, make nur als Referenz) + Mail mit PDF an `QONTO_INBOX_EMAIL` (Vercel-Env) → Konrad gibt Zahlung frei → `mark_expense_paid`. Mails `expense_submitted` (an Paulina), `expense_approved`/`expense_rejected`. | Bankdaten erscheinen nie in `mail_log`, `audit_log` oder Antworten außer über die eine RPC; Assistenz bekommt 42501 beim Bankdaten-Schritt; ohne finale Reisekosten-Freigabe kein Antrag |
| A7 | Speaker-Ticket + Begleitticket | Regel: `pipeline_status = confirmed` ⇒ Anspruch auf Speaker-Ticket (`pass_type` aus A1, `lounge_access` als Flag). RPC `request_companion_ticket(profile_id, email, first_name, last_name)` (ein kostenloses Begleitticket, gleicher Pass-Typ, ohne Lounge) ⇒ `ticket`-Zeile Status `requested`; Team bestätigt (`confirm_companion_ticket`) ⇒ **Ausstellung direkt über die vivenu-API** (Freiticket; Vorlage = make.com-Prozess vom Vorjahr, einmal prüfen und übernehmen; Anbindung mit Welle 1 A1/A2) + Mail `companion_ticket_confirmed`. | Zweite Anfrage ⇒ 23505; ohne `confirmed` ⇒ P0001 `not_eligible` |
| A8 | Mails + Erinnerung | Vorlagen EN/DE: `speaker_invite`, `assistant_invite`, `hospitality_confirmed`, `companion_ticket_confirmed`, `expense_submitted`, `expense_approved`, `expense_rejected`, `presentation_reminder` (48 h vor Slot, einmal, optional ICS-Anhang = S). Auslösung über Trigger/RPCs in die bestehende Warteschlange; Erinnerung im Housekeeping des Cron. | Jede Vorlage genau einmal je Ereignis; Reminder nur für Sessions mit Slot und ohne aktuelle Präsentation |
| A9 | Scope-Helfer für das Lead-Portal | RPC `my_manager_scope()` (Bühnen, Bühne×Tag, Slots, Edition des Aufrufers) für Filter und Board-Vorauswahl; `manager_speakers(edition_id?)` (Liste im Scope mit Pipeline, Session, nächster Schritt); `speaker_next_steps(profile_id)` (fehlende Pflichtangaben: Profil, Consent, Inhalt, Präsentation, Ticket) für Onboarding-Karten und Lead-Übersicht. | Manager sieht nur Speaker seines Scopes; Team alle |
| A10 | Tests + Doku | `supabase/tests/v2_speaker.sql` (Scope-Rechte, Assistenz-Grenzen, Deadline, Hospitality-Gate, Vault-Bankdaten, Begleitticket), `datenmodell-v2.md`, `schema.md`, Mail-Plan, Zugangs-Liste (`SEVDESK_API_TOKEN`, `QONTO_INBOX_EMAIL`) | Alle Tests grün, Advisor ohne ERROR |

## B · Frontend (Build-Session)
| # | Thema | Inhalt | Akzeptanz |
|---|---|---|---|
| B1 ✅ (PR #6, 10.09.) | Speaker-Portal-Shell `/speaker` | Bereich für Rollen `speaker`/`speaker_assistant` (Edition), `requireArea("speaker")`; **Englisch als Standardsprache**, DE umschaltbar; Welcome mit Event-Infos aus `event`/`event_day` (Einlass/Programmzeiten), Support **nur über Rollen-Postfach** (`speaker@chef-treff.de`) und Portal-Hinweis, keine privaten Kontaktdaten (Antwort 71); Navigationskarten **statusgesteuert** aus `speaker_next_steps()` (R9): nur sichtbar, was gerade dran ist | Assistenz sieht dasselbe Portal mit Hinweis „im Namen von …"; kein privater Kontakt im Markup |
| B2 ✅ (PR #6, 10.09.) | Onboarding + Profil `/speaker/profil` | Formular: Anrede/Titel (Dr., R8), Vorname, Nachname, Pronomen, Job-Titel, Organisation, Sprache, LinkedIn/Socials, Kurz-/Langbio (EN Pflicht, DE optional), Foto-Upload (Bucket) mit Rechte-Consent (`photo_video`, `speaker_release`); Tech-Rider (Mikro, Laptop, Video); Consent-Schritt wie Onboarding Talent (nur Änderungen schreiben); **Assistenz einladen** (`invite_assistant`) und entfernen; Assistenz sieht Consent-Block nur lesend | Nach ≤ 5 Minuten „ready"-fähig; kein Feld ohne Vokabular-Label; Assistenz-Grenzen sichtbar |
| B3 ✅ (PR #7, 10.09.) | Deine Session `/speaker/session` | Aus `my_sessions`: Zeit + Bühne (oder „Slot folgt"), Einreichung (Titel, Beschreibung, Themen, Sprache) mit Anzeige **eingereicht vs. final**, Präsentations-Upload (PDF/PPTX/KEY, Versionen, Vorschau-Link, Deadline-Countdown aus `deadline`, gesperrt nach Frist), Slid@Home-Haken (nur mit Consent), Technik-Check-Status lesend; FAQ-Kacheln aus statischem Text (Wissensbasis folgt Welle 4) | Upload nach Frist zeigt Sperre + Kontakt; zweite Version ersetzt sichtbar die erste |
| B4 | Travel & Hospitality `/speaker/travel` | Anreise-FAQ (Auto/ÖPNV, CCH), Hotel- und Shuttle-Buchung **nur bei `hospitality_status = eligible`** (sonst Hinweis „wird vom Team freigeschaltet"), Consent `hospitality_data` vor der ersten Buchung, Optionen aus `hospitality_options()` mit freien Plätzen, eigene Buchungen mit Storno (R10: klare Slots, Kapazität sichtbar) | Kein Buchungs-UI ohne Freischaltung; Warteliste wird als solche angezeigt |
| B5 | Tickets `/speaker/tickets` | Eigenes Speaker-Ticket (QR groß, Pass-Typ, Lounge-Hinweis) sobald vorhanden, sonst Status „nach Bestätigung"; Begleitticket-Anfrage (E-Mail, Name) mit Status; Speaker Counter/Backstage-Infos aus `event` | Anfrage in ≤ 1 Formular; Status aus `ticket.status` |
| B6 | Reisekosten `/speaker/reisekosten` | Assistent (Wizard): Belege hochladen (Bucket), Positionen erfassen, Bankdaten (nur Speaker; Eingabe maskiert, nie vorausgefüllt aus dem Client), PDF-Vorschau der Auslagenrechnung, „Freigeben" = `submit_expense`, Statusverlauf; Assistenz sieht den Antrag ohne Bankdaten-Schritt | Bankdaten tauchen in keinem Netzwerk-Response außer dem PDF-Download auf; Abbruch behält Entwurf |
| B7 | Speaker-Lead-Portal `/speaker-leads` | Bereich für `speaker_manager` (+ Team), `requireArea("speaker-leads")`: **Pipeline** (Liste/Spalten nach `pipeline_status`, Filter aus `my_manager_scope()`), Speaker anlegen/einladen (`upsert_speaker`, Personensuche `search_people`), Detail mit Feld-Whitelist (Kontaktdaten sichtbar nur im Scope), Reception-Haken, Session zuordnen (`set_session_speakers` im Scope), Einreichungen freigeben (`approve_session_content`) mit Diff, **Programm-Board** als Scope-Variante (bestehende Komponente, andere Bühnen read-only), Slot-Infos je Speaker, Hospitality-Status setzen (falls Team-Recht), nächste Schritte je Speaker | Manager außerhalb des Scopes sieht 404; jede Änderung im Audit; Board zeigt eigene Bühne editierbar, fremde grau |
| B8 | Admin-Ergänzungen (S) | Kleine Pflegeseiten: Hospitality-Kontingente, Deadlines je Edition, Reisekosten-Freigabe-Queue (Admin), Technik-Check-Queue (Produktion/Programm) mit „geprüft/Problem" | CRUD über RPCs/RLS, kein service_role im Frontend |

## C · Regeln (gelten für A und B)
- **Sprache:** Speaker-Bereich EN zuerst, DE vollständig vorhanden; Lead-Portal DE zuerst. Mails an Speaker in `preferred_language`.
- **Rechte:** Speaker/Assistenz nur eigenes Profil; Assistenz **ohne** Consent und Bankdaten (Antwort 58). Manager: nur Scope (Bühne×Tag oder Slot, Edition), Feld-Whitelist, Kontaktdaten nur im Scope; Team alles; jede Fremdänderung im Audit. Keine öffentliche Speaker-Bewerbung (Antwort 23).
- **Datenschutz:** Bankdaten ausschließlich im Vault, Zugriff über eine RPC mit Audit; Belege und Präsentationen im privaten Bucket mit Pfad-Policies; Foto nur mit Rechte-Consent; Löschen des Profils (bestehend) räumt Speaker-Daten mit (Suppression bleibt). Keine privaten Kontaktdaten von Team/Freelancern im Portal.
- **Status statt Sichtbarkeit:** Hospitality, Ticket, Reisekosten und Uploads sind über Statusfelder freigeschaltet, die das Team setzt — nicht über versteckte Links.
- **Nichts doppelt pflegen:** Slot/Bühne/Zeit kommen aus `slot`/`stage`; finale Session-Inhalte stehen in `session`, Einreichungen in `session_submission`.
- Ein PR je Baustein (B1+B2, B3, B4+B5, B6, B7, B8), ein Review-Durchgang, Nachbesserungen als Diff.

## D · Definition of Done Welle 2
- Ende-zu-Ende gegen Frankfurt mit Wegwerf-Konten (Resend-Testadressen): Lead legt Speaker an → Einladung → Login → Profil + Consent + Assistenz → Einreichung → Freigabe durch Lead → Veröffentlichung im Board → Präsentations-Upload (vor/nach Deadline) → Hospitality nach Freischaltung → Begleitticket → Reisekosten bis PDF (A6b nur mit Tokens). Protokoll `docs/tests/welle-2-e2e.md`.
- Build/Lint/Test grün, Advisor ohne ERROR, `supabase/tests/v2_speaker.sql` grün, Doku aktualisiert und gespiegelt.
- Feedback-Runde mit Konrad (Struktur und Abläufe) zu Speaker-Portal und Lead-Portal.

## E · Entscheidungen Konrad (10.09.2026)
1. **Speaker Reception:** Name bleibt vorerst; der Session-Titel wird später angepasst (ist ein normales Feld). Technisch nur der Haken `reception_eligible`.
2. **Präsentations-Deadline:** 48 h vor dem Slot, spätestens 14.04.2027 12:00 — **nach der Deadline werden Präsentationen weiter angenommen** und als `late` markiert (keine Sperre).
3. **Reisekosten:** Erstattungsfähigkeit wird beim Eintragen als Status gesetzt (`travel_costs_covered`) und **einmal final durch den Program Lead (Paulina) freigegeben**; Freigabe der Anträge ebenfalls durch Paulina (Rolle `area_lead_speaker`) oder Admin. Keine Obergrenze im System.
4. **SevDesk/Qonto:** Token liegt vor; SevDesk läuft heute über make.com (Szenario Team-Portal als Vorlage für die Feldzuordnung, Umsetzung direkt per API); Qonto-Eingangsadresse setzt Konrad als `QONTO_INBOX_EMAIL` in Vercel (Wert nicht im Repo).
5. **`speaker_type`:** keynote, panelist, moderator, masterclass_host, jury, other — Company Tours haben keinen Host. Pass-Regel: Masterclass ⇒ Professional ohne Lounge, alle anderen ⇒ Speaker mit Lounge.
6. **Begleitticket:** eins je Speaker, gleicher Pass-Typ, ohne Lounge; **Ausstellung direkt über die vivenu-API** (make.com-Prozess vom Vorjahr als Vorlage, prüfen und übernehmen).
7. **Hotels 2027:** Radisson Blu Hamburg Dammtor (Standard), Grand Elysée (Premium), The Fontenay (VIP) als Kontingente; Zahlen von Laura bis 01.11.
8. **Foto-Pipeline:** nur bei Restzeit, sonst Welle 3.

## Status
- 10.09.2026: Entwurf; Konrads Entscheidungen (Abschnitt E) eingearbeitet, **freigegeben**. **A1–A4 live** (Migrationen 0025–0029, Tests `v2_speaker.sql`, `v2_speaker_content.sql`). **B1+B2 gemergt** (PR #6), **B3 gemergt** (PR #7). A5 live (0030), **A6 live** (0031–0033, Test `v2_expenses.sql`). Build-Session baut B4 (Travel & Hospitality). Sprachregel: Fallback je Bereich (Speaker-Bereich EN, wenn keine Sprache gewählt), keine neue Sprachlogik.
