# Arbeitsauftrag Welle 0 · Fundament (08.–14.09.2026)

> Ausführbar von jeder Claude-Session (Architektur-Session = Fable, Build-Session = Opus). Grundlage: Masterplan v0.1d §2–§4, §7, Ergänzungen v0.1a–d; Entscheidungslog bis 08.09. spät. Ziel: Nach Welle 0 kann jede Welle auf Schema, Rollen, UI-Kit, Mail und Doku-Gerüst aufsetzen, ohne Fundament nachzuziehen.

## A · Schema v2 (Architektur-Session)
Migrationen unter `supabase/migrations/2026090818000N_v2_*.sql`, idempotent formuliert, per Supabase-MCP angewendet und in der Migrationshistorie geführt.

| # | Datei | Inhalt | Akzeptanz |
|---|---|---|---|
| 1 ✅ | `v2_identity_roles` (live 20260908141744) | `person` + `title`, `city`, `tier` (lead/talent), `pronouns`, `photo_url`, `deleted_at` · `organization` + `type`, `slug`, `website`, `active`, `sevdesk_contact_id` · `role_assignment` (role, scope_type global/edition/portal/org/stage/stage_day/slot, scope_id, edition_id, portal, valid_from/to, granted_by) · `consent_record` · `suppression` (E-Mail-Hash) · `audit_log` · Helper `has_role()`, `is_admin()`, `is_staff()` (rollenbasiert, `staff_user` bleibt kompatibel), `my_roles()` · RPC `delete_my_profile()` (anonymisieren + Suppression) | Talent sieht nur eigene Rollen/Consents; Rollen schreiben nur service_role; `has_role('speaker_manager','stage_day',<id>)` liefert im Scope true, außerhalb false (SQL-Test in `supabase/tests/`) |
| 2 ✅ | `v2_edition_programme` (live 20260908142441) | `event` + `slug`, `edition_id`, `is_edition`, `timezone`, `venue`, `status` · `event_day` · `stage` (type, room, capacity, partner_org_id, stage_lead_person_id, changeover_min, default_duration_min, sort_order) · `stage_day` (open_from/to, slot_quota) · `slot` (start_at/end_at, slot_type content/fixed_block/placeholder/partner_block/frame, status, sort_order, source_ref, internal_title/notes; **Exclusion-Constraint gegen Überlappung je Bühne**, btree_gist) · `session` (Titel/Beschreibung DE+EN, language, format, access_mode, eligibility_rule, capacity, ticket_required, application_deadline, publish_status, host_org, moderation, swapcard_id) · `session_speaker` · `track` · `slot_history` · RPC `move_slot()` (Scope-Prüfung, Warnungen als JSON, Bestätigung nach Veröffentlichung, History) · Views `programme_public`, `stage_day_slot_stats` | Zwei überlappende Slots auf einer Bühne werden abgelehnt; `move_slot` außerhalb des Scopes wird abgelehnt; `programme_public` zeigt nur `published`; `internal_*` sind für `authenticated` nicht lesbar (Spalten-Grant) |
| 3 | `v2_application_ticket` | `question_catalog`, `session_question` (max. 2 eigene, approved_by; Trigger), `application` (Status-Maschine applied → shortlisted → accepted → confirmed → attended/no_show, waitlisted → promoted, declined/expired/withdrawn; rank, answers, consent_share, confirm_by), `decision_release`, `registration.session_id`, `ticket_type_map`, `ticket` (barcode = QR, vivenu ids, holder, personalization_status, addons), `org_ticket_allocation`, `checkin` · RPCs `apply_to_session()`, `withdraw_application()`, `confirm_application()` | Bewerbung nach Frist wird abgelehnt; Doppelbewerbung unmöglich; Talent sieht nur eigene Bewerbungen/Tickets; Partner-Kontakte sehen nur eigene Kontingente |
| 4 | `v2_integration_comms` | Schema `integration`: `webhook_event` (Quelle, Event-ID, Payload, Status; unique Quelle+ID), `sync_job`, `sync_error` · `public.external_ref` · `mail_template` (key, locale, subject, body_md, version), `mail_log` (status queued/sent/delivered/bounced/failed/suppressed, provider_id) | Schema `integration` nicht über Data-API exponiert; doppelte Webhooks werden ignoriert (unique) |
| 5 | `v2_seed_vocab` | Vokabulare: `role`, `scope_type`, `contact_role` (+ shop), `organization_type`, `slot_status` (offen · angefragt · bestätigt_titel_offen · final · nicht_bespielt), `slot_type`, `session_format`, `access_mode`, `application_status`, `pass_type`, `consent_type`, `language`, `stage_type`, `publish_status` · Referenz-Edition **FLS27** (Woche → Summit 27 16./17.04., Hackathon 27 15./16.04.) mit Tagen und den fünf Bühnen 2027 (ohne ZEIT) als Startdaten | `select count(*) from vocab_term` steigt entsprechend; Edition per Slug `fls27` abrufbar |

Regeln: jede Tabelle `enable row level security` + Grants explizit; keine Policy für Staff-Schreibzugriffe (Server nutzt service_role nach `requireRole()`); alle Funktionen `security definer set search_path = public, extensions`; `updated_at`-Trigger überall; Kommentare (`comment on`) an jeder Tabelle für die generierte Schema-Doku.

## B · App-Fundament (Build-Session)
| # | Thema | Inhalt | Akzeptanz |
|---|---|---|---|
| 6 | `proxy.ts` | ersetzt `middleware.ts` (Next 16); Session-Refresh; Login-Pflicht für alle Bereiche außer `/`, `/login`, `/auth/*`, `/tickets/bestaetigung`; optimistischer Rollen-Check nur als Redirect, echte Prüfung serverseitig | Nicht eingeloggt → `/login?next=…`; `/admin` ohne Rolle → `/` |
| 7 | Rollen-Gate | `lib/auth.ts`: `requireRole(role, scope?)`, `requireStaff()` auf `has_role/is_staff`; `getMyAreas()` für den Bereichs-Umschalter; Bereiche als Route-Gruppen `(talent) (speaker) (speaker-leads) (partner) (volunteers) (hackathon) (produktion) (admin)`; Layout zeigt nur Bereiche mit Rolle | Person mit zwei Rollen sieht zwei Bereiche; Direktaufruf ohne Rolle → 404/Redirect |
| 8 | UI-Kit | `app/globals.css` mit Tokens aus dem Design-Briefing (Navy `#081A35`, Events-Akzent `#6D6DEF`/`#5B5BD9`/`#E8E8FC`, Border, Muted, Success/Warning/Error, 8-pt-Raster), **kein Dark Mode**; Fonts via `next/font/local` aus `public/fonts/*.woff2` (Sharp Sans SemiBold = Text, ExtraBold = Headline, Laica Italic = Akzent); Komponenten `Button`, `Input`, `Select`, `Field`, `Card`, `Badge`, `Table`, `Drawer`, `Toast`, `EmptyState`, `PageHeader`, `Stepper` in `components/ui/` | Storybook-freie Demo-Seite `/admin/ui` zeigt alle Komponenten; Lighthouse Kontrast ≥ AA |
| 9 | i18n | `lib/i18n/`: Dictionaries `de.json`/`en.json`, `getDictionary(locale)`, Locale aus `person.preferred_language` → Cookie → `Accept-Language`; Umschalter im Header; alle bestehenden Seiten (Login, Profil, Admin) auf Dictionary umgestellt; Vokabular-Labels über `label_de/label_en` | Kein hartkodierter UI-Text in `app/`; Umschalten wechselt alle Texte ohne Reload-Fehler |
| 10 | Mail | `lib/mail/`: Resend-Client, `sendTemplate(key, locale, to, vars)`, Schreiben in `mail_log`, Suppression-Check, Dev-Fallback ohne Key (loggt statt sendet); Templates `login_magic_link` (Supabase-Hook optional), `welcome`, `test` | Testmail landet in `mail_log` mit `provider_id`; unterdrückte Adresse wird nicht gesendet |
| 11 | Doku-Gerüst | `docs/runbooks/` (deploy, rollback, restore, key-rotation, incident als Skelette), `scripts/gen-schema-doc.mjs` → `docs/schema.md` aus `information_schema` + `comment on`; `README.md` Neuaufsetzen in 10 Schritten | `node scripts/gen-schema-doc.mjs` erzeugt aktuelle Schema-Doku; Reproduktionsschritte vollständig |

## C · Definition of Done Welle 0
- `npm run build` und `npm run lint` fehlerfrei; Vercel-Preview grün.
- Supabase-Advisor (Security + Performance) ohne offene Warnungen der Kategorie „error".
- SQL-Tests für RLS/Scopes laufen (`supabase/tests/*.sql`, per `execute_sql`).
- Doku aktualisiert (Masterplan-Referenzen, `docs/schema.md`, Entscheidungslog), Drive gespiegelt.
- Konrad-Feedback zum UI-Kit und Bereichs-Umschalter eingeholt (80 %-Prinzip).

## Status 08.09.2026 (abends)
- ✅ A1 Identität & Rollen, ✅ A2 Edition & Programm (inkl. Board-RPCs), ✅ Zusatz A2b Sicherheits-Härtung (`v2_security_hardening`) — alle live und getestet.
- ⏳ A3 Bewerbung & Tickets, A4 Integration & Kommunikation, A5 Vokabular + FLS27-Startdaten.
- ⏳ B6–B11 App-Fundament (noch nicht begonnen).
