# SQL-Smoke-Tests (Schema v2)

Ausführung: Inhalt einer Datei per Supabase-MCP `execute_sql` oder im SQL-Editor laufen lassen (Owner-Kontext). Jede Datei ist eine Transaktion mit `rollback` am Ende und hinterlässt keine Daten. Ergebniszeilen mit `ALLOWED (BUG)` bedeuten: eine Regel greift nicht. Voraussetzung: mindestens eine Person mit `auth_user_id` (ein eingeloggter Account). Die Tests entfernen deren Rollen und Staff-Eintrag **innerhalb der Transaktion**, damit Negativtests (fremde Bühne, Entscheidung ohne Rolle) auch mit dem Admin-Konto greifen; der Rollback stellt alles wieder her.

| Datei | Migrationen | Prüft |
|---|---|---|
| `v2_roles_programme.sql` | 0006, 0007 | Scope-Rechte, Überlappung je Bühne (23P01), Board-Warnungen, Pflichtfelder beim Veröffentlichen, Bestätigungspflicht, History/Statistik |
| `v2_application_ticket.sql` | 0009, 0020 | Bewerbungs-Pipeline (Eignung, Doppelbewerbung, Entscheider-Rechte, Maskierung bis Freigabe, Ticketpflicht, Kollision + Ersetzen), Anmeldung mit Warteliste, Personalisierung, max. 2 eigene Fragen, Mail-Warteschlange (Eingang, Zusage nach Freigabe mit Frist, Anmeldung), Housekeeping (Frist abgelaufen ⇒ expired, Warteliste rückt nach ⇒ Mail) |
| `v2_programme_editor.sql` | 0015, 0018 | Backlog-Sessions, Anhängen im Scope, Speaker setzen (Bestätigung bleibt erhalten), Leertexte ⇒ NULL, Veröffentlichen nur Programm-Team und nur mit Titel DE+EN, Detach-Sperre, keine TRUNCATE/REFERENCES/TRIGGER-Grants für API-Rollen |
| `v2_board_questions.sql` | 0016, 0017 | Realtime-Policies/Trigger, Programmzeiten, Fragen-RPCs (Katalog vs. eigene Fragen) |
| `v2_roles_admin.sql` | 0022–0024 | Admin-Lesewege: `applications_for_session` (Entscheider, 42501 sonst), `applications_overview`, `roles_of_person`, `assign_role` (idempotent, Vokabular, Scope-Check), `revoke_role` (Ablaufdatum, letzter Admin geschützt), `search_people` (Team; E-Mail nur Admin), Audit-Einträge, `search_organizations` (Team) |
| `v2_speaker.sql` | 0025 | Speaker-Profil: Anlegen nur Manager/Team, Pass-Regel (Masterclass ⇒ Professional ohne Lounge), Rolle `speaker` je Edition, Idempotenz über E-Mail (case-insensitiv), Team-Felder für Manager gesperrt, Suppression, Einladung erst ab `confirmed` (Mail `speaker_invite`), `manager_speakers`, Scope-Verlust ⇒ 42501, eigenes Profil lesen/schreiben (Whitelist), Assistenz einladen/entfernen (Rolle + Mail), Reisekosten-Freigabe nur `area_lead_speaker`/Admin |
| `v2_speaker_content.sql` | 0028 | Session-Inhalte: `my_sessions`, Einreichung (Titel Pflicht, Ersetzen ⇒ superseded), Freigabe nur Team/Scope mit Kopie nach `session` (Sprache entscheidet DE/EN, Overrides), keine Doppel-Freigabe; Deadline-Seed, `presentation_window`; Uploads: Pfadregel, Objekt muss im Bucket liegen, Versionen, `late` statt Sperre; Slid@Home nur mit Consent; Technik-Check nur Team; `speaker_next_steps` mit Inhalt/Präsentation |
| `v2_hospitality.sql` | 0030 | Hospitality: Freischaltung über Status **und** Consent `hospitality_data`, Hotels nur bis eigenem Tier, ein aktives Hotel je Profil, Überbuchung ⇒ `waitlisted`, Storno lässt Wartende nachrücken und setzt den Status zurück, Bestätigung nur Team (Mail `hospitality_confirmed`), Kontingent-Pflege und Überblick nur Team, Shuttle zählt Gäste |
| `v2_expenses.sql` | 0031–0033 | Reisekosten: Antrag nur mit `travel_costs_covered` + finaler Freigabe, Positionsprüfung (Kategorie-Vokabular, Betrag, Datum), Belegpflicht, Bankdaten nur Speaker + nur im Vault (Maske in der Tabelle, IBAN-Prüfsumme), Einreichung mit Rechnungsnummer + Mail an `area_lead_speaker`, Freigabe/Ablehnung/Auszahlung nur Freigeber, Bankdaten lesen nur Freigeber mit Audit, Ablehnung ⇒ zurück in Entwurf, Nummer bleibt |
| `v2_speaker_tickets.sql` | 0034, 0036 | Speaker-Ticket entsteht automatisch ab Pipeline `confirmed` (requested, ohne Barcode, Pass/Lounge aus dem Profil, zieht bei Profiländerung nach), Begleitticket nur ab `confirmed` (P0001), eins je Speaker (23505), E-Mail-Prüfung, nicht der Speaker selbst; Rückzug durch Speaker, Bestätigung/Ablehnung (mit Grund) nur Team mit Mails, Ausstellung (`set_ticket_issued`) nur Team/service_role und nur im passenden Status, `my_speaker_tickets` zeigt den Begleit-Barcode nicht und der Assistenz keinen Speaker-Barcode (nur `issued`), Absage ⇒ offene Freitickets zurückgezogen, `hospitality_block_reason` = `declined` |
| `v2_presentation_reminder.sql` | 0035 | Präsentations-Erinnerung: erst `reminder_lead_hours` vor der wirksamen Fälligkeit (Deadline ∧ 48 h vor Slot), genau einmal je Speaker × Session, nur mit Slot in der Zukunft, nicht bei vorhandener aktueller Präsentation oder abgesagter Session, nur service_role/Admin/Programm-Team (42501 sonst), Housekeeping liefert `reminders`; `upsert_deadline` pflegt den Vorlauf (Bereich 0–720 h); `my_manager_scope()` für Team, ohne Rolle und mit Bühnen-/Slot-Scope |
| `v2_content_manager.sql` | 0037 | Einreichungen: Speaker-Manager im Scope sieht `pending_submissions` seiner Speaker und darf `approve_session_content`/`reject_session_content` (auch ohne `can_edit_session`); ohne Scope 0 Treffer und 42501; `manager_speakers` liefert `internal_notes` |
| `v3_products.sql` | 0038 | Produktstamm: Import-Stand (162 Produkte, Stücklisten der Standpakete), Spalten-Grants (Einkaufspreis, Marge, Lieferant nicht für `authenticated`), Fristen FLS27 für Partner, `upsert_product`/`upsert_product_component` nur Partner-Team (42501), Teilupdate, Kategorie-Vokabular, `admin_products` mit Einkaufsdaten nur für Team |
| `v3_partner.sql` | 0040 | Partner-Org-Kontext: Kontakte anlegen (Person per E-Mail, Membership, Rolle `partner_contact` je Org, Einladungsmail), genau ein `primary_ops` (P0001 `primary_exists`/`primary_required`), Rollen-Vokabular, Übertragen des Hauptkontakts, Entfernen entzieht die Rolle; `my_partner_orgs`, `partner_overview` (Preise nur für Ops/Signing/Team), `additional` darf keine Kontakte verwalten, Onboarding-Pflege setzt `filled`, Pass-Typ nur talent/startup, fremde Org 42501, Spalten-Grants (`notes_internal` verborgen), Team-Übersicht + Statuswechsel |
| `v3_partner_deliverables.sql` | 0041/0042 | Partner-Checkliste: Trigger legt Pflichten nur aus gebuchten Leistungen an (global/Kategorie/SKU) mit Fristen aus `deadline`, Storno ⇒ `not_required`, erneute Buchung ⇒ `open`; Pfadregel `partner_asset_path_allowed`; `register_partner_asset` (Objekt fehlt P0002, PNG-Logo 22023 `file_rules`, Pfad ≠ Art 22023, Versionierung mit `is_current`); Onboarding `filled` erst mit Logo; `submit_deliverable` (ohne Datei/Antworten 22023, doppelt P0001, Eingangsbestätigung an Einreichende **und** Hauptkontakt = 2 Empfänger); Review nur Team (42501), Ablehnung ohne Grund 22023, mit Grund ⇒ Mail mit Grund, erneute Einreichung + Annahme; `partner_review_queue`, `upsert_booth` (Teilupdate, Notizen nicht in `partner_overview`), `upsert_deliverable_template` + `resync_deliverables`, Formular-Pflicht, `partner_admin_overview`-Zähler, Grants (kein Insert/Update für `authenticated`, anon nichts, `sync_deliverables` intern), Audit |
| `v3_partner_ingest.sql` | 0043/0044 | HubSpot-Ingest: nur service_role (authenticated 42501), `set_edition_hubspot` nur Team, Gate sammelt alle Fehler (Kommunikationsname, Rechnungs-E-Mail, zwei Hauptkontakte, ungültige Rolle/E-Mail, unbekannte SKU, unbekannte Pipeline) ohne Teilschreibungen, `sync_error` + Mail `partner_gate_failed` an den Owner; Erfolg: Org, `org_edition invited` mit Rechnungs-E-Mail aus dem Buchhaltungskontakt (kein Login), Leistungen mit Preis-Snapshot, Kontingent `partner=4`, Checkliste (4 Pflichten), Kontakte + `partner_contact`/`standbuehne_editor` (Scope org) bis Editionsende, zwei Einladungen; Idempotenz (`already`), zweiter Deal derselben Firma (`primary_conflict` bzw. Nachbuchung, erster Deal bleibt), Webhook-Duplikat, Sync-Job + Fehler, Team-Log/`resolve_sync_error`/`partner_deals`, `can_edit_stage` über `stage.partner_org_id`, Portal-Kontakt mit `valid_to`, Grants, Audit |
| `v3_partner_reminders.sql` | 0045/0046 | Partner-Housekeeping: Fälligkeiten folgen geänderten Fristen (`refreshed`), `overdue` setzen und wieder aufheben (auch in `partner_overview.checklist`), ein Digest je Org × Edition an `primary_ops` + `additional` (nicht `event_app_member`) mit überfälligen/fälligen Pflichten (Ticket-Codes 2027 nicht dabei), kein zweiter Digest innerhalb von 7 Tagen, nach einer Woche nur noch offene Pflichten (angenommene fehlen), zurückgewiesene Pflicht ohne Frist im Digest, `run_application_housekeeping` liefert `partner`, interne Funktionen ohne EXECUTE für `authenticated`, Audit |
| `v3_partner_applications.sql` | 0047 | Bewerber-Auswahl für Partner-Formate: `can_decide_session` eigene/fremde Host-Org, `partner_sessions` mit Zählern (fremde Org 42501), `partner_applications` mit Consent-Filter (Name/Antworten nur bei `consent_share`) und Audit je Abruf (`application.partner_view` mit Org, Zeilen, team=false), fremde Session 42501, Entscheidung über `decide_application` ohne Mail, `release_decisions` als Partner 42501, `event_app_member` ohne Entscheidungsrecht, abgelaufene Rolle 42501 bei bleibender Mitgliedschaft, `my_partner_stages` ohne/mit `standbuehne_editor` (Scope org, `stage.partner_org_id`) inkl. `can_edit_stage`, Volatilität 0056: `person_id` ohne Einwilligung `null` für Partner. |
| `v3_shop.sql` | 0048 | Messeshop: Phase aus `deadline`, Katalog (75 sichtbare Produkte, 7 Anfrage-Produkte nicht bestellbar, Bestand), Entwurf mit Nummer `MS-JJJJ-NNNN`, `request_only`/`unknown_sku` 22023, Bestätigung (Summe 73 500 ct netto, 7 % USt, Reservierung −2, eine Mail an Bestätigende=Hauptkontakt), `order_pending` bis `shop_edit`, `out_of_stock` mit Detail und unverändertem Status, delta-bewusste Nachreservierung (3 = ausverkauft), Storno gibt frei, Phasenende sperrt Bearbeitung (`phase_closed`) und erlaubt neuen Entwurf, Finalisierung (pending ⇒ completed + Mail, zweiter Lauf 0/0, Entwürfe ⇒ cancelled), Phase 3 nur `late_orderable`, geschlossen ⇒ `phase_closed`, Anfrage mit Mail an `area_lead_partner`, Team-Übersicht (Nutzer 42501), Support-Zeile auf verbindlicher Bestellung mit Reservierung, Report nur completed, Anfrage beantworten, Team-Storno gibt frei, Grants (kein Insert, Lagerbuch unsichtbar, interne Funktionen ohne EXECUTE), Audit |
| `v3_ticket_allocations.sql` | 0049 | Ticket-Kontingente: `set_edition_vivenu` nur Team, Kontingente aus Ticket-Produkten per Trigger (Startup-Org ⇒ `startup`), Partner sieht ohne vivenu keinen Code, Onboarding-Wahl wechselt den Pass-Typ, Menge folgt der Buchung, Service-Sicht mit Tickettypen aus `ticket_type_map` und Undershop der Org, Rückschreiben aus vivenu (Code sichtbar, aus der Pending-Liste raus), Mengenänderung setzt `synced_at` zurück (Coupon bleibt), Storno ⇒ `disabled`/unsichtbar, erneute Buchung ⇒ pending mit erhaltener Coupon-ID, Fehlerstatus, Team-Korrektur mit Audit (Nutzer 42501), Anfrage „mehr Tickets“ mit Mail an `area_lead_partner`, Grants |
| `v3_shop_invoices.sql` | 0052 | SevDesk-Kandidaten nur Team (42501), je Org alle `completed`-Bestellungen ohne Referenz mit zusammengefassten Positionen (Gitterbox 2+1 = 3) und Summen (105 000 / 7 350 / 112 350 ct), Rechnungsdaten aus `org_edition`; `record_shop_invoice` je Bestellung (`SD-INV-1#MS-…`), setzt Kontakt-ID nur wenn leer, zweiter Lauf legt nichts doppelt an, Kandidat verschwindet; pending-Bestellung und fremde Bestellung ⇒ P0001 `order_not_completed`; `set_org_sevdesk_contact`, `shop_invoice_refs`, `external_ref` ohne Grants, Audit |
| `v3_deliverable_extras.sql` | 0053/0054 | `my_partner_assets` (alle Fassungen, Beschriftung aus der Pflicht, fremde Org 42501); `answers_schema` (Pflichtfeld fehlt ⇒ P0001 `answers_incomplete` mit Feldschlüssel, vollständig ⇒ `submitted`); `fulfilled_by_sku`: Entwurf ohne Wirkung, bestätigte Bestellung ⇒ `accepted` mit Bestellnummer, Storno ⇒ wieder `open`; Partner manuell ⇒ P0001 `fulfilled_by_order`, Team manuell bleibt bei Storno stehen; `shop_sync_fulfilled_deliverables` nicht für authenticated ausführbar. |
| `v3_event_app.sql` | 0055/0057 | `set_edition_swapcard` (Team, trimmt); `event_app_exhibitors(edition)` liefert Org mit Level, Stand, Website, `event_app_member`-Kontakt, SVG-Logo erst nach Freigabe der Pflicht, PNG-Logo mit Pfad und Asset-ID nach Freigabe; ohne Edition nur Editionen mit Swapcard-ID; `set_event_app_ref` als Upsert (eine Zeile je Org×Edition, im Export sichtbar); ungültiges System 22023; Partner ohne Team-Rolle 42501 auf Export, Referenz und Event-ID. |
| `v3_template_resync.sql` | 0056 | `upsert_deliverable_template` zieht bestehende Organisationen laufender Editionen sofort nach (globale Pflicht bei beiden Orgs `open`, Audit `resynced_org_editions = 2`), SKU-Pflicht nur bei passender Buchung, Änderung an der Vorlage lässt Pflichten stehen, Partner ohne Team-Rolle 42501. |
| `v3_roles_session_logo.sql` | 0057–0060 | Rolle aus Produkt: I-79895 ⇒ `standbuehne_editor` für den Hauptkontakt (Scope org, Edition, `valid_to` = Editionsende + 1, `note auto:product`), Übergabe verschiebt sie, Storno entzieht, erneute Buchung vergibt, manuelle Rolle bleibt; `set_edition_hubspot` mit Erfolgs-Phase in `hubspot_editions`; Vokabular kennt `foundation` und `service` neben `initiative` (0059/0060); `logo_vector` nur SVG (EPS ⇒ 22023 `file_rules`), `filled` erst mit SVG + PNG; `upsert_session` als reiner Bühnen-Editor: Gastgeberin = eigene Org, fremde ⇒ 42501 `host_org_required` (Anlegen und Ändern), Titel ändern geht. |
| `v3_shop_stock_early.sql` | 0061 | `shop_upsert_line` weist 99 von 10 sofort mit P0001 `out_of_stock` `I-17066:10` ab, 10 geht, nach Bestätigung (verfügbar 0) und Wiederöffnen darf dieselbe Bestellung 10 behalten, 11 ⇒ `:10`, eine zweite Org bekommt `:0`. |
| `v3_external_refs.sql` | 0062 | `set_external_ref` als Upsert (eine Zeile je System×Typ×Objekt, Meta ersetzt), `list_external_refs`; unbekanntes System, ungültiger Typ, leere ID ⇒ 22023; Partner ohne Team-Rolle 42501 auf beiden. |
| `v3_grants_hardening.sql` | 0063 | anon ohne Tabellen-Grants (außer `vocab_term` lesen), authenticated ohne tote Schreib-Grants (event, role_assignment, audit_log, session) und ohne Lese-Grants auf organization, behält Policy-getragene Grants (consent_record insert, person update als Spalten-Grant, product-Spalten ohne Einkaufsdaten); `admin_products` für `production_team` 42501, für `area_lead_partner` Zeilen. |
| `v4_volunteers.sql` | 0065–0068 | Bewerbung ohne Einwilligung ⇒ P0001 `consent_required`, unter 18 am ersten Eventtag ⇒ `too_young`, unbekannte Shirt-Größe/Bereich ⇒ 22023, zweite Bewerbung ⇒ 23505; Zuteilung nur für angenommene Volunteers, Kapazität+Überbuchung voll ⇒ Warteliste (ausdrückliches `assigned` ⇒ P0001 `shift_full`), Überschneidung ⇒ `shift_overlap`, Absage zieht die Warteliste nach; Zusage gibt die Rolle `volunteer` (Scope edition), Absage beendet sie; Erinnerung genau einmal; Tabellen ohne Grants für `authenticated`. **0066:** leere Einträge und JSON-null in `day_prefs` fallen weg, unbekannte und unförmige Ids ⇒ P0002 `day_not_found` (statt 22004/22P02); `update_my_volunteer_profile` prüft die Tage jetzt genauso; der Test legt einen Wegwerf-`event_day` und einen Wegwerf-Bereich `volunteer_area` an, wenn es sie noch nicht gibt. **0067:** eine Absage beendet die Rolle `volunteer` sofort (delete statt `valid_to`), eine von Hand vergebene bleibt. **0068:** ein Tag eines Kind-Events der Edition wird angenommen, ein Tag eines fremden Events ⇒ `day_not_found`, auch an der Schicht. |
| `v4_volunteer_lead.sql` | 0070 | `is_volunteer_team()` ist für einen reinen `volunteer_lead` falsch; `volunteer_admin_overview`, `shift_plan` und `assign_shift` ⇒ 42501; `my_lead_shifts()` liefert genau die eigenen Schichten mit Namen und Stand, ohne Mailadressen, ohne Warteliste, nichts von fremden Schichten. |
| `v4_vivenu_ingest.sql` | 0071–0078 | Ticket anlegen (Status und Personalisierung abgebildet, Pass-Typ aus `ticket_type_map`, Person über die Inhaber-Mail im Extra-Feld); Secret in `ticket_secret` statt an `ticket`; Coupon-Einlösungen werden neu gezählt (dreimal dasselbe Ereignis ⇒ ein Ticket, eine Einlösung), Storno senkt sie wieder; unbekannter Status lässt den alten stehen; unbekanntes Event ⇒ `unknown_event`; als angemeldete Person 42501; keine Grants auf Funktionen und Secret-Tabelle. **0075–0078:** älterer `updatedAt` ⇒ `stale` verworfen, unbekannter Tickettyp lässt `pass_type` leer und meldet das, `backfill_ticket_pass_types()` trägt nach und ist idempotent, ein karges Ticket ohne addons/currency/personalizationStatus läuft durch. |
| `v4_merch_config.sql` | 0064 | `shop_confirm` weist eine Merch-Zeile ohne Konfiguration (P0001 `merch_incomplete`, detail `<sku>:logo`), mit falscher Größensumme (`:sizes`), unbekanntem Auswahlwert (`:farbe`) und zu langem Aufdruck (`:print`) ab; vollständig ⇒ `pending`; ein Produkt ohne Schema bleibt unberührt; die Konfiguration überlebt eine reine Mengenänderung; `merch_fields`/`merch_problem` sind für `authenticated` gesperrt. |
| `v4_produktion.sql` | 0082 | Regie-RPCs für Fremde dicht (auch lesend, auch `programme_team`); Cue ohne Slot; Bühne und Tag aus verschiedenen Veranstaltungen ⇒ 22023 `invalid_cue`; Teilupdate lässt stehen, was nicht mitkommt; offene Slots verschwinden nach dem Verknüpfen; Stand-Checkliste Haken setzen und nehmen, fremde Position ⇒ P0002; Shop-Artikel ohne oder mit unbekanntem Dienstleister ⇒ P0001; Slot einer anderen Bühne/eines anderen Tags ⇒ 22023; keine Grants; Bestellliste je Dienstleister. |
| `v4_wissensbasis.sql` | 0083 | ohne Anmeldung 28000, fremde Zielgruppe 42501; Overlay je Edition in beide Richtungen; Entwurf und abgelaufen unsichtbar; Rollenfilter; Schreiben nur für die Bereichsleitung **aller** Zielgruppen (fremder Artikel nicht über eine neue Zielgruppenliste übernehmbar, fremde Zielgruppe nicht ergänzbar, nicht zurücknehmbar, Editor zeigt ihn nicht); Slug doppelt ⇒ P0001; Archivieren statt Löschen; keine Grants. |
| `v4_volunteer_tickets.sql` | 0084 | Coupon nur für Angenommene; `set_volunteer_coupon` nur service_role; Einlösung über den Coupon und über Undershop + Person; Storno löst nichts ein; Zusage zurückgenommen ⇒ `revoked` + Eintrag in der Widerrufsliste; erneute Zusage ⇒ `none`; Quittung räumt die Liste; Erinnerung nach sieben Tagen genau einmal; Team-Liste für Fremde 42501; Mail-Link über `{{portal_url}}`; keine Grants auf Widerrufstabelle und Trigger. |
| `v4_hackathon.sql` | 0085 | ohne Anmeldung 28000, Team-RPC ohne Rolle 42501; unbekannter Skill 22023; Gründer wird Kapitän, zweites Team P0001; Maximum 8 auf jedem Weg (Trigger); Einreichung unter drei Personen P0001; gleichmäßige Verteilung, zweiter Lauf fasst nichts an; letztes Mitglied zieht das Team zurück; gewichtete Note 6,80, Punkte über 10 ⇒ 22023; Partner-Jury sieht und bewertet nur Teams der eigenen Challenge (fremdes Team 42501); Hilfs- und Trigger-Funktionen für authenticated gesperrt; keine Grants. |
| `v4_produktion_ticketartikel.sql` | 0087 | Ticket-Kontingent (`addon`/`tickets`/`pass_type`) weder auf der Stand-Checkliste noch in der Bestellliste, Shop-Artikel schon; Artikel nur mit `pass_type` fällt ebenfalls raus; Rechteprüfung bleibt (42501). Vor 0087 rot in 01–03. |
| `v4_wissensbasis_sprachrueckfall.sql` | 0088 | Artikel nur auf Englisch kommt auch bei `de` (mit `language = 'en'`); bei beiden Sprachen gewinnt die gefragte; Überlagerung der Edition sticht den evergreen auch in der anderen Sprache; eine Zeile je Slug; unbekannte Sprache = Deutsch; fremde Zielgruppe 42501. Vor 0088 rot in 01, 03–05. |
| `v4_hackathon_sprache.sql` | 0089 | deutsche Fassung kommt bei `de`, fehlt sie bleibt Englisch, Vorgabe Englisch, leere deutsche Fassung zählt nicht, `my_hack` reicht die Regel durch; ohne Login 28000, Jury ohne Rolle 42501. Vor 0089 rot in 01, 04, 05. |
| `v4_checkin.sql` | 0090 | ohne Kiosk-Rolle 42501; gültiges Ticket ⇒ `ok`, `ticket.checked_in_at` gesetzt; zweiter Scan ⇒ `already` mit Zeit des ersten, als `duplicate` vermerkt; fremder Code ⇒ `unknown` ohne Zeile; gültiger Code einer anderen Edition ⇒ `unknown` (keine Auskunft); storniertes Ticket zweimal `invalid`; leerer Code 22023; `checkin_stats` Kiosk ja, fremde Edition 42501; Kiosk-Konto sieht fremde Tickets nicht (echter Rollenwechsel); `purge_checkins` nur > 30 Tage, ohne Team 42501, service_role erlaubt; `checked_in` gilt als gültig. |
| `v5_ansprechpartner_invalid_email.sql` | 0102 (`20260915115807`) | fremde Mail-Domain beim Anlegen und Ändern ⇒ 22023 `invalid_email` statt 23514 (seit 0114: `contact_consent_required`, fremde Adressen sind mit Einwilligungsdatum erlaubt); Grossschreibung der Domain angenommen; Teilupdate lässt die Adresse stehen; Pflichtfelder weiterhin `fields_required`. |
| `v5_sponsoring_level.sql` | 0141 (20260922095139) | `sponsoring_level_key` normalisiert (`start_up`, `main_stage_loge`, leer/null ⇒ null); Export liefert Schlüssel und Rang aus dem Vokabular (`Premium` ⇒ premium/40), unbekanntes Level normalisiert ohne Rang, ohne Level beides null; Vokabular acht Einträge mit eindeutigen Rängen; Helfer für authenticated gesperrt, Export ohne Team-Rolle 42501; Pflicht `logo_vector` nennt Transparenz DE+EN. |
| `v5_portal_video.sql` | 0092 (20260915114209) | Pflegen ohne Team-Rolle 42501; fremder Anbieter mit eigenem Schlüssel `invalid_video_url` statt nacktem 23514; Lesen über Schlüssel und Zielgruppe; fremde Zielgruppe sieht nichts, auch mit Schlüssel; Editionsfassung sticht die allgemeine; keine Grants. |
| `v5_org_schritte.sql` | 0093 (20260915114240) | Fremde Organisation kann nicht abhaken (42501); der Katalog kommt vollständig, auch ohne Haken; Haken mit Name und Zeitpunkt; unbekannter Schritt ⇒ 22023 `invalid_step`; zweimal setzen verschiebt den Zeitpunkt nicht; Haken wegnehmen löscht die Zeile; Fortschrittsliste für Partner 42501, mit Team-Rolle 2 von 6; keine Grants. |
| `v5_shop_po_nummer.sql` | 0095 (20260915114654) | Ohne Eingabe gilt die Vorgabe aus den Stammdaten; eine Eingabe schlaegt sie; leerer Text bedeutet keine Nummer und laesst die bestehende stehen statt einen leeren String zu schreiben; `shop_my_orders` gibt die Nummer heraus; Bestaetigen fuer eine fremde Organisation 42501. |
| `v5_wiki_inhalte.sql` | 0096 (20260915114852) | Zehn Artikel angelegt, **keiner** veroeffentlicht (die Texte tragen die Daten von 2026); jahresunabhaengig, damit ein Overlay je Edition darueber passt; als Entwurf im Portal unsichtbar, nach dem Freischalten sichtbar; fremde Zielgruppe 42501; keine private Mobilnummer im Bestand. |
| `v5_messestand.sql` | 0094 (20260915115415) | Frist `booth_backdrop` heisst jetzt `booth_changes_until` und steht auf dem 02.04.2027, die schon erzeugten Pflichten ziehen mit; Paketuebersicht liefert Flaeche, Mass und Ausstattung aus der Stueckliste; Ausstellerliste zeigt nur Staende **mit** Nummer und ist ohne Partner-/Speaker-Zielgruppe zu (42501); Editionsdateien pflegen ohne Team-/Produktionsrolle 42501, unbekannte Art 22023 `invalid_kind`, Pfad ausserhalb der Edition 22023 `invalid_path`, erfundene Zielgruppe 22023 `invalid_audience`; Paketliste ohne Partner-Zielgruppe 42501; Produktion darf Standnummern vergeben; keine Grants. |
| `v5_speaker_anreise.sql` | 0098 (20260915115707) | Ohne Speaker-Profil P0002; An-/Abreise eintragen und zurueckleben; unbekanntes Verkehrsmittel 22023 `invalid_travel_mode`; Teilspeicherung laesst die andere Richtung stehen; Abreise vor Anreise 23514; Liste fuer Speaker 42501, mit Bereichsleitung sichtbar; keine Grants. |
| `v5_speaker_zusage_anrede.sql` | 0099 (20260915115751) | Zusage setzt `confirmed_at`, ein weiterer Wechsel innerhalb der Zusage verschiebt es nicht; Absage setzt Datum und Grund, unbekannter Grund 22023 `invalid_decline_reason`, Rueckweg raeumt beides; Briefanrede nur fuer das Team (42501), getrimmt, leer wird NULL; der Anrede-Vorschlag baut den Normalfall, schweigt ohne Geschlechtsangabe und ist ohne Team-Rolle zu (42501); Lead-Board gibt die neuen Spalten heraus. |
| `v5_catering.sql` | 0100 (20260915115505) | Eigene Angabe setzen und lesen, unbekannte Ernaehrungsform 22023 `invalid_diet`; `set_diet` hat **keinen** Parameter fuer eine fremde Person; `catering_notes` hat keine Personenspalte; der Freitext steht nicht im Audit-Log; Auswertung ohne Produktions-/Team-Rolle 42501; nur bestaetigte Speaker und angenommene Volunteers zaehlen; Hinweis auf 300 Zeichen gekuerzt; Loeschfrist 30 Tage nach Ende der Edition (29 Tage danach steht sie noch, 31 Tage danach weg, laufende Edition schuetzt, zweiter Lauf loescht nichts, Protokoll nur mit Zahlen). |
| `v5_regie_fuer_leads.sql` | 0101 (20260915115544) | Ohne Rolle bleibt die Regie zu (42501); Speaker-Lead der Buehne darf lesen und schreiben, eine fremde Buehne nicht; beim Aendern gilt die Buehne des Cues, nicht die im Rumpf; Loeschen prueft dieselbe Buehne; `my_regie_stages` bietet nur an, was erlaubt ist; Produktion darf ueberall. |

| `v6_challenge_frist.sql` | 0111 (`20260917190402`) | Die alte Regel `weeks_before` liefert nachgestellt NULL (der Befund, der die Migration ausgeloest hat); Frist der Edition steht auf dem 18.03.2027; Vorlage zeigt auf `deadline_key`; neue Pflicht bekommt die Frist, bestehende wurde nachgezogen; mit Frist wird die Pflicht ueberfaellig (vorher unmoeglich); `upsert_deliverable_template` weist eine unbekannte Regel mit 22023 `invalid_due_rule` ab, laesst `{}` und `offset_days` zu; die Fassung aus 0056 (`answers_schema`, `fulfilled_by_sku`, `unknown_sku`, Resync) bleibt erhalten; Pflege ohne Partner-Team 42501. |
| `v6_shop_zwei_phasen.sql` | 0112 (`20260917192416`) | Genau zwei Phasenfristen (`shop_phase_2` entfernt); `shop_phase` rechnet zweistufig (heute Phase 1 ohne `late_only`, nach der ersten Frist Phase 2 mit `late_only`, danach geschlossen); eine Org ohne Stand bekommt einen leeren Katalog und P0001 `booth_required` beim Einlegen; Standbuehne oeffnet den Shop, **Hackathon-Stand nicht**, Standflaeche schon; Lunch-Paket steht nicht mehr im Katalog, ist aber ueber die Pflicht auch ohne Stand bestellbar (`order_lunch_package` legt eine bestaetigte Bestellung ueber den Shop-Weg an, die Pflicht wird `accepted`); die Lunch-Vorlage ist `required = false` ohne Kategorie und wird von `mark_overdue_deliverables` nicht gemahnt; ein Angebot mit abgelaufener Frist steht nicht im Erinnerungs-Digest, vor der Frist als Hinweis schon, und eine echte Pflicht wird weiterhin gemahnt; ein Warenkorb mit fremder Zeile wird **nicht** mitbestaetigt (confirmed=false, Entwurf bleibt draft, Lunch-Zeile liegt bereit); ein anderer unsichtbarer Artikel bleibt gesperrt; Menge null ⇒ 22023, fremde Org ⇒ 42501. |
| `v6_formate_rpcs.sql` | 0133 (20260921115331) | Ohne gebuchtes Produkt kein Side-Event (P0001 `no_entitlement`), mit Produkt entsteht es und der **Slot** traegt die Zeit (Weg A), Session wartet auf Freigabe; Anspruch danach aufgebraucht; zwei Gespraeche zur selben Zeit am selben Tisch ⇒ `slot_overlap` **durch den Ausschluss-Constraint**, nicht durch eigene Logik; am Tisch zaehlt der Tisch, nicht das Gespraech (zweiter Slot geht ohne zweites Produkt); fremde Flaeche 42501; `format_details` nimmt nur die Schluessel des Formats und prueft Laenge, URL-Form und Vokabular; `partner_update_session` aendert nur die Whitelist (Kapazitaet ⇒ `not_editable`); loeschen nur ohne Zusagen (`slot_locked`); Katalogfragen nur mit `partner_selectable`; ein Antrag braucht einen Zweck, hoechstens zwei je Session, keiner ist vor der Freigabe sichtbar; `partner_add_speaker` legt Person, Profil (`invited`, `partner_editable_until_login`, `created_by_org_id`) und Zuordnung an und blockt bei bestaetigtem Speaker; alle vier Helfer fuer authenticated gesperrt; Einzelgespraech setzt Kapazitaet 1 und Gruppengespraech uebernimmt die Angabe, erfundener Modus abgewiesen; Freigabe nur mit Rolle, Ablehnung ohne Grund abgewiesen, Freigabe hebt Session auf published und Slot auf final; Export ohne Einwilligung leer, mit Einwilligung eine Zeile, jeder Abruf im Protokoll; Datenschutzhinweis in beiden Sprachen. **Auflagen 21.09.:** eine per Mailadresse „geclaimte" **bestehende** Person gibt dem Partner kein Pflegerecht (nur eine hier neu angelegte), die Zuordnung bleibt trotzdem vermerkt; eine Textaenderung an einer **veroeffentlichten** Session schickt sie zurueck auf `review` und den Slot auf `requested`, derselbe Titel noch einmal und eine reine `format_details`-Aenderung dagegen nicht; die zurueckgeschickte Session steht in `partner_sessions_pending` (der Formatfilter dort ist gefallen, sonst waere sie unsichtbar aus dem Programm verschwunden); `image_asset_id` muss eine UUID und eine Datei **dieser** Organisation sein. Die Freigabe ohne englischen Titel scheitert mit **22023 `fields_required`** und nennt das fehlende Feld — nicht mit dem rohen 23514 aus `session_publish_check`, das keine Oberflaeche uebersetzen kann; mit beiden Titeln geht sie durch. |
| `v6_company_tours.sql` | 0134 (20260921115332) | `tour_lead` als Kontakttyp erlaubt, erfundener nicht; **die Pflege-RPC kennt ihn ebenfalls** (ein neuer Typ steht an drei Stellen — Vokabular, CHECK und Whitelist in upsert_edition_contact), die Freelancer-Regel aus 20260918105038 bleibt erhalten; sechs Touren 2027 mit dem **CCH** als Sammelpunkt (2026 Handelskammer) und gestaffeltem Start; je Tour drei Stopps à 90 Minuten, zunaechst ohne Partner; ein Partner sieht nur seinen Stopp, samt Tour Lead mit Name, Mail und Telefon; ein externer Tour Lead braucht `contract_consent_at`; der Partner fuellt die neun Angaben und `filled_at` wird gesetzt; Zeiten, Reihenfolge und Zuordnung sind fuer ihn gesperrt; gesuchte Profile nur aus dem Vokabular; fremder Stopp 42501; derselbe Partner nicht zweimal auf einer Tour; Tour Lead vom falschen Typ abgewiesen; Pflege nur Team/Programm; keine Grants auf beiden Tabellen. **Auflage 21.09.:** `lead_contract_consent_at` steht nicht mehr in der Rueckgabe von `partner_company_tour` — interner Nachweis, keine Angabe fuer den Partner; geprueft an der Signatur, nicht am Wert. |
| `v6_formate_schema.sql` | 0132 (20260921115330) | `interview_table` im Vokabular, die vier anderen Formate waren schon da; `stage.type` nimmt die zwei neuen Typen und weist einen erfundenen ab; `session.partner_org_id`/`format_details` mit Vorgabe `{}`; host und partner duerfen nicht auf verschiedene Organisationen zeigen; Bestand uebernommen; **Menue**: eine Tisch-Buehne blendet „Standbuehne" nicht ein, eine echte schon; **Rechte**: `standbuehne_editor` darf die Standbuehne, nicht den Tisch derselben Org; `partner_selectable` mit Vorgabe falsch und ein Frageantrag ohne Freigabe; das Talk-Flag faellt beim ersten Login und nur dann; Trigger-Funktion fuer authenticated gesperrt. Der Artikel der Interview Tables (I-66084, Konrad 21.09.) ist angelegt, aktiv, in `stage_products`, mit `format_key` und **ohne erfundenen Preis**; ein zweiter Lauf ueberschreibt einen von Konrad gepflegten Namen nicht; fehlt der Artikel, bricht der Schritt ab, statt drei gruene Zeilen zu schreiben. **Auflagen 21.09.:** der Login-Trigger nimmt ein **geliehenes** zweites Konto aus dem Bestand (eine erfundene `auth_user_id` verletzt den Fremdschluessel auf `auth.users`) und setzt aus, statt gruen zu werden, wenn es keins gibt; `upsert_stage` legt eine `interview_table`-Buehne an und weist einen Typ, den das Vokabular nicht kennt, mit 22023 ab. `partner_overview` ist aus dem Snapshot nach 0124 neu aufgesetzt — die erste Fassung haette `logo_dark`/`logo_light` und die tageweise Standbelegung still zurueckgedreht. |
| `v6_partner_format_key.sql` | 0110 (`20260917190103`) | Vokabular `partner_format` vollstaendig; Seed trennt Talk, Masterclass und Standbuehne trotz gemeinsamer Kategorie `stage_products`; Side Event (I-81745) zugeordnet, obwohl es in „Specials“ liegt; `interview_table` bewusst ohne Produkt; Zusatzleistungen der Company Tour ohne Schluessel; `partner_overview` liefert `format_key` je gebuchtem Produkt und **nur** das gebuchte; `upsert_product` weist einen erfundenen Schluessel mit 22023 `invalid_format` ab, leerer Text loescht, Teilupdate laesst stehen; Pflege ohne Partner-Team 42501; Spalten-Grant lesen ja, schreiben nein. |
| `v5_admin_speaker.sql` | 0103 (20260915123741) | Lead-Person kann `owner_person_id` nicht mehr ueber `update_speaker` setzen (`team_only_fields`), das Team schon; weiterreichen darf nur, wer heute selbst betreut; Empfaenger ohne Lead-Rolle 22023 `invalid_owner`, unbekannte Person P0002; freigeben (`null`) nur das Team; jede Uebergabe steht mit Vorher/Nachher im Protokoll; Detailblatt gibt dem Team die interne Notiz, der Lead-Person nicht (`internal_notes_visible`), ohne Verwaltungsrecht 42501; `speaker_managers` nennt nur aktive Lead-Personen und ist ohne Rolle zu; `manager_speakers` gibt die interne Notiz wieder heraus (Nachtrag zu 0099). |

| `v5_speaker_leads_admin.sql` | 0104 (20260915124834) | Lead-Verwaltung und die Liste der unbetreuten Speaker ohne Team-Rolle 42501; eine Lead-Person erscheint mit ihren aktiven Rollen inklusive Zuweisungs-ID (Griff zum Entziehen); Zahlen betreut/zugesagt/abgesagt stimmen, offene Schritte werden ueber alle betreuten Speaker summiert; eine abgelaufene Rolle macht niemanden zum Lead; unbetreute Speaker erscheinen, Absagen nicht; nach der Zuordnung verschwindet der Speaker aus der Gegenliste und zaehlt bei der Lead-Person; `my_manager_scope` nennt die eigene Personen-ID und laesst den Rest unveraendert. |
| `v5_pass_type_choice.sql` | 0105 (20260915130339) | Neue Org-Edition einer Startup-Organisation bekommt `startup`, jede andere Kategorie `talent`, ein ausdruecklich gesetzter Wert bleibt stehen; ein Update auf NULL bleibt NULL (die Vorbelegung greift nur beim Anlegen, sonst liesse sich der Rueckfall nie wieder einstellen); Setzen ohne Partner-Team 42501, erfundener Wert 22023 `invalid_pass_type_choice`, `null` stellt den Rueckfall wieder her; das Kontingent zieht nach (startup -> talent) — der Schritt hat aufgedeckt, dass ein gleichnamiger Trigger den Sync-Trigger aus 0049 entfernt haette; jede Aenderung mit Vorher/Nachher im Protokoll. |
| `v5_team_verwaltung.sql` | 0106 (20260917093050) | Teamliste ohne Admin-Rolle 42501 (sie nennt Namen und Mailadressen); wer eine Teamrolle hat steht drin, mit aufgeloestem Scope statt UUID; Teilnehmerrollen (`talent` und Co.) zaehlen nicht; abgelaufene Rollen auch nicht; mehrere Rollen einer Person ergeben eine Zeile; `has_account` zeigt, wer sich ueberhaupt einloggen kann; die Zahl der globalen Admins steht in jeder Zeile; `team_role_keys` enthaelt keine Teilnehmerrollen. |

| `v5_admin_ueber_rolle.sql` | 0107 (20260917093102) | Mit aktiver Admin-Rolle ist `is_staff()` wahr; ohne Rolle nicht, eine RPC dahinter weist mit 42501 ab; `session_context()` meldet dasselbe (daran haengt die Oberflaeche); eine abgelaufene Admin-Rolle zaehlt nicht — 5 Schritte. Die Schritte zum Schnitt gegen `staff_user` (02/07/08) sind seit `20260917183022` gegenstandslos (Tabelle entfernt). |

| `v5_wissensbasis_assistent.sql` | 0108 (20260917102018) | Ohne Login 28000, fremde Zielgruppe 42501, leere Frage 22023 `empty_query`, Kiosk-Geraetekonto 42501; ein Entwurf liefert keine Abschnitte, erst das Veroeffentlichen; Abschnitt = H2-Block mit H3 im Text; ein langer Abschnitt wird geteilt und jedes Teilstueck traegt die Ueberschrift; Treffer mit Rang > 0; abgelaufener Artikel und fremde Zielgruppe fallen heraus; 21. Frage in der Stunde P0001 `rate_limited`; `kb_question_log` hat keine Personenspalte; Aufraeumen loescht Fragen aelter als 90 Tage und Zaehlerzeilen aelter als 24 Stunden; eine Protokollzeile **ohne genommenen Slot** P0001 `no_slot` (sonst waere `kb_log_question` ein offenes Schreibrecht auf das Protokoll); `kb_question_report` ohne Rechte 42501, mit Admin Zeilen und die Fragen ohne Treffer zuerst. |

| `v5_kb_search_rueckfall.sql` | 0109 (20260917102942) | Eine Frage, deren Woerter alle vorkommen, trifft weiterhin genau den Abschnitt; eine Frage mit einem Wort, das im Artikel nicht steht („geliefert" statt „einsenden"), findet ihn jetzt trotzdem — vor 0109 war das Ergebnis leer (Befund aus dem Walkthrough an echten Artikeln); der richtige Artikel steht oben; Unsinn findet auch als ODER nichts; die Zielgruppe gilt im Rueckfall genauso. |
| `v6_aufraeumen.sql` | `20260917183022` | Beschreibung je Organisation (Spalten, Onboarding, Übersicht, Aussteller, Ingest), `photo_url` weg, `staff_user` weg, Snapshot-Trigger `organization_name` — 8 Schritte |

| `v6_programm_grundgeruest.sql` | 0110 (20260917184553) | Ohne Programm-Recht ist jeder Schreibweg zu (42501); derselbe Tag zweimal **aendert** ihn statt an der Eindeutigkeit zu scheitern; unbekannte Buehnenart 22023 `invalid_stage_type`; Oeffnung nach Schliessung 22023 `invalid_times`; `upsert_stage_day` legt die fehlende Zeile an und aendert die bestehende, ohne die uebrigen Felder zu verlieren; Track anlegen und loeschen; Buehne mit Slot, Buehne mit Rollenzuweisung und Track mit Session jeweils P0001 `in_use`; `programme_skeleton` liefert Tage, Buehnen, Buehnen-Tage und Tracks mit den Zahlen, die am Loeschen haengen, und ist ohne Recht zu; das Protokoll traegt Vorher **und** Nachher. |
| `v6_session_grafiken.sql` | 0111 (20260917185916) | Ohne Marketing-/Team-Rolle kein Eintrag (42501); erfundene Bildart 22023 `invalid_kind`; Pfad einer fremden Session 22023 `invalid_path`; mehrere Buehnenfotos bleiben **alle** gueltig, eine zweite Slot-Grafik loest die erste ab; das **erste** Buehnenfoto verschickt genau eine Mail je Speaker, das zweite keine; `my_session_photos` zeigt die eigenen Auftritte und nicht die fremden; hart loeschen nur Admin (Marketing 42501), die RPC gibt den Pfad zum Aufraeumen zurueck; Pflegelisten ohne Rolle zu; `sessions_for_assets` zaehlt Fotos und **gueltige** Grafiken. |
| `v6_mail_protokoll.sql` | 0113 (20260918104946) | Protokoll, Detail und erneutes Einreihen jeweils ohne Admin-Rolle 42501; die Suche trifft Adresse und Betreff und behandelt `%` als Zeichen, nicht als Platzhalter; Filter nach Status, Vorlage und Zeitraum greifen einzeln und zusammen; `total` steht in jeder Zeile und zaehlt ueber die Seite hinaus; das Detail nennt die eingesetzten Variablen und sagt mit `resendable`, ob der Knopf angeboten wird; `queued` und `suppressed` P0001 `not_resendable`, eine unbekannte Zeile P0002 `mail_not_found`; erneut einreihen legt eine **zweite** Zeile mit `queued` und `meta.resend_of` an, laesst die alte unveraendert und steht als `mail.requeue` im Protokoll. |
| `v6_profil_loeschen.sql` | 0115 (20260918105546) | Ohne Login 28000; eine Person ohne Bindung hat keine Huerde und loescht **sofort**; Teamrolle, zugesagter Auftritt, Volunteer-Einteilung, Organisation und **offener Reisekostenantrag** sind Huerden; mit Huerde entsteht ein Antrag statt einer Loeschung; zweiter Antrag P0001 `already_requested`; Warteschlange ohne Admin 42501; Ablehnen ohne Begruendung 22023 `note_required`, mit Begruendung geschlossen und Antwort eingereiht; erfundene Aktion 22023 `invalid_action`, unbekannter Vorgang P0002; Loeschen aus der Warteschlange anonymisiert. **Schritt 17 liest das Schema selbst aus**: nach dem Lauf steht der Name der Person in keiner Zeile einer Tabelle mit `person_id` und in keinem Kind des Speaker-Profils mehr — eine neue Spalte oder Tabelle faellt damit auf, ohne dass jemand den Test pflegt. Dazu: die Bucket-Pfade stehen in `storage_purge_queue`, die Bankdaten sind weg und die Buchung bleibt. Die drei Testpersonen sind **geliehene** Konten (`person.auth_user_id` zeigt auf `auth.users`), ihre Bindungen werden zu Beginn geloest. |
| `v6_initiativen.sql` | 0116 (20260918141036) | Liste, Stufe und Leistungen jeweils ohne Partner-Team 42501; erfundene Stufe 22023 `invalid_stage`, unbekannte Teilnahme P0002; die Stufe steht mit Vorher **und** Nachher im Protokoll; `abgelehnt` ist eine Stufe und kein Loeschen — die Zeile bleibt in der Liste; die Liste nennt nur Initiativen, keine Partner; `assign_org_products` legt Zeilen mit `source = 'agreement'` und Preis **0** an (nicht null), die Pflichten entstehen ueber den Trigger aus 0041 von allein; ein zweiter Aufruf **ersetzt** den Stand, laesst HubSpot-Zeilen aber unberuehrt; eine unbekannte SKU P0002 `unknown_sku` **und nichts ist geloescht** (erst pruefen, dann schreiben); qty 0 22023 `invalid_items`; die Zahlen der Liste stimmen; die vier INI-Produkte stehen im Stamm, unsichtbar im Shop und mit Preis 0. |
| `v6_produktabgleich.sql` | 0120 (20260918144512) | Liste ohne Partner-Team 42501, fremdes Zielsystem 22023 `invalid_system`; der Stamm geht hinaus, **`INI-%` und reine Messeshop-Artikel (`source_hubspot = false`) bleiben hier** (Barter und Eigenes gehoeren in kein Fremdsystem); ein gemerkter Fremdschluessel kommt beim naechsten Lauf mit, derselbe zweimal ergibt **eine** Zeile mit dem neuen Wert (das ist die Grundlage dafuer, dass ein zweiter Lauf aendert statt anzulegen); HubSpot und SevDesk stehen je Produkt nebeneinander; unbekannte SKU P0002; ein `external_ref` ohne Ziel wird abgewiesen; **das Ruecksschreiben gehoert dem Server** — aus einem angemeldeten Kontext 42501, im Servicekontext (Claims ohne `sub`) steht die Zeile. Die letzten zwei Schritte schliessen die Luecke, die der erste Lauf hatte: der Test lief mit Nutzerrolle, der Betrieb laeuft unter der Service Role, wo `has_role(...)` immer false ist. |
| `v6_sevdesk_shopartikel.sql` | 0131 (20260921112022) | Die Regel ist **je System verschieden**: nach HubSpot gehen nur Artikel mit `source_hubspot` (86), nach SevDesk alles ausser Barter (162, davon 76 reine Shop-Artikel, die vorher fehlten) — damit sich eine Rechnung am Ende einem Artikel zuordnen laesst. `INI-%` bleibt in **beiden** Faellen hier: Leistungen mit Preis 0 waeren in einem Rechnungssystem eine Einladung, sie versehentlich zu berechnen. Inaktive Artikel gehen mit und werden drueben inaktiv gesetzt, statt weggelassen zu werden. |
| `v6_sevdesk_belege.sql` | 0122 (20260918145925) | Zielliste und Rueckfall ohne Partner-Team 42501; eine Organisation **mit** SevDesk-Kennung steht in der Liste, eine ohne nicht; `register_sevdesk_document` aus einem angemeldeten Kontext 42501 (sie gehoert dem Server); der Rueckfall legt ab **und protokolliert**; ein Pfad, der zu einer anderen Organisation gehoert, 22023 `path_mismatch` (sonst lag die Rechnung im Ordner eines Fremden, den die Leseregel des Buckets oeffnet); erfundene Belegart 22023 `invalid_kind`; derselbe Beleg zweimal ergibt **eine** Zeile mit neuem Stand; ein zweiter Beleg **ueberholt den ersten nicht** — jede Rechnung bleibt gueltig (genau das kann `register_partner_asset` aus 0053 nicht); Eintrag ohne Datei im Bucket P0002; und die Zielliste liest **auch im Servicekontext**, sonst waere der naechtliche Lauf beim ersten Aufruf gescheitert. |
| `v6_rabattstufen.sql` | 0123 (20260921101601) | Von Hand setzen ohne Partner-Team 42501; die Ableitung schreibt die **100er**-Zeile aus den gebuchten Produkten; von Hand 100 setzen P0001 `derived_allocation` (die Zeile gehoert der Ableitung, eine Aenderung waere beim naechsten Lauf wieder weg), erfundener Satz 22023 `invalid_discount`; zwei Kontingente desselben Pass-Typs mit verschiedenem Satz stehen nebeneinander — was die alte Eindeutigkeit verhinderte; **der naechste Abgleich laesst die 50er unberuehrt** (der eigentliche Punkt: ohne `discount_percent = 100` in den Aufraeumschritten haette er die Handarbeit ueberschrieben); faellt das Produkt weg, raeumt er die 100er ab und die 50er bleibt; Menge 0 deaktiviert statt zu loeschen; jede Handaenderung mit Vorher/Nachher im Protokoll; der CHECK haelt auch gegen einen direkten Insert. |
| `v6_staende_tagesweise.sql` | 0124 (20260921103627) | Belegen ohne Partner-/Produktions-Team 42501; ein Stand ohne Tag gilt fuer beide Tage; **zwei Organisationen teilen denselben Stand an verschiedenen Tagen** — was die alte Eindeutigkeit an `booth.org_edition_id` verhinderte; zwei Belegungen desselben Standes am selben Tag 23505, zwei Zeilen „beide Tage" ebenso (`nulls not distinct`); Tag einer fremden Edition 22023 `invalid_day`, unbekannter Stand P0002; `booth_day_plan` zeigt je Tag, wer dort steht, markiert geteilte Staende und fuehrt eine Belegung fuer beide Tage an **beiden** Tagen; `org_has_booth` erkennt die Teilnahme ueber die Zuordnung; `upsert_booth` legt Stand und Belegung an und aendert beim zweiten Aufruf statt zu verdoppeln; `partner_admin_overview` liefert die Standnummer auch bei geteiltem Stand (die Unterabfrage hatte vorher kein `limit` und waere abgebrochen); Loesen entfernt die Belegung, nicht den Stand; alles im Protokoll. Der Test legt sich **eigene Veranstaltungstage** an — `event_day` ist fuer FLS27 leer. Dazu zwei Schritte am Schema: auf `booth` steht genau **eine** Lesepolitik, sie geht ueber `booth_assignment` und nennt `booth.org_edition_id` nicht mehr (die alte Policy liess den `drop column` mit 2BP01 scheitern — Fremdschluessel und Eindeutigkeit nimmt Postgres mit, eine Policy nicht), und die Spalte ist wirklich weg. |
| `v6_vokabularpflege.sql` | 0130 (20260921104956) | Lesen und Schreiben ohne Admin 42501; Schluessel mit Leerzeichen 22023 `invalid_key`, fehlende englische Beschriftung `fields_required`; anlegen und aendern inklusive Reihung und Aktivkennzeichen; die Verwendungszahl eines frischen Begriffs ist **0 und nicht null**; ein unbenutzter Begriff laesst sich loeschen, ein benutzter nicht (P0001 `in_use`), einer mit Unterbegriffen nicht (`has_children`, ohne Kinder geht er) — und **einer aus einem Vokabular ohne Verzeichniseintrag auch nicht** (`usage_unknown`): Schweigen ist kein „wird nicht benutzt". Unbekannter Begriff P0002; alles im Protokoll. Die Schritte 10 und 11 legen sich eigene Begriffe an, statt Schluessel aus dem Bestand zu raten. |
| `v6_rechte_nullsicher.sql` | 0118 (20260918142035) | Aus Sicht einer angemeldeten Fremden gegen ein Profil **ohne Assistenz**: `speaker_next_steps`, `expense_eligibility`, `request_companion_ticket`, `register_speaker_asset` je 42501; `speaker_asset_path_allowed` liefert false, nicht NULL; statisch keine ungeschuetzte OR-Kette mit `assistant_person_id` mehr im Schema; der Speaker selbst liest weiter. Vor dem Hotfix waren 01, 02 und 05 rot. |
| `v6_mail_vorlagen.sql` | 0112 (20260917185108) | Ohne Admin weder schreiben noch lesen (42501, die Liste nennt Betreff und Text); unbekannte Sprache 22023 `invalid_locale`; leerer Betreff oder Text 22023 `fields_required` (der Versand naehme die Vorlage sonst und verschickte eine leere Mail); anlegen und aendern, Version steigt, ein weggelassenes Feld bleibt stehen; **die Zahl der wartenden Mails steht in der Zeile** — wer den Text aendert, aendert die wartenden Mails mit; die Historie liefert den vollen Text vor der Aenderung, `restore_mail_template` holt ihn als neue Fassung zurueck. |
| `v6_freelancer_kontakte.sql` | 0114 (20260918105038) | Ohne Pflegerecht 42501; Hausadresse ohne Einwilligungsdatum unveraendert angenommen; fremde Adresse ohne Datum 22023 `contact_consent_required` (vorher `invalid_email`), mit Datum angenommen; eine Namenskorrektur laeuft ohne erneutes Datum durch (Feld nicht mitgeschickt), das Datum ausdruecklich leeren wird abgewiesen — Widerruf heisst entfernen, nicht nullen; der Buddy erscheint bei der betreuten Speakerin in `my_contacts()`; `delete_edition_contact(.., 'consent_withdrawn')` gibt den Bildpfad zurueck (die Serverroute raeumt das Foto weg), schreibt `contact.remove` mit Grund und Akteur ins Protokoll, dort aber **ohne** Adresse und Telefonnummer (die ueberleben den Widerruf nicht; beim normalen Loeschen einer Dienstadresse bleiben sie stehen), und der Verweis am Speaker-Profil steht danach auf null. |
| `v6_shuttle.sql` | 0119 (20260918142855) | Shuttle-Fahrten: fremdes Profil ⇒ 42501, eigene Fahrt entsteht als `requested` mit der Adresse der handelnden Person, Pflichtangaben und widersprüchliche Zeiten ⇒ 22023, Obergrenze fünf als **Schwelle** (sechste ohne Begründung ⇒ P0001 `shuttle_limit` mit Zahl im detail, mit Begründung geht sie durch und wird gemerkt), Freigabe nur Speaker-Team, zweites Freigeben ⇒ P0001 `shuttle_not_open`, Stornieren nimmt genau eine Fahrt aus der Zählung (nach sechs offenen greift die Schwelle nach der ersten Stornierung noch, erst die zweite öffnet), eigene Liste zeigt nur eigene Fahrten, Admin-Liste ohne Rolle ⇒ 42501, Trigger haengt am vorhandenen `set_updated_at()`, Tabelle ohne Grants für anon/authenticated |
| `v6_session_tech.sql` | 0117 (`20260918141037`) | Technik am Slot: fremde Session ⇒ 42501, Freitext bleibt Freitext, unbekannter Schlüssel ⇒ 22023 `invalid_tech_key` mit dem Schlüssel im detail, über 500 Zeichen ⇒ `tech_too_long`, leere Werte entfallen statt als `""` zu bleiben, Vorbelegung aus `tech_rider` nur beim ersten Schreiben, Assistenz darf schreiben, `my_sessions` liefert `tech` **und** hat keine der alten Spalten verloren (Lehre aus 0099), Helfer `session_tech_keys` für authenticated gesperrt |
| `v6_regie_tech.sql` | 0121 (20260918143817) | Technik-Ansage im Regieplan: `regie_view` traegt nach drop + create die alten Spalten **und** `tech` hinten (Lehre aus 0099), `authenticated` darf ausfuehren und `anon` nicht (der drop nimmt die Grants mit), ohne Zustaendigkeit 42501 und fremde Buehne ebenso (Regel aus 0101 unveraendert), ein Cue mit Session zeigt die Ansage, einer ohne Session ein leeres Objekt statt null, und `mic_assignments` bleibt die Disposition der Regie — die Ansage steht daneben, sie ersetzt sie nicht |
| `v6_reception.sql` | 0125 (20260918151535) | Speaker Reception: ohne `reception_eligible` ist die Liste **leer** (keine Meldung, die verraet, dass es eine gibt) und die Anmeldung wird abgewiesen; mit Kennzeichen erscheint nur die veroeffentlichte; Zusage zaehlt **Plaetze** (Person plus Begleitung); ungueltiger Status und zu viele Begleitungen ⇒ 22023; Obergrenze ⇒ P0001 `reception_full` mit freien Plaetzen im detail; die **eigene** bisherige Zusage zaehlt beim Aendern nicht mit (sonst liesse sich eine Begleitung nie nachtragen); eine Absage gibt Plaetze frei und bleibt als Zeile stehen; nach der Frist ⇒ P0001 `reception_closed`; Team-Sichten ohne Rolle ⇒ 42501, mit Rolle stimmen Zahlen, Gaesteliste und der Entwurf; Loeschen mit Zusagen ⇒ 22023 `has_guests`, ohne geht es; Helfer und beide Tabellen ohne Grants |
| `v6_ai_rate_limit.sql` | 0126 (20260918204334) | Zaehler fuer Assistenten: leere und zu lange Art ⇒ 22023 `invalid_kind`; der erste Aufruf zaehlt und meldet den Rest; ueber der Grenze ⇒ P0001 `rate_limited` mit der Grenze im detail; **zwei Arten, zwei Toepfe** (der Grund fuer die eigene Tabelle: wer das Wiki ausreizt, hat den Titel-Assistenten noch); `kb_rate_limit` bleibt unberuehrt; das Aufraeumen nimmt nur Fenster aelter als 24 Stunden und laesst junge stehen; Tabelle ohne Grants, Aufraeumen fuer authenticated gesperrt |
| `v6_branding.sql` | 0116 (`20260918122700`) | Die Vorlage `digital_branding` hat jetzt eine Frist (vorher `{}` — derselbe stille Ausfall wie bei der Challenge) und zeigt auf `booth_changes_until`; die Masse 1920 x 1080 stehen in beiden Sprachen in der Beschreibung; Dateiregeln nehmen PDF, PNG und JPG, **kein** SVG und kein ZIP; eine neue Pflicht bekommt die Frist, eine bestehende wurde nachgezogen, und mit Frist wird sie ueberfaellig. |
| `v6_format_sessions_flaeche.sql` | 0140 (20260922095138) | `partner_format_sessions` liefert `stage_id` und `event_day_id`, und beide zeigen auf die Zeilen, an denen der Slot haengt. Der Test legt **zwei Tische mit demselben Namen** an — genau dafuer gibt es die Spalte; ueber `stage_name` waeren die Gespraeche des einen beim anderen gelandet, und eine eigene Zeile belegt, dass die Namen wirklich gleich sind (sonst pruefte der Schritt nichts). Die bestehenden Spalten stehen unveraendert vorn (an der Reihenfolge der Rueckgabe geprueft, nicht an Werten); fremde Organisation 42501. |
| `v6_zusatztickets.sql` | Vorschlag ohne Nummer | Eine Ticketanfrage traegt Anzahl und Tickettyp **strukturiert** (nicht nur im Freitext); die Mail nutzt die eigene Vorlage `ticket_request_received` statt der Messeshop-Vorlage und erreicht jemanden (Vorbedingung: ein Empfaenger existiert, sonst waere „keine Messeshop-Mail" gruen ohne jede Mail); die Vorlage gibt es DE und EN mit Anzahl, Typ und Link zu den Kontingenten; der Partner sieht Anfrage, Stand und Antwort in `my_ticket_requests`, eine gewoehnliche Messeshop-Anfrage dort nicht; der CHECK laesst Typ ohne Anzahl nicht zu (erster Lauf: doch — `quantity > 0` ist bei NULL selbst NULL); `ticket_requests_admin` nur fuers Partner-Team, offene zuerst; fremde Organisation 42501, `anon` gesperrt. |
| `v6_kontakte_bearbeiten_cc.sql` | Vorschlag ohne Nummer | Pflegerecht nur für selbst angelegte Personen ohne Login (`partner_contacts.editable`; vorhandene Person und Hauptkontakt nicht); Adresse korrigieren zieht die **wartende** Einladung mit um, eine verschickte wird neu gestellt und `invited_at` rückt nach; Namenswechsel bei vorhandener Person und nach dem ersten Login ⇒ `contact_not_editable` (Vorbedingung: vorher hatte sie das Recht), Position geht, unveränderte Werte sind kein Fehler; Position Pflicht, fremde Adresse `email_in_use`, kaputte `invalid_email`, Rollen über `set_contact_roles`; **Kopie** an einer echten Partner-Mail (Rückweisung): nur am Hauptkontakt, nur reine CC-Kontakte, alle fünf Mail-Funktionen hängen sie an, **keine Adresse im Protokoll** (`meta.cc_person_ids`), `mail_cc_recipients` löst beim Versand auf und lässt Gelöschte weg; Einladungen nie mit Kopie; `partner_mail_cc` und `mail_cc_recipients` für `authenticated` gesperrt, `service_role` darf auflösen; Rollenbezeichnungen; Audit nennt Felder, keine Adresse. |
| `v6_eure_daten.sql` | Vorschlag ohne Nummer | Adresszusatz speichert der Partner (getrimmt) und `partner_overview` liefert ihn; eine mitgeschickte Kundennummer schreibt `update_partner_onboarding` **nicht** (Vorbedingung: vorher leer); `set_org_customer_number` als Partner 42501, als Team gesetzt und für den Partner sichtbar, Audit mit alt/neu, derselbe Wert kein zweites Audit, leer löscht, mehr als 40 Zeichen `too_long`, dieselbe Nummer an einer zweiten Firma `customer_number_taken`; `shop_invoice_candidates` behält die alten Spalten und liefert den Adresszusatz einer Firma mit abgeschlossener Bestellung als Wert; `anon` gesperrt. |
| `v6_partner_belege.sql` | Vorschlag ohne Nummer | `my_partner_documents` ordnet Belege ein: Angebot `angebot`, gewöhnliche Rechnung `rechnung`, Rechnung mit Verweis aus einer Messeshop-Bestellung **dieser** Organisation `messeshop_rechnung`; der Verweis einer fremden Bestellung auf dieselbe SevDesk-Id zählt nicht (Vorbedingung: die fremde Referenz existiert); Uploads stehen nicht in der Belegliste; fremde Organisation 42501, `anon` gesperrt. |
| `v6_rueckgabegrund.sql` | Vorschlag ohne Nummer | Die Rückgabe durch die Programmleitung hält den Grund fest (getrimmt, mit Person und Zeit) und der Partner liest ihn in `partner_format_sessions` (Vorbedingung: vorher keiner); eine zweite Rückgabe ersetzt die erste; eine Rückgabe ohne Grund bleibt `fields_required` und ändert nichts; die Freigabe löscht den Grund (Vorbedingung: die Session erfüllt die Freigabe); der Grund steht **nicht** an `session` und `partner_session_return` hat RLS und keine Grants für `authenticated`/`anon`; `partner_format_sessions` behält die alten Spalten vorn, zwei neue hinten, `authenticated` darf ausführen, `anon` nicht; fremde Organisation 42501. |
| `v6_logo_druck_einwilligung.sql` | Vorschlag ohne Nummer | Die Vorlage `logo_vector` nimmt nur noch `svg` und `eps`, und die **zweite** Stelle im Code zieht mit: `register_partner_asset` weist `.ai` ab und laesst `.svg` durch (ein Test nur auf Abweisung waere auch gruen, wenn die Funktion alles abwiese; das Objekt muss dafuer im Bucket liegen, sonst kommt `object_not_found` und der Schritt belegt nichts). Ohne Einwilligung steht NULL in der Spalte — kein Druck auf die Wand; das Erteilen vermerkt Person und Zeitpunkt und schreibt ins Audit; eine **zweite** Bestaetigung datiert nicht um (der Nachweis sagt, wann erteilt wurde, nicht wann zuletzt geklickt); der Widerruf setzt beide Felder zurueck; fremde Organisation 42501, `anon` gesperrt. |
| `v6_partner_speaker_pflege.sql` | 0139 (20260921121115) | Einen **selbst eingetragenen** Speaker zeigt `partner_speakers` mit Pflegerecht und Programmfeldern, und der Partner pflegt Name, Titel, Position und Biografie in beiden Tabellen; ein Feld ausserhalb der Whitelist (Telefon) ⇒ P0001 `not_editable`; eine nur per Mailadresse **zugeordnete bestehende** Person gibt kein Pflegerecht und ausser Name und Status auch keine Daten heraus (Vorbedingung belegt, dass sie vorher kein Speaker-Profil hatte — sonst bliebe der Zweig ungeprueft); nach dem ersten Login faellt beides weg (P0001 `speaker_not_editable`, Anzeige ohne Details; das Konto wird geliehen, nicht erfunden); fremde Organisation 42501; `anon` gesperrt. |
| `v6_wiki_hackathon.sql` | 0129 (20260921110522) | Sechs Hackathon-Artikel fuer Partner, alle als `draft` und jahresunabhaengig; **keine Telefonnummer und keine Mailadresse im Bestand** (geprueft auf Muster, nicht auf die eine bekannte Nummer — sonst faende der Test nur, was er schon kennt); keine Firmennamen aus den Challenges der Vorjahre; keine Daten, Orte oder Links von 2026; die sieben Grafikanforderungen der Rueckwand vollstaendig samt Endformat 1610 × 2790 mm (Konrad 21.09.); als Entwurf im Portal unsichtbar und nach dem Freischalten sichtbar (mit ausdruecklicher Vorbedingung, damit „unsichtbar" nicht wie „kein Zugriff" aussieht); ein zweiter Lauf legt nichts doppelt an. |
| `v6_hackathon_backdrop.sql` | 0117 (20260921111140) | Die Pflicht `hackathon_backdrop` haengt an der **Kategorie** `hackathon`, nicht an einer SKU — wer Challenge, Stand oder Impuls gebucht hat, bekommt die Flaeche; Konrads sieben Grafikanforderungen stehen vollstaendig in beiden Sprachen, samt Endformat 1610 × 2790 mm (Konrad 21.09.); geprueft wird nur, was pruefbar ist (PDF ja, PNG nein) — Masse und Seitenverhaeltnis sieht das Team bei der Freigabe, eine behauptete Pruefung waere schlimmer als keine; Frist ist die der Challenge (18.03.2027), und mit Frist wird die Pflicht ueberfaellig; ein Partner ohne Hackathon bekommt sie nicht, und die Rueckwand des **Messestands** bleibt unberuehrt (zwei Flaechen). |
| `v6_speaker_kontakt.sql` | 0127 (20260921101742) | Kontakt ohne Portalzugang: ohne Einwilligung ⇒ 22023 `speaker_contact_consent_required` (die Daten gehoeren einem Dritten), mit Einwilligung steht er am Profil, erfundene Kontaktart ⇒ `invalid_contact_kind`; eine reine Namenskorrektur geht **ohne** erneutes Mitschicken der Einwilligung durch (Lehre aus 0114); wer alle Felder leert, nimmt Einwilligungsdatum und Kontaktart mit; `my_speaker_profile` und `speaker_detail` liefern ihn, ohne Kontakt steht dort null; die Assistenz darf pflegen; der CHECK greift auch an der Funktion vorbei (23514); `anonymize_person` leert alle sechs Felder **und** traegt die Adresse bewusst **nicht** in die Sperrliste ein |
| `v6_ea1_aussteller_level.sql` | 0135 (20260921112508) | EA1 · Aussteller-Level aus den Produkten: erfundenes Level am Produkt 22023 `invalid_sponsoring_level`, leerer Text nimmt es wieder weg; die Startbelegung sitzt (zwoelf Pakete, „Standbuehne" bewusst offen); ein gebuchtes Paket setzt das Level mit Quelle `product`, **das beste Level gewinnt** (der Premium-Aufpreis schlaegt das General-Paket), eine stornierte Zeile zaehlt nicht; die Kategorien nennen nur die gebuchten **Pakete** in Vokabular-Reihenfolge — ein Barhocker macht niemanden zum Hackathon-Partner; ohne gebuchtes Produkt faellt die Ableitung auf den HubSpot-Freitext zurueck (Quelle `hubspot`, die alten Spalten daneben unveraendert), ohne beides ist das Level null und die Kategorienliste ein **leeres Array statt null**; `event_app_exhibitors` bleibt ohne Partner-Team zu (42501) und liest im Servicekontext weiter (Lehre 0120); der Verzeichniseintrag aus 0130 steht, die Vokabularpflege zaehlt das Produkt mit. |
| `v6_oberflaechen_rabatt_staende.sql` | 0137 (20260921114217) | Oberflaechen zu 0123 und 0124: alle vier Listen ohne Recht zu (42501); `ticket_allocations_admin` nennt jetzt Rabattsatz **und** Teilnahme (ohne beides standen die 100er- und die 50er-Zeile derselben Org ununterscheidbar nebeneinander) und hat keine der sechzehn alten Spalten verloren (Lehre aus 0099); **die Menge einer 100er-Zeile ist gesperrt** (P0001 `derived_allocation`) — der naechste Abgleich haette die Handarbeit stillschweigend ueberschrieben —, dieselbe Menge noch einmal zu schicken ist keine Aenderung und geht durch, Status, Coupon und Notiz bleiben an der 100er offen (Reparaturweg fuer vivenu), an der 50er ist die Menge frei; `booth_day_plan` liefert die `assignment_id`, mit der die Oberflaeche loest, und sie passt (`remove_booth_assignment` nimmt genau diese Belegung weg); `booths_free` zeigt einen unbelegten Stand, verschweigt den fuer beide Tage vergebenen und zaehlt Tagesbelegungen mit; `org_editions_picker` nennt Name und Art der aktiven Teilnahmen, **keine Kontaktdaten** und keine inaktive Organisation. Der Test nimmt die vorhandenen Veranstaltungstage und legt nur an, was fehlt. |
| `v6_branche_aussteller.sql` | 0138 (20260921120434) | Branche des Ausstellers: ohne Pflegerecht 42501, erfundene Branche 22023 `invalid_industry`; setzen und mit leerem Text wieder wegnehmen; eine Namenskorrektur **ohne** das Feld laesst sie stehen (Lehre aus 0114); `event_app_exhibitors` und `partner_overview` geben sie mit; **ein deaktivierter Begriff geht nicht mehr hinaus** — in der Spalte bleibt er stehen, die Ausstellerliste liefert null, damit Swapcard behaelt statt eine tote Auswahl zu bekommen; die vierzehn Schluessel des Swapcard-Feldes stehen im Vokabular (0 fehlen) und die 27 Ausgabespalten aus 0135 sind alle noch da (Lehre aus 0099); Verzeichniseintrag steht und die Vokabularpflege zaehlt die Organisation mit. |
| `v6_sponsoren_kategorien.sql` | 0139 (Vorschlag) | Logo-Wand: die sieben Kategorien des 27er-Events stehen im Vokabular und Konrads Zuordnung sitzt Stufe fuer Stufe; **niemand rutscht durch** — wer nur eine Masterclass gebucht hat oder gar nichts, bekommt `official_partner` statt null, ebenso `main_stage_loge` (bewusst nicht zugeordnet) und eine deaktivierte Kategorie; die Standflaeche bestimmt die Kategorie und das **beste** Level gewinnt (Premium-Aufpreis schlaegt General); Signature ⇒ Presenting, Start-Up ⇒ Startup, Gemeinschaftsstand ⇒ Official; die Zuordnung laesst sich ueber die Vokabularpflege umhaengen, ohne Migration; die 28 Ausgabespalten aus 0138 sind alle noch da. Dazu der Fund am Fremdschluessel: `set_event_app_ref` schrieb **immer** `object_type = 'exhibitor'` — ein Sponsorenlauf haette die Standreferenz derselben Teilnahme ueberschrieben; jetzt stehen beide nebeneinander, eine erfundene Art wird mit 22023 abgewiesen. |
| `v6_ea2_speaker_eventapp.sql` | 0136 (20260923111232) | EA2 · Speaker in die Event-App: die Liste ist ohne Speaker-/Partner-Team zu (42501) und liest im Servicekontext weiter (der Lauf haengt daran, Lehre 0120); nur **bestaetigte** Profile stehen drin, abgesagte und `contacted` nicht; **die Zusage ist die Grundlage, keine eigene Einwilligung** (Konrad 22.09.) — ein bestaetigtes Profil steht auch ohne jede Einwilligung in der Liste, und eine widerrufene `event_app`-Einwilligung aendert daran nichts; Name, Titel, Organisation (Freitext schlaegt Verknuepfung) und E-Mail kommen mit, das Foto mit Pfad, **Kennung und Medientyp** (daraus baut der Lauf die oeffentliche Kopie); der oeffentliche Bucket `speaker-photos` steht, ist lesbar und hat **keine** Policy fuer anon oder authenticated; der Personenverweis gehoert dem Server (42501 aus dem angemeldeten Kontext), ein zweiter Aufruf **aendert** statt zu verdoppeln, unbekannte Person P0002, fremdes System 22023, Personen- und Ausstellerverweis nebeneinander; `person` wird nicht als Ganzes herausgegeben. Dazu der Fund: **`consent_current` entschied bei gleichem `granted_at` zufaellig** — Schritt 03c setzt bewusst denselben Zeitstempel; der Fund betrifft **alle** Einwilligungen (Newsletter, Fotofreigabe, ab EA4 die Event-App). |
| `v6_person_portraet.sql` | Vorschlag (TAL-012) | Porträt im Teilnehmer-Profil: Bucket `person-photos` privat, 5 MB, drei Bildtypen; die Pfadregel lässt die Person nur `<eigene person_id>/<datei>` schreiben und lesen, fremde Pfade, Unterordner, leere Dateinamen und Unsinn sind `false` (nicht NULL); das Team liest fremde Porträts, schreibt sie aber nicht; `set_my_photo` weist fremde Pfade (22023 `path_mismatch`) und fehlende Objekte (P0002 `object_not_found`) ab, meldet beim Ersetzen und Entfernen das alte Bild in `storage_purge_queue`; `anonymize_person` meldet das Porträt an und leert `photo_path`; kein Spalten-Update-Grant auf `photo_path`, anon ohne EXECUTE. Probelauf 24.09.2026: 11/11 grün. |

Simulation eines eingeloggten Nutzers innerhalb der Transaktion:
`perform set_config('request.jwt.claims', json_build_object('sub', <auth_uid>, 'role', 'authenticated', 'email', <email>)::text, true);`

Nach jedem Umzug/Neuaufbau alle vier Dateien laufen lassen (Runbook `supabase-umzug.md`, Schritt „Prüfen"). Zusätzlich die Realtime-Probe `node --env-file=.env.local scripts/realtime-probe.mjs` (prüft Policies auf `realtime.messages` mit echtem Realtime-Dienst statt nur in SQL).
| `v6_consent_current_invoker.sql` | 0150 | `consent_current` folgt der RLS von `consent_record`: authenticated ohne Person sieht 0 Zeilen (vorher alle) |
| `v6_ip_hash_streichen.sql` | 0158 | `ip_hash` in `audit_log`/`consent_record` weg; `consent_current` und `audit_log` lesen weiter |
| `v6_profilfelder.sql` | 0161 (20260924135111) | Profilfelder: neun Vokabulare mit der erwarteten Anzahl; die sieben neuen Felder per Spalten-Grant schreibbar, `cv_path` nicht; der Vokabular-Wächter weist erfundene Werte mit 22023 `invalid_vocab_value` (detail = Spalte) ab, ein **später deaktivierter** Begriff blockiert das Speichern anderer Felder nicht; Abschlussjahr außerhalb 1950–2100 ⇒ 23514; `person_interest` nimmt die vier neuen Listen, eine fremde nicht; `person_language` als `authenticated`: eigene Zeile ja, fremde Person von RLS verweigert, unbekanntes Niveau am Fremdschlüssel; Lebenslauf-Pfadregel; der **gastgebende Partner liest nur mit `consent_share`** und schreibt nie; `set_my_cv` mit Pfad-, Objekt- und Löschlisten-Prüfung; `anonymize_person` räumt Lebenslauf, Freitexte und Sprachen weg, die Porträt-Zeile wirkt weiter. Probelauf 24.09.2026: 12/12 grün. |
| `v6_next_up.sql` | Vorschlag (TAL-006) | „Next Up“ auf Home: ohne Rolle lesen ja, Admin-Liste und Schreiben 42501; `marketing_team` und `area_lead_talent` pflegen, `area_lead_partner` nicht; Audit je Änderung; Titel Pflicht; Links nur `https://` oder Portalpfad — `javascript:`, `http:`, `//host`, `/\host` und Leerzeichen ⇒ 22023 `invalid_link_url`; Home zeigt nur aktive Einträge im Fenster; unbekannte Id P0002; Tabelle ohne Grants für anon/authenticated. Probelauf 24.09.2026: 9/9 grün. |
| `v6_profilfelder.sql` | Vorschlag (TAL-013) | Profilfelder: neun Vokabulare mit der erwarteten Anzahl; die sieben neuen Felder per Spalten-Grant schreibbar, `cv_path` nicht; der Vokabular-Wächter weist erfundene Werte mit 22023 `invalid_vocab_value` (detail = Spalte) ab, ein **später deaktivierter** Begriff blockiert das Speichern anderer Felder nicht; Abschlussjahr außerhalb 1950–2100 ⇒ 23514; `person_interest` nimmt die vier neuen Listen, eine fremde nicht; `person_language` als `authenticated`: eigene Zeile ja, fremde Person von RLS verweigert, unbekanntes Niveau am Fremdschlüssel; Lebenslauf-Pfadregel; der **gastgebende Partner liest nur mit `consent_share`** und schreibt nie; `set_my_cv` mit Pfad-, Objekt- und Löschlisten-Prüfung; `anonymize_person` räumt Lebenslauf, Freitexte und Sprachen weg, die Porträt-Zeile wirkt weiter. Probelauf 24.09.2026: 12/12 grün. |
| `v6_folien_teilnehmende.sql` | Vorschlag (TAL-001) | Folien nach dem Summit: mit Ticket der Edition erscheint **genau** die eine Datei, die alle Bedingungen erfüllt (Vorbedingung); ohne Ticket oder mit Ticket einer anderen Edition leer; storniertes Ticket zählt nicht, `checked_in` zählt; ältere Fassung, Foto mit Freigabe, Entwurfs-Session und zukünftiger Slot erscheinen nie; zurückgenommene Freigabe verschwindet sofort; Speaker-Name als Text, feste Spaltenliste; ohne Person 28000; anon ohne EXECUTE. Probelauf 24.09.2026: 7/7 grün (Wegwerf-Editionen). |
| `v6_luma_events.sql` | Vorschlag (TAL-007) | Community-Events aus Luma: Sync-Funktionen nur im Serverkontext (42501 angemeldet, EXECUTE entzogen); Event anlegen und nachziehen mit **einer** `external_ref` und Datum in Berliner Zeit; Gast ohne Person ⇒ `matched: false`, keine Zeile; Portal-Anmeldung ohne Gast-Id plus späterer Abgleich ⇒ **eine** Zeile mit nachgetragener Gast-Id; Check-in ⇒ attended, Warteliste ⇒ waitlisted; unbekannter Status 22023, unbekanntes Event P0002; `my_community_registrations` nur eigene; keine Portal-Mail. Probelauf 24.09.2026: 8/8 grün. |
| `v6_community_events_admin.sql` | Vorschlag (TAL-008) | Admin-Sicht Community-Events: ohne Rolle 42501, ohne Person 28000; `area_lead_talent` sieht Event und Zähler (angemeldet, teilgenommen, gesamt), `marketing_team` die Teilnehmenden mit Name und Status, **ohne E-Mail** in der Spaltenliste; `area_lead_partner` nicht; Events ohne Luma-Verweis erscheinen nicht; `talent_team` (0162) sieht die Liste. Probelauf 25.09.2026: 6/6 grün. |
| `v6_my_tickets.sql` | Vorschlag (TAL-015) | Eigene Tickets: das gültige Ticket der Edition erscheint mit Pass-Typ, Code und Editionsname (Vorbedingung); ein Ticket, das nur über die Käufer-Mail an der Person hängt, erscheint nicht; storniert nie, `requested` ohne Code; Wallet nur mit vivenu-Id **und** Secret; Spaltenliste ohne Preis, Mail, Notiz, Secret; ohne Person 28000, anon ohne EXECUTE. Probelauf 25.09.2026: 5/5 grün. |
| `v6_pronomen_cv_url.sql` | 0171 (SPK-066, TAL-013 B3) | Spalten `person.pronouns`/`cv_url` weg; `my_speaker_profile` ohne Schlüssel `pronouns`; `pronouns` im Payload von `update_my_speaker_profile` stumm ignoriert, `title` weiter geschrieben; `anonymize_person` läuft (Wegwerf-Person); Grants: anon ohne EXECUTE, authenticated ohne EXECUTE auf `anonymize_person` |
| `v6_format_details_public.sql` | Vorschlag (TAL-002/003) | Format-Details für Teilnehmende: Side-Event mit Ort, Gastgeber und Bildpfad (abgelehntes Bild fehlt); Interview Table mit Stelle, Link, Modus, gesuchten Profilen; Company Tour über `company_tour.session_id` mit Sammelpunkt und Stopps — **ohne** `contact_*` und interne Notiz; Entwurf und Keynote nie; ohne Person 28000, anon ohne EXECUTE; `company_tour.session_id` eindeutig. Probelauf 25.09.2026: 5/5 grün. |
| `v6_session_inhalt_status.sql` | Vorschlag (SPK-050) | Status der Session-Inhalte: `my_sessions` liefert an `latest_submission` den Schlüssel `is_change` — eine einzelne übernommene Einreichung ist keine Änderung (Vorbedingung), eine neue nach einer übernommenen ist eine (auch nach deren Übernahme), eine erste offene nicht; `tech`, `co_speakers` und Titel kommen unverändert mit; EXECUTE für `authenticated` bleibt. Gegen die Live-Fassung schlagen 01–04 fehl |
| `v6_reisemittel.sql` | Vorschlag (SPK-059) | Verkehrsmittel: „Fernbus“ und „Wohnt in Hamburg“ sind stillgelegt, nicht gelöscht — beide Begriffe gibt es weiter (Vorbedingung) und behalten ihr Label; Bahn, Flug, Auto und Anders bleiben angeboten. Gegen die Live-Fassung schlägt 02 fehl |
| `v6_eine_sprache.sql` | Vorschlag (SPK-052) | Eine Sprache je Session: die zwei DEMO-Sessions gibt es (Vorbedingung) und stehen auf `de`, keine Session mehr auf `mixed`; das Constraint weist `mixed` ab und nimmt `en`; `submit_session_content` und `partner_update_session` antworten auf `mixed` mit 22023 `invalid_language`, `partner_update_session` auch auf `language: null` (vorher 23502); `mixed` im Vokabular stillgelegt, `de`/`en` aktiv; EXECUTE bleibt. Gegen die Live-Fassung schlagen 02, 03, 05, 06, 06b und 07 fehl |
| `v6_standbuehne_regeln.sql` | Vorschlag (PART-079, PART-080) | **Fenster seit PART-090 überholt** (`v6_standbuehne_oeffnungszeiten`): die Schritte 01, 02, 04 und 06 prüfen die alte Regel und werden danach rot. Standbühne: ein Partner (Rolle `standbuehne_editor`) legt keinen Slot vor Öffnung + 90 Minuten und keinen nach 19:00 an — P0001 `outside_partner_window` mit dem Fenster im detail; `move_slot` hält dasselbe Fenster, innerhalb geht es (Vorbedingung); Admin darf abweichen; das Fenster bindet nur auf `partner_booth`, ohne Tagesrahmen nur das Ende 19:00; `partner_request_publish` prüft Titel DE/EN und Beschreibung vorher (22023 `fields_required`), setzt `review` und `partner_org_id`, die Session steht danach in `partner_sessions_pending`; zweiter Aufruf ändert nichts; `partner_withdraw_publish` setzt `draft`; fremde Standbühne 42501; anon ohne EXECUTE, Helfer intern. Probelauf 24.09.2026: 13/13 grün. |
| `v6_programm_lesen.sql` | Vorschlag (LEAD-032) | Programm lesen mit echtem Rollenwechsel (`set local role authenticated`): Partner mit Standbühnen-Rolle liest Veröffentlichtes und den Entwurf der eigenen Organisation, **nicht** Entwurf, Slot und Speaker eines anderen Partners; Stage Lead liest Entwurf, Slot und Speaker der eigenen Bühne, nicht die fremden, das Board (`programme_board`) zeigt nur die eigene Bühne; Programm-Team der Edition liest alles; Partner-Kontakt ohne Standbühne liest den eigenen Entwurf, keinen Board-Kanal; Standbühnen-Editor ohne Kontaktrolle liest den Entwurf seiner Organisation ohne Slot und die Session samt Slot auf der eigenen Standbühne, B's nicht; EXECUTE an allen Prädikaten der Policies. Gegen die Live-Fassung schlagen 03–06, 08–14 und 16 fehl |
| `v6_mail_vars_intern.sql` | 0181 (F11) | `session_mail_vars` ohne EXECUTE für anon/authenticated, Aufruf als authenticated 42501, Definer-Aufrufer unverändert |
| `sicherheit_rollenkonten.sql` | Security-Check Teil 2 (dauerhaft) | Rollen-Probe mit echtem Rollenwechsel: Externe ohne Rolle sehen keine fremden Zeilen in Tabellen mit `person_id`/`org_id`, keine fremden Entwürfe, keine sensiblen Tabellen; Partner-Kontakt sieht keine fremden Organisationen — nach jeder Policy-/Grant-Migration laufen lassen |
| `v6_regie_anweisungen.sql` | Vorschlag (LEAD-031) | Regieanweisungen: der Stage Lead sieht den Slot seiner Bühne in `lead_regie_slots` (Vorbedingung) und setzt die fünf Anweisungen — es entsteht **ein** Cue mit den Zeiten des Slots, Mikrofon und Medien als `{text}`; ein zweiter Aufruf ändert nur den mitgeschickten Schlüssel; Zeiten sind nicht schreibbar (P0001 `not_editable`); `upsert_regie_cue` und `delete_regie_cue` weisen ihn ab (42501); ohne Rolle nichts; Produktion überschreibt und verschiebt; Programm-Team der Edition schreibt den Plan weiter; EXECUTE an den neuen Funktionen. Gegen die Live-Fassung: 05/06 ERLAUBT (BUG), die neuen Funktionen fehlen |
| `v6_testdaten_person.sql` | Vorschlag (Regel „Konrads Konto sieht alles“, LEAD-028) | Test-Personen fürs Testdaten-Skript: ohne Konto (service_role) entsteht eine TEST-Person mit genau einer primären E-Mail (der verzögerte Trigger ist zufrieden), ein zweiter Aufruf gibt dieselbe zurück; mit Konto 42501; Vorname nicht TEST oder Adresse ohne `+zztest`: 22023 `not_test_data`; kein EXECUTE für authenticated und anon |
| `v6_lead039_einordnung.sql` | Vorschlag (LEAD-039 Schnitt 1) | Einordnung der Speaker-Pipeline mit echtem Rollenwechsel: der Owner schreibt die sieben Felder und die Bühnen in Frage (doppelte Bühne zählt einmal) und liest sie über `manager_speakers` zurück — dort bleibt `internal_notes`; unbekannte Kategorie, `@` in „Kontakt via“, Text über 300 Zeichen und eine Bühne außerhalb der Edition werden abgewiesen, ohne etwas zu schreiben; der Stage Lead der Bühne mit Session darf ändern (K-36 F1), eine Person ohne Rolle nichts (42501, keine Zeile unter RLS); der Speaker selbst liest weder Profilzeile noch Bühnen, `my_speaker_profile` enthält keines der Felder; ein stillgelegter Begriff sperrt das Profil nicht; `anonymize_person` leert alles; EXECUTE nur für authenticated, kein Schreibrecht auf die Tabelle. Gegen live: scheitert (Tabelle fehlt) |
| `v6_moderation_stage_leads.sql` | Vorschlag (LEAD-042) | Moderationssuche im Board mit echtem Rollenwechsel: ohne `p_moderation` nur Speaker der Edition wie bisher; mit `p_moderation` dazu Stage Leads der Edition (`is_stage_lead`), abgelaufene Rollen und globale Manager nicht, wer Speaker und Stage Lead ist nur einmal (als Speaker); ohne Rolle 42501; alte Signatur entfernt, neue für authenticated. Gegen live: 02–06 scheitern (Parameter fehlt) |
| `v6_standbuehnen_gaeste.sql` | Vorschlag (PART-081) | Standbühnen-Gäste: ohne Einwilligung kein Gast; ein Partner legt einen Gast an (zugesagt mit `confirmed_at`, Einwilligung mit Zeitstempel, keine Lounge) **ohne** Rolle `speaker` und **ohne** Freiticket — Vorbedingung: ein regulär zugesagter Speaker bekommt über denselben Trigger eins; CHECK gegen Lounge (23514); `invite_speaker` und `partner_add_speaker` weisen Gäste ab (`stage_guest`), `upsert_speaker` gibt keine Rolle, `speaker_ticket_create` `not_eligible`; Swapcard-Export nur mit veröffentlichter Session am Slot; `partner_speakers` ohne Gäste; `already_speaker`; fremde Organisation 42501 (lesen, ändern, entfernen, zuordnen, Dateien), fremde Standbühne 42501; gebuchter Talk der eigenen Organisation zuordenbar, der einer fremden nicht (PART-088); Porträt-Pfad nur Foto eigener Gäste; bearbeiten, entfernen (Person bleibt); anon ohne EXECUTE, Helfer intern. Probelauf 25.09.2026: 17/17 grün. |
| `v6_lead039_verlauf.sql` | Vorschlag (LEAD-039 Schnitt 2, LEAD-025/027) | Verlauf der Speaker-Pipeline mit echtem Rollenwechsel: der Owner legt Notiz und Aufgabe an, `manager_speakers` zeigt offene Aufgaben, nächsten Schritt und letzte Aktivität; Frist, Text, Art, Länge und Zuständige werden geprüft; der Stage Lead der Bühne liest den ganzen Verlauf und schreibt eigene Einträge, fremde nicht ändern, löschen oder abhaken; Abhaken setzt `done_at` und ändert die Zähler, Notizen lassen sich nicht abhaken; ohne Rolle und als Speaker selbst nichts; das Team löscht und sieht die Übersicht, der Stage Lead die Übersicht nicht; `anonymize_person` leert den Verlauf; Rechte und alte Spalten von `manager_speakers`. Gegen live: scheitert (Tabelle fehlt) |
| `v6_standpakete_eigenproduktion.sql` | Vorschlag (PART-085, PART-086) | Standpakete: Eigenproduktion General (I-84869) und Premium (I-36848) mit Stromanschluss 230V ×1, Teppich 9 bzw. 18 qm, Standbeleuchtung ×1 und nichts sonst; „Agency Area Partner“ (I-40175) stillgelegt, nicht gelöscht; `booth_packages` aus Partnersicht ohne die Initiativen-Stände (INI-STAND-1T/2T), mit Standbühne und All-Inclusive-Stand (Vorbedingung) und der Stückliste der Eigenproduktion; zweiter Lauf ohne Dubletten. Probelauf 25.09.2026: 4/4 grün. |
| `v6_gaeste_team_listen.sql` | Vorschlag (SPK-070) | Standbühnen-Gäste mit echtem Rollenwechsel: `manager_speakers` kennzeichnet sie (`stage_guest`) und behält die alten Spalten; `unassigned_speakers` führt sie nicht als „ohne Betreuung“; `send_presentation_reminders` erinnert den Speaker, nicht den Gast; das erste Bühnenfoto schickt „Bühnenfotos bereit“ nur an den Speaker mit Profil, nicht an Gast und Moderation ohne Profil (LEAD-042); `speaker_leads_admin` zählt einen übernommenen Gast nicht beim Lead mit; `speaker_travel_list` führt ihn nicht in der Anreise. Gegen live: alle sechs scheitern (Spalte fehlt, Gast in den Listen, Mails an Gast und Moderation, Gast beim Lead gezählt) |
| `v6_standbuehne_oeffnungszeiten.sql` | Vorschlag (PART-090, ersetzt das Fenster aus PART-079) | Standbühne: das Fenster sind die Öffnungszeiten der Bühne — ein Partner legt direkt zur Öffnung und bis zum Schluss an (beides war mit 0179 gesperrt), davor und danach P0001 `outside_partner_window` mit `von–bis` im detail; `move_slot` hält dasselbe Fenster; ohne `stage_day`-Zeile gilt der Programmrahmen des Tages, eine Zeile nur mit Kontingent fällt je Grenze darauf zurück; nur ein Beginn und kein Tagesrahmen: Ende 24:00, das detail ist nie NULL (kein 22004 aus dem `raise` der Aufrufer), ganz ohne Rahmen keine Grenze; Admin bindet nicht; Interview Table nicht betroffen; Funktion bleibt SECURITY DEFINER mit festem search_path und intern. Wegwerf-Veranstaltung mit eigenen Tagen, alles zurückgerollt. Probelauf 25.09.2026: 14/14 grün; Gegenprobe ohne Migration: 10 Schritte rot. |
| `v6_speaker_mail_weiche.sql` | Vorschlag (PART-091), braucht `v6_talk_speaker_zugang` davor | Mail-Weiche mit echtem Rollenwechsel: `speaker_mail_recipient` (Kontakt mit Zugang, sonst Rückfall auf die Speakerin); Präsentations-Erinnerung und „Bühnenfotos bereit“ gehen je Empfänger einmal und nennen beide verwalteten Speaker eines Panels; `ticket_final_mail` über die Weiche; `invite_speaker` weist den verwalteten Speaker ab (P0001 `speaker_managed_by_partner`) und lädt den regulären ein; `speaker_detail` zeigt `mail_via`. Gegen live: alle sechs scheitern (Funktion fehlt, Mails an die Speaker, Einladung geht durch, Feld fehlt) |
| `v6_talk_speaker_zugang.sql` | Vorschlag (PART-091) | Speaker eines gebuchten Slots mit echten Partnerrollen: eigener Zugang legt ein reguläres Profil an (`lead`, kein Kontakt, keine Regel, keine Mail); verwaltet macht den `primary_ops`-Kontakt zum Kontakt `partner` mit Zugang und Einwilligung von heute, gibt ihm `speaker_assistant` der Edition, setzt `mail_via_contact_id` und schickt genau eine `partner_speaker_contact`-Mail an ihn, keine an den Speaker; ein zweiter Slot legt keinen zweiten Kontakt an und schickt keine zweite Mail; verwaltet für jemanden mit eigenem Zugang P0001 `speaker_has_access`, ohne Operations-Kontakt P0001 `no_ops_contact` (nichts angelegt), Kontakt als Speaker 23514 `contact_is_speaker`; `partner_speakers` nennt den Kontakt nur beim verwalteten Speaker; die Regel zeigt nur auf Kontakte desselben Profils (23503), Entfernen des Kontakts hebt nur die Regel auf; Gäste bekommen keinen Talk-Slot mehr (42501), eine alte Talk-Zuordnung lässt sich abnehmen, die Standbühne geht weiter; alte Signatur weg, `authenticated` darf, `anon` nicht; Vokabular `partner` und Vorlage DE/EN. Probelauf 25.09.2026: 14/14 grün; ohne Migration bricht der Test ab (Spalte fehlt). |
| `v6_tour_bewerbungen_partner.sql` | Vorschlag (PART-046) | Company Tour, Bewerbungen für den Partner eines Stopps mit echter Partnerrolle: der Partner liest die Bewerbungen der Tour-Session — mit Einwilligung Name, Profil und Antworten mit Fragetext in der Reihenfolge der Fragen (unbekannter Schlüssel bleibt als Text), ohne Einwilligung die Zeile ohne Personenbezug (jedes Feld einzeln geprüft), Bewerbungen anderer Sessions fehlen; jeder Abruf im Audit (`application.partner_view`, `tour`, Zeilenzahl); fremder Stopp 42501; Tour ohne Session keine Zeilen; Stopp ohne Organisation 42501, unbekannter Stopp P0002 `stop_not_found`; SECURITY DEFINER, fester search_path, `anon` ohne EXECUTE. Probelauf 25.09.2026: 7/7 grün. |
| `v6_speaker_profilwahl.sql` | Vorschlag (SPK-071) | Profilwahl mit echtem Rollenwechsel: T mit eigenem Profil und als Kontakt von X und Y — `my_speaker_profiles` (eigenes zuerst, gewählt), `set_my_speaker_profile` (danach zeigen `my_speaker_profile_id`, `my_speaker_profile`, `my_sessions` X; `update_my_speaker_profile` ohne id schreibt X), fremdes Profil 42501, unbekanntes P0002, nach entzogenem Zugang gilt die Wahl nicht mehr (Vorbedingung: sie zeigt noch auf X), die Tabelle ist für Angemeldete nicht lesbar. Gegen live: alle sieben scheitern |
| `v6_speaker_mail_via_admin.sql` | Vorschlag (SPK-072) | Umleitung im Admin mit echtem Rollenwechsel: T (programme_team) setzt einen Kontakt mit Zugang (Spalte, Audit, `speaker_mail_recipient` folgt), ein Kontakt ohne Zugang → P0001 `contact_without_access` (Spalte unverändert), ein Kontakt eines anderen Profils → P0002 `contact_not_found`, K (marketing_team) → 42501, Aufheben mit zweitem Audit-Eintrag und Rückfall auf den Speaker. Gegen live: alle fünf scheitern |
| `v6_masterclass_fragen.sql` | Vorschlag (PART-045) | Katalogfragen einer Masterclass mit echter Partnerrolle: der Partner wählt zwei wählbare Fragen — die Teamfrage (nicht wählbar, Pflicht) bleibt, eine schon gesetzte wählbare Frage behält Pflicht und Platz, die neue kommt hinten an, die eigene Frage bleibt; eine leere Auswahl nimmt nur die wählbaren weg; eine Teamfrage in der Auswahl P0001 `question_not_selectable` ohne Änderung; fremde Session 42501; SECURITY DEFINER, fester search_path, `anon` ohne EXECUTE. Probelauf 25.09.2026: 5/5 grün; Gegenprobe gegen die Live-Fassung: Teamfrage verschwindet, Pflicht fällt weg (01/02 rot). |
